---
gsd_state_version: 1.0
milestone: v1.0.0
milestone_name: milestone
status: executing
last_updated: "2026-07-21T11:48:27.049Z"
progress:
  total_phases: 12
  completed_phases: 7
  total_plans: 42
  completed_plans: 41
  percent: 58
---

# STATE — Vizpatch (schmaler KI-Email-Agent)

## Current Milestone

**v1 — Vizpatch produktiv beim ersten Kunden (Esso-Tankstelle Leonberg)**

## Current Phase

**Phase 12 — Datei-Upload-Anhänge an Entwürfe (v1.8) 🔧 In Ausführung — Plan 12-02/3 abgeschlossen.** Zehntes Chat-Werkzeug `entwurf_mit_anhang` in `webui/src/chat_tools.py` (Plan 12-01): Pending-Upload-Store (`register_pending_upload`/`_consume_pending_upload`, Prozess-lokal, TTL 3600s, HMAC-Sitzungsschlüssel wie `_authorized_move_sessions`), `_build_new_draft_mit_anhang` (RFC-5322-MIME-multipart + Base64-Anhang via `EmailMessage.add_attachment()`, Threading-Header bei Antwort-Entwürfen erhalten), IMAP-APPEND in den Drafts-Ordner (kein Senden). Registriert in `TOOL_HANDLERS`/`TOOL_SCHEMAS`/`_SESSION_SCOPED_TOOLS`, NICHT in `_ANON_AWARE_TOOLS` (kein Text-Body zum Anonymisieren). Beide hartkodierten Guard-Allowlists synchronisiert — AST-/Schema-Kein-Auto-Send-Wächter (CTOOL-05) bleiben grün. Metadaten-DATEN-Block (`attachment_meta`) in `_build_initial_messages`, durchgereicht bis `run_agentic_chat` — trägt nur Dateiname/Größe/Typ, nie den Inhalt (ATT-05/D-96). tmp-Cleanup im `finally` deckt Erfolg/IMAP-Fehler/Größenüberschreitung ab. **Plan 12-02:** neuer authentifizierter, streamender Upload-Endpoint `POST /chat/{agent_id}/upload` (`webui/src/main.py`) — server-generierte tempfile, 1-MB-Chunk-Read (`file.file.read`, kein Full-Memory-Load), Live-Byte-Zähler gegen `MAX_ATTACHMENT_MB` (Default 15, `agent/docker-compose.yml`-Compose-Env), 413 + tmp-Cleanup bei Überschreitung, `os.path.basename`-Sanitizing gegen Path-Traversal. `chat_send` reicht das neue Formfeld `attachment_meta` (defensiv geparst, analog `_parse_mail_context`) als DATEN an `run_agentic_chat` durch. **ATT-01…05 code-seitig vollständig erfüllt.** 489 webui Tests grün / 3 skipped (keine Regression). **Noch offen: Plan 12-03 (Chat-UI-Upload-Widget) — dieser Plan liefert Endpoint + Durchreichung, aber noch kein Browser-Pfad (File-Input/fetch) zum Auslösen.**

**Vorherige Phase — Phase 10 — Reversible Pseudonymisierung vor LLM (v1.6, Variante A) ✅ Abgeschlossen.** Alle 4 Plans ausgeführt: 10-01 (reversible `Anonymizer`-Engine in `agent/src/pii.py`, byte-identische `webui/src/pii.py`, 6 getypte Regex IBAN/KARTE/EMAIL/URL/DATUM/TELEFON + RAM-Mapping + `warn_residual_placeholders`-Nachlauf, angebunden an classify+generate), 10-02 (WebUI `style_extract.py` anonymisiert-vor-Truncate + `chat.py` `deanonymize_stream`-Streaming-Puffer + anonymizer-fähiges `build_chat_prompt`), 10-03 (agentische Tool-Schleife `chat_tools.py`: geteilte `Anonymizer`-Instanz pro Turn, De-Anon von Text UND Tool-Argumenten vor Handler-Aufruf, Flag-aus-Fallback), 10-04 (DSGVO/AVV-Doku ehrlich auf Variante A: strukturierte PII maskiert, Namen/Orte weiter ans LLM → pseudonym ≠ anonym ErwG 26, AVV bleibt nötig). **ANON-01…05 vollständig.** **10-04 Task 2 (rechtliche DSGVO/AVV-Abnahme) am 2026-07-20 vom Betreiber/DSB (info@vizionists.com) freigegeben**

**Parallel abgeschlossen — Phase 8 (Neuplanung) — Outlook-Add-in als COM/VSTO für Outlook classic (v1.4) ✅ Code-komplett (2026-07-20).** Nach dem Pivot Office.js → COM/VSTO (Office.js läuft nur auf M365/Exchange, der Kunde nutzt Outlook classic auf IMAP) wurden alle 4 Plans ausgeführt: 08-01 (COM-freie Core-Bibliothek: SSE-Zeilenparser + ChatClient mit Origin/Basic-Auth-CSRF-Workaround + DTOs + DPAPI-Settings, xUnit), 08-02 (VSTO-Hülle: Ribbon-Toggle + CustomTaskPane + WinForms-ChatView mit inkrementellem SSE-Rendering + session_id), 08-03 (defensiver MailContextReader übers Outlook-Objektmodell + SettingsDialog mit DPAPI-Persistenz), 08-04 (struktureller Kein-Auto-Send-Quellwächter `scripts/check-addin-no-autosend.sh` — grün gegen 19 *.cs, rot bei eingeschmuggeltem `.Send(`/`item.Delete()`; ClickOnce/LAN/HTTPS-Runbook `deployment/README.addin-outlook.md`). MSBuild-Solution baut fehlerfrei, `dotnet test` 22/22 grün. **OUT-05…09 code-seitig erfüllt; die gebündelte menschliche Live-Abnahme in echtem Outlook classic (D-89: Installation, Werkzeuge inkl. Bestätigungs-Gate, Draft via IMAP-Sync, Kein-Auto-Send) bleibt offen** (konsolidierte Checkliste in `08-04-SUMMARY.md`, Muster Phase 6/7). Kein-Auto-Send ist damit jetzt DOPPELT strukturell abgesichert: serverseitig (webui AST-Wächter) + clientseitig (Add-in-Quellwächter). — Anbieter-Identität Vizionists GmbH eingetragen, Wortlaut in Datenschutzerklärung (Ziffer 4/5/6/11) + AVV (§3/§4/§6) abgenommen. Integrationsentscheidung: Anonymisierung auf Aufrufer-Ebene VOR `.format()` (nicht in `llm.py`) — erfüllt D-06-Absicht ohne D-08 (context.md roh) zu verletzen. Tests zuletzt: 462 webui + 134 agent grün. ANON-06 (NER für Namen) bleibt deferred.

**Vorherige Phase — Phase 9 — Agentischer Chat mit Postfach-Werkzeugen (v1.5) ✅ Code-komplett.** Alle 5 Plans ausgeführt: 09-01 (Walking Skeleton: Anthropic-Tool-Use-Schleife + `mails_suchen` + SSE-Tool-Aktivität), 09-02 (Entwürfe-Werkzeuge: `mail_lesen`/`entwuerfe_auflisten`/`entwurf_lesen` + Drafts-Ordner-SPECIAL-USE-Erkennung, CTOOL-02 vollständig), 09-03 (`entwurf_bearbeiten` + Papierkorb-Erkennung/Move-Helfer, CTOOL-03 vollständig), 09-04 (`mail_in_papierkorb`/`entwurf_in_papierkorb` mit gehärtetem Bestätigungs-Token-Gate, CTOOL-04 vollständig) und 09-05 (struktureller Kein-Auto-Send-AST-Wächter + Datenschutz-Ziffer-6/AVV-§6.2-Angleichung, CTOOL-05 vollständig). **CTOOL-01…05 alle erfüllt.** 368 webui Tests grün / 3 skipped, Drift-Guards intakt, kein Live-Klick-Checkpoint mehr offen für Phase 9 (rein code-basierte Phase).

**Vorheriger Meilenstein — Phase 5 — Multi-LLM, Multi-Agent & Verschlüsselung (v1.2) ✅ Code ausgeführt.** Alle 6 Plans (05.01–05.06) committet, plus Post-Review-Fixes (WR-01…06, CR-01). Codebasis inhaltlich bei **v1.2**: Multi-Agent-Loop (Ein-Container, `/config/agents/<id>/`), Multi-LLM-Adapter (`agent/src/llm.py`, Anthropic/OpenAI/Google, Provider-Autodetect D-51), Fernet-Verschlüsselung at-rest (`crypto.py` Agent+WebUI). **Offen:** Verifikation 05.06 Tasks 1–3 (Modell-ID-Check, LLM-04-Fixtures je Provider ≥ 11/14, MA-05-Parallelbetrieb, Migrations-Abnahme) ist **ehrlich als deferred markiert** — braucht echte OpenAI/Google-API-Keys.

**Anmerkung:** Phase 5 wurde entgegen der ursprünglichen Roadmap-Reihenfolge **vor** dem Esso-Rollout ausgeführt. Migration Single→default wurde gegen eine Kopie des Live-Layouts getestet (kein Regressions-Risiko), der eigentliche Esso-Rollout steht aber noch aus.

**Nächster Fokus: Esso Leonberg live bekommen — nicht Feature-Ausbau.**

## Phase Status

| Phase | Status | Started | Completed |
|---|---|---|---|
| 1 — Agent MVP bauen | ✅ Completed | 2026-07-09 | 2026-07-10 |
| 2 — Deployment beim Kunden | ✅ Completed | 2026-07-11 | 2026-07-12 |
| 3 — Tuning & Übergabe | 🔧 In Vorbereitung | 2026-07-12 | — |
| 4 — Web-UI & Multi-Kunde | ✅ End-to-End verifiziert (63 agent + 89 webui Tests grün + Live-Draft in Entwürfe) | 2026-07-12 | 2026-07-14 |
| 5 — Multi-LLM, Multi-Agent & Verschlüsselung (v1.2) | ✅ Code ausgeführt (6/6 Plans + Fixes WR-01…06, CR-01) — **05.06-Verifikation (Tasks 1–3) deferred** bis OpenAI/Google-Keys vorliegen | 2026-07-15 | 2026-07-17 |
| 6 — Schreibstil-Adaption pro Agent (v1.3) | ✅ Code-komplett — 06.01–06.03 abgeschlossen, 06.04 Task 1 (A/B-Fixtures) fertig; **06.04 Task 2 (A/B-Ton-Abnahme) = menschlicher Checkpoint offen** | 2026-07-17 | — |
| 7 — Agenten-Chat im WebUI (v1.3) | ✅ Code-komplett — alle 4 Plans (07-01…07-04) abgeschlossen; nur **Browser-Klick-Abnahme des Chats offen** (menschlicher Check) | 2026-07-17 | 2026-07-17 |
| 8 — Outlook-Add-in für den Agenten-Chat (COM/VSTO, v1.4) | ✅ Code-komplett — alle 4 Plans ausgeführt: 08-01 (COM-freie Core: SSE-Parser + ChatClient + DPAPI-Settings) + 08-02 (VSTO-Hülle: Ribbon + CustomTaskPane + SSE-ChatView) + 08-03 (Mail-Kontext übers Objektmodell + Settings-Dialog) + 08-04 (Kein-Auto-Send-Quellwächter `scripts/check-addin-no-autosend.sh` grün + ClickOnce/LAN/HTTPS-Runbook `deployment/README.addin-outlook.md`); MSBuild grün, 23/23 Tests grün; **alle 5 SC am 2026-07-21 live in echtem Outlook classic abgenommen** (SSE-Chat, Mail-Kontext, Werkzeuge/Gate/Draft via IMAP-Sync, Settings-Persistenz) | 2026-07-17 | 2026-07-21 |
| 9 — Agentischer Chat mit Postfach-Werkzeugen (v1.5) | ✅ Code-komplett — alle 5 Plans ausgeführt: 09-01 (Walking Skeleton) + 09-02 (Entwürfe-Werkzeuge, CTOOL-02) + 09-03 (`entwurf_bearbeiten` + Papierkorb-Erkennung, CTOOL-03) + 09-04 (Bestätigungs-Token-Gate, CTOOL-04) + 09-05 (AST-Kein-Auto-Send-Wächter + Datenschutz/AVV-Angleichung, CTOOL-05) | 2026-07-18 | 2026-07-18 |
| 12 — Datei-Upload-Anhänge an Entwürfe (v1.8) | 🔧 In Ausführung — Plan 12-02/3 abgeschlossen: `entwurf_mit_anhang` + Pending-Upload-Store (12-01), Upload-Endpoint `/chat/{agent_id}/upload` + `MAX_ATTACHMENT_MB`-Compose-Env + `attachment_meta`-Durchreichung (12-02, ATT-01/03), 489 webui Tests grün. Offen: 12-03 (Chat-UI-Widget) | 2026-07-21 | — |

## Phase-4-Plans (alle abgeschlossen 2026-07-13)

| Plan | Was | Wave | Status |
|---|---|---|---|
| 04.01 | Walking Skeleton: 2. Compose-Service `webui`, FastAPI + Uvicorn + Docker-CLI, `/healthz` + `docker-compose.yml`-Bind-Mount | 1 | ✅ |
| 04.02 | Basic-Auth + Konfig-Formular (read/write `.env` + `context.md`, chmod 600, Password-UX "leer = unverändert") | 2 | ✅ |
| 04.03 | Steuerung + Status (Docker-SDK-Wrapper Start/Stop/Restart, HTMX-Auto-Refresh, Last-Poll aus SQLite) | 3 | ✅ |
| 04.04 | Context-KI-Assistent (Sonnet 4.6 + `prompts/context-seed.txt` + Prompt-Injection-Anker) | 4 | ✅ |
| 04.05 | Update (GHCR-Pull + Tarball-Upload) + `install-autostart.sh` + Deployment-Paket v1.1.0 | 5 | ✅ |

## Phase-2-Plans (alle abgeschlossen 2026-07-11)

| Plan | Was | Status |
|---|---|---|
| 02.01 | Auto-Provider-Detection (D-23), `provider_config.py`, `dnspython` | ✅ |
| 02.02 | Auto-CREATE Drafts-Ordner (D-25), 6 neue Tests | ✅ |
| 02.03 | Konversations-Kontext via Live-IMAP (D-26), 17 neue Tests, 63 gesamt | ✅ |
| 02.04 | Docker Bind-Mount (D-16) + `build-deployment-package.sh` + `deployment/`-Templates | ✅ |
| 02.05 | 14 `.eml`-Test-Fixtures + `PRE-DEPLOYMENT-TEST.md` + Report-Template | ✅ |
| 02.06 | `PREFLIGHT.md` + `AVV-CHECKLIST.md` + `KUNDEN-INTERVIEW.md` | ✅ |
| 02.07 | `RUNBOOK.md` (443 Zeilen, 7 Schritte, Vor-Ort + Remote) | ✅ |

## Pre-Deployment-Test — ABGESCHLOSSEN 2026-07-12 ✅

**Ergebnis:** 10/10 korrekt, Ø 4.0/5 Qualität, alle Reboot-Checks grün. Freigegeben.
**Report:** `.planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST-REPORT.md`
**3 Bugs gefixt:** html_to_text, msg.message_id, INBOX-Restore nach History-Fetch.
**Offener Punkt:** `IMAP_SENT_FOLDER=Gesendete Objekte` vor Kundentermin in .env.example eintragen.
**Anleitung:** `.planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST.md`

## Blockers / Open Preflight (Kundenseite)

- **PRE-01** offen: Tankstelle bestätigt E-Mail-Provider (vereinfacht durch D-23: nur E-Mail + Passwort + Drafts-Ordner-Name)
- **PRE-02** offen: Server-Bestätigung (Docker, min. 512 MB RAM)
- **PRE-04** offen: AVV mit Anthropic
- **PRE-05** offen: Firmen-Inhalte für `context.md` vom Kunden (OSINT-Rohbau in `deployment/context.md.tankstelle-erstversion.md` vorhanden)

## Next Action

**Weg zum Esso-Rollout (Reihenfolge festgelegt am 2026-07-17 mit Betreiber):**

1. **Deployment-Paket v1.2.0 bauen:** `bash scripts/build-deployment-package.sh v1.2.0` → produziert `dist/deployment-paket-v1.2.0/` mit Agent- + WebUI-Image-Tarballs (inkl. llm.py, crypto.py, agents_io.py, migration.py) + install-autostart.sh + docker-compose + README. Grundlage für Vor-Ort-Rollout.
2. **Rest fertigstellen:** verbleibende 05.06-Verifikations-Punkte (soweit für Anthropic-only-Rollout nötig) + Feinschliff.
3. **DSGVO + AVV mit Anthropic** abschließen (Vertrag/Checkliste) — Voraussetzung für Live-Verarbeitung echter Kundenmails.
4. **Update zum Kunden ausrollen — fertig.** Vor-Ort-Termin Esso Leonberg mit Browser-UI statt SSH-Setup.

**Autostart-Test:** bewusst NICHT mehr auf separater VM — wird direkt beim Kunden auf dem echten Ubuntu-Server getestet (Entscheidung 2026-07-17). Der WSL-Autostart-Test war durch den Docker-Desktop-Bind-Mount-Cache-Bug ungültig (Memory `wsl_docker_desktop_bindmount.md`), eine Zwischen-VM ist den Aufwand nicht wert.

### Ältere Notizen zur Phase 4 (nur zur Historie)

Kern-Artefakte:

- `.planning/phases/04-web-ui-multi-kunde/04-CONTEXT.md` — D-27..D-44 Entscheidungen (FastAPI+Jinja2+HTMX, Docker-Socket + Basic-Auth, LLM-Seed via Sonnet 4.6, systemd via Post-Install-Skript, kein HTTPS in v1)
- `.planning/phases/04-web-ui-multi-kunde/04-RESEARCH.md` — Stack-Versionen (FastAPI 0.139, docker 7.2, python-multipart 0.0.32), File-Struktur, Docker-Socket-GID-Passing, Update-Flow, Prompt-Template
- `.planning/phases/04-web-ui-multi-kunde/04.01..05-PLAN.md` — je 3–5 Tasks mit read_first + acceptance_criteria

## Phase-5-Plans (geplant 2026-07-15, Ausführung nach Esso-Rollout)

| Plan | Was | Wave | Autonomous |
|---|---|---|---|
| 05.01 | Fernet-Krypto-Fundament (crypto.py Agent+WebUI, Key-Datei chmod 600, Deps openai/google-genai/cryptography, Version 1.2.0) | 1 | ja |
| 05.03 | LLM-Adapter im Agent (llm.py, LLM_PROVIDER/LLM_API_KEY, Fernet-Decrypt, classify+generate auf Adapter) | 2 | ja |
| 05.04 | agents_io (per-Agent .env+context.md+AGENT_ENABLED, Slug-Guard, rename/delete, read_env_raw) + idempotente Migration Single→default mit Agent-Key-Guard | 2 | ja |
| 05.02 | Agent-Multi-Account-Loop (EIN Container, per-Zyklus-Discovery, Aktiv-Flag, Fehler-Isolation, IMAP-Timeout, last_cycle-Heartbeat) | 3 | ja |
| 05.05 | WebUI agent_id-Routing, /agents-CRUD, Agent-Dropdown + API-Key-Autodetect (D-51), Status-Liste, llm_seed pro Agent, Multi-Agent-Zero-Reset | 3 | ja |
| 05.06 | Verifikation: Modell-ID-Check, LLM-04-Fixtures je Provider (≥ 11/14), MA-05-Parallelbetrieb, Esso-Migrations-Abnahme, Paket v1.2.0 | 4 | nein (Checkpoints) |

Artefakte: `.planning/phases/05-multi-llm-multi-agent-verschl-sselung-v1-2/` (05-CONTEXT.md D-46..D-51, 05-RESEARCH.md, 05-PATTERNS.md, 6 PLAN.md). Requirements LLM-01…04, MA-01…05, SEC-01…03 in REQUIREMENTS.md.

## Accumulated Context

### Roadmap Evolution

- Phase 6 hinzugefügt (2026-07-16): Schreibstil-Adaption pro Agent (v1.3) — automatische Stil-Extraktion aus Gesendet-Ordner beim Setup, style.md pro Agent, Re-Learn-Button; STY-01…05
- Phase 7 hinzugefügt (2026-07-16): Agenten-Chat im WebUI (v1.3) — SSE-Streaming, context/style/Status-Wissen, einbettbares Partial; CHAT-01…05
- Phase 8 hinzugefügt (2026-07-16): Outlook-Add-in für den Agenten-Chat (v1.4) — Office.js-Taskpane über CHAT-05-Partial, HTTPS-Runbook; OUT-01…04
- Verworfen (2026-07-16): Standalone-.exe/PyInstaller-Distribution — Architektur-Umbau zu groß, Docker bleibt Deployment-Standard. Docker-lose Ubuntu-Variante (systemd + venv, Steuerungs-Abstraktion statt docker_ctrl) als mögliche Mini-Phase notiert, nur bei konkretem Kundenbedarf.
- Detail-Planung (`/gsd:plan-phase 6/7/8`) bewusst NACH Phase-5-Execution — die Phasen bauen auf Phase-5-Artefakten auf (agents-Layout, LLM-Adapter), gegen die der Planner erst planen kann, wenn sie im Code existieren.
- **App-weiter Security-Audit + Härtung (2026-07-20):** Audit über die ganze App (`10-SECURITY-AUDIT.md`) — SSRF/Command-Injection/Path-Traversal/XSS/Prompt-Injection sauber. Behoben: CSRF (Same-Origin-Middleware, Add-in-Origin-Ausnahme nur für `/chat/send|embed`), Setup-Zwang bei fehlendem Passwort (`/save` bleibt offen für Bootstrap, `VIZPATCH_ALLOW_NO_AUTH`-Bypass), Trusted-Proxy-XFF (`TRUSTED_PROXY`), Rate-Limits (CRUD 30/min, `/reset` 3/min, LLM 10/min), Größen-Limits (Chat 8k/200k, context/style 64 KB), CRLF-Header-Injection in Draft-Buildern, generische Fehlermeldungen. 8 `fix(security)`-Commits, webui 462 + agent 134 Tests grün. **WebUI-Update-Buttons entfernt (2026-07-20)** — Updates laufen per SSH (`deployment/UPDATE-RUNBOOK-SSH.md`, voller Agent+WebUI-Update, verlustfrei via `./config` + Volume `agent-data`). Chat-Design verfeinert.
- **CTOOL-04 gelockert (2026-07-19, Betreiber-Entscheidung):** Papierkorb-Verschiebungen im agentischen Chat verlangen die Bestätigung nur noch **einmal pro Chat-Sitzung** (statt pro Aktion). Erst-Verschiebung → weiterhin gehärtetes Zwei-Schritt-Token-Gate (schützt gegen Prompt-Injection-Erstmissbrauch); danach ist die Sitzung serverseitig autorisiert (In-Memory-HMAC-Set, `session_id`-Nonce vom chat.js, neu bei Reset). Reversibel/Papierkorb/Protokollierung unverändert. Code + Tests (411 webui grün) + Rechtstexte (CTOOL-04, ROADMAP SC3, Datenschutz Ziffer 6, AVV §3/§4/§6) angeglichen. Commits `98190c7`, `af53b6c`.
- Phase 12 hinzugefügt (2026-07-21): Datei-Upload-Anhänge an Entwürfe — Betreiber-Feature-Wunsch aus der Phase-8-Live-Abnahme (Variante C: Ad-hoc-Upload, alle Dateitypen, nur Stationsleitung, ~15 MB Limit, Kein-Auto-Send bleibt strukturell). Todo unter `.planning/todos/pending/`. Geplant (3 Plans/3 Waves, plan-checker grün) und Plan 12-01 (serverseitiges Kern-Werkzeug `entwurf_mit_anhang`) am 2026-07-21 ausgeführt.

## History

- **2026-07-21** — **Phase 12, Plan 12-01 ausgeführt** (Werkzeug `entwurf_mit_anhang` + Pending-Upload-Store, ATT-02/ATT-04/ATT-05): Task 1 Pending-Upload-Store (`_pending_uploads`, `register_pending_upload`/`_consume_pending_upload`, TTL 3600s, HMAC-`_session_key` wiederverwendet, autouse-Reset-Fixture `conftest.py`). Task 2 `_build_new_draft_mit_anhang` (Erweiterung von `_build_new_draft` um `EmailMessage.add_attachment()`, set_content ZUERST/add_attachment DANACH) + Handler `entwurf_mit_anhang` (konsumiert Pending-Upload per `session_id`, IMAP APPEND in Drafts-Ordner, Threading erhalten, Defense-in-Depth-Größenprüfung gegen `MAX_ATTACHMENT_MB`, tmp-Cleanup in `finally` bei Erfolg/IMAP-Fehler/Größenüberschreitung) — registriert in `TOOL_HANDLERS`/`TOOL_SCHEMAS`/`_SESSION_SCOPED_TOOLS`, NICHT in `_ANON_AWARE_TOOLS`; beide hartkodierten Guard-Allowlists synchronisiert, AST-/Schema-Kein-Auto-Send-Wächter bleiben grün. Task 3 Metadaten-DATEN-Block (`attachment_meta`) in `_build_initial_messages`, durchgereicht bis `run_agentic_chat` — nur Name/Größe/Typ, nie Dateiinhalt (D-96). **480 webui Tests grün / 3 skipped** (Baseline 462, +18 neue, keine Regression). Commits `c48db8b`, `711eb79`, `67aeb3a`. **Nur die serverseitige Kern-Fähigkeit — kein Browser-Pfad, der sie auslösen kann.** Nächster Schritt: Plan 12-02 (Upload-Endpoint `/chat/{agent_id}/upload`).
- **2026-07-21** — **Phase 8 (COM/VSTO-Outlook-Add-in) ausgeführt + live abgenommen — ABGESCHLOSSEN.** Alle 4 Plans auf einer Windows-Maschine mit VS Community 2026 (.NET-Desktop + Office/SharePoint-Workload) ausgeführt: 08-01 (COM-freie `VizpatchAddin.Core` — SSE-Parser/ChatClient/DPAPI-Settings, real gebaut + 22/22 xUnit grün), 08-02 (VSTO-Hülle Ribbon+CustomTaskPane+SSE-ChatView; Blocker: VSTO-4.0-Basisklassen sind Interfaces → `OutlookAddInBase` aus VS-Vorlage gespiegelt), 08-03 (MailContextReader übers Objektmodell + SettingsDialog DPAPI), 08-04 (Kein-Auto-Send-Quellwächter `scripts/check-addin-no-autosend.sh` grün + ClickOnce/LAN-Runbook). **Verifier: 16/16 strukturell → nach Live-Abnahme `passed`.** **Code-Review** fand 1 Blocker + 6 Warnings → **alle behoben** (CR-01 UI-Thread-Freeze via `ReadLineAsync()`, WR-01 COM-Freigabe, WR-05/06 Cancellation/finally, WR-02/03/04 Wächter gehärtet), Rebuild grün, **23/23 Tests**. **Live-Abnahme in echtem Outlook classic (Betreiber, Stationsleitung) alle 5 SC grün:** SSE-Chat streamt, Mail-Kontext trifft die konkrete Mail, Werkzeuge+Bestätigungs-Gate+Draft via IMAP-Sync, Settings persistieren über Neustart, Passwort DPAPI-only. OUT-05…09 SATISFIED. **Beim Rollout auf die WSL-Ubuntu-Test-Instanz (`/home/enver_shala/vizpatch-test`, image-basiert `vizpatch-webui:v1.5.2`) drei Bugs gefunden+gefixt:** (1) veraltete WebUI (Update-Buttons) → Image aus aktuellem Quellstand neu gebaut; (2) **leerer `ADDIN_FRAME_ANCESTORS`-Compose-Env überschrieb den Code-Default → 403 für jeden Add-in-Request** (Laufzeit-Fix via Compose-`.env` + Code-Fix `os.getenv(...) or DEFAULT` committet `3ae770c`); (3) Add-in-Chat-Kontrast (weiß-auf-weiß) + WebUI „2. Agent nicht anlegbar" + „Provider erkannt"→„LLM-Provider erkannt" (Commit `a6edda6`). **Offene Feature-Wünsche des Betreibers als Todos erfasst** (`.planning/todos/pending/`): Datei-Upload-Anhänge an Entwürfe (Variante C: Ad-hoc-Upload, alle Dateitypen, nur Stationsleitung, ~15 MB Limit, Kein-Auto-Send bleibt) — **nächste geplante Phase**; context.md-Pflege-Werkzeug im Chat; Bestätigungs-Gate reduzieren/abschaltbar (`CHAT_REQUIRE_TRASH_CONFIRM`, WebUI-Toggle) inkl. Rechtstext-Angleichung. **Nächster Schritt:** `/gsd:plan-phase` für die Anhang-Upload-Phase.
- **2026-07-20** — **Phase 8 neu geplant (Pivot).** Kunde nutzt Outlook classic → Add-in von Office.js (M365-only, on hold, archiviert unter `08-.../archive-officejs/`) auf **COM/VSTO** (Thin-Client gegen bestehende `/chat`-API, SSE, Werkzeuge+Drafts serverseitig) umgestellt. CONTEXT (D-82…D-89), ROADMAP, REQUIREMENTS (OUT-01…04 superseded, OUT-05…09 neu) aktualisiert. Research + Planner (4 Plans, 4 Waves) + Plan-Checker (PASS, keine Blocker) durchlaufen. Kritischer Fund: nativer `HttpClient` braucht expliziten `Origin`-Header (in `ADDIN_FRAME_ANCESTORS`), sonst 403 durch `enforce_same_origin` — Fix ohne Backend-Änderung. Build/Live-Tasks = `autonomous: false` (kein VS/Office auf Dev-Maschine). Nächster Schritt: `/gsd:execute-phase 08` auf Windows+VS+Office+LAN-Backend.
- **2026-07-09** — Projekt initialisiert. Initialer Multi-Tenant-Plan.
- **2026-07-09** — Phase 1 Context (Runde 1) mit InboxZero-Basis, Vizionists-managed Hosting.
- **2026-07-09 (Pivot 1)** — "Kunde hat Server". Software-Delivery-Modell mit Deployment-Repo, Runbooks, bootstrap.sh.
- **2026-07-09 (Pivot 2)** — "So schmal wie möglich". Reduziert auf 4 Deliverable-Dateien (Compose, .env, Caddyfile, README).
- **2026-07-09 (Pivot 3 — FINAL)** — Kunde ist Tankstelle. InboxZero verworfen. **Eigenbau-Python-Miniagent** neue Basis. Provider-agnostisches IMAP-Polling, SQLite-State, 1 Docker-Container, ~700 Zeilen. 3 Phasen statt 4. ~3 Werktage bis Live.
- **2026-07-09** — Phase 1 geplant. **4 Plans, 3 Waves**: 01-skeleton → 02-imap-draft + 03-llm (parallel) → 04-main-tests-release. ~30 atomare Tasks, 21/22 Phase-1-Requirements gecovert.
- **2026-07-10** — Phase 1 ✅ ausgeführt. Alle 4 Plans grün, ~30 Tasks abgearbeitet. `agent/`-Repo neu initialisiert, Commit `25bb1f8`, Tag `v1.0.0` lokal. 26/26 pytest grün unter Python 3.14. Docker-Build unverifiziert (Docker nicht auf Host).
- **2026-07-10** — Phase 2 Discuss Runden 1–5 abgeschlossen. Decisions: D-16 Bind-Mount, D-23 Auto-Provider-Detection, D-24 manueller Drafts-Ordner-Name, D-25 Auto-CREATE, D-26 Konversations-Kontext via Live-IMAP, D-04 Tarball-Delivery, D-22 Zwei-Konfig-Trennung.
- **2026-07-11** — Phase 2 alle 7 Code-Pläne ausgeführt. 63 Tests grün. `deployment/`-Ordner, `scripts/build-deployment-package.sh`, 14 `.eml`-Fixtures, `RUNBOOK.md`, `PREFLIGHT.md`, `AVV-CHECKLIST.md`, `KUNDEN-INTERVIEW.md` erstellt. Pre-Deployment-Test-Artefakte fertig.
- **2026-07-12** — Pre-Deployment-Test vollständig ausgeführt. 10/10 Fixtures korrekt, Ø 4.0/5. 3 Bugs gefunden + gefixt (html_to_text, msg.message_id, INBOX-Restore). IONOS Sent-Folder in provider_config.py korrigiert. Deployment-Paket v1.0.0 gebaut (54 MB Tarball). Phase 2 ✅ abgeschlossen.
- **2026-07-12** — Entscheidung: Phase 4 (Web-UI) wird vorgezogen, auch für ersten Kunden (Esso). Phase 3 läuft danach parallel.
- **2026-07-12** — Phase 4 geplant. Formal-Grundlagen ergänzt: Phase-4-Details in ROADMAP.md, UI-01…UI-05 in REQUIREMENTS.md, `04-CONTEXT.md` (D-27..D-44) und `04-RESEARCH.md` erstellt. 5 Plans in 5 sequentiellen Waves (04.01 Walking Skeleton → 04.05 Deployment-Paket v1.1.0). Plan-Checker Iteration 1 fand 4 BLOCKER + 8 WARNINGS, Iteration 2 alle behoben → Verification passed. Bereit für `/gsd:execute-phase 4`.
- **2026-07-13** — Produkt umbenannt: **KEA / kea-tankstelle → Vizpatch**. Rename in aktivem Code (`agent/src/*.py` Logger-Namen, `agent/Dockerfile` User, `agent/docker-compose.yml` Image+Container-Name, `agent/pyproject.toml` Package-Name, `agent/README.md`), aktiver Planung (`CLAUDE.md`, `.planning/*`, Phase-4-Plans + CONTEXT + RESEARCH), Skripten (`build-deployment-package.sh`) und Deployment-Templates. Historische Docs (Phase 1/2, `research/SUMMARY-inboxzero-obsolete.md`) und `dist/deployment-paket-v1.0.0/` unangetastet. Details: `.planning/quick/20260712-rename-kea-to-vizpatch/`.
- **2026-07-13** — Positionierung nachgeschärft: "für Tankstelle" aus Produkt-Titeln entfernt (Vizpatch ist branchen-agnostisch, Tankstelle nur erster Kunde). Drafts-Ordner nun `Vizpatch` (Enver hat ihn im Mail-Client manuell so angelegt). **DEL-08 unverändert public** (Repo `EnverShala/vizpatch` ist öffentlich — GHCR-Package damit ebenfalls anonym pullbar). **D-45 neu in Phase-4-CONTEXT**: bestätigt kein PAT-Setup beim Kunden, `pull_and_restart` ohne `auth_config`.
- **2026-07-13** — Phase 4 Code vollständig implementiert (5 Plans, 5 Waves). Commits: 04.01 Walking Skeleton, 04.02 Basic-Auth+Konfig-Formular, 04.03 Steuerung+Status, 04.04 KI-Assistent, 04.05 Update+Autostart+Deployment-Paket. **59 Tests grün, 1 skipped (chmod Windows)**. webui/ hat FastAPI+Jinja2+HTMX+Docker-SDK, alle Endpoints (/, /save, /agent/{action}, /agent/status, /context/generate, /update/pull, /update/upload, /healthz). Deployment-Paket v1.1.0 Builder bereit (`bash scripts/build-deployment-package.sh v1.1.0`). Ausstehend: manueller Browser-Checkpoint vor Vor-Ort-Termin Esso Leonberg.
- **2026-07-15** — Phase 5 komplett geplant (Scope erweitert von Multi-LLM-only auf Multi-LLM + Multi-Agent + Secrets-Verschlüsselung, Direktauftrag Betreiber). Roadmap + 12 neue Requirements (LLM/MA/SEC), 05-CONTEXT (D-46..D-50: ein Container pro Agent, /config/agents/<id>/-Layout, Fernet+Key-Datei ohne Master-Passwort, Provider pro Agent, Dropdown-Semantik), Research (SDK-Versionen verifiziert, OpenAI/Google-Modell-IDs LOW-confidence → Verifikations-Task), Pattern-Mapping (22 Dateien), 6 Pläne in 4 Waves. Plan-Checker Runde 1: 3 Blocker + 5 Warnings (context.md nicht agent-parametrisiert, rename_agent fehlte, /reset-Regression) → Revision → Runde 2 PASSED. Ausführung bewusst verschoben bis Esso-Rollout abgeschlossen.
- **2026-07-15 bis 2026-07-17** — **Phase 5 ausgeführt** (v1.2). Alle 6 Plans committet: 05.01 Fernet-Krypto-Fundament (Agent+WebUI, Versionsbump 1.2.0), 05.03 Multi-LLM-Adapter (`agent/src/llm.py`, LLM_API_KEY/LLM_PROVIDER, Fernet-Decrypt), 05.04 agents_io + Single→default-Migration, 05.02 Multi-Account-Loop (Ein-Container, per-Zyklus-Discovery, Fehler-Isolation, IMAP-Timeout, Heartbeat), 05.05 WebUI agent_id-Routing + /agents-CRUD + API-Key-Autodetect (D-51) + Multi-Agent-Zero-Reset, 05.06 Verifikation/Ship. **Post-Review-Fixes:** CR-01 (Mails ohne Message-ID sauber überspringen), WR-01 (OpenAI-Call-Shape: max_completion_tokens, keine temperature), WR-02 (detection_source überlebt Status-Write), WR-03 (Config-Load-Fehler pro Agent sichtbar), WR-04 (fehlgeschlagene Drafts-Probe nicht cachen), WR-05 (abweichende OWN_EMAIL_ADDRESS beim IMAP-Save nicht zurücksetzen), WR-06 (Drift-Guard für duplizierte crypto.py). **05.06-Verifikation Tasks 1–3 ehrlich als deferred markiert** (Modell-ID-Check, LLM-04-Fixtures je Provider ≥ 11/14, MA-05-Parallelbetrieb, Migrations-Abnahme) — brauchen echte OpenAI/Google-API-Keys. Letzter Commit `d7b5a36` "Aktueller Stand, Limit ausgeschöpft".
- **2026-07-16/17** — Phase 6 (Schreibstil-Adaption, v1.3) geplant: 4 Plans, 3 Waves, Plan-Checker-Revision (Commits `438cc1a`, `f2e50de`). Ausführung bewusst nach Esso-Rollout.
- **2026-07-17** — **STATE.md-Korrektur:** Der Header behauptete fälschlich noch "Phase 5 = geplant, completed_phases: 2". Realität laut Git-Historie: Phase 5 ausgeführt. Korrigiert auf completed_phases: 4 (1/2/4/5), 23 Plans total / 19 ausgeführt, percent 50. Betreiber-Entscheidung: Autostart nicht mehr auf Zwischen-VM, sondern direkt beim Kunden testen. Nächste Schritte: Deployment-Paket v1.2.0 → Rest + DSGVO/AVV → Update zum Kunden.
- **2026-07-17** — **Phase 6, Plan 06.01 ausgeführt** (Agent-seitige style.md-Injection, STY-02): `Config` lädt pro Agent ein optionales `style.md` (Guard-Muster analog `context_md`, defaultete Trailing-Felder `style_md`/`enable_style_adaption` für Rückwärtskompat), `ENABLE_STYLE_ADAPTION`-Flag (Default true, D-54). `generate.py` injiziert `{style_md}` konditional, `prompts/generate.txt` hat neue „# Schreibstil"-Sektion + Hierarchie-Satz (context.md=WAS, style.md=nur WIE, übersteuert Fach-Inhalt nie — T-06-01-Mitigation). TDD-Zyklus vollständig (RED `c0c7776` → GREEN `ac62da1`+`801450f`). 4 neue Tests, volle Agent-Suite 109 passed/1 skipped. Definiert das style.md-Zielformat (6 D-56-Abschnitte) für Plan 06.02 (WebUI-Extraktion, Interface-First). Commits: `c0c7776`, `ac62da1`, `801450f`.
- **2026-07-17** — **Phase 6, Plan 06.02 ausgeführt** (WebUI-Schreibstil-Extraktions-Service, STY-01/04/05): `webui/src/style_extract.py::extract_style(agent_id)` als reine Service-Funktion — verbindet sich selbst per IMAP zum Gesendet-Ordner (SPECIAL-USE `\Sent`-Erkennung analog `detect_drafts_folder()`, Fallback `provider_config.resolve_imap_config`), filtert auf echte Antwort-Mails (Fwd:/Wg: verworfen, In-Reply-To/re:/aw: + Mindestlänge), redigiert jeden Body via `pii.redact()` und ruft den provider-agnostischen `llm.llm_call()`-Adapter mit dem Draft-Modell des Agenten (D-55). `agents_io.py` um `read_style_md`/`write_style_md_atomic`/`read_style_note`/`write_style_note_atomic` erweitert (Klartext, D-57; `style_note.md` überlebt Re-Learn, D-54). `webui/prompts/style-extract.txt` mit den 6 D-56-Abschnitten. Drift-Guard-Muster (WR-06) für `pii.py`/`llm.py` fortgesetzt; **Deviation (Rule 3):** `provider_config.py` zusätzlich dupliziert (im Plan nur im Interfaces-Block referenziert, nicht in der Task-1-Dateiliste — beim Import blockierend sichtbar geworden, per selbem etablierten Muster gefixt). `StyleExtractionEmpty` als typisierte Exception bei < 3 verwertbaren Mails ohne Freitext; IMAP-Fehler (Login, fehlender Sent-Ordner) crashen nie (graceful, 0 Mails). TDD-Zyklen Task 2 + Task 3 vollständig RED→GREEN. **191 webui Tests grün / 3 skipped** (23 neue Tests), Agent-Suite unverändert 109/1. Commits: `3259c7c`, `a2e7029`, `2b68af3`, `4cbe443`, `4052228`, SUMMARY `d9ee7cc`. Bereit für Plan 06.03 (Endpoints/UI).
- **2026-07-17** — **Phase 6, Plan 06.03 ausgeführt** (Style-Endpoints + WebUI-Fieldset, STY-01/03/05): `POST /style/relearn` provider-agnostisch (bewusst kein Anthropic-only-Gate wie bei `/context/generate`), persistiert `style_note` VOR der Extraktion (überlebt auch einen fehlschlagenden Re-Learn-Versuch), Fehler-Kaskade `StyleExtractionEmpty`→400 (STY-05-Hinweistext)/`ValueError`→400/`RuntimeError`→500. `/save` um eigenständiges Section-Save-Fieldset (`style_md`/`style_note`/`enable_style_adaption`) erweitert. **Auto-Extraktion bei Neuanlage-Transition** (STY-01): Cred-Ist-Zustand VOR jedem Request (`creds_before_complete`) gegen den Zustand danach (`creds_after_complete`) verglichen — feuert best-effort NUR beim echten Übergang unvollständig→vollständig, graceful bei Fehlschlag (Save bleibt erfolgreich). **Esso-Guard verifiziert** (3 dedizierte Tests): migrierte Agenten mit bereits vollständigen Creds lösen beim Speichern von context.md oder einer Passwort-Rotation NIE eine Extraktion aus. `index.html` bekam ein vollständiges style-Fieldset (Enable-Checkbox Default an, Freitext-Feld, style_md-Textarea, Re-Learn-Button mit `confirm()`-Bestätigung analog `generateContext()`, Section-Save). TDD-Zyklus Task 1+2 vollständig RED→GREEN (16 neue Tests). **207 webui Tests grün / 3 skipped**. Commits: `65922d7`, `fd852ab`, `7f0613f`, SUMMARY `4bb8f8c`. Bereit für Plan 06.04 (A/B-Abnahme-Checkpoint).
- **2026-07-17** — **Phase 6, Plan 06.04 Task 1 ausgeführt** (A/B-Fixtures für den SC2-Nachweis, STY-02/STY-05 vorbereitend): `agent/tests/fixtures/style_ab/` mit `style-locker-ton.md` (Beispiel-style.md nach D-56-Schema, extrem lockerer Du-Ton), `standard-oeffnungszeiten.eml` (sachlich-neutraler Fall) und `beschwerde-verspaetung.eml` (Hierarchie-Test T-06-01) + `README.md` mit Abnahme-Ablauf (WebUI-Klick-Pfad oder direkter `generate_draft_text()`-Aufruf, echter LLM-Call). Commit `9f690cf`, SUMMARY `.planning/phases/06-schreibstil-adaption/06-04-SUMMARY.md`. **Task 2 (blockierender `checkpoint:human-verify`) bewusst NICHT ausgeführt** — braucht `docker compose up`, echten IMAP/LLM-Call und subjektive Ton-Bewertung durch den Betreiber. SC2 ist damit NOCH NICHT bewiesen, Plan 06.04 ist NICHT abgeschlossen. Nächster Schritt: Betreiber führt den Checkpoint (Klick-Pfad + A/B-Ton-Vergleich + beide Esso-Guards + STY-05-Hinweis) durch und meldet "approved" oder konkrete Abweichungen.
- **2026-07-14** — Phase-4-Nachtrag "Zero-Config-Overhaul + Live-Verification". Commits: `68d2a7a` (Zero-Config + bcrypt + UX-Overhaul, 27 Dateien), `c4986cd` (Drafts-Ordner Auto-Discovery via IMAP SPECIAL-USE, 10 Dateien), Section-Save-Buttons (in Arbeit). Kern-Änderungen: **(a)** WebUI startet ohne Vor-Config — `docker-entrypoint.sh` seedet `/config/.env` + `/config/context.md` beim ersten Start; **(b)** Login optional (Empty = kein Schutz + Warnbanner) mit bcrypt-Hashing + "Aktuelles/Neues Passwort"-Change-UX; **(c)** OWN_EMAIL_ADDRESS auto = IMAP_USER (Feld aus Formular entfernt); **(d)** Drafts-Ordner Auto-Discovery via RFC 6154 SPECIAL-USE (Fallback: provider_config); **(e)** Wait-for-Config-Loop im Agent (kein Restart-Loop bei leerer .env); **(f)** Danger-Zone / Zero-Reset-Button (löscht .env + context.md + state.db + Agent-Container); **(g)** Section-weise Save-Buttons (jedes Fieldset einzeln übernehmbar via HTMX, kein Page-Reload). **End-to-End-Test**: echte Test-Mail von privatem Gmail an IONOS-Postfach → Classify REPLY_NEEDED → Sonnet-Draft → APPEND in "Entwürfe" (via SPECIAL-USE detected) → im IONOS-Webmail sichtbar mit aktivem "Senden"-Button. **149 Tests grün** (63 agent + 86 webui + 3 neue Section-Save-Tests → 89 webui). Zombie-Container `kea-agent` (Prä-Rename) entfernt.
- **2026-07-17** — **Phase 7, Plan 07-01 ausgeführt** (SSE-Walking-Skeleton, CHAT-01/03/05): neues **webui-only** `webui/src/chat.py` als Streaming-Sibling zu `llm.py` (design_note-Reconciliation D-59 vs. D-62 vs. WR-06-Drift-Guard — `llm.py` bleibt byte-identisch unangetastet). `resolve_chat_target(agent_id)` löst Provider/Key/Modell GENAU des gewählten Agenten auf (`read_env_raw` + `crypto.decrypt_value` + wiederverwendetes `style_extract.MODEL_DRAFT_DEFAULTS`, keine neue Modell-Tabelle). `stream_chat()` streamt Anthropic/OpenAI (WR-01-Shape, `max_completion_tokens`)/Google, Fallback auf Anthropic bei unbekanntem Provider, `api_key` nie im Log. `GET /chat/{agent_id}/embed` rendert ein echt chrome-loses Partial (`chat.html`, kein `{% extends %}`, nur `/static`-Ressourcen — 404 bei unbekanntem Agent via `list_agent_ids()`). `POST /chat/{agent_id}/send` streamt SSE (`text/event-stream`, `data:`-Mehrzeilen-Encoding, `event: done`/`event: error`) — bewusst ohne Existenz-Check (ChatConfigError → 400 statt 404 bei unbekanntem-aber-validem Agent, Plan-Checker-Guidance). `webui/static/chat.js` (Vanilla fetch+ReadableStream-SSE-Client, kein CDN) + `chat.css` neu. TDD-Task-1 (RED/GREEN via test_chat.py) + Task-2-Endpoint-Tests vollständig. **225 webui Tests grün / 3 skipped** (18 neue, Baseline 207/3). Drift-Guards `test_llm_sync.py`/`test_model_defaults_sync.py` unverändert grün. Reset-Button UI-mäßig angelegt, Verhalten bewusst erst Plan 07-03. Commits: `57bcf7b`, `b749323`, SUMMARY-Commit folgt. Bereit für Plan 07-02 (System-Prompt/Wissen: context.md+style.md+Agent-Status).
- **2026-07-17** — **Phase 7, Plan 07-02 ausgeführt** (System-Prompt-Wissensinjektion, CHAT-02/CHAT-03, D-64): neues `webui/prompts/chat-system.txt` (Muster `context-seed.txt`) mit D-63-Klausel ("rein beratend", "kann keine Mails senden") + explizitem Prompt-Injection-Anker, der context.md/style.md/Status als DATEN statt Instruktion kennzeichnet (T-07-05-Mitigation). `chat.py::build_system_prompt(agent_id)` injiziert `context_md`/`style_md` (Platzhalter bei fehlender Datei) + eine über `_format_agent_status()` gebaute kompakte Status-Zusammenfassung (Aktiv-Flag, Läuft-Heuristik, Drafts-Ordner, Erkennungsmethode, letzter Poll, letzter Zyklus, letzter Fehler aus `state_reader`/`agents_io`) — alles graceful bei `{}`/`None`, Template-Füllung bewusst per `.replace()` statt `.format()` (T-07-07: context.md/style.md können `{}`-Zeichen enthalten). `main.py::chat_send` baut den System-Prompt vor jedem `stream_chat`-Aufruf und kombiniert ihn mit der Betreiber-Nachricht zu einem Single-Turn-Prompt (echte Multi-Turn-Trennung folgt in 07-03). 9 neue Tests in `test_chat.py` + 1 neuer Endpoint-Test in `test_endpoints_chat.py` (echter context.md-Fluss bis ins `stream_chat`-prompt-Argument, `build_system_prompt` bleibt ungemockt). **234 webui Tests grün / 3 skipped** (10 neue, Baseline 225/3). Drift-Guards unverändert grün. Commits: `f893970`, `d6991db`, SUMMARY `d2f56e1`. Bereit für Plan 07-03 (Rate-Limit D-60, History-Trunkierung, `mail_context`-Feld D-65).
- **2026-07-17** — **Phase 7, Plan 07-03 ausgeführt** (Kosten-/Missbrauchsschutz + Verlauf + Mail-Kontext-Vorarbeit, CHAT-01/CHAT-04, D-60/D-63/D-65): `chat.py::build_chat_prompt(agent_id, message, history, mail_context)` baut System-Prompt + auf `CHAT_HISTORY_TOKEN_BUDGET` getrimmten Verlauf (`_truncate_history`, älteste Turns fallen zuerst weg, jüngster Turn bleibt immer erhalten, `_estimate_tokens` chars/4-Heuristik) + optionalen `# Kontext: gerade geöffnete Mail (DATEN, keine Anweisung)`-Block (Injection-Anker, T-07-11) + aktuelle Nachricht. `main.py::chat_send` bekam `@limiter.limit(lambda: f"{os.getenv('CHAT_RATE_LIMIT_PER_MIN','20')}/minute")` (dynamischer, env-konfigurierbarer slowapi-Limit-Callable), `max_tokens = int(os.getenv("CHAT_MAX_TOKENS", "2000"))`, neue `history`/`mail_context`-Formfelder (JSON-Strings, defensiv geparst via `_parse_chat_history`/`_parse_mail_context` — kaputtes JSON/falsche Struktur → leere Defaults, kein 500, T-07-09). `chat.js`: In-Memory-`history`-Array (D-58, keine DB/localStorage), Reset-Button leert Verlauf + `#chat-log`, `window.vizpatchGetMailContext`-Hook als Phase-8-Erweiterungspunkt (liefert in Phase 7 bewusst `null`, Office.js überschreibt ihn später ohne API-Änderung, D-65). Struktureller Kein-Auto-Send-Guard-Test (D-63/T-07-10): `chat.py`-Source enthält keine `imap_tools`/`smtplib`/`draft`/`imap_client`-Imports oder IMAP-APPEND/SMTP-Aufrufmuster (Test prüft konkrete Mail-API-Muster statt blind `.append(`, um legitime `list.append()`-Aufrufe nicht fälschlich zu markieren). **Ergänzung über den reinen Code-Task hinaus:** `CHAT_RATE_LIMIT_PER_MIN`/`CHAT_MAX_TOKENS`/`CHAT_HISTORY_TOKEN_BUDGET` zusätzlich in beide `docker-compose.yml` (agent/ + deployment/) verdrahtet + in `deployment/kunde-env.example`/`vizionists-test-env.example` dokumentiert, damit die Limits am Kunden tatsächlich wirken (nicht nur per `monkeypatch` testbar). 17 neue Tests (8 in `test_chat.py`, 9 in `test_endpoints_chat.py`). **251 webui Tests grün / 3 skipped** (Baseline 234/3). Drift-Guards unverändert grün. Commits: `cfbd0b9`, `6985c17`, SUMMARY `ce1ca11`. Bereit für Plan 07-04 (Haupt-WebUI-Chat-Einbettung, letzter Plan der Phase 7).
- **2026-07-17** — **Phase 7, Plan 07-04 ausgeführt** (Haupt-WebUI-Chat-Integration + automatisierter Einbettbarkeits-Nachweis, CHAT-01/CHAT-05 — **letzter Plan der Phase, Phase 7 damit vollständig abgeschlossen**): `webui/src/templates/_chat.html` als neue, einzige Markup-Quelle des Chat-Bodys extrahiert — `chat.html` (embed-Route) UND `index.html` (Haupt-WebUI) includen beide `{% include "_chat.html" %}` statt das Markup zu duplizieren (T-07-14). `index.html` bekam einen neuen `section.chat-section` innerhalb des bestehenden `{% if agent_id %}`-Blocks (Chat erscheint nur bei gewähltem, existierendem Agent), lädt `/static/chat.css`+`/static/chat.js` einmalig zusätzlich. **Rule-1-Bugfix während Task 1:** `chat.css`s globale `html, body`/`100vh`-Regeln (aus 07-01, korrekt für den eigenständigen chrome-losen Rahmen) hätten beim Laden auf `index.html` das gesamte Haupt-WebUI-Layout überschrieben — auf `body.chat-standalone` gescopet, im eingebetteten Kontext bekommt `#chat-root` stattdessen eine feste 480px-Höhe. `webui/tests/fixtures/embed_test.html` neu: nackte, chrome-lose Test-HTML-Seite als Phase-8-Vorarbeit (demonstriert Fremd-Host-Einbettung, referenziert nur lokale/relative Dateien). Automatisierter CHAT-05-Nachweis in `test_endpoints_chat.py`: regex-basierter Scan aller `src=`/`href=`/`url(...)`-Referenzen im embed-Body + `chat.js`/`chat.css` auf `http://`/`https://`/protokoll-relative `//`-Präfixe — muss leer sein (T-07-12); zusätzlich Prüfung, dass der embed-Body kein `<h1>Vizpatch`-Chrome zeigt. CHAT_*-Env-Doku (D-60) war bereits vollständig aus Plan 07-03 vorhanden — nur verifiziert, keine Änderung nötig. 5 neue Tests (3 Task 1, 2 Task 2). **256 webui Tests grün / 3 skipped** (Baseline 251/3). Drift-Guards unverändert grün. Commits: `224566e`, `195efe0`, SUMMARY `0bd8a6f`. **Phase 7 (Agenten-Chat im WebUI, v1.3) komplett** — Phase 8 (Outlook-Add-in) kann auf `_chat.html`/`/chat/{agent_id}/embed` + `window.vizpatchGetMailContext` aufsetzen, keine API-Änderung nötig.
- **2026-07-17** — **Phase 8, Plan 08-01 ausgeführt** (Taskpane-Serving-Route + pfad-abhängige CSP, OUT-02, D-66): `webui/src/main.py::add_security_headers` klassifiziert jeden Pfad — `/addin/*` und `/chat/<segment>/embed` bekommen eine gelockerte CSP (`X-Frame-Options` entfernt, `frame-ancestors` von `'none'` auf `'self'` + feste Office/Outlook-Domains, env-override `ADDIN_FRAME_ANCESTORS`, kein `*`-Wildcard — T-08-01), alle anderen Pfade (u. a. `/healthz`) behalten die strikte Policy unverändert (T-08-02, Regressionstest). Neue Route `GET /addin/taskpane.html` (auth-geschützt) rendert `addin_taskpane.html`: Agent-Dropdown + iframe auf `/chat/{agent}/embed` (same-origin) + office.js-Tag von `https://appsforoffice.microsoft.com/lib/1/hosted/office.js` als einzige externe Ressource der Seite (T-08-SC), belegt durch einen No-external-resource-Wächter analog Phase 7. `script-src`-Erweiterung um die Office.js-CDN-Origin ausschließlich auf `/addin/`-Pfaden — `/chat/*/embed` bleibt ohne CDN-Freigabe (T-08-03). 11 neue Tests (6 in `test_security.py`, 5 in `test_endpoints_addin.py`). **266 webui Tests grün / 3 skipped** (Baseline 256/3). Drift-Guards unverändert grün. Commits: `7208d10`, `1bece2e`, SUMMARY `bedcc13`. **Hinweis:** vor Ausführungsbeginn war `.planning/STATE.md` bereits mit einer reinen Frontmatter-Zählerkorrektur (`total_plans: 26→30`, kein Prosa-Inhalt) außerhalb dieser Ausführung modifiziert — per `git checkout` zurückgesetzt, Zähler dieser Ausführung manuell auf `total_plans: 27`/`completed_plans: 27` konsistent nachgezogen (die übrigen 3 offenen Phase-8-Pläne 08-02…08-04 zählen erst bei ihrer eigenen Ausführung). Bereit für Plan 08-02 (XML-Manifest + Office.js-Mail-Kontext via `postMessage`).
- **2026-07-17** — **Phase 8, Plan 08-02 ausgeführt** (XML-Manifest + Office.js-Mail-Kontext via `postMessage`, OUT-01/OUT-03/OUT-04-Code-Anteil, D-67/D-69/D-70): `webui/src/templates/addin_manifest.xml` neu — klassisches `OfficeApp`/`MailApp`-Manifest, `{ADDIN_BASE_URL}`-Platzhalter, `Permissions=ReadItem` (keine ReadWrite-Berechtigung), `Requirements/Sets Mailbox MinVersion=1.5` — **verifiziert per Context7-Dokumentations-Lookup** (`/officedev/office-js-docs-reference`), dass 1.5 tatsächlich die korrekte Mindest-Anforderung für `Office.EventType.ItemChanged` ist (löst die Plan-Checker-Warnung mit einem Beleg statt einer Annahme). Neue Route `GET /addin/manifest.xml` (auth-geschützt): validiert `ADDIN_BASE_URL` (https://-Pflicht, keine XML-Sonderzeichen, T-08-06) und ersetzt per `str.replace` — reine Textdatei, kein Jinja2-Autoescape-Konflikt mit XML. `webui/static/addin/taskpane.js` neu: liest `item.subject`/`item.from.emailAddress`/`item.body.getAsync(Office.CoercionType.Text)` rein lesend und postet `{type:'vizpatch-mail-context',...}` mit expliziter `targetOrigin=window.location.origin` (nie `'*'`, T-08-04) an `#addin-chat-frame`; `ItemChanged`-Handler + iframe-`load`-Repost decken Mailwechsel ab. `chat.js` bekam einen `message`-Listener mit `event.origin`-Prüfung, der `window.vizpatchGetMailContext` über eine `lastMailContext`-Closure-Variable befüllt — der Phase-7-`sendMessage()`-Pfad hängt `mail_context` dadurch automatisch an, ohne selbst geändert zu werden. Struktureller Kein-Auto-Send-Wächter (`test_addin_readonly.py`, analog Phase-7-Muster mit Kommentar-Filterung): belegt per Positiv-/Negativ-Fall, dass `taskpane.js` keine Office-Schreib-/Compose-/Send-APIs (`setAsync`/`saveAsync`/`displayReplyForm`/`displayReplyAllForm`/`displayNewMessageForm`/`makeEwsRequestAsync`/`sendAsync`) enthält und das Manifest ausschließlich `ReadItem` erlaubt. **2 kleine Rule-1/Rule-2-Fixes während Task 1:** (a) minimale PNG-Icons per Pillow generiert, da kein Icon-Asset im Repo existierte (kein toter `IconUrl`-Pfad); (b) ein XML-Kommentar, der selbst das Substring "ReadWriteItem" enthielt, kollidierte mit dem eigenen Abwesenheits-Test — umformuliert. 18 neue Tests (6 Manifest, 6 Mail-Kontext, 6 Read-only-Wächter). **284 webui Tests grün / 3 skipped** (Baseline 266/3). Drift-Guards unverändert grün. **OUT-03 vollständig abgehakt** in REQUIREMENTS.md; OUT-01/OUT-04 bewusst NICHT abgehakt (ihr Text verlangt zusätzlich Sideloading-Doku bzw. HTTPS-Runbook-Kapitel, die erst in 08-03 entstehen — dieser Plan liefert nur den Code-/Struktur-Anteil). Commits: `fc64295`, `8ea56af`, `ae29c05`, SUMMARY-Commit folgt. **Hinweis:** `roadmap.update-plan-progress 8` hat `total_plans` erneut fälschlich auf 30 gesetzt (Phase-8-`plan_count=4` statt der bisher ausgeführten 2) — per `git checkout` zurückgesetzt, Zähler manuell auf `total_plans: 28`/`completed_plans: 28` nachgezogen. Bereit für Plan 08-03 (HTTPS-Runbook + Sideloading-/M365-Doku + Auth-Fluss + Deployment-Template-Env).
- **2026-07-17** — **Phase 8, Plan 08-03 ausgeführt** (HTTPS-Runbook + Sideloading-/M365-Doku + Auth-Fluss, OUT-01/OUT-02/OUT-04-Doku-Anteil, D-70): `deployment/README.addin.md` neu — vier Kapitel: (1) HTTPS vor der WebUI (Caddy-Reverse-Proxy, Let's-Encrypt für öffentliche Domain bzw. `tls internal` für LAN-only, Ports/Firewall, expliziter Hinweis dass der Reverse-Proxy die `frame-ancestors`-CSP der Add-in-Routen aus 08-01 nicht überschreiben darf), (2) `ADDIN_BASE_URL` setzen (.env-Eintrag, Neustart, Erreichbarkeits-Check von `/addin/taskpane.html` + `/addin/manifest.xml`), (3) Add-in verteilen (Sideloading in neuem Outlook + OWA via „Eigenes Add-In hinzufügen"/„Aus Datei", zentrale M365-Admin-Center-Verteilung als Alternative für Global-/Exchange-Admins), (4) Auth-Fluss (bestehendes WebUI-Basic-Auth im iframe/Taskpane-Fenster, D-68) + explizite Read-only-/Kein-Auto-Send-Klarstellung (D-70). `deployment/Caddyfile.example` neu: Reverse-Proxy-Vorlage mit zwei Zertifikats-Varianten (öffentliche Domain/automatisches Let's-Encrypt vs. LAN/`tls internal`), `reverse_proxy` auf Port 8080. `ADDIN_BASE_URL`/`ADDIN_FRAME_ANCESTORS` in `deployment/docker-compose.phase4.yml` + `deployment/kunde-env.example` verdrahtet (Plan-Vorgabe) — **plus Konsistenz-Fix (Rule 2):** dieselbe Ergänzung zusätzlich in `agent/docker-compose.yml` (lokale Dev-Compose) und `deployment/vizionists-test-env.example` (interne Test-Umgebung), analog dem 07-03-Präzedenzfall für `CHAT_*`, um Config-Drift zu vermeiden. `webui/tests/test_addin_docs.py` neu: 4 deterministische Doku-Wächter-Tests (README-Schlüsselbegriffe case-insensitive, Caddyfile `reverse_proxy`+`8080`, kunde-env.example-Vars, Compose-YAML per `yaml.safe_load` statt Roh-Grep). **288 webui Tests grün / 3 skipped** (Baseline 284/3, +4 neue). Drift-Guards unverändert grün. **OUT-01, OUT-02, OUT-04 jetzt vollständig abgehakt** in REQUIREMENTS.md — die jeweils in 08-01/08-02 offen gelassenen Doku-Anteile (Sideloading/M365, Auth-Fluss, HTTPS-Runbook) sind geliefert; die separate Live-Sideload-Abnahme (D-71) bleibt Plan 08-04. Commits: `1c7c53f`, `ea1d78c`, `43e219f`, SUMMARY-Commit folgt. **Letzter offener Plan der Phase: 08-04 (menschlicher Sideload-Abnahme-Checkpoint).**
- **2026-07-18** — **Phase 9 geplant** (Agentischer Chat mit Postfach-Werkzeugen, v1.5): `09-CONTEXT.md` (D-72..D-81 — Anthropic-Tool-Use mit Fallback für andere Provider, webui-only `chat_tools.py`-Drift-Guard D-73, Werkzeugsatz mails_suchen/mail_lesen/entwuerfe_auflisten/entwurf_lesen/entwurf_bearbeiten/Papierkorb-Move mit Bestätigungs-Gate, PII-Redaction + Untrusted-DATEN-Anker für Tool-Ergebnisse, Kein-Auto-Send bleibt strukturell). 5 Plans in 5 sequentiellen Waves (09-01 Walking Skeleton → 09-02 Entwürfe-Tools → 09-03 entwurf_bearbeiten → 09-04 Papierkorb-Tools mit Bestätigungs-Gate → 09-05 Doku-Angleichung Datenschutz/AVV). Requirements CTOOL-01…05 in REQUIREMENTS.md. Plan-Checker-Revision (Blocker 1 + Warnings 2/3 behoben). Commits `63de93f`, `6edece7`, `f2e50de`.
- **2026-07-18** — **Phase 9, Plan 09-01 ausgeführt** (Walking Skeleton: Anthropic-Tool-Use-Schleife + `mails_suchen` + SSE-Tool-Aktivität, CTOOL-01/02): neues **webui-only** `webui/src/chat_tools.py` (D-73-Drift-Guard: importiert `chat`/`crypto`/`pii`/`provider_config`, nie `llm.py`). `open_agent_mailbox()` baut den per-Agent-IMAP-Verbindungsblock aus `style_extract.py` nach (Fernet-entschlüsselte Creds, `_IMAP_TIMEOUT_SECONDS`, D-79). `mails_suchen(agent_id, query, folder, limit)` — erstes read-only-Werkzeug (D-74 Teil 1): INBOX-Volltextsuche via `imap_tools.AND(text=...)`, redigiert jeden Body via `pii.redact()` VOR der Rückgabe (D-78/T-09-02), crasht nie hart bei IMAP-/Login-/Fetch-Fehlern (T-09-05). `TOOL_SCHEMAS`/`TOOL_HANDLERS`-Registry + `wrap_tool_result()` (Untrusted-DATEN/Injection-Anker, D-78/T-09-01) als Erweiterungs-Kontrakt für 09-02…09-04. `run_agentic_chat()`: Anthropic-`messages.create(tools=...)`-Runden-Schleife mit `MAX_TOOL_ROUNDS=5`-Obergrenze (T-09-04, Endlosschutz — terminiert mit erklärendem Text statt Endlos-Loop), sauberer Fallback auf `chat.build_chat_prompt`+`chat.stream_chat` für jeden Nicht-Anthropic-Provider oder `ENABLE_CHAT_TOOLS=false` (D-72/T-09-06, kein Absturz), unbekannter Tool-Name → Fehlertext statt Crash, `api_key` nie in einem Logger-Aufruf (T-09-03, Quelltext-Scan-Test). `main.py::chat_send` ruft jetzt `chat_tools.run_agentic_chat` statt `chat.stream_chat` direkt; SSE übersetzt Tool-Events zu einem eigenen `event: tool`-Frame (D-80) und Text-Events wie bisher zu `data:`-Frames — **PLAN-CHECKER-W1 umgesetzt:** `chat.resolve_chat_target(agent_id)` bleibt EAGER vor dem `StreamingResponse`-Aufbau, sonst würde ein invalider `agent_id`/`ChatConfigError` erst mitten im Stream statt als 400 auftauchen (Phase-7-Regression). `chat.js` zeigt Tool-Aktivität als eigene, dezente Statuszeile im Chat-Log. `chat-system.txt`: Rolle von "rein beratend" zu "beratend UND handelnd auf ausdrückliche Anweisung" erweitert, Injection-Anker deckt jetzt auch Werkzeug-Ergebnisse ab, Kein-Auto-Send-Klausel bleibt explizit. **Test-Anpassung:** `test_endpoints_chat.py`s `_write_agent`-Helper-Default auf `provider="openai"` umgestellt, damit bestehende SSE-Mechanik-Regressionstests weiterhin über den (jetzt in `chat_tools._run_fallback_chat` gekapselten) Fallback-Pfad mit gemocktem `chat.stream_chat` laufen — die eigentliche Anthropic-Tool-Use-Schleife wird separat in `test_chat_tools.py` mit gemocktem `anthropic.Anthropic` getestet. 18 neue Tests (16 in `test_chat_tools.py`, 2 in `test_endpoints_chat.py`). **314 webui Tests grün / 3 skipped** (Baseline 296/3). Drift-Guard-Suiten (`test_llm_sync.py`/`test_pii_sync.py`/`test_crypto_sync.py`/`test_provider_config_sync.py`/`test_model_defaults_sync.py`) unverändert grün, `git diff` gegen `webui/src/{llm.py,pii.py,crypto.py,provider_config.py}` leer. Commits: `596b97f`, `5e126b6`, `7c5980e`, SUMMARY-Commit folgt. **Hinweis:** Frontmatter-Zähler `completed_plans` war vor Ausführungsbeginn bereits fälschlich auf 30 hochgezählt (Baseline 29 + 1 ohne abgeschlossenen Plan) und wurde durch einen eigenen SDK-Aufruf versehentlich auf 31 weitergezählt — manuell auf den korrekten Wert **30** (29 Baseline + dieser eine abgeschlossene Plan) zurückgesetzt; `total_plans: 35`/`total_phases: 9`/`completed_phases: 5` aus der Phase-Planung übernommen (5 neue Phase-9-Pläne). Bereit für Plan 09-02 (Entwürfe-Werkzeuge: `mail_lesen`, `entwuerfe_auflisten`, `entwurf_lesen`).
- **2026-07-18** — **Phase 9, Plan 09-02 ausgeführt** (Entwürfe-Werkzeuge: `mail_lesen`/`entwuerfe_auflisten`/`entwurf_lesen` + Drafts-Ordner-SPECIAL-USE-Erkennung, CTOOL-02 vollständig): `_detect_drafts_folder(mailbox, fallback)` erkennt `\Drafts` via SPECIAL-USE (RFC 6154), analog `style_extract._detect_sent_folder` (D-79); `_resolve_drafts_folder(mailbox, env)` davor `IMAP_DRAFTS_FOLDER`-Env-Override, sonst `provider_config`-Fallback über die um einen `drafts`-Schlüssel erweiterte `_agent_imap_settings()`. `mail_lesen(agent_id, uid, folder="INBOX")` liest eine einzelne Mail vollständig (PII-redigierter, gekürzter Body, D-78/T-09-07), unbekannte uid/IMAP-Fehler → `{"fehler": ...}`, kein Crash. `entwuerfe_auflisten(agent_id, limit)` listet den erkannten Drafts-Ordner NUR mit Metadaten (uid/an/betreff/datum) — kein Body (Datenminimierung, T-09-08); fehlender Ordner → leere Liste. `entwurf_lesen(agent_id, uid)` liest einen Entwurf inkl. PII-redigiertem Body UND den Threading-Headern (`in_reply_to`/`references`, aus `msg.headers` extrahiert analog `agent/src/draft.py::build_reply_draft`) — Grundlage für 09-03s `entwurf_bearbeiten`, das denselben Thread beim Neu-Anlegen erhalten muss. Alle vier read-only-Werkzeuge (inkl. `mails_suchen` aus 09-01) jetzt vollständig in `TOOL_SCHEMAS`/`TOOL_HANDLERS` registriert, `run_agentic_chat` dabei unverändert (generischer Dispatch über `TOOL_HANDLERS.get(block.name)` bereits aus 09-01 vorbereitet). 15 neue Tests in `test_chat_tools.py` (SPECIAL-USE-Treffer/Fallback/Exception, PII-Redaction für `mail_lesen`/`entwurf_lesen`, Datenminimierung für `entwuerfe_auflisten`, unbekannte uid/fehlender Ordner → `fehler`-dict statt Crash, invalider `agent_id` → `ValueError`). **329 webui Tests grün / 3 skipped** (Baseline 314/3). Drift-Guard-Suiten unverändert grün, `git diff` gegen `webui/src/{llm.py,pii.py,crypto.py,provider_config.py}` + `agent/src/{imap_client.py,draft.py}` leer. Commits: `af32cee`, `aac3c35`, SUMMARY `c625c77`. **CTOOL-02 vollständig abgehakt** in REQUIREMENTS.md. **Hinweis:** `state.advance-plan`/`state.update-progress` schlugen mit einem Parse-Fehler fehl (STATE.md-Prosaformat statt des von der SDK erwarteten "Current Plan: X of Y"-Musters) und ein vorheriger fehlgeschlagener `state.advance-plan`-Aufruf hatte `completed_plans` bereits fälschlich auf 32 hochgezählt und `last_updated` auf einen früheren Zeitstempel zurückgesetzt — per `git checkout -- .planning/STATE.md` zurückgesetzt, Zähler danach manuell auf den korrekten Wert **31** (30 Baseline + dieser eine abgeschlossene Plan) gesetzt; `roadmap.update-plan-progress 9` lief hingegen korrekt (2/5 Plans). Bereit für Plan 09-03 (`entwurf_bearbeiten`).
- **2026-07-18** — **Phase 9, Plan 09-03 ausgeführt** (`entwurf_bearbeiten` + Papierkorb-Erkennung/Move-Helfer, CTOOL-03): `_detect_trash_folder(mailbox, fallback=None)` erkennt `\Trash` via SPECIAL-USE, sonst prüft eine feste Kandidatenliste (`Trash`/`Papierkorb`/`Deleted Items`/`[Gmail]/Trash`/`INBOX.Trash`) GEGEN die tatsächliche Ordnerliste (anders als bei Drafts kein blindes Zurückfallen) — kein Treffer → typisierte `TrashFolderNotFound` (D-76, nie raten/anlegen). `_move_to_trash(mailbox, uid, source_folder)` führt reines IMAP MOVE aus, niemals expunge/delete (T-09-13). `entwurf_bearbeiten(agent_id, uid, neuer_text, neuer_betreff=None)` (D-75): liest den Original-Entwurf, baut per `_build_edited_draft` ein RFC-5322-Rebuild analog `agent/src/draft.py::build_reply_draft` — neues Message-ID, aber In-Reply-To/References UNVERÄNDERT aus dem Original übernommen (Threading erhalten) —, APPENDet die neue Fassung mit `\Draft`-Flag und verschiebt ERST DANACH das Original per `_move_to_trash` (Reihenfolge APPEND→MOVE: die neue Fassung liegt immer sicher, bevor das Original verschwindet). Kein Senden (D-77), strukturell kein SMTP-Pfad. In `TOOL_SCHEMAS`/`TOOL_HANDLERS` registriert, `run_agentic_chat` unverändert. **Commits pro Task (Abweichung von 09-01/09-02s Sammel-Commit-Muster):** Task 1 (Papierkorb-Erkennung/Move-Helfer) und Task 2 (`entwurf_bearbeiten`) je einzeln committet, da Task 1 eigenständig testbar/für 09-04 wiederverwendbar ist. **1 kleiner Doku-Fix während Task 1:** Docstring an `_move_to_trash` traf versehentlich das eigene Akzeptanzkriterium (`grep -c "expunge\|\.delete("` sollte 0 sein) — auf "EXPUNGE"/"Delete-Aufruf" umformuliert, funktional unverändert. 12 neue Tests in `test_chat_tools.py` (Papierkorb-SPECIAL-USE/Kandidaten-Fallback/Exception, Move-Assertion inkl. Expunge/Delete-Abwesenheit, Threading-Erhalt auf den tatsächlichen APPEND-Bytes, kein Trash → Fehler ohne Move, unbekannte uid, leerer Text, invalider `agent_id`, kein SMTP/Send im Quelltext, Registry-Eintrag); Registry-Test von 4- auf 5-Tool-Satz erweitert. **341 webui Tests grün / 3 skipped** (Baseline 329/3). Drift-Guard-Suiten unverändert grün, `git diff` gegen `webui/src/{llm.py,pii.py,crypto.py,provider_config.py}` + `agent/` leer. Commits: `a639a6f`, `364125e`, SUMMARY-Commit folgt. **CTOOL-03 vollständig abgehakt** in REQUIREMENTS.md. **Hinweis:** `state.advance-plan`/`state.update-progress` schlugen erneut mit Parse-Fehlern fehl, UND `state.advance-plan` hatte trotz Fehlermeldung `completed_plans` bereits fälschlich auf 33 hochgezählt (sollte 32 sein) sowie `percent` auf einen inkonsistenten Wert (56) gesetzt — Zähler manuell auf den korrekten Wert **32** (31 Baseline + dieser eine abgeschlossene Plan) und `percent: 62` (Fortschreibung des historischen Musters +3 pro Plan ohne Phasenabschluss) korrigiert; `roadmap.update-plan-progress 9` lief korrekt (3/5 Plans). Bereit für Plan 09-04 (Papierkorb-Tools mit Bestätigungs-Gate).
- **2026-07-18** — **Phase 9, Plan 09-04 ausgeführt** (`mail_in_papierkorb`/`entwurf_in_papierkorb` mit gehärtetem Bestätigungs-Token-Gate, CTOOL-04 vollständig): destruktive Werkzeuge verschieben per `_move_to_trash` (nie Expunge) in den Papierkorb — **W2-Hardening statt der PLAN.md-Literalspezifikation** (bloßes `confirmed is True`): das Plan-Checker-Warning zu 09-04 verlangte explizit ein backend-erzeugtes, an das exakte Ziel gebundenes Token statt eines vom Modell selbst gesetzten Booleans (Prompt-Injection-Risiko aus Mail-Inhalt). Umgesetzt als `_confirmation_token(agent_id, tool, uid, folder)` (HMAC-SHA256, 32 Hex-Zeichen, abgeleitet aus dem bereits vorhandenen persistenten Fernet-Key `crypto._load_or_create_key`, SEC-01/02 — zustandslos, kein neuer Server-Store) + `_confirmation_ok(...)` (`confirmed is True` UND `hmac.compare_digest`-Token-Match). Ohne gültiges Gate: Lese-Fetch der uid liefert Zielbeschreibung (Betreff/Absender/Datum/Ordner) + das gültige Token zurück, OHNE Move. Jede ausgeführte Verschiebung wird protokolliert (`logger.info("mail_moved_to_trash"/"draft_moved_to_trash", ...)`, uid/Ordner, kein Mailtext/Secret). Beide Werkzeuge in `TOOL_SCHEMAS`/`TOOL_HANDLERS` registriert (deutsche Beschreibung mit expliziter Zwei-Schritt-Regel fürs LLM). **Task 2** bewies den vollständigen Zwei-Runden-Flow end-to-end durch `run_agentic_chat` (zwei separate Aufrufe simulieren zwei Chat-Turns) sowie — als dokumentierte Verbesserung gegenüber der im PLAN.md selbst benannten Grenze — dass ein bare `confirmed=true` OHNE vorherigen Token-Schritt jetzt technisch NICHTS mehr bewegt (nicht nur durch die System-Prompt-Regel verhindert), und dass `MAX_TOOL_ROUNDS` auch bei wiederholten destruktiven Tool-Anfragen greift. 20 neue Tests in `test_chat_tools.py`. **361 webui Tests grün / 3 skipped** (Baseline 341/3). Drift-Guard-Suiten unverändert grün, `git diff` gegen `webui/src/{llm.py,pii.py,crypto.py,provider_config.py}` + `agent/` leer (`crypto.py` nur AUFGERUFEN via `crypto._load_or_create_key()`, nicht verändert). Commit: `0645cba`, SUMMARY-Commit folgt. **CTOOL-04 vollständig abgehakt** in REQUIREMENTS.md. **Hinweis:** `state.advance-plan`/`state.update-progress` schlugen erneut fehl UND hatten trotz Fehlermeldung `completed_plans` still auf 34 hochgezählt (sollte 33 sein) sowie `percent` auf einen inkonsistenten Wert (56) gesetzt — Zähler manuell auf den korrekten Wert **33** (32 Baseline + dieser eine abgeschlossene Plan) und `percent: 65` (Fortschreibung des historischen Musters +3 pro Plan ohne Phasenabschluss) korrigiert; `roadmap.update-plan-progress 9` lief korrekt (4/5 Plans). Bereit für Plan 09-05 (struktureller Kein-Auto-Send-Wächter + Datenschutz-/AVV-Angleichung + Phasen-Verifikation).
- **2026-07-18** — **Phase 9, Plan 09-05 ausgeführt** (struktureller Kein-Auto-Send-AST-Wächter + Datenschutz-Ziffer-6/AVV-§6.2-Angleichung + Phasen-Abschluss-Verifikation, CTOOL-05 vollständig — **letzter Plan der Phase, Phase 9 damit vollständig abgeschlossen**): `test_chat_tools.py` bekam einen AST-basierten Kein-Auto-Send-Wächter statt der im Plan wörtlich vorgeschlagenen `'#'`-Kommentarzeilen-Filterung — Deviation (Rule 1), da `chat_tools.py` an Zeile 438 eine reale Docstring-Erwähnung „kein Sende-Pfad, kein SMTP (D-77)" enthält, die ein naiver Zeilenfilter NICHT ausgeschlossen und den Wächter beim ersten Lauf selbst-invalidiert hätte. `_scan_ast_for_forbidden_smtp_send_api` läuft über `ast.parse`/`ast.walk` und findet echte `import smtplib`/`sendmail(...)`/`send_message(...)`/`SMTP(...)`-Knoten, ignoriert Docstrings/Kommentare strukturell. Analog wurde der TOOL_SCHEMAS-Beschreibungs-Scan von reinem Substring-Test auf Wortgrenzen-Regex (`\b`) umgestellt, weil die realen Tool-Beschreibungen aus 09-01..09-04 bereits „Absender" (enthält Teilstring „send") und „In-Reply-To" (enthält Teilstring „reply") enthalten — mit einer kleinen Allowlist für „Sendet NICHTS"/„Kein-Auto-Send"/„In-Reply-To". Jeder Scan-Helfer kommt mit Positiv-/Negativ-/Gegenprobe-Tests (analog Phase-8-Muster `test_addin_readonly.py`). `TOOL_HANDLERS`/`TOOL_SCHEMAS`-Whitelist-Assertion (exakt 7 Werkzeuge) ergänzt. `_datenschutz.html` Ziffer 6 komplett neu formuliert (Liste statt Prosa): suchen/lesen (PII-redigiert), Entwurf umformulieren, Papierkorb-Move nur nach ausdrücklicher Bestätigung (kein Expunge) — Kein-Auto-Send bleibt explizit. `AVV-CHECKLIST.md` §6.2 bekam einen neuen Punkt „Agentische Postfach-Werkzeuge (Phase 9)" mit demselben Verarbeitungszweck, Muster-/Prüf-Rahmen unverändert. 7 neue Tests in `test_chat_tools.py`. **368 webui Tests grün / 3 skipped** (Baseline 361/3). Alle 5 Drift-Guard-Sync-Suiten grün, `git diff` gegen `webui/src/{llm.py,pii.py,crypto.py,provider_config.py}` + `agent/` leer. Datenschutz-Endpoint-Test (`test_endpoints_datenschutz.py`) unverändert grün (8 passed). Commits: `639b58d` (Task 1), `a38bb3d` (Task 2), SUMMARY `ae0a60c`. **CTOOL-05 vollständig abgehakt** in REQUIREMENTS.md — **CTOOL-01…05 damit alle erfüllt, Phase 9 (v1.5) code-komplett.** **Hinweis:** `state.advance-plan` schlug erneut mit demselben Parse-Fehler fehl UND hatte trotzdem `completed_plans` fälschlich auf 35 (sollte 34 sein) sowie `completed_phases` auf 6 hochgezählt (Letzteres zufällig korrekt, da dieser Plan tatsächlich Phase 9 abschließt) — per `git checkout -- .planning/STATE.md` zurückgesetzt, Zähler manuell auf die korrekten Werte **completed_plans: 34** (33 Baseline + dieser eine abgeschlossene Plan), **completed_phases: 6**, **percent: 68** (Fortschreibung des historischen +3-Musters) gesetzt. **Nächster Fokus bleibt der Esso-Rollout** (Deployment-Paket v1.2.0, DSGVO/AVV-Abschluss mit Anthropic, Vor-Ort-Termin) — kein weiterer Feature-Ausbau vorgesehen, sofern nicht explizit beauftragt.
- **2026-07-20** — **Phase 8 (Neuplanung COM/VSTO), Plan 08-01 ausgeführt** (Kern-Transportbibliothek `VizpatchAddin.Core`, OUT-06/OUT-07/OUT-09-Transport-/Sicherheits-Anteil): neues Verzeichnis `outlook-addin/` mit klassischer `VizpatchOutlookAddin.sln` + zwei SDK-style-net48-Projekten (`VizpatchAddin.Core` COM-frei + `VizpatchAddin.Tests` xUnit). **SseLineParser** (höchstes Risiko) bildet das exakte Server-SSE-Framing aus `webui/src/main.py` (`_sse_data_frame`/`chat_send`) deterministisch ab (message/tool/done/error, Multi-Line-`data:` mit `\n`, `TrimStart`, `id:`/`retry:` ignoriert), rein string-basiert. **ChatClient** baut den Form-POST an `{BackendUrl}/chat/{AgentId}/send` (message/history/mail_context/session_id), setzt den **CSRF-Origin-Header** (== `AddinOriginToken`, Default `https://outlook.office.com` — in `ADDIN_FRAME_ANCESTORS` gelistet, umschifft den 403-Befund OHNE Backend-Änderung) + Basic-Auth (base64 `user:pass`), streamt via `HttpCompletionOption.ResponseHeadersRead` + `Timeout.InfiniteTimeSpan` zeilenweise durch den SseLineParser; TLS-Trust ausschließlich pro `HttpClientHandler` gescoped (Thumbprint-Pinning Default, Blanket-Trust nur bei explizitem `TrustAnyCertificate`), kein globaler prozessweiter Callback, kein UI-Kontext-Detachment. **SecureSettingsStore** legt das Basic-Auth-Passwort ausschließlich DPAPI-verschlüsselt (`DataProtectionScope.CurrentUser`, Base64) unter `%AppData%\Vizpatch\OutlookAddin\settings.json` ab (Round-Trip-Test belegt: kein Klartext in der Datei). `SessionIdGenerator` (GUID pro Sitzung, analog `chat.js`), `MailContext`/`ChatTurn`-DTOs (Backend-JSON-Feldnamen subject/sender/body bzw. role/content). **Toolchain-Abweichung:** die Session-Maschine hat jetzt dotnet SDK 10.0.302 + .NET-Framework-4.8-Targeting-Packs — daher wurden `dotnet restore`/`build`/`test` REAL ausgeführt (nicht nur grep-Struktur-Assertions): **22/22 xUnit-Tests grün** (MailContext 2, AddinSettings 3, SseLineParser 10, ChatClientRequest 7). **Deviations (Rule 3):** (a) `System.Net.Http`-BCL-Referenz in beide net48-SDK-csproj ergänzt (Build-Fix CS0234/CS0246); (b) `dotnet new sln` erzeugte `.slnx` → auf klassisches `.sln` umgestellt; (c) Doc-Kommentare umformuliert, weil sie die verbotenen grep-Literale (`HttpClient`/`ServicePointManager`/`ConfigureAwait(false)`) als Beschreibung enthielten. Der VSTO-COM-Teil (Ribbon, Custom Task Pane, Outlook-Objektmodell) bleibt Plan 08-02; Live-Abnahme in echtem Outlook classic = menschlicher Checkpoint (D-89). Commits: `e0ebb11` (Task 1), `c324b2b` (Task 2), `4d41cad` (Task 3), SUMMARY-Commit folgt. `roadmap.update-plan-progress 08 08-01 complete` lief korrekt (1/4 Plans, Phase-Status „In Progress"). **OUT-06/OUT-07/OUT-09 in REQUIREMENTS.md abgehakt** (Transport-/Sicherheits-Fundament; die UI-/Objektmodell-/Runbook-Anteile dieser Requirements liefert erst 08-02/08-03). **Frontmatter-Zähler bewusst NICHT über die SDK angefasst** (dokumentierte wiederholte Korruption in dieser Phasenhistorie) — nur Prosa + `roadmap.update-plan-progress`. Bereit für Plan 08-02 (VSTO-COM-Hülle auf `VizpatchAddin.Core`).
- **2026-07-20** — **Phase 8 (Neuplanung COM/VSTO), Plan 08-02 ausgeführt** (VSTO-Outlook-classic-Add-in-Hülle: Ribbon-Toggle + CustomTaskPane + nativer SSE-Chat, OUT-05/OUT-06; OUT-07 technisch tragend, Live noch offen) — **code-komplett, Live-Checkpoint (Task 3) bewusst OFFEN**. Neues VSTO-Projekt `outlook-addin/VizpatchAddin/` (net48, **Legacy-csproj**, in die Solution aufgenommen, Projektreferenz auf `VizpatchAddin.Core`). **ThisAddIn** registriert im Startup eine `CustomTaskPane` „Vizpatch-Chat" (Default unsichtbar, Breite 420) und synchronisiert die Sichtbarkeit bidirektional mit einem **Ribbon-Toggle** (XML-Ansatz `IRibbonExtensibility.GetCustomUI`; `VisibleChanged` → `InvalidateControl`); Object-Model-sicher — nur das übergebene `Application`-Objekt, kein `new Outlook.Application`/`CreateObject` (Pitfall 3). **ChatView** (reines WinForms, D-83) ruft `ChatClient.StreamChatAsync` (08-01) auf und rendert den SSE-Stream inkrementell (`switch(evt)`: tool→Hinweiszeile, done→Turn-Ende, error→Fehler, default→Text-Chunk), reicht eine stabile `session_id` (`SessionIdGenerator`) durch, Zurücksetzen erzeugt eine neue + leert Verlauf/Log (analog `chat.js`); **kein `ConfigureAwait(false)`** (onFrame läuft auf UI-Thread) + defensives `BeginInvoke`; **keine Outlook-Send/Save/Move/Delete/CreateItem-APIs** (D-87, grep-Gate grün); `mail_context` = null (kommt in 08-03). **Toolchain-Realität:** die Session-Maschine hat jetzt VS Community 2026 (18.8, Office/SharePoint-Workload) + MSBuild 18.8.2 — daher wurde der VSTO-Build **real** ausgeführt: `msbuild VizpatchOutlookAddin.sln -t:restore` + `-t:rebuild -p:Configuration=Debug` **fehlerfrei** (alle 3 Projekte, VSTO-Manifeste `VizpatchAddin.dll.manifest` + `.vsto` erzeugt), **`dotnet test` 22/22 grün** (08-01-Core-Tests unverändert). **Deviations (alle Rule 3, Build-blockierend, real behoben):** (a) VSTO-4.0-Basisklasse ist `OutlookAddInBase` (nicht das Interface `OutlookAddIn`) — `ThisAddIn.Designer.cs`/`.xml` **1:1 aus der echten VS-VSTO-Outlook-Projektvorlage gespiegelt** statt frei rekonstruiert; (b) Referenzauflösung: stdole 7.0.3300 aus GAC, echte Interop-Assembly-Namen, Kontrakt-DLLs **und** `*.v4.0.Utilities`, `useofficeinterop`-Constant → strong-name-PIA statt COMReference (kein registriertes Outlook-TypeLib nötig); (c) `$(VSToolsPath)` auf den VS-18-Zweig gesetzt (MSB4226-Fix) + ClickOnce-Manifest-Signierung mit selbstsigniertem Temporary Key (via .NET `CertificateRequest`, da `Cert:`-PSProvider fehlte); (d) `StreamChatAsync` ist callback-basiert (`Action<string,string> onFrame`) — an die reale 08-01-Signatur adaptiert statt des im Plan skizzierten `await foreach`. **Interop bewusst NICHT via NuGet** (T-08-SC): Office-/VSTO-Referenzen in `VizpatchAddin.OfficeRefs.targets` ausgelagert (VS-eigene PIA via HintPath, EmbedInteropTypes) → die `.csproj` bleibt frei von Interop-Literal (grep-Gate grün). **Task 3 (`checkpoint:human-verify`, gate=blocking) NICHT ausgeführt/NICHT gefaked** — Live-Sideload in echtem Outlook classic gegen eine reale Backend-Instanz (Ribbon-Toggle, CTP, Chat-Turn ohne 403, Reset) bleibt der gebündelten menschlichen Phasen-Abnahme vorbehalten; präzise Schritte in `08-02-SUMMARY.md`. Commits: `fa64a67` (Task 1), `b90e922` (Task 2), SUMMARY-Commit folgt. `roadmap.update-plan-progress 08 08-02 complete` lief korrekt (2/4 Plans, Phase-Status „In Progress"). **OUT-05 abgehakt; OUT-06 Add-in-Anteil geliefert; OUT-07 technisch tragend (Live-Nachweis offen).** **Frontmatter-Zähler bewusst NICHT über die SDK angefasst** (dokumentierte wiederholte Korruption in dieser Phasenhistorie) — nur Prosa + `roadmap.update-plan-progress`. Bereit für Plan 08-03 (Settings-Dialog + Mail-Kontext-Extraktion + LAN/HTTPS-Runbook).
- **2026-07-20** — **Phase 8 (Neuplanung COM/VSTO), Plan 08-03 ausgeführt** (Mail-Kontext übers Outlook-Objektmodell + Settings-Dialog, OUT-08 vollständig / OUT-09 Settings-Anteil) — **code-komplett, Live-Checkpoint (D-89) bewusst OFFEN**. **MailContextReader** (`outlook-addin/VizpatchAddin/MailContextReader.cs`, neu): `TryBuildMailContext(Outlook.Application)` holt bevorzugt `ActiveInspector().CurrentItem`, sonst `ActiveExplorer().Selection[1]` (1-basiert), gekapselt in `try/catch (COMException) -> null`; nur bei `Outlook.MailItem` entsteht ein `MailContext` aus `Subject`/`SenderEmailAddress` (Fallback `SenderName`)/`Body` (null-safe auf ""), Nicht-Mail-Items (Termin/Kontakt/Aufgabe) → `null` (D-86); MailItem-Referenz per `Marshal.ReleaseComObject` freigegeben; nur das übergebene `Application`-Objekt (Object-Model-Guard greift nicht), keine Schreib-/Versand-APIs (D-87). **ChatView** bekam eine opt-in-Checkbox „Aktuelle Mail einbeziehen" (Default AUS, Datensparsamkeit): ist sie aktiv, baut `SendAsync` den Kontext VOR dem `await` (auf dem UI-Thread, wo das Objektmodell lebt) via `Globals.ThisAddIn.Application` und reicht ihn als `mail_context` an `ChatClient.StreamChatAsync` (statt des 08-02-`null`); kein Kontext → dezente Hinweiszeile statt Fehler. **SettingsDialog** (`outlook-addin/VizpatchAddin/SettingsDialog.cs`, neu, WinForms-Form): Felder BackendUrl/AgentId/Username/Password (maskiert)/AddinOriginToken (Default + `ADDIN_FRAME_ANCESTORS`-Hinweis)/CertThumbprint + Checkbox TrustAnyCertificate (Default AUS, roter TLS-Warntext); Laden via `SecureSettingsStore.Load()`, Speichern via `SecureSettingsStore.Save()` (Passwort DPAPI, nie Klartext); „leer = unverändert" fürs Passwortfeld (analog WebUI-Konfig); über einen „Einstellungen"-Button in der ChatView erreichbar, nach OK werden die Settings neu geladen. **Build real ausgeführt** (VS Community 2026 18.8, MSBuild): `msbuild VizpatchOutlookAddin.sln -t:restore` + `-p:Configuration=Debug` **fehlerfrei** (alle 3 Projekte), **`dotnet test` 22/22 grün** (08-01-Core-Tests unverändert). Baseline-Build vor Beginn belegte grünen Ausgangszustand. **Deviations:** (a) **Rule 3, Build-blockierend** — `TextBox.PlaceholderText` existiert in WinForms net48 nicht (CS1061); entfernt (der „leer = unverändert"-Hinweis steht ohnehin als Label), ungenutztes `_hadStoredPassword`-Feld mitentfernt; (b) **Rule 3, non-blocking** — Plan-grep-Negativ-Assertions (`new Outlook.Application|CreateObject`, `\.(Send|Save|Move|Delete)\(|CreateItem`) trafen erläuternde Doc-Kommentare in `MailContextReader.cs` (identisch zu 08-01 Dev #3); Kommentare umformuliert, Code unberührt, danach alle sechs Task-1-grep-Gates grün. Beide grep-Gates (Task 1 + Task 2) grün. **Live-Checkpoint (D-89) NICHT ausgeführt/NICHT gefaked** — Mail-Kontextbezug in echtem Outlook, Settings-Persistenz nach Neustart, Kein-Klartext-Passwort in `settings.json`, TrustAnyCertificate-Warnung bleiben der gebündelten menschlichen Phasen-Abnahme vorbehalten (präzise Schritte in `08-03-SUMMARY.md`). Commits: `b261e00` (Task 1), `3bf12e9` (Task 2), SUMMARY-Commit folgt. `roadmap.update-plan-progress 08 08-03 complete` (3/4 Plans). **OUT-08 in REQUIREMENTS.md abgehakt** (Mail-Kontext defensiv übers Objektmodell); OUT-09 Settings-Dialog-Anteil geliefert, LAN/HTTPS-Runbook bleibt Plan 08-04. **Frontmatter-Zähler bewusst NICHT über die SDK angefasst** (dokumentierte wiederholte Korruption in dieser Phasenhistorie) — nur Prosa + `roadmap.update-plan-progress`. Bereit für Plan 08-04 (LAN/HTTPS-Runbook + gebündelte Live-Abnahme).
