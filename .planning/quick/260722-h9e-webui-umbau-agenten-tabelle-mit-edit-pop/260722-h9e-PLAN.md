---
phase: 260722-h9e-webui-umbau
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - webui/src/main.py
  - webui/src/templates/index.html
  - webui/src/templates/_status_card.html
  - webui/src/templates/_agent_form.html
  - webui/src/templates/_chat_panel.html
  - webui/static/chat.js
  - webui/static/style.css
  - webui/tests/test_endpoints_agent.py
  - webui/tests/test_endpoints_config.py
  - webui/tests/test_endpoints_chat.py
autonomous: true
requirements:
  - UMBAU-D1  # EIN Speichern-Button + Datenschutz-Checkbox darüber, nutzt POST /save
  - UMBAU-D2  # Chat-Bindung via Radio + sofortiger HTMX-Swap von #sec-chat, chat.js re-init
  - UMBAU-D3  # Modal = natives <dialog> + hx-get eines Partials, kein Vorab-Rendern (Secret-Leak)
  - UMBAU-D4  # Globale Settings (Login/Autostart/Danger-Zone) bleiben außerhalb des Popups
  - UMBAU-D5  # „Neuer Agent" öffnet dasselbe Popup im Anlege-Modus

must_haves:
  truths:
    - "Der Betreiber sieht eine Tabelle aller Agenten mit einem Auswahl-Radio links pro Zeile."
    - "Klick auf einen Agenten öffnet ein Popup, in dem dessen Config vorbefüllt ist (Secrets maskiert)."
    - "„Neuer Agent" öffnet dasselbe Popup leer im Anlege-Modus mit einem Namensfeld."
    - "Speichern im Popup (ein Button, Datenschutz-Checkbox direkt darüber) persistiert über POST /save; danach spiegelt die Übersicht den neuen Stand."
    - "Ein Radio-Klick tauscht den Chat-Bereich sofort ohne Full-Page-Reload gegen den gewählten Agenten aus."
    - "Der 30-Sekunden-Status-Refresh setzt die Chat-Radio-Auswahl NICHT zurück."
    - "WebUI-Login, Autostart und Danger-Zone bleiben außerhalb des Popups auf der Hauptseite."
  artifacts:
    - path: "webui/src/templates/_agent_form.html"
      provides: "Popup-Formular-Partial (Edit + Anlege-Modus), per-Agent-Felder + Datenschutz-Checkbox + EIN Speichern-Button"
      contains: "new_agent_name"
    - path: "webui/src/templates/_chat_panel.html"
      provides: "Chat-Swap-Partial für #sec-chat inkl. Re-Init-Aufruf"
      contains: "initVizpatchChat"
    - path: "webui/src/main.py"
      provides: "Routen GET /agents/{id}/edit, GET /agents/new, GET /chat/{id}/panel, /save-Anlege-Zweig"
      contains: "def agent_edit"
    - path: "webui/src/templates/_status_card.html"
      provides: "Agenten-Tabelle mit Radio-Spalte + Bearbeiten-Trigger + Neuer-Agent-Button"
      contains: "chat-agent"
    - path: "webui/static/chat.js"
      provides: "Re-initialisierbare Chat-Init-Funktion statt selbstausführender IIFE"
      contains: "window.initVizpatchChat"
  key_links:
    - from: "webui/src/templates/_status_card.html (radio)"
      to: "GET /chat/{id}/panel → #chat-panel"
      via: "hx-get / hx-target / hx-trigger=change"
      pattern: "hx-get=\"/chat/.*panel"
    - from: "webui/src/templates/_status_card.html (Bearbeiten/Neuer Agent)"
      to: "GET /agents/{id}/edit bzw. /agents/new → #agent-dialog-body → showModal()"
      via: "hx-get + htmx:afterSwap showModal"
      pattern: "hx-get=\"/agents/.*edit"
    - from: "index.html (afterSwap-Listener)"
      to: "Radio-Auswahl nach 30s-Refresh von #status-card"
      via: "htmx:afterSwap re-check des gespeicherten Agenten"
      pattern: "vizpatchSelectedAgent"
    - from: "_agent_form.html (Speichern-Formular)"
      to: "POST /save (+ new_agent_name im Anlege-Modus)"
      via: "form action=/save method=post"
      pattern: "action=\"/save\""
---

<objective>
Die WebUI-Startseite wird vom Dropdown-basierten Single-Form-Flow auf eine Agenten-Tabelle
mit Bearbeiten-Popup und radio-gesteuerter Chat-Auswahl umgebaut.

Purpose: Klarere Multi-Agent-Bedienung — jede Zeile ein Agent, Config im Modal, Chat pro Zeile
umschaltbar. Kein Backend-Datenmodell-Umbau (WebUI ist bereits multi-agent via `agents_io`).

Output:
- Neue Routen (Edit-/Anlege-Partial, Chat-Panel-Partial, /save-Anlege-Zweig) in `webui/src/main.py`
- Neue Partials `_agent_form.html` + `_chat_panel.html`
- Umgebaute `_status_card.html` (Radio-Spalte + Bearbeiten-Trigger + „Neuer Agent")
- Umgebaute `index.html` (natives `<dialog>` + globale Settings außerhalb, Dropdown/Inline-Form entfernt)
- Re-initialisierbare `chat.js`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260722-h9e-webui-umbau-agenten-tabelle-mit-edit-pop/260722-h9e-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Aus der Codebasis extrahiert — Executor braucht KEINE weitere Exploration. -->

webui/src/agents_io.py (Datenschicht, agent-id-parametrisiert):
```python
MASKED = "****"
def list_agent_ids() -> list[str]          # sortiert
def read_env_masked(agent_id) -> dict[str,str]   # Secrets als "****"
def read_context_md(agent_id) -> str
def read_style_md(agent_id) -> str
def read_style_note(agent_id) -> str
def get_missing_config(agent_id) -> list[str]
def slugify(name_or_email) -> str           # eindeutiger Slug, kollisionsfrei
def write_env(agent_id, updates: dict[str,str]) -> None
```

webui/src/main.py (Bestehendes, WIEDERVERWENDEN, nicht duplizieren):
- `_build_agent_statuses() -> list[dict]`  — pro Agent {id,enabled,running,last_poll,status_json,missing}
- `index(...)` baut den per-Agent-Kontext (env_vals, context_md, style_md, style_note,
  drafts_status, missing, privacy_consent_accepted/at) — diese Zusammenstellung wird in Task 1
  in einen Helper ausgelagert und von der neuen Edit-Route wiederverwendet.
- `POST /save` (Zeilen ~803–1018): akzeptiert `agent_id` + per-Agent-Felder; Datenschutz-Consent
  gated NUR den echten Agent-Konfig-Save; Redirect `/?saved=1` bei Erfolg.
- `chat_embed(agent_id)` (GET /chat/{id}/embed): 404-Guard `if agent_id not in agents_io.list_agent_ids()`.
- Auth-Muster der bestehenden Routen: `user: str = Depends(auth.require_auth)`; state-ändernde
  Routen zusätzlich `dependencies=[Depends(auth.require_setup)]`. GET-Partials brauchen nur `require_auth`.

chat.js: aktuell eine selbstausführende IIFE `(function(){ ... })();` mit modul-lokalem
`history`/`sessionId`. Muss zu `window.initVizpatchChat` werden, damit sie auf einem frisch
per HTMX eingefügten `#chat-root` erneut laufen kann.

CSP (webui/src/main.py add_security_headers): strikt `script-src 'self' 'unsafe-inline'` —
Inline-`<script>` in Swap-Partials ist erlaubt und wird von HTMX beim Swap ausgeführt;
KEINE externen Skripte.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend-Routen für Popup-Partial, Chat-Panel-Partial und /save-Anlege-Zweig</name>
  <files>webui/src/main.py, webui/src/templates/_agent_form.html, webui/src/templates/_chat_panel.html, webui/tests/test_endpoints_agent.py, webui/tests/test_endpoints_chat.py</files>
  <behavior>
    - GET /agents/{id}/edit (auth) → 200 mit den per-Agent-Fieldsets als Fragment (KEIN base.html);
      Secret-Felder maskiert („****" bzw. leer), context.md/style.md vorbefüllt; 404 bei unbekanntem Agenten.
    - GET /agents/new (auth) → 200 mit demselben Formular im Anlege-Modus: leere Felder + Namensfeld
      `name="new_agent_name"`; enthält NICHT die Bearbeiten-nur-Elemente (Umbenennen/Löschen, KI-/Relearn-Helfer).
    - GET /chat/{id}/panel (auth) → 200 mit `#chat-root data-agent-id="{id}"` + Inline-Aufruf `initVizpatchChat`;
      404 bei unbekanntem Agenten (gleicher Guard wie chat_embed).
    - POST /save mit leerem agent_id + `new_agent_name="Esso Leonberg"` + Creds + `privacy_consent=on`
      → legt Agent `esso-leonberg` (AGENT_ENABLED=false) an, schreibt dessen .env/context, Redirect 303.
    - POST /save mit leerem agent_id UND leerem new_agent_name + per-Agent-Feldern → weiterhin „Kein Agent ausgewählt.".
  </behavior>
  <action>
    In `webui/src/main.py`: Die per-Agent-Kontextzusammenstellung aus `index()` (env_vals/context_md/
    style_md/style_note/drafts_status/missing/privacy_consent_accepted/privacy_consent_at) in einen
    Helper `_agent_form_ctx(agent_id: str) -> dict` auslagern und in `index()` weiter nutzen (keine
    Verhaltensänderung von `index()` in diesem Task außer der Extraktion).

    Route `agent_edit` — `@app.get("/agents/{agent_id}/edit", response_class=HTMLResponse)`,
    `user: str = Depends(auth.require_auth)`: 404 wenn `agent_id not in agents_io.list_agent_ids()`,
    sonst `_agent_form.html` mit `_agent_form_ctx(agent_id)` + `mode="edit"` + `agent_id` rendern.

    Route `agent_new` — `@app.get("/agents/new", response_class=HTMLResponse)`,
    `Depends(auth.require_auth)`: `_agent_form.html` mit `mode="create"`, `agent_id=""`, leeren
    Werten (`env={}`, `context_md=""`, `style_md=""`, `style_note=""`, `drafts_status={}`, `missing=[]`,
    `privacy_consent_accepted` aus Root-.env wie in index()) rendern.

    Route `chat_panel` — `@app.get("/chat/{agent_id}/panel", response_class=HTMLResponse)`,
    `Depends(auth.require_auth)`: 404-Guard wie `chat_embed`, dann `_chat_panel.html` mit `agent_id` rendern.

    `POST /save`: neuen optionalen Parameter `new_agent_name: str | None = Form(None)` ergänzen.
    GANZ AM ANFANG des Handlers (nach `is_htmx`, VOR der 64-KB-Prüfung/`existing_agent_env`-Lesung):
    wenn `not agent_id` und `(new_agent_name or "").strip()`, dann
    `agent_id = agents_io.slugify(new_agent_name)` und `agents_io.write_env(agent_id, {"AGENT_ENABLED": "false"})`
    — der restliche Handler (Consent-Gate, Agent-Writes, Redirect `/?saved=1`) läuft danach unverändert
    mit gesetztem `agent_id` (per UMBAU-D5/D1; die Zwei-Stufigkeit bleibt, fühlt sich für den Nutzer wie
    EIN Submit an). Kein separater `POST /agents`-Aufruf aus dem Popup nötig; bestehendes `POST /agents`
    unverändert lassen.

    `_agent_form.html` (neues Partial, KEIN `{% extends %}`): ein `<form action="/save" method="post">`
    mit `<input type="hidden" name="agent_id" id="active-agent-id" value="{{ agent_id }}">`. Die
    Fieldsets IMAP (`#sec-imap`), LLM (`#sec-llm`), context (`#sec-context`), Schreibstil (`#sec-style`)
    aus `index.html` HIERHER verschieben (identische Feldnamen: imap_user/imap_password/imap_drafts_folder/
    llm_api_key/context_md/style_md/style_note/enable_style_adaption). Die abschnittsweisen
    HTMX-„Diesen Abschnitt übernehmen"-Buttons (`.section-save`) ENTFERNEN — im Popup gilt EIN Speichern
    (UMBAU-D1). Direkt VOR dem Speichern-Button die Datenschutz-Section (Checkbox `name="privacy_consent"`
    + `_datenschutz.html`-Details) platzieren, darunter EIN `<button type="submit" class="btn-save-all">Speichern</button>`.
    Im `mode="create"` zusätzlich ganz oben ein `name="new_agent_name"`-Textfeld (Pflichtfeld) und die
    Bearbeiten-nur-Blöcke ausblenden: die KI-Context-Helfer-`<details>`, den „Schreibstil neu lernen"-Button
    und die Umbenennen/Löschen-Formulare NUR bei `mode="edit"` rendern (`{% if mode == 'edit' %}`), da sie
    einen bereits gespeicherten Key/Postfach voraussetzen. Umbenennen/Löschen (bisher in `index.html`
    `.agent-picker`) als eigene `<form>`s (posten an `/agents/{id}/rename` bzw. `/agents/{id}/delete`,
    unverändert) im Edit-Modus ans Popup-Ende setzen.

    `_chat_panel.html` (neues Partial): `{% include "_chat.html" %}` gefolgt von
    `<script>window.initVizpatchChat && window.initVizpatchChat();</script>` — HTMX führt Inline-Skripte
    in eingefügtem Inhalt aus (CSP `'unsafe-inline'` erlaubt es). Kein `<script src>` hier (chat.js ist
    beim Panel-Swap bereits von der Hauptseite geladen).

    KEINE fenced code blocks in diesem Action-Text umsetzen — Feldnamen/Signaturen sind oben benannt.
  </action>
  <verify>
    <automated>cd webui && python -m pytest tests/test_endpoints_agent.py tests/test_endpoints_chat.py -q</automated>
  </verify>
  <done>Neue Tests grün: /agents/{id}/edit liefert maskierte Felder + 404-Guard; /agents/new hat new_agent_name; /chat/{id}/panel liefert chat-root + 404-Guard; /save legt bei new_agent_name den Agenten an. Keine Regression in den bestehenden Agent-Endpoint-Tests.</done>
</task>

<task type="auto">
  <name>Task 2: Agenten-Tabelle mit Radio-Spalte, Bearbeiten-Trigger, „Neuer Agent" und natives &lt;dialog&gt;-Modal</name>
  <files>webui/src/templates/_status_card.html, webui/src/templates/index.html, webui/static/style.css, webui/tests/test_endpoints_config.py</files>
  <action>
    `_status_card.html`: In der `agent-status-table` links eine Spalte ergänzen. Neuer erster `<th>` (leer/„Chat")
    und pro Zeile `<td><input type="radio" name="chat-agent" value="{{ agent.id }}"
    hx-get="/chat/{{ agent.id }}/panel" hx-target="#chat-panel" hx-swap="innerHTML" hx-trigger="change"></td>`.
    Die Agent-Namens-Zelle klickbar für die Bearbeiten-Aktion machen: `{{ agent.id }}` als
    `<button type="button" class="agent-edit-link" hx-get="/agents/{{ agent.id }}/edit"
    hx-target="#agent-dialog-body" hx-swap="innerHTML">{{ agent.id }}</button>`. Die bestehende Start/Stop-
    Aktion-Spalte UNVERÄNDERT lassen. WICHTIG: Radio und Start/Stop-Button dürfen die Bearbeiten-Aktion
    NICHT auslösen — deshalb ist NUR der Namens-Button der Öffner (keine ganze-Zeile-Klickfläche), damit
    Radio-/Start-Stop-Klicks isoliert bleiben. Über der Tabelle einen `<button type="button" id="btn-new-agent"
    hx-get="/agents/new" hx-target="#agent-dialog-body" hx-swap="innerHTML">+ Neuer Agent</button>` einfügen.

    `index.html`:
    - Die `.agent-picker`-Section (Dropdown `#agent-select` + Anlege-/Umbenennen-/Löschen-`<details>`) KOMPLETT
      entfernen (Anlegen läuft jetzt über „Neuer Agent" → Popup; Umbenennen/Löschen sind ins Popup gewandert).
    - Das große Inline-`#agent-config-form` (IMAP/LLM/context/style/datenschutz + „Alles speichern") KOMPLETT
      entfernen — es lebt jetzt im per HTMX geladenen `_agent_form.html`.
    - Das globale `#global-settings-form` (WebUI-Login `#sec-webui` + Autostart `#sec-autostart`) sowie die
      `.danger-zone` UNVERÄNDERT auf der Hauptseite belassen (UMBAU-D4).
    - Ein leeres Modal-Gerüst ergänzen: `<dialog id="agent-dialog"><button type="button" id="agent-dialog-close">
      Schließen</button><div id="agent-dialog-body"></div></dialog>`.
    - Das bestehende `<script>` mit `detectProvider/updateProviderHint/generateContext/relearnStyle` als
      seitenweite Funktionen BEIBEHALTEN, aber die Jinja-Interpolation von `agent_id` entfernen: in
      `generateContext`/`relearnStyle` die agent_id NICHT mehr aus `'{{ agent_id }}'` lesen, sondern zur
      Laufzeit aus dem Popup-Formular: `document.getElementById('active-agent-id').value`. So funktionieren
      die Helfer im per HTMX geladenen Popup weiter (die Feld-IDs firma_input/context_md/style_note/style_md
      liegen dann im Dialog-DOM). `updateProviderHint` bleibt rein clientseitig unverändert nutzbar.
    - Modal-Steuerung (Inline-Skript): einen einzigen `document.body.addEventListener('htmx:afterSwap', ...)`
      registrieren, der bei `evt.detail.target.id === 'agent-dialog-body'` `document.getElementById('agent-dialog').showModal()`
      aufruft. `#agent-dialog-close` ruft `agent-dialog.close()`. (Der Radio-/Auswahl-Erhalt-Teil dieses
      Listeners kommt in Task 3 dazu.)

    `style.css`: Stile für `dialog#agent-dialog` (max-width ~640px, padding, border-radius, `::backdrop`
    abgedunkelt), `.agent-edit-link` (link-artiger Button: transparent, blau `#1e3a5f`, unterstrichen,
    Cursor pointer), `#btn-new-agent` (analog `.agent-buttons button`) und die neue Radio-Spalte (schmal,
    zentriert). Kein Umbau bestehender Regeln nötig.

    Tests `test_endpoints_config.py`: die beiden Tests, die den alten Aufbau prüfen, an die neue Struktur
    anpassen — `test_get_index_shows_agent_dropdown_and_create_form_when_no_agents` und
    `test_get_index_shows_agent_config_form_for_active_agent`. Neue Assertions: GET / enthält
    `id="agent-dialog"`, `type="radio" name="chat-agent"` (sobald Agenten existieren) und
    `id="btn-new-agent"`, und enthält NICHT mehr `id="agent-select"` / `action="/agents"` inline /
    `name="llm_api_key"` (letzteres lebt jetzt nur im per-HTMX-Partial, nicht in `/`).
  </action>
  <verify>
    <automated>cd webui && python -m pytest tests/test_endpoints_config.py -q</automated>
  </verify>
  <done>GET / rendert das &lt;dialog&gt;-Gerüst, die Tabelle mit Radio-Spalte + „Neuer Agent"-Button und die globalen Settings/Danger-Zone; das alte Dropdown + Inline-Config-Form sind weg. Angepasste Config-Endpoint-Tests grün.</done>
</task>

<task type="auto">
  <name>Task 3: Chat-Bindung — Radio→#sec-chat-Swap, chat.js re-initialisierbar, Auswahl-Erhalt über den 30s-Refresh</name>
  <files>webui/static/chat.js, webui/src/templates/index.html, webui/tests/test_endpoints_chat.py</files>
  <action>
    `chat.js`: Die selbstausführende IIFE `(function(){ ... })();` in `window.initVizpatchChat = function () { ... }`
    umwandeln. Am Funktionsanfang `const root = document.getElementById('chat-root'); if (!root) return;`
    (idempotent, kein Fehler wenn kein Chat auf der Seite). ALLE modul-lokalen Zustände (`history`,
    `sessionId`, `pendingAttachment`, `lastMailContext`) in die Funktion verschieben → jeder Init startet
    mit frischem Zustand für den gewählten Agenten (gewollt beim Agent-Wechsel). Am Dateiende einmalig
    `document.addEventListener('DOMContentLoaded', function () { window.initVizpatchChat(); });` ergänzen,
    damit der Initial-Chat auf der Hauptseite weiter bootet. Das Message-`event.origin`-Gate (T-08-04),
    das SSE-Parsing und die Attachment-Upload-Logik inhaltlich unverändert lassen — nur in die Funktion
    einrücken. (Beim Panel-Swap ruft `_chat_panel.html` aus Task 1 `initVizpatchChat()` erneut auf; die
    alten Element-Listener verschwinden mit dem ersetzten `#sec-chat`-Inhalt, kein Doppel-Binding.)

    `index.html`:
    - Die Chat-Section so umbauen, dass der swap-bare Inhalt in einem eigenen Container liegt:
      `<section class="chat-section" id="sec-chat"><h2>Agenten-Chat</h2><p class="hint">…</p>
      <div id="chat-panel">{% include "_chat.html" %}</div></section>` — das `hx-target` der Radios
      (`#chat-panel`) tauscht NUR das Panel, Überschrift/Hinweis bleiben stehen. `chat.js` weiterhin per
      `<script src="/static/chat.js">` einbinden (lädt einmal, stellt `initVizpatchChat` bereit).
    - Der initiale Chat rendert für den Default-Agenten (`agent_id`, den `index()` bereits als agents[0]
      liefert). In `_status_card.html` bzw. per Init-Skript das Radio dieses Default-Agenten initial
      `checked` setzen; dazu die Template-Variable `agent_id` an `_status_card.html` mit durchreichen
      (im `{% include %}`-Kontext bzw. im `/agents/status`-Render — dort ist keine Auswahl bekannt, daher
      Default-Auswahl clientseitig führen, siehe unten).
    - Auswahl-Erhalt über den 30s-Refresh (`#status-card` `hx-swap="outerHTML"`): im seitenweiten
      Inline-Skript `window.vizpatchSelectedAgent` führen. (a) Auf `change` eines
      `input[name="chat-agent"]` `window.vizpatchSelectedAgent = radio.value` setzen. (b) Den in Task 2
      angelegten `htmx:afterSwap`-Listener erweitern: bei `evt.detail.target.id === 'status-card'` das
      Radio mit `value === window.vizpatchSelectedAgent` per `.checked = true` RE-selektieren (nur die
      visuelle Markierung — programmatisches Setzen feuert KEIN change-Event, also KEIN erneuter
      Chat-Swap alle 30s). Beim ersten Laden `vizpatchSelectedAgent` auf `"{{ agent_id }}"` initialisieren
      und das passende Radio checken.
    - WICHTIG (UMBAU-D2): der 30s-Refresh tauscht nur `#status-card`, NICHT `#chat-panel` — der Chat bleibt
      also ohnehin stehen; nur die Radio-Markierung muss re-appliziert werden. Sicherstellen, dass der
      afterSwap-Handler `#status-card` und `#agent-dialog-body` (Task 2) sauber unterscheidet.

    `test_endpoints_chat.py`: einen Test ergänzen, der GET /chat/{id}/panel prüft (chat-root mit
    data-agent-id + `initVizpatchChat`-Aufruf im Body, 404 bei unbekanntem Agenten) — falls in Task 1
    noch nicht abgedeckt, hier vervollständigen.
  </action>
  <verify>
    <automated>cd webui && python -m pytest tests/test_endpoints_chat.py -q && grep -q "window.initVizpatchChat" static/chat.js && grep -q "vizpatchSelectedAgent" src/templates/index.html</automated>
  </verify>
  <verify>
    <human-check>Im Browser: Radio einer anderen Agenten-Zeile klicken → der Chat unten wechselt sofort ohne Full-Reload auf diesen Agenten und ist bedienbar (Nachricht senden). ~30s warten (Status-Refresh) → die Radio-Auswahl bleibt erhalten, der Chat bleibt beim gewählten Agenten.</human-check>
  </verify>
  <done>chat.js exportiert `initVizpatchChat` und bootet initial via DOMContentLoaded; Radio-Klick swappt #chat-panel und re-initialisiert den Chat; der 30s-Refresh setzt die Auswahl nicht zurück (JS re-check). Chat-Tests grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → WebUI-API | Betreiber-Eingaben (Config, Chat) queren hier; WebUI hat Docker-Socket-/Host-Zugriff |
| WebUI → /config (Bind-Mount) | Secrets (IMAP-/API-Key) werden verschlüsselt at-rest abgelegt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-h9e-01 | Information Disclosure | Vorab-Rendern aller Agenten-Formulare würde maskierte Secrets ALLER Agenten ins DOM legen | mitigate | UMBAU-D3: Formular-Partial wird lazy per `hx-get /agents/{id}/edit` nur für den geklickten Agenten geladen; Secrets bleiben maskiert („****", `read_env_masked`). |
| T-h9e-02 | Elevation of Privilege | Neue GET-Partial-Routen (/agents/{id}/edit, /agents/new, /chat/{id}/panel) | mitigate | Alle drei erfordern `Depends(auth.require_auth)`; agent-bezogene Routen mit 404-Guard `agent_id in list_agent_ids()` (Path-Traversal fängt AGENT_ID_PATTERN in agents_io ab). |
| T-h9e-03 | Tampering (CSRF) | /save-Anlege-Zweig (state-ändernd) | accept/mitigate | /save läuft weiter über die bestehende Same-Origin-Middleware (CR-01); der Anlege-Zweig fügt keine neue unsichere Methode hinzu, nur einen Parameter. |
| T-h9e-04 | Tampering | Inline-`<script>` in Swap-Partials (chat panel) | accept | CSP bleibt strikt `script-src 'self' 'unsafe-inline'`; kein externes Skript; Inline-Init ruft nur die bereits geladene `initVizpatchChat`. Keine neuen npm/pip-Installationen in diesem Plan. |
</threat_model>

<verification>
- `cd webui && python -m pytest -q` — die volle WebUI-Suite bleibt grün (keine Regression über die 3 Tasks).
- Manuell/Browser (Task 3 human-check): Tabelle mit Radios, Bearbeiten-Popup (vorbefüllt, Secrets maskiert),
  „Neuer Agent"-Popup (leer + Namensfeld), Speichern schließt Popup + Übersicht aktualisiert, Radio-Wechsel
  swappt den Chat sofort, 30s-Refresh erhält die Auswahl.
</verification>

<success_criteria>
- Agenten werden als Tabelle mit Radio-Auswahl-Spalte + Start/Stop dargestellt; kein `#agent-select`-Dropdown mehr.
- Klick auf einen Agentennamen öffnet ein natives `<dialog>` mit dessen per-Agent-Config (lazy via hx-get, Secrets maskiert).
- „Neuer Agent" öffnet dasselbe Popup leer mit Namensfeld; Speichern legt den Agenten via `POST /save` (new_agent_name) an.
- Ein Speichern-Button unten im Popup, Datenschutz-Checkbox direkt darüber; nutzt `POST /save`.
- Radio-Klick tauscht `#chat-panel` sofort per HTMX; `chat.js` re-initialisiert; kein Full-Page-Reload.
- Der 30s-Status-Refresh setzt die Radio-Auswahl nicht zurück.
- WebUI-Login, Autostart und Danger-Zone bleiben außerhalb des Popups auf der Hauptseite.
- `python -m pytest` (webui) grün.
</success_criteria>

<output>
Create `.planning/quick/260722-h9e-webui-umbau-agenten-tabelle-mit-edit-pop/260722-h9e-SUMMARY.md` when done
</output>
