---
phase: 10-reversible-pseudonymisierung-vor-llm-bermittlung-v1-6
plan: 04
type: execute
wave: 4
depends_on: ["10-03"]
files_modified:
  - agent/.env.example
  - webui/src/templates/_datenschutz.html
  - deployment/AVV-Vizionists-Betreiber.md
  - agent/context.md.example
  - webui/tests/test_endpoints_datenschutz.py
autonomous: false
requirements: [ANON-05]

must_haves:
  truths:
    - "Der Flag ENABLE_PII_REDACTION ist in .env.example als reversible Pseudonymisierung strukturierter PII dokumentiert (Default an)"
    - "Datenschutzerklärung + AVV beschreiben die Pseudonymisierung strukturierter PII vor LLM-Übermittlung"
    - "Beide Dokumente enthalten den EHRLICHEN Hinweis: Namen/Orte gehen in Variante A weiter ans LLM, pseudonym != anonym (ErwG 26), AVV bleibt nötig, finale Aussage = DSB"
    - "Soft-Empfehlung dokumentiert: kein Fremd-PII in context.md (context.md wird nicht maskiert)"
  artifacts:
    - path: "webui/src/templates/_datenschutz.html"
      provides: "Ziffer 4/5/6 auf reversible Pseudonymisierung + ehrlichen Restrisiko-Hinweis aktualisiert"
      contains: "Pseudonym"
    - path: "deployment/AVV-Vizionists-Betreiber.md"
      provides: "§3/§4 auf Pseudonymisierung strukturierter PII + Restrisiko-Hinweis aktualisiert"
      contains: "seudonym"
    - path: "agent/.env.example"
      provides: "Aktualisierter Kommentar zu ENABLE_PII_REDACTION"
      contains: "ENABLE_PII_REDACTION"
  key_links:
    - from: "webui/src/templates/_datenschutz.html"
      to: "tatsächliches Feature-Verhalten (Variante A)"
      via: "ehrlicher Hinweis Namen exponiert / pseudonym != anonym"
      pattern: "pseudonym|Pseudonym"
---

<objective>
Abschluss-Scheibe: die DSGVO/AVV-Neubewertung dokumentieren (ANON-05) und den Feature-Flag-Kommentar aktualisieren. Bewusst EHRLICH: Variante A schützt strukturierte PII (IBAN/Karte/Telefon/E-Mail/URL/Datum), aber Namen/Orte gehen weiter ans LLM — pseudonymisierte Daten bleiben rechtlich personenbezogen (ErwG 26), der AVV bleibt nötig, die endgültige Aussage trifft der/die Datenschutzbeauftragte. Ein menschlicher Verify-Checkpoint stellt sicher, dass die rechtliche Formulierung vom Betreiber/DSB abgenommen ist.

Purpose: Erfüllt ANON-05 (Dokumentations-Anteil) und ROADMAP-Success-Criterion 5 (DSGVO/AVV-Neubewertung dokumentiert, ehrlicher Hinweis).

Output: Aktualisierte Datenschutzerklärung, AVV, .env.example-Kommentar, context.md-Soft-Empfehlung; abgenommene Rechtstexte.
</objective>

<phase_goal>
**Als** Betreiber **will ich** eine ehrliche, aktuelle Datenschutzerklärung und einen AVV, die genau beschreiben, welche PII pseudonymisiert wird und welche (Namen/Orte) weiterhin an den Cloud-LLM geht, **damit** ich gegenüber Kunden und meinem/meiner Datenschutzbeauftragten transparent und rechtssicher auftrete.

*(User-Story aus ROADMAP-Prosa-Goal + 10-CONTEXT.md abgeleitet.)*
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-reversible-pseudonymisierung-vor-llm-bermittlung-v1-6/10-CONTEXT.md
@.planning/phases/10-reversible-pseudonymisierung-vor-llm-bermittlung-v1-6/10-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DSGVO/AVV-Dokumente + Flag-Kommentar auf Variante A aktualisieren</name>
  <files>agent/.env.example, webui/src/templates/_datenschutz.html, deployment/AVV-Vizionists-Betreiber.md, agent/context.md.example, webui/tests/test_endpoints_datenschutz.py</files>
  <read_first>
    - webui/src/templates/_datenschutz.html (Ziffer 4 "PII-Redaction", Ziffer 5 KI-Dienste, Ziffer 6 Agenten-Chat)
    - deployment/AVV-Vizionists-Betreiber.md (§3 Zweck, §4 Art der Daten)
    - agent/.env.example (Zeile ~62, ENABLE_PII_REDACTION=true)
    - agent/context.md.example (für die Soft-Empfehlung "kein Fremd-PII")
    - webui/tests/test_endpoints_datenschutz.py (welche Strings/Assertions der Test auf die Datenschutzseite prüft — nicht brechen bzw. mitziehen)
  </read_first>
  <action>Aktualisiere `webui/src/templates/_datenschutz.html`: In Ziffer 4 den Bullet „PII-Redaction" umformulieren zu reversibler Pseudonymisierung — nenne die getypten Platzhalter-Typen (E-Mail, Telefon, IBAN, Kreditkarte, URL, Datum), dass die Ersetzung lokal und reversibel im Arbeitsspeicher geschieht (Platzhalter zum LLM, echte Werte nur zurück in den Draft/Chat) und dass das Zuordnungs-Mapping den Server nie verlässt. Ergänze in Ziffer 5 (oder als eigenen Absatz) den EHRLICHEN Hinweis: „Namen, Firmen und Orte werden in der aktuellen Ausbaustufe NICHT maskiert und daher weiterhin an den KI-Anbieter übermittelt; pseudonymisierte Daten bleiben rechtlich personenbezogen (Erwägungsgrund 26 DSGVO) — der Einsatz eines Auftragsverarbeitungsvertrags bleibt erforderlich, die abschließende datenschutzrechtliche Bewertung trifft der/die Datenschutzbeauftragte." Passe in Ziffer 6 die Verweise „durch die PII-Redaction (Ziffer 4) maskiert" auf die neue Pseudonymisierungs-Formulierung an. Aktualisiere `deployment/AVV-Vizionists-Betreiber.md` §3/§4 analog: strukturierte PII wird vor Übermittlung pseudonymisiert; derselbe ehrliche Restrisiko-Hinweis (Namen exponiert, pseudonym != anonym, AVV bleibt). Aktualisiere den Kommentar über `ENABLE_PII_REDACTION` in `agent/.env.example`: der Flag steuert jetzt die reversible Pseudonymisierung strukturierter PII über alle LLM-Pfade (Default an; „false" = Klartext an den Anbieter wie vor v1.6). Ergänze in `agent/context.md.example` einen kurzen Kommentar/Hinweis (als Markdown-Kommentar oder Fußnote), dass context.md NICHT maskiert wird und daher kein Fremd-PII enthalten sollte (D-08). Wenn `webui/tests/test_endpoints_datenschutz.py` auf konkrete Strings prüft, die sich geändert haben, ziehe die Assertions synchron nach (Test muss grün bleiben).</action>
  <verify>
    <automated>(im Verzeichnis webui/) python -m pytest tests/test_endpoints_datenschutz.py -x — grün; grep -n "seudonym" webui/src/templates/_datenschutz.html deployment/AVV-Vizionists-Betreiber.md liefert Treffer</automated>
  </verify>
  <acceptance_criteria>
    - `grep -in "pseudonym" webui/src/templates/_datenschutz.html` liefert >=1 Treffer
    - `grep -in "Erwägungsgrund 26\|ErwG 26\|personenbezogen" webui/src/templates/_datenschutz.html` liefert Treffer (ehrlicher Hinweis vorhanden)
    - `grep -in "seudonym" deployment/AVV-Vizionists-Betreiber.md` liefert Treffer
    - agent/.env.example: Kommentar bei ENABLE_PII_REDACTION erwähnt Pseudonymisierung/strukturierte PII
    - agent/context.md.example enthält den „kein Fremd-PII / wird nicht maskiert"-Hinweis
    - `python -m pytest tests/test_endpoints_datenschutz.py` grün
  </acceptance_criteria>
  <done>Alle Doku-Oberflächen beschreiben Variante A korrekt und ehrlich; Datenschutz-Endpoint-Test grün.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: DSGVO/AVV-Abnahme (menschlicher Checkpoint)</name>
  <action>Keine Automatisierung — reiner menschlicher Verifikations-Schritt: der Betreiber prüft die aktualisierten Rechtstexte gemäß how-to-verify und legt sie dem/der Datenschutzbeauftragten zur Freigabe vor.</action>
  <what-built>Datenschutzerklärung (Ziffer 4/5/6), AVV (§3/§4) und .env-/context.md-Hinweise wurden auf die reversible Pseudonymisierung strukturierter PII (Variante A) aktualisiert — inklusive des ehrlichen Hinweises, dass Namen/Orte weiterhin an den KI-Anbieter gehen und der AVV nötig bleibt.</what-built>
  <how-to-verify>
    1. Öffne die WebUI-Datenschutzseite (Route `/datenschutz`) im Browser und lies Ziffer 4, 5 und 6.
    2. Prüfe: Wird die reversible Pseudonymisierung strukturierter PII (E-Mail/Telefon/IBAN/Kreditkarte/URL/Datum) korrekt beschrieben?
    3. Prüfe den EHRLICHEN Restrisiko-Hinweis: „Namen/Orte gehen weiter ans LLM, pseudonym != anonym (ErwG 26), AVV bleibt nötig, finale Bewertung = DSB". Stimmt diese Aussage mit deiner Erwartung überein?
    4. Öffne `deployment/AVV-Vizionists-Betreiber.md` §3/§4 und prüfe die gleiche Aussage.
    5. Lege die aktualisierten Texte dem/der Datenschutzbeauftragten zur finalen Freigabe vor (ROADMAP SC5: die endgültige „AVV-nicht-nötig"-Aussage trifft der/die DSB — in Variante A bleibt der AVV klar nötig).
  </how-to-verify>
  <resume-signal>Tippe „approved", wenn die Rechtstexte fachlich korrekt und ehrlich sind — oder beschreibe die nötigen Formulierungs-Korrekturen.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dokumentierte Zusage -> tatsächliches Systemverhalten | Eine Datenschutzerklärung, die mehr Schutz verspricht als das System liefert, ist ein Compliance-/Haftungsrisiko |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-17 | Repudiation / Compliance | Datenschutzerklärung + AVV | mitigate | Ehrlicher Hinweis, dass Namen/Orte exponiert bleiben und der AVV nötig bleibt (kein Over-Claiming); menschlicher Verify-Checkpoint mit DSB-Freigabe |
| T-10-18 | Information Disclosure | context.md (roh an LLM) | accept/mitigate | Soft-Empfehlung „kein Fremd-PII in context.md" in context.md.example dokumentiert (D-08) |
</threat_model>

<verification>
- Datenschutz-Endpoint-Test grün.
- Grep belegt Pseudonymisierungs- und ErwG-26-Formulierungen in Datenschutzerklärung + AVV.
- Menschliche Abnahme der Rechtstexte erfolgt (Checkpoint).
</verification>

<success_criteria>
- ANON-05 (Doku-Anteil) erfüllt: DSGVO/AVV-Neubewertung dokumentiert, ehrlicher Restrisiko-Hinweis, Flag + context.md-Ausnahme dokumentiert.
- Rechtstexte vom Betreiber/DSB abgenommen.
</success_criteria>

<output>
Create `.planning/phases/10-reversible-pseudonymisierung-vor-llm-bermittlung-v1-6/10-04-SUMMARY.md` when done
</output>
