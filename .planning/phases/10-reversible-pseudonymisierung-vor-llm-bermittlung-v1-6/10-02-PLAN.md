---
phase: 10-reversible-pseudonymisierung-vor-llm-bermittlung-v1-6
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - webui/src/style_extract.py
  - webui/src/chat.py
  - webui/tests/test_style_extract.py
  - webui/tests/test_chat.py
autonomous: true
requirements: [ANON-03, ANON-04]

must_haves:
  truths:
    - "Gesendet-Mail-Bodies werden VOR dem Truncate anonymisiert (nicht mehr Truncate-vor-Redact), bevor sie in den Stil-Extraktions-Prompt gehen"
    - "Der beratende Chat-Prompt (build_chat_prompt) anonymisiert Nachricht, Verlauf und Mail-Kontext feldweise — context.md/style.md im System-Prompt bleiben roh"
    - "Ein über SSE-Chunk-Grenzen zerrissener Platzhalter ([IBAN_ + 1]) wird durch den Streaming-Puffer korrekt zusammengesetzt und de-anonymisiert"
  artifacts:
    - path: "webui/src/chat.py"
      provides: "deanonymize_stream(chunks, anonymizer)-Puffer-Helfer + anonymizer-fähiges build_chat_prompt"
      contains: "def deanonymize_stream"
    - path: "webui/src/style_extract.py"
      provides: "Anonymize-vor-Truncate-Fix in _fetch_sent_mail_bodies + De-Anonymisierung des style.md-Outputs"
      contains: "Anonymizer"
  key_links:
    - from: "webui/src/style_extract.py"
      to: "webui/src/pii.py::Anonymizer"
      via: "eine Instanz über alle bodies, anonymize vor [:MAX_BODY_CHARS]"
      pattern: "Anonymizer\\(\\)"
    - from: "webui/src/chat.py::build_chat_prompt"
      to: "webui/src/pii.py::Anonymizer"
      via: "anonymize von message/history/mail_context, System-Prompt roh"
      pattern: "anonymize\\("
---

<objective>
Zweite Scheibe: die zwei WebUI-Pfade, die NICHT die agentische Tool-Schleife sind — Schreibstil-Extraktion (`style_extract.py`) und der beratende Chat-Prompt-Bau + streaming-sicheres De-Anonymisieren (`chat.py`). Stellt die Bausteine bereit (`deanonymize_stream`, anonymizer-fähiges `build_chat_prompt`), die Plan 10-03 für den Fallback-Chat-Pfad konsumiert.

Purpose: Erfüllt ANON-03 für den Stil- und den beratenden Chat-Pfad und ANON-04 (kein Platzhalter-Leck, streaming-sicher). Behebt zugleich den im Research dokumentierten Reihenfolge-Bug (Truncate-vor-Redact) in `style_extract.py`.

Output: Gefixte `style_extract.py`, erweiterte `chat.py` mit Puffer-Helfer und feldweiser Anonymisierung im Prompt-Bau.
</objective>

<phase_goal>
**Als** Betreiber **will ich**, dass auch die Schreibstil-Ableitung aus meinen Gesendet-Mails und mein beratender Assistenz-Chat strukturierte PII pseudonymisieren, bevor Inhalte an den Cloud-LLM gehen — und dass der Chat mir trotzdem die echten Werte anzeigt —, **damit** kein Kanal am Datenschutz vorbei Klartext-PII an den Anbieter schickt.

*(User-Story aus ROADMAP-Prosa-Goal + 10-CONTEXT.md abgeleitet.)*
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-reversible-pseudonymisierung-vor-llm-bermittlung-v1-6/10-RESEARCH.md
@.planning/phases/10-reversible-pseudonymisierung-vor-llm-bermittlung-v1-6/10-01-SUMMARY.md

<interfaces>
<!-- Aus Plan 10-01 verfügbar (webui/src/pii.py, byte-identisch): -->
webui/src/pii.py:
  class Anonymizer:
    def anonymize(self, text: str) -> str
    def deanonymize(self, text: str) -> str
  # pro Request instanziieren, Mapping nur im RAM

<!-- Bestehende Signaturen, die dieser Plan ändert: -->
webui/src/style_extract.py:
  MAX_BODY_CHARS  (Modulkonstante)
  def _fetch_sent_mail_bodies(env, imap_password, sample_count) -> list[str]
    # HEUTE (Bug): body = body.strip()[:MAX_BODY_CHARS]; bodies.append(pii.redact(body))  <- Truncate VOR Redact
  def extract_style(agent_id: str) -> str
    # ruft _fetch_sent_mail_bodies, dann LLM; gibt style.md-Text zurück

webui/src/chat.py:
  MAX_MAIL_CONTEXT_BODY_CHARS = 2000
  def build_system_prompt(agent_id) -> str          # injiziert context.md + style.md + Status — BLEIBT ROH
  def build_chat_prompt(agent_id, message, history=None, mail_context=None) -> str
    # setzt system + getrimmten Verlauf + mail_context-DATEN-Block + aktuelle Nachricht zusammen
  def stream_chat(provider, api_key, model, prompt, max_tokens, temperature) -> Iterator[str]
    # yieldet Token-Chunks (Anthropic text_stream) — Chunk-Grenzen NICHT tag-ausgerichtet
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: style_extract.py — Anonymize-vor-Truncate + De-Anonymisierung des Outputs</name>
  <files>webui/src/style_extract.py, webui/tests/test_style_extract.py</files>
  <read_first>
    - webui/src/style_extract.py (Zeilen 120–200, _fetch_sent_mail_bodies + extract_style)
    - webui/tests/test_style_extract.py (bestehende Mock-/Fixture-Muster)
    - webui/src/pii.py (Anonymizer-API aus 10-01)
    - .planning/phases/10-reversible-pseudonymisierung-vor-llm-bermittlung-v1-6/10-RESEARCH.md §"Integration in style_extract.py" + Pitfall 1
  </read_first>
  <behavior>
    - test_sent_bodies_anonymized_before_truncate: ein Gesendet-Body mit einer IBAN kurz vor der MAX_BODY_CHARS-Grenze -> die IBAN wird als vollständiger [IBAN_1]-Tag erkannt (nicht durch den Schnitt zerstört).
    - test_sent_bodies_no_raw_pii: gesammelte bodies enthalten keine Roh-IBAN/-E-Mail/-Telefonnummer.
    - test_extract_style_deanonymizes_output (Flag an): LLM-Mock liefert style.md-Text mit "[EMAIL_1]" -> extract_style-Rückgabe enthält keinen Platzhalter.
    - test_style_flag_off_raw (ENABLE_PII_REDACTION=false): bodies bleiben roh (Rückfall).
  </behavior>
  <action>In `webui/src/style_extract.py`: importiere Anonymizer aus `pii`. Lies den Flag `ENABLE_PII_REDACTION` per-Agent aus `read_env_raw(agent_id)` in `extract_style` (Default true, Muster `(env.get("ENABLE_PII_REDACTION") or "true").strip().lower() != "false"`) und reiche das bool an `_fetch_sent_mail_bodies` weiter (Signatur erweitern um `enable_pseudonym: bool = True`). In `_fetch_sent_mail_bodies`: instanziiere EINE `Anonymizer()`-Instanz für ALLE bodies (gleiche Werte über mehrere Mails -> gleicher Tag). Für jeden body die Reihenfolge umdrehen: erst `anonymizer.anonymize(body.strip())` (wenn Flag an), DANN `[:MAX_BODY_CHARS]` schneiden (Pitfall 1). Bei Flag aus: bestehendes Verhalten (roh, kein anonymize). Der bisherige `pii.redact(...)`-Aufruf entfällt zugunsten des Anonymizers bei aktivem Flag. In `extract_style`: nach dem LLM-Aufruf den zurückgegebenen style.md-Text bei aktivem Flag durch `anonymizer.deanonymize(...)` schicken (Konsistenz D-05, verhindert wörtlich zitierte Platzhalter in style.md). Dazu muss dieselbe Anonymizer-Instanz aus `_fetch_sent_mail_bodies` zugänglich sein — gib sie mit zurück (Tuple) oder instanziiere sie in `extract_style` und reiche sie hinein.</action>
  <verify>
    <automated>(im Verzeichnis webui/) python -m pytest tests/test_style_extract.py -x — grün</automated>
  </verify>
  <acceptance_criteria>
    - webui/src/style_extract.py: anonymize-Aufruf steht VOR dem `[:MAX_BODY_CHARS]`-Schnitt (Grep-Reihenfolge prüfbar)
    - Kein `pii.redact(` mehr im aktiven (Flag-an-)Pfad von _fetch_sent_mail_bodies
    - Neuer Test test_sent_bodies_anonymized_before_truncate PASSED
    - test_extract_style_deanonymizes_output PASSED
    - `python -m pytest tests/test_style_extract.py tests/test_pii_sync.py` (webui) grün
  </acceptance_criteria>
  <done>Stil-Extraktion anonymisiert korrekt vor dem Truncate und liefert einen platzhalterfreien style.md-Text.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: chat.py — deanonymize_stream-Puffer + anonymizer-fähiges build_chat_prompt</name>
  <files>webui/src/chat.py, webui/tests/test_chat.py</files>
  <read_first>
    - webui/src/chat.py (Zeilen 100–280, build_system_prompt/build_chat_prompt/stream_chat)
    - webui/tests/test_chat.py (bestehende Tests zu build_chat_prompt/stream — Mock-Muster)
    - webui/src/pii.py (Anonymizer-API)
    - .planning/phases/10-reversible-pseudonymisierung-vor-llm-bermittlung-v1-6/10-RESEARCH.md §"Streaming-sicheres De-Anonymisieren" + Pitfall 2 + Pitfall 4
  </read_first>
  <behavior>
    - test_build_chat_prompt_anonymizes_message: build_chat_prompt mit anonymizer + PII-haltiger message -> System-Prompt-Teil (context.md/style.md) roh, aber der "Aktuelle Nachricht"-Block enthält [IBAN_1] statt Rohwert.
    - test_build_chat_prompt_system_stays_raw: eine context.md mit Telefonnummer -> der System-Prompt-Teil enthält KEINEN Platzhalter (D-08, Pitfall 4).
    - test_build_chat_prompt_history_and_mailcontext_anonymized: history-content + mail_context.body mit PII -> beide anonymisiert; gemeinsamer Wert = gemeinsamer Tag.
    - test_build_chat_prompt_no_anonymizer_raw: ohne anonymizer-Argument (None) bleibt alles roh (Rückwärtskompatibilität zu bestehenden Aufrufern/Tests).
    - test_deanonymize_stream_reassembles_split_tag: Mock-Iterator ["...Ihre IBAN ist [IBA", "N_1]."] mit passendem Anonymizer -> zusammengesetzte Ausgabe enthält die echte IBAN, kein Fragment.
    - test_deanonymize_stream_flushes_non_tag_bracket: ein "[" ohne folgendes "]" innerhalb von ~20 Zeichen wird trotzdem ausgeliefert (kein Hängenbleiben).
  </behavior>
  <action>In `webui/src/chat.py`: füge eine Modulfunktion `deanonymize_stream(chunks, anonymizer)` hinzu (Generator): puffert Text; wenn am Pufferende ein offenes `[` ohne folgendes `]` steht, wird der sichere Teil vor dem `[` de-anonymisiert ausgeliefert und der Rest zurückgehalten; Sicherheitsnetz: übersteigt der zurückgehaltene Puffer ~20 Zeichen ohne schließende Klammer, wird er trotzdem de-anonymisiert ausgeliefert (kein unbegrenztes Puffern); am Ende Rest flushen. Referenz-Logik in RESEARCH.md §"Streaming-sicheres De-Anonymisieren". Erweitere `build_chat_prompt` um einen optionalen Parameter `anonymizer: pii.Anonymizer | None = None`: `build_system_prompt(...)` (context.md/style.md/Status) bleibt UNVERÄNDERT roh; nur `message`, jeder `history`-`content` und die `mail_context`-Felder (`subject`/`sender`/`body`) werden — wenn `anonymizer` gesetzt — durch `anonymizer.anonymize(...)` geschickt, BEVOR sie in die parts eingesetzt werden. Die Truncate-Schritte (`[:MAX_MAIL_CONTEXT_BODY_CHARS]`, `_truncate_history`) müssen NACH dem Anonymisieren greifen (Redact-vor-Truncate). Ohne `anonymizer` (None) verhält sich `build_chat_prompt` exakt wie heute. `stream_chat` selbst bleibt unverändert (der Puffer wird vom Aufrufer in 10-03 um `stream_chat` gelegt).</action>
  <verify>
    <automated>(im Verzeichnis webui/) python -m pytest tests/test_chat.py -x — grün; danach python -m pytest tests/ — keine Regression</automated>
  </verify>
  <acceptance_criteria>
    - webui/src/chat.py enthält `def deanonymize_stream(`
    - build_chat_prompt akzeptiert `anonymizer`-Parameter; bestehende Aufrufe ohne das Argument bleiben lauffähig (Default None)
    - test_build_chat_prompt_system_stays_raw beweist: System-Prompt (context.md) bleibt roh
    - test_deanonymize_stream_reassembles_split_tag beweist: über Chunk-Grenze zerrissener Tag wird korrekt aufgelöst
    - `python -m pytest tests/` (webui) vollständig grün (keine Regression)
  </acceptance_criteria>
  <done>chat.py stellt den streaming-sicheren De-Anon-Puffer und die feldweise Prompt-Anonymisierung bereit; System-Prompt/context.md bleiben roh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebUI-Prozess -> Cloud-LLM (Stil-Extraktion / beratender Chat) | Gesendet-Mail-Inhalte bzw. Chat-Nachricht + Mail-Kontext (Kunden-PII) verlassen den Server |
| SSE-Stream WebUI -> Browser des Betreibers | De-anonymisierte Antwort-Chunks (echte PII) werden an den Browser geliefert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Information Disclosure | style_extract._fetch_sent_mail_bodies -> LLM | mitigate | Anonymize VOR Truncate; kein pii.redact-Rest; Test test_sent_bodies_no_raw_pii |
| T-10-08 | Information Disclosure | chat.build_chat_prompt (message/history/mail_context) -> LLM | mitigate | Feldweise Anonymisierung nur der Mail-/Chat-Felder; System-Prompt/context.md bleiben roh (D-08) |
| T-10-09 | Information Disclosure | SSE-Stream (zerrissener Platzhalter) | mitigate | deanonymize_stream-Puffer setzt tag-übergreifende Chunks zusammen (Pitfall 2) |
| T-10-10 | Information Disclosure | context.md/style.md versehentlich maskiert | accept/mitigate | D-08: nur Einzelfelder anonymisiert, nie der System-Prompt; Regressionstest test_build_chat_prompt_system_stays_raw |
| T-10-11 | Information Disclosure | Anonymizer-Mapping (RAM) | mitigate | Instanz-lokal pro Extraktion/Chat-Turn, nie geloggt/persistiert (D-04) |
</threat_model>

<verification>
- webui/-Test-Suite grün, Drift-Guard grün.
- `grep -n "anonymize\|deanonymize_stream" webui/src/chat.py webui/src/style_extract.py` zeigt die Anbindung.
- Reihenfolge-Check: in _fetch_sent_mail_bodies steht anonymize vor `[:MAX_BODY_CHARS]`.
</verification>

<success_criteria>
- ANON-03 (Stil + beratender Chat) erfüllt: beide Pfade anonymisieren feldweise vor dem Call.
- ANON-04 erfüllt: style.md platzhalterfrei; Streaming-Puffer verhindert Fragment-Leck.
- Keine Regression an bestehenden WebUI-Tests; context.md/style.md nachweislich roh.
</success_criteria>

<output>
Create `.planning/phases/10-reversible-pseudonymisierung-vor-llm-bermittlung-v1-6/10-02-SUMMARY.md` when done
</output>
