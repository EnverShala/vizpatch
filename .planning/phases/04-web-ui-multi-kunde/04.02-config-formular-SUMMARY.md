# 04.02 Summary — Basic-Auth + Konfig-Formular

**Abgeschlossen:** 2026-07-13
**Status:** ✅ Alle Acceptance-Criteria erfüllt

## Was wurde gebaut

- `webui/src/auth.py` — HTTP Basic Auth via `HTTPBasic` + `secrets.compare_digest`, liest WEBUI_USER/WEBUI_PASSWORD aus Env
- `webui/src/config_io.py` — Manual-Line-Parser für `.env` (Kommentare erhalten, Reihenfolge erhalten), Masking für Secret-Keys, atomic `context.md`-Write via `os.replace`, lazy Path-Evaluation per `os.getenv` pro Funktionsaufruf (kein Modul-Level ENV_PATH)
- `webui/src/templates/base.html` + `index.html` — Jinja2-Templates mit Konfig-Formular, leere Passwort-Felder, Placeholder-Warnung, AUTOSTART_ENABLED-Checkbox
- `webui/static/style.css` — ~100 Zeilen handgeschriebenes CSS
- `webui/src/main.py` — erweitert mit GET `/`, POST `/save`, `/_auth_check`; Password-UX W7 (leer = unverändert via `if imap_password.strip() != ""`)
- `webui/tests/conftest.py` — `authed_client`-Fixture ergänzt
- `webui/tests/test_auth.py` — 4 Tests
- `webui/tests/test_config_io.py` — 8 Tests (chmod-Test auf Windows als skip markiert)
- `webui/tests/test_endpoints_config.py` — 5 Tests inkl. Password-UX-Test
- `webui/Dockerfile` — `COPY static/ ./static/` ergänzt
- `deployment/kunde-env.example` — WEBUI_USER, WEBUI_PASSWORD, AUTOSTART_ENABLED, DOCKER_GID ergänzt

## Testergebnis

```
18 passed, 1 skipped (chmod 600 auf Windows), 0 failures
```

## Bugfix während Implementierung

FastAPI 0.139.0 wirft 422 bei leeren Form-Feldern mit `Form(...)`. Behoben: `context_md: str = Form("")` (Default leer statt Pflichtfeld).

## Commit

`6ff99a9 feat(04.02): Basic-Auth + Konfig-Formular (GET / + POST /save + config_io + auth)`
