---
phase: 04-web-ui-multi-kunde
plan: 05
type: execute
wave: 5
depends_on:
  - 04.02
  - 04.03
  - 04.04
files_modified:
  - webui/src/docker_ctrl.py
  - webui/src/main.py
  - webui/src/templates/index.html
  - webui/src/templates/_update_log.html
  - scripts/install-autostart.sh
  - scripts/build-deployment-package.sh
  - deployment/docker-compose.phase4.yml
  - deployment/README.phase4.md
  - deployment/kunde-env.example
  - webui/tests/test_docker_ctrl_update.py
  - webui/tests/test_endpoints_update.py
  - tests/test_install_autostart.sh
autonomous: true
requirements:
  - UI-05
tags:
  - webui
  - update
  - ghcr
  - tarball
  - systemd
  - deployment-package

must_haves:
  truths:
    - "Betreiber klickt 'Update pullen' in der WebUI, der webui-Container zieht ghcr.io/EnverShala/vizpatch:latest via docker-SDK, taggt lokal, führt docker compose up -d agent aus, zeigt Log-Ausgabe im UI"
    - "Alternative: Betreiber lädt einen vizpatch-vX.Y.Z.tar Tarball hoch, webui streamt File nach /tmp, ruft client.images.load, restartet agent, zeigt Log"
    - "Post-Install-Skript scripts/install-autostart.sh legt bei sudo ./install-autostart.sh enable die systemd-Unit /etc/systemd/system/vizpatch.service an, schreibt DOCKER_GID in .env idempotent (sed-Replace mit .bak-Backup), ruft systemctl enable — nach sudo reboot startet der Compose-Stack automatisch"
    - "scripts/build-deployment-package.sh baut in einer einzigen Ausführung sowohl vizpatch:v1.1.0 als auch vizpatch-webui:v1.1.0, packt beide Tarballs + context-seed.txt + install-autostart.sh + neue Compose + neue README ins Deployment-Paket dist/deployment-paket-v1.1.0/"
  artifacts:
    - path: "webui/src/docker_ctrl.py"
      provides: "Neu: pull_and_restart(image_ref), load_and_restart(tarball_path) — mit subprocess-Fallback für docker compose up -d agent"
      contains: "def pull_and_restart"
    - path: "scripts/install-autostart.sh"
      provides: "Idempotentes Post-Install-Skript enable|disable|status; schreibt vizpatch.service, hebt DOCKER_GID aus getent group docker, sed-Replace mit .bak-Backup in .env"
      contains: "vizpatch.service"
    - path: "scripts/build-deployment-package.sh"
      provides: "Erweitert um zweiten Docker-Build (webui), zweiten docker save, scripts/-Ordner-Kopie, docker-compose.phase4.yml als Ziel-Compose, README.phase4.md"
      contains: "vizpatch-webui"
    - path: "deployment/docker-compose.phase4.yml"
      provides: "Ziel-Compose beim Kunden — beide Services, DOCKER_GID-Interpolation, Bind-Mounts wie in 04.01 Task 3 (inkl. docker-compose.yml:/config/docker-compose.yml:ro), aber image: statt build: für beide Services"
      contains: "vizpatch-webui:v1.1.0"
    - path: "deployment/README.phase4.md"
      provides: "Kunde/Vor-Ort-Setup-Anleitung: docker load beider Tarballs, .env füllen, docker compose up -d, WebUI öffnen, install-autostart.sh optional"
    - path: "webui/src/templates/_update_log.html"
      provides: "Partial-Template mit <pre>-Block für den Pull/Load-Log"
  key_links:
    - from: "webui/src/main.py POST /update/pull"
      to: "webui/src/docker_ctrl.py pull_and_restart"
      via: "Function-Call"
      pattern: "pull_and_restart"
    - from: "webui/src/main.py POST /update/upload"
      to: "webui/src/docker_ctrl.py load_and_restart"
      via: "Function-Call mit streamed tarball_path"
      pattern: "load_and_restart"
    - from: "scripts/install-autostart.sh"
      to: "/etc/systemd/system/vizpatch.service + /opt/vizpatch/.env"
      via: "cat > $UNIT_PATH + sed -i.bak für DOCKER_GID"
      pattern: "vizpatch\\.service"
    - from: "scripts/build-deployment-package.sh"
      to: "dist/deployment-paket-v1.1.0/{vizpatch,vizpatch-webui}-v1.1.0.tar"
      via: "docker save"
      pattern: "docker save"
---

## Phase Goal

**As a** Vizionists-Techniker (Deployment) und Tankstellen-Betreiber (Runtime), **I want to** ein einzelnes Deployment-Paket mit beiden Images bauen und ausliefern, den Betreiber im Browser Updates einspielen lassen und beim Reboot des Kundenservers automatisch beide Services starten, **so that** der Rollout an weitere Tankstellen radikal beschleunigt wird und der Betreiber ohne SSH auf Update-Runden reagieren kann.

<objective>
UI-05 (Update-Aktion: GHCR-Pull + Tarball-Upload + Autostart-Checkbox schreibt/entfernt systemd-Unit via Post-Install-Skript) + Deployment-Paket-Builder-Erweiterung (D-40, D-41) ende-zu-ende funktional.

Purpose: Ohne Update-Weg müsste jeder Fix per SSH+scp eingespielt werden. Ohne Autostart überlebt das System keinen Reboot ohne manuellen Eingriff. Ohne Paket-Builder-Diff funktioniert das Vor-Ort-Setup mit zwei Containern nicht.

Output: docker_ctrl.py um pull_and_restart + load_and_restart erweitert, main.py um /update/pull + /update/upload erweitert, Update-Log-Partial, install-autostart.sh (idempotent via sed -i.bak) + bash-Test (9 Tests), build-deployment-package.sh erweitert, docker-compose.phase4.yml + README.phase4.md unter deployment/, 2 pytest-Files mit >= 6 Tests, 1 bash-Test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-web-ui-multi-kunde/04-CONTEXT.md
@.planning/phases/04-web-ui-multi-kunde/04-RESEARCH.md
@.planning/phases/04-web-ui-multi-kunde/04.01-walking-skeleton-PLAN.md
@.planning/phases/04-web-ui-multi-kunde/04.02-config-formular-PLAN.md
@.planning/phases/04-web-ui-multi-kunde/04.03-steuerung-status-PLAN.md
@.planning/phases/04-web-ui-multi-kunde/04.04-context-ki-assistent-PLAN.md
@webui/src/docker_ctrl.py
@webui/src/main.py
@scripts/build-deployment-package.sh
@agent/docker-compose.yml
@deployment/kunde-env.example
</context>

<interfaces>
Aus `webui/src/docker_ctrl.py` (nach 04.03):
- `_get_client()`, `get_agent_status()`, `control_agent(action)` bereits vorhanden
- Modul hat `subprocess` importiert, `AGENT_CONTAINER_NAME = "vizpatch-agent"`, `COMPOSE_DIR` bereits definiert

Aus `scripts/build-deployment-package.sh` (bestehend, 72 Zeilen):
- VERSION default "v1.0.0" → wird auf "v1.1.0"
- IMAGE_TAG single value → wird zu zwei (AGENT_IMAGE_TAG + WEBUI_IMAGE_TAG)
- `docker build -t "${IMAGE_TAG}" agent/` → dupliziert für webui
- `docker save` → zwei Tarballs
- Copy-Blöcke für docker-compose.yml, prompts/, deployment/, README.md → erweitert um scripts/ und webui/prompts/context-seed.txt
- SHA256-Checksum → für beide Tarballs

Aus `deployment/kunde-env.example` (nach 04.02): WEBUI_USER, WEBUI_PASSWORD, AUTOSTART_ENABLED, DOCKER_GID bereits ergänzt.

Aus 04.01 Task 3 (docker-compose.yml Bind-Mount): agent/docker-compose.yml hat `./docker-compose.yml:/config/docker-compose.yml:ro` — die Compose-File wird im webui-Container unter `/config/docker-compose.yml` bereitgestellt, damit `subprocess.run(["docker", "compose", "up", "-d", "agent"], cwd="/config")` funktioniert.

Docker-SDK-Interface für Update-Flow:
- `client.api.pull(image_ref, stream=True, decode=True)` → Iterator von dicts mit "status" und "progress"
- `client.images.get(image_ref).tag("vizpatch", tag="v1.1.0")` → lokalen Tag setzen damit Compose es findet
- `client.images.load(fileobj)` → nimmt file-like Objekt (nicht bytes) für Streaming
- `subprocess.run(["docker", "compose", "up", "-d", "agent"], cwd=COMPOSE_DIR, ...)` — funktioniert weil Docker-CLI + compose-plugin im webui-Image (04.01 Task 1) UND weil docker-compose.yml unter /config/docker-compose.yml verfügbar ist (04.01 Task 3 Bind-Mount)

systemd-Unit-Format (aus RESEARCH.md Zeile ~633-651):
- `[Unit] Description=... Requires=docker.service After=docker.service network-online.target Wants=network-online.target`
- `[Service] Type=oneshot RemainAfterExit=yes WorkingDirectory=/opt/vizpatch EnvironmentFile=/opt/vizpatch/.env ExecStart=$(which docker) compose up -d ExecStop=$(which docker) compose down TimeoutStartSec=120`
- `[Install] WantedBy=multi-user.target`
- **Wichtig aus Pitfall 6:** `EnvironmentFile=/opt/vizpatch/.env` MUSS drin sein, sonst kennt systemd DOCKER_GID nicht

Compose-Änderung für Deployment (docker-compose.phase4.yml):
- `image: vizpatch:v1.1.0` (statt build:) für agent-Service — Kundenserver macht KEIN docker build
- `image: vizpatch-webui:v1.1.0` (statt build:) für webui-Service
- Rest identisch mit agent/docker-compose.yml aus 04.01 Task 3 (inkl. docker-compose.yml-Bind-Mount)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: docker_ctrl.py — pull_and_restart + load_and_restart</name>
  <files>webui/src/docker_ctrl.py, webui/tests/test_docker_ctrl_update.py</files>
  <behavior>
    - pull_and_restart(image_ref="ghcr.io/EnverShala/vizpatch:latest") ruft client.api.pull(image_ref, stream=True, decode=True), iteriert Chunks, sammelt "status"/"progress" strings, returns list[str]
    - pull_and_restart taggt danach client.images.get(image_ref).tag("vizpatch", tag=VERSION) wo VERSION aus env WEBUI_AGENT_VERSION default "v1.1.0"
    - pull_and_restart ruft subprocess.run(["docker", "compose", "up", "-d", "agent"], cwd=COMPOSE_DIR) und appended stdout/stderr an log
    - load_and_restart(tarball_path: Path) öffnet den Path als binary, calls client.images.load(fileobj) — MIT dem Fileobjekt (nicht f.read()), extrahiert Image-Tags
    - load_and_restart ruft danach subprocess.run(["docker", "compose", "up", "-d", "agent"], cwd=COMPOSE_DIR)
    - Wenn pull APIError raised: log-line "pull_error: ...", subprocess wird NICHT ausgeführt, return log
    - Wenn tarball_path nicht existiert: raise FileNotFoundError
  </behavior>
  <read_first>
    - webui/src/docker_ctrl.py (aktueller Stand aus 04.03)
    - .planning/phases/04-web-ui-multi-kunde/04-RESEARCH.md Sektion "Update Flow — GHCR + Tarball" (Snippets Zeile ~745-793)
    - .planning/phases/04-web-ui-multi-kunde/04-RESEARCH.md Sektion "Common Pitfalls" Pitfall 7 (docker-CLI im Container, subprocess-Aufruf) und Anti-Patterns "docker.images.load(f.read())" (Memory-Overhead)
  </read_first>
  <action>
    Erweitere `webui/src/docker_ctrl.py` um zwei neue Funktionen:

    `def pull_and_restart(image_ref: str = "ghcr.io/EnverShala/vizpatch:latest") -> list[str]`:
    - `log: list[str] = []`
    - `client = _get_client()`
    - try: iteriere client.api.pull mit stream=True, decode=True, appende `chunk.get('status', '')` und `chunk.get('progress', '')` als kombinierten String an log
    - except APIError as e: appende "pull_error: {e}" an log und return log (kein Restart bei Fehler)
    - Tag registrieren: `version = os.getenv("WEBUI_AGENT_VERSION", "v1.1.0")`; `client.images.get(image_ref).tag("vizpatch", tag=version)`; appende "tagged vizpatch:{version}" an log
    - Compose-Restart: `result = subprocess.run(["docker", "compose", "up", "-d", "agent"], cwd=COMPOSE_DIR, capture_output=True, text=True, check=False)`; appende result.stdout, bei stderr appende "stderr: {result.stderr}"
    - return log

    `def load_and_restart(tarball_path: Path) -> list[str]`:
    - `if not tarball_path.exists(): raise FileNotFoundError(tarball_path)`
    - `log: list[str] = []`
    - `client = _get_client()`
    - `with tarball_path.open("rb") as f: images = client.images.load(f)` — file-object statt f.read() (Anti-Pattern in RESEARCH.md)
    - appende "loaded: {[img.tags for img in images]}" an log
    - Compose-Restart wie in pull_and_restart
    - return log

    Modul-Level `COMPOSE_DIR = os.getenv("WEBUI_COMPOSE_DIR", "/config")` sollte bereits aus 04.03 existieren. Der subprocess-Aufruf `docker compose up -d agent` funktioniert korrekt, weil:
    - Docker-CLI + compose-plugin sind im webui-Image (04.01 Task 1).
    - docker-compose.yml ist unter `/config/docker-compose.yml` als read-only Bind-Mount verfügbar (04.01 Task 3 + 04.05 Task 4 für Kunden-Compose).
    - `/var/run/docker.sock` ist gemountet, sodass docker-CLI mit dem Host-Daemon spricht.

    Erzeuge `webui/tests/test_docker_ctrl_update.py`:
    - Test 1 `test_pull_and_restart_iterates_stream`: mock client.api.pull returns iter([{"status": "Pulling"}, {"status": "Downloading", "progress": "50%"}, {"status": "Pull complete"}]). mock subprocess.run returns (stdout="ok", returncode=0). Call. Assert log contains "Pull complete" und "ok". Assert subprocess called mit expected args.
    - Test 2 `test_pull_and_restart_tags_locally`: mock client.images.get(...).tag called_once_with("vizpatch", tag="v1.1.0").
    - Test 3 `test_pull_api_error_logs_and_returns`: mock client.api.pull raises APIError("network"). Call, assert subprocess.run NOT called (via `assert_not_called()`), log enthält "pull_error".
    - Test 4 `test_load_and_restart_streams_file`: erstelle tmp .tar file (dummy content, 100 bytes ok für Test), mock client.images.load returns [MagicMock(tags=["vizpatch:v1.1.0"])]. Call load_and_restart(tmp_path). Assert client.images.load called mit einem file-object (nicht bytes); verify via `assert_called_once()` und `call_args[0][0]` ist file-like.
    - Test 5 `test_load_missing_file_raises`: `load_and_restart(Path("/nonexistent.tar"))` → FileNotFoundError.
    - Test 6 `test_env_version_override`: monkeypatch WEBUI_AGENT_VERSION=v1.2.3 → images.get().tag called with tag="v1.2.3".
  </action>
  <verify>
    <automated>cd webui && pytest tests/test_docker_ctrl_update.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Datei `webui/src/docker_ctrl.py` enthält `def pull_and_restart(` und `def load_and_restart(` und `client.api.pull(` und `client.images.load(`.
    - Datei `webui/src/docker_ctrl.py` enthält NICHT `f.read()` innerhalb von load_and_restart-Funktion (Anti-Pattern) — verifiziert durch: `grep -A5 "def load_and_restart" webui/src/docker_ctrl.py` zeigt kein `.read()`-Aufruf ohne Argumente.
    - Datei `webui/src/docker_ctrl.py` enthält `subprocess.run(` mit "docker" und "compose".
    - `pytest webui/tests/test_docker_ctrl_update.py -v` >= 6 passed.
  </acceptance_criteria>
  <done>
    pull_and_restart + load_and_restart implementiert, Streaming statt bytes-Load, subprocess-Fallback für Compose-Restart, 6 Tests grün.
  </done>
</task>

<task type="auto">
  <name>Task 2: POST /update/pull + POST /update/upload + Update-Log-Partial</name>
  <files>webui/src/main.py, webui/src/templates/_update_log.html, webui/src/templates/index.html, webui/tests/test_endpoints_update.py</files>
  <read_first>
    - webui/src/main.py (aktueller Stand nach 04.03/04.04)
    - webui/src/docker_ctrl.py (aus Task 1)
    - .planning/phases/04-web-ui-multi-kunde/04-RESEARCH.md Sektion "FastAPI Upload-Handler" (Streaming-Upload Snippet Zeile ~801-820)
    - .planning/phases/04-web-ui-multi-kunde/04-RESEARCH.md Sektion "HTML-Form dazu" (Zeile ~824-829)
  </read_first>
  <action>
    Erweitere `webui/src/main.py`:
    - Neue Imports: `UploadFile`, `File` aus fastapi, `tempfile`, `Path` aus pathlib.
    - Neuer Endpoint `@app.post("/update/pull", response_class=HTMLResponse)`:
      - Signature: `def update_pull(request: Request, user: str = Depends(auth.require_auth))`.
      - Body: `image_ref = os.getenv("WEBUI_UPDATE_IMAGE_REF", "ghcr.io/EnverShala/vizpatch:latest"); log = docker_ctrl.pull_and_restart(image_ref); return templates.TemplateResponse(request, "_update_log.html", {"log": log, "source": "pull", "image_ref": image_ref})`.
    - Neuer Endpoint `@app.post("/update/upload", response_class=HTMLResponse)`:
      - Signature: `def update_upload(request: Request, tarball: UploadFile = File(...), user: str = Depends(auth.require_auth))`.
      - Body: Validate `if not tarball.filename or not tarball.filename.endswith(".tar"): raise HTTPException(400, "Nur .tar-Files erlaubt")`.
      - Streaming in tmp-File: `with tempfile.NamedTemporaryFile(delete=False, suffix=".tar", dir="/tmp") as tmp: while chunk := tarball.file.read(1024 * 1024): tmp.write(chunk); tmp_path = Path(tmp.name)`.
      - Try/finally: `try: log = docker_ctrl.load_and_restart(tmp_path); finally: tmp_path.unlink(missing_ok=True)`.
      - Return `templates.TemplateResponse(request, "_update_log.html", {"log": log, "source": "upload", "filename": tarball.filename})`.

    Erzeuge `webui/src/templates/_update_log.html`:
    - `<div id="update-log">`
    - `<h3>Update-Log ({% if source == "pull" %}GHCR-Pull: {{ image_ref }}{% else %}Tarball: {{ filename }}{% endif %})</h3>`
    - `<pre>{% for line in log %}{{ line }}{{ '\n' }}{% endfor %}</pre>`
    - `</div>`

    Erweitere `webui/src/templates/index.html`:
    - Neue Sektion am Ende (nach Konfig-Formular): `<section class="update-section"><h2>Update</h2>`
    - GHCR-Pull-Button: `<button hx-post="/update/pull" hx-target="#update-log" hx-swap="innerHTML" hx-confirm="Update pullen und agent neu starten?">Latest von GHCR pullen</button>`
    - Tarball-Upload-Form: `<form hx-post="/update/upload" hx-encoding="multipart/form-data" hx-target="#update-log" hx-swap="innerHTML"><input type="file" name="tarball" accept=".tar" required><button type="submit">Tarball hochladen und Update starten</button></form>`
    - Container: `<div id="update-log"><!-- Update-Log landet hier --></div>`
    - `</section>`

    Erzeuge `webui/tests/test_endpoints_update.py` (nutzt `authed_client`-Fixture aus 04.02-conftest):
    - Test 1 `test_update_pull_requires_auth`: `authed_client.post("/update/pull")` → 401.
    - Test 2 `test_update_pull_calls_docker_ctrl`: Mock docker_ctrl.pull_and_restart returns ["Pulling", "Pull complete", "ok"]. POST mit auth → 200, response.text contains "Pull complete" und `id="update-log"`.
    - Test 3 `test_update_upload_rejects_non_tar`: POST /update/upload mit filename="image.zip" → 400.
    - Test 4 `test_update_upload_streams_to_tmp`: Mock docker_ctrl.load_and_restart returns ["loaded"]. POST mit einer dummy .tar Datei (bytes im TestClient `files={"tarball": ("test.tar", b"dummy content", "application/octet-stream")}`) → 200, response.text contains "loaded". Assert docker_ctrl.load_and_restart called_once_with einem Path.
    - Test 5 `test_index_shows_update_section`: GET / mit auth → response.text contains `hx-post="/update/pull"` und `name="tarball"`.
  </action>
  <verify>
    <automated>cd webui && pytest tests/test_endpoints_update.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Datei `webui/src/main.py` enthält `@app.post("/update/pull"` und `@app.post("/update/upload"` und `UploadFile` und `tempfile.NamedTemporaryFile`.
    - Datei `webui/src/main.py` enthält `tmp_path.unlink(missing_ok=True)` (tmp cleanup).
    - Datei `webui/src/templates/_update_log.html` enthält `id="update-log"` und `<pre>`.
    - Datei `webui/src/templates/index.html` enthält `hx-post="/update/pull"` und `name="tarball"` und `accept=".tar"`.
    - `pytest webui/tests/test_endpoints_update.py -v` >= 5 passed.
    - `pytest webui/tests/ -v` — ALLE bisherigen Tests bleiben grün.
  </acceptance_criteria>
  <done>
    POST /update/pull und POST /update/upload funktional, Streaming-Upload nutzt tempfile, HTMX-Update-Log-Panel im UI. Alle Tests grün.
  </done>
</task>

<task type="auto">
  <name>Task 3: install-autostart.sh (idempotent via sed -i.bak) + Bash-Skript-Test (9 Checks)</name>
  <files>scripts/install-autostart.sh, tests/test_install_autostart.sh</files>
  <read_first>
    - .planning/phases/04-web-ui-multi-kunde/04-RESEARCH.md Sektion "systemd Autostart Unit" (Unit-Content Zeile ~633-651) und "scripts/install-autostart.sh" (kompletter Bash-Skript-Draft Zeile ~657-737)
    - .planning/phases/04-web-ui-multi-kunde/04-RESEARCH.md Sektion "Common Pitfalls" Pitfall 1 (Docker-Socket-GID variiert) und Pitfall 6 (systemd + EnvironmentFile)
    - agent/docker-compose.yml (referenz für WorkingDirectory-Convention)
  </read_first>
  <action>
    Erzeuge `scripts/install-autostart.sh` mit exakter Struktur aus RESEARCH.md Zeile 657-737 (Executor liest die Vorlage aus RESEARCH.md und passt an). Wichtige Details:
    - Shebang `#!/usr/bin/env bash`, `set -euo pipefail`.
    - Root-Check: wenn `$EUID -ne 0` → Error "Muss mit sudo laufen" + exit 1.
    - Variablen: `ACTION="${1:-status}"`, `UNIT_PATH="/etc/systemd/system/vizpatch.service"`, `DEPLOY_DIR="${VIZPATCH_DIR:-/opt/vizpatch}"`.
    - Case-Statement für `enable | disable | status`:
      - `enable`:
        - Check `$DEPLOY_DIR` existiert, sonst Error+Exit.
        - Check `command -v docker`, sonst Error+Exit.
        - `DOCKER_GID=$(getent group docker | cut -d: -f3)` — dynamisch die Host-GID abgreifen.
        - **Idempotente sed-Replace für DOCKER_GID (W6-Fix, spezifisch mit .bak-Backup):**
          - Wenn `.env` bereits eine `DOCKER_GID=`-Zeile enthält: `sed -i.bak "s|^DOCKER_GID=.*|DOCKER_GID=$DOCKER_GID|" "$DEPLOY_DIR/.env"` — ersetzt die bestehende Zeile in-place, macht .bak-Backup.
          - Wenn `.env` KEINE `DOCKER_GID=`-Zeile enthält (z.B. Erst-Setup): `echo "DOCKER_GID=$DOCKER_GID" >> "$DEPLOY_DIR/.env"` (append).
          - Der Check erfolgt via `grep -q '^DOCKER_GID=' "$DEPLOY_DIR/.env"`.
        - Setze `AUTOSTART_ENABLED=true` in .env (analog: sed -i.bak wenn Zeile existiert, sonst append).
        - Cat > $UNIT_PATH mit dem Unit-Content aus RESEARCH.md. WICHTIG: `EnvironmentFile=$DEPLOY_DIR/.env` (Pitfall 6), `ExecStart=$(which docker) compose up -d`, `ExecStop=$(which docker) compose down`, `Type=oneshot`, `RemainAfterExit=yes`, `WorkingDirectory=$DEPLOY_DIR`, `Requires=docker.service After=docker.service network-online.target Wants=network-online.target`, `[Install] WantedBy=multi-user.target`.
        - `systemctl daemon-reload && systemctl enable vizpatch.service`.
        - Success-Message + Hinweis `sudo reboot` für Boot-Test.
      - `disable`:
        - Wenn $UNIT_PATH existiert: `systemctl disable vizpatch.service || true; rm -f "$UNIT_PATH"; systemctl daemon-reload`.
        - AUTOSTART_ENABLED=false in .env setzen (sed -i.bak wenn vorhanden).
        - Success-Message.
      - `status`:
        - Wenn $UNIT_PATH existiert: `systemctl status vizpatch.service --no-pager || true`.
        - Sonst: Meldung "vizpatch.service ist NICHT installiert."
      - default (*): Usage + exit 1.

    Erzeuge `tests/test_install_autostart.sh` (Bash-Skript-Test für die Kern-Funktionen — läuft ohne echten Sudo/Root, testet Skript-Struktur):
    - Shebang, `set -e`.
    - Test 1: `bash -n scripts/install-autostart.sh` — Syntax-Check (Skript parsed valide).
    - Test 2: `grep -q "getent group docker" scripts/install-autostart.sh` — Dynamische GID-Detection ist drin (Pitfall 1 Mitigation).
    - Test 3: `grep -q "EnvironmentFile=" scripts/install-autostart.sh` — Pitfall 6 Mitigation.
    - Test 4: `grep -q "Type=oneshot" scripts/install-autostart.sh` — Systemd-Type korrekt.
    - Test 5: `grep -q "systemctl enable vizpatch.service" scripts/install-autostart.sh` — enable-Aktion.
    - Test 6: `grep -q "systemctl disable vizpatch.service" scripts/install-autostart.sh` — disable-Aktion.
    - Test 7: Root-Check-Existenz — `grep -q "EUID" scripts/install-autostart.sh || grep -q "id -u" scripts/install-autostart.sh`.
    - Test 8: Skript-Ausführung ohne sudo als non-root → sollte mit exit 1 abbrechen. Simulate mit `output=$(bash scripts/install-autostart.sh enable 2>&1 || true); echo "$output" | grep -q "sudo"` (aussagekräftige Fehlermeldung).
    - **Test 9 (NEU, W6-Fix) — sed-Idempotenz für DOCKER_GID:**
      - Erstelle eine temp-.env-Datei: `TMP_ENV=$(mktemp) && trap "rm -f $TMP_ENV $TMP_ENV.bak" EXIT`.
      - Schreibe initial: `echo -e "IMAP_USER=test@x.de\nDOCKER_GID=999\nWEBUI_USER=admin" > "$TMP_ENV"`.
      - Führe die sed-Logik aus dem Skript direkt aus (extrahiere die relevante Zeile — Beispiel: `sed -i.bak "s|^DOCKER_GID=.*|DOCKER_GID=999|" "$TMP_ENV"`). Verifiziere: `grep -c "^DOCKER_GID=" "$TMP_ENV"` == 1.
      - Zweites sed mit anderem Wert: `sed -i.bak "s|^DOCKER_GID=.*|DOCKER_GID=998|" "$TMP_ENV"`.
      - Assertion: `[[ $(grep -c "^DOCKER_GID=" "$TMP_ENV") == 1 ]]` (nur eine Zeile) UND `grep -q "^DOCKER_GID=998$" "$TMP_ENV"` (Wert korrekt aktualisiert, kein "998" plus altes "999").
    - Jedes Test-Statement mit `echo "PASS: <name>"` bzw. `echo "FAIL: <name>"; exit 1`. Final `echo "All install-autostart.sh checks passed"`.

    Mache `scripts/install-autostart.sh` und `tests/test_install_autostart.sh` beide ausführbar via `chmod +x` (falls Windows: git-Attribut `executable` — auf Windows-Dev-Env ist chmod no-op, aber Linux-Kunde braucht das Bit).
  </action>
  <verify>
    <automated>bash tests/test_install_autostart.sh</automated>
  </verify>
  <acceptance_criteria>
    - Datei `scripts/install-autostart.sh` existiert und ist >= 50 Zeilen groß.
    - `bash -n scripts/install-autostart.sh` exit 0 (Syntax valide).
    - Datei enthält alle Zeichenfolgen: `getent group docker`, `EnvironmentFile=`, `Type=oneshot`, `systemctl enable vizpatch.service`, `systemctl disable vizpatch.service`, `WorkingDirectory=`, `[Install]`, `WantedBy=multi-user.target`.
    - Datei enthält `sed -i.bak` (W6 idempotenter Replace mit Backup). Grep-Assertion: `grep -c 'sed -i.bak' scripts/install-autostart.sh` >= 1.
    - Datei enthält `s|^DOCKER_GID=.*|DOCKER_GID=` (sed-Pattern für DOCKER_GID-Replace).
    - `bash tests/test_install_autostart.sh` exit 0 und gibt "All install-autostart.sh checks passed" aus.
    - Test 9 (sed-Idempotenz) läuft grün: nach zwei aufeinanderfolgenden DOCKER_GID-Sets (999 → 998) existiert exakt EINE DOCKER_GID-Zeile mit Wert 998.
  </acceptance_criteria>
  <done>
    install-autostart.sh idempotent für enable|disable|status via sed -i.bak, DOCKER_GID-Detection, EnvironmentFile-Mitigation, alle 9 Bash-Test-Checks grün (inkl. sed-Idempotenz-Test).
  </done>
</task>

<task type="auto">
  <name>Task 4: deployment/docker-compose.phase4.yml + deployment/README.phase4.md + kunde-env.example ergänzen (inkl. docker-compose.yml Bind-Mount)</name>
  <files>deployment/docker-compose.phase4.yml, deployment/README.phase4.md, deployment/kunde-env.example</files>
  <read_first>
    - agent/docker-compose.yml (nach 04.01 Task 3 — Ziel-Struktur mit beiden Services inkl. docker-compose.yml-Bind-Mount)
    - .planning/phases/04-web-ui-multi-kunde/04-RESEARCH.md Sektion "Docker Compose — vorher/nachher" (Ziel-Stand Zeile ~312-353)
    - .planning/phases/04-web-ui-multi-kunde/04-RESEARCH.md Sektion "Deployment Package" (Zeile ~831-903)
    - deployment/kunde-env.example (nach 04.02)
    - .planning/phases/04-web-ui-multi-kunde/04-RESEARCH.md Sektion "Sicherheits-Hinweis für README" (Zeile ~446-449)
  </read_first>
  <action>
    Erzeuge `deployment/docker-compose.phase4.yml` — Ziel-Compose für den Kundenserver. Unterschied zu `agent/docker-compose.yml`:
    - agent-Service: `image: vizpatch:v1.1.0` (KEIN `build:`-Directive, weil Kunde nur Tarball via docker load hat, keinen Source-Baum).
    - webui-Service: `image: vizpatch-webui:v1.1.0` (dito).
    - Rest identisch zu `agent/docker-compose.yml`: `env_file: .env`, alle Bind-Mounts wie in 04.01 Task 3 spezifiziert **inkl. `./docker-compose.yml:/config/docker-compose.yml:ro`** (B3-Fix — webui benötigt Compose-File erreichbar für subprocess-Fallback in control_agent/pull_and_restart/load_and_restart), `ports: - "8080:8080"` für webui, `group_add: - "${DOCKER_GID:-999}"`, `docker.sock:/var/run/docker.sock`, `logging: driver: json-file`, `agent-data:/data` (bei agent rw, bei webui ro), Named Volume `agent-data:` top-level.
    - Keine `container_name:` Einträge wegen möglicher Multi-Kunden-Konflikte? Behalten wie in agent-Compose (vizpatch-agent, vizpatch-webui — pro Kunde ohnehin nur einer).

    Erzeuge `deployment/README.phase4.md` — Ausliefer-Setup-Anleitung. Inhalt (auf Deutsch, für Vizionists-Techniker im Vor-Ort-Termin lesbar):
    - Überschrift "Vizpatch-Deployment v1.1.0 — Setup mit WebUI".
    - Sektion "Was ist im Paket": `vizpatch-v1.1.0.tar` (Agent-Image), `vizpatch-webui-v1.1.0.tar` (WebUI-Image), `docker-compose.yml` (beide Services), `.env`-Templates aus `deployment/`, `prompts/`-Ordner (inkl. `context-seed.txt` neu in Phase 4), `scripts/install-autostart.sh`, `context.md`-Templates.
    - Sektion "Setup-Schritte" nummeriert:
      1. `sha256sum -c vizpatch-v1.1.0.tar.sha256` und `sha256sum -c vizpatch-webui-v1.1.0.tar.sha256` (Integrität).
      2. `docker load -i vizpatch-v1.1.0.tar` (~30s).
      3. `docker load -i vizpatch-webui-v1.1.0.tar` (~15s).
      4. `mkdir -p /opt/vizpatch && cd /opt/vizpatch` — dann Paket-Inhalt hierher kopieren (docker-compose.yml, prompts/, scripts/, deployment/).
      5. `.env` erstellen: `cp deployment/kunde-env.example .env && chmod 600 .env`. Editieren mit `nano .env` — IMAP_USER, IMAP_PASSWORD, ANTHROPIC_API_KEY, IMAP_DRAFTS_FOLDER, OWN_EMAIL_ADDRESS, WEBUI_USER, WEBUI_PASSWORD (mind. `openssl rand -base64 24` als PW).
      6. `context.md` initial füllen (später kann Betreiber via WebUI-LLM-Seed nachbessern).
      7. `docker compose up -d` — beide Container starten.
      8. Browser auf `http://<server-ip>:8080/` — Basic-Auth-Prompt sollte kommen. Login mit WEBUI_USER/WEBUI_PASSWORD.
      9. Health-Check: `curl http://localhost:8080/healthz` → `{"status":"ok"}`.
      10. Optional Autostart: `sudo bash /opt/vizpatch/scripts/install-autostart.sh enable` — legt systemd-Unit an. Test: `sudo reboot`, warte 1 Min, dann `docker ps` — beide Container wieder up.
    - Sektion "Sicherheits-Warnung": Kompletter Text aus RESEARCH.md Zeile 446-449 (Docker-Socket-Root-Warnung, WebUI nur im LAN, WEBUI_PASSWORD zufällig generieren).
    - Sektion "Rollback (manuell, v1)": `docker images vizpatch` → alten Tag finden → `docker tag vizpatch:<alter-tag> vizpatch:v1.1.0 && docker compose up -d agent`.
    - Sektion "Troubleshooting": Docker-Socket-GID falsch (permission denied im webui-Log) → `install-autostart.sh` erneut ausführen (setzt DOCKER_GID neu via idempotentem sed). WebUI erreichbar aber /healthz 502 → `docker logs vizpatch-webui`. chmod-Warnungen für .env → `sudo chown 1000:1000 /opt/vizpatch/.env`. webui-Container-Log zeigt "no such file: /config/docker-compose.yml" → docker-compose.yml-Bind-Mount fehlt, docker-compose.yml aus dem Paket muss im gleichen Verzeichnis liegen wie die .env.

    Prüfe/erweitere `deployment/kunde-env.example` — sollte nach 04.02 bereits WEBUI_USER/WEBUI_PASSWORD/AUTOSTART_ENABLED/DOCKER_GID haben. Falls diese Placeholder fehlen: ergänzen. Zusätzlich Kommentar bei DOCKER_GID: "Wird durch scripts/install-autostart.sh mit der tatsächlichen Host-Docker-GID überschrieben (idempotent via sed). Falls Autostart nicht genutzt: manuell `getent group docker | cut -d: -f3` ausführen und Wert eintragen."
  </action>
  <verify>
    <automated>test -f deployment/docker-compose.phase4.yml && test -f deployment/README.phase4.md && grep -c "vizpatch-webui:v1.1.0" deployment/docker-compose.phase4.yml && grep -c "vizpatch:v1.1.0" deployment/docker-compose.phase4.yml && grep -q "install-autostart.sh" deployment/README.phase4.md && grep -q "sha256sum" deployment/README.phase4.md && grep -q "docker-compose.yml:/config/docker-compose.yml:ro" deployment/docker-compose.phase4.yml</automated>
  </verify>
  <acceptance_criteria>
    - Datei `deployment/docker-compose.phase4.yml` existiert und `docker compose -f deployment/docker-compose.phase4.yml config --quiet` exit 0 (ggf. mit dummy .env). Alternative: yaml-Syntax-Check via `python -c "import yaml; yaml.safe_load(open('deployment/docker-compose.phase4.yml'))"` exit 0.
    - Datei `deployment/docker-compose.phase4.yml` enthält `image: vizpatch:v1.1.0` und `image: vizpatch-webui:v1.1.0`.
    - Datei `deployment/docker-compose.phase4.yml` enthält NICHT `build:` (nur images bei Kunden-Compose).
    - Datei `deployment/docker-compose.phase4.yml` enthält `${DOCKER_GID:-999}` und `/var/run/docker.sock:/var/run/docker.sock` und `agent-data:/data:ro`.
    - **Datei `deployment/docker-compose.phase4.yml` enthält `./docker-compose.yml:/config/docker-compose.yml:ro`** (B3-Fix: webui benötigt Compose-File erreichbar für subprocess-Fallback). Grep-Assertion: `grep -c "docker-compose.yml:/config/docker-compose.yml:ro" deployment/docker-compose.phase4.yml` >= 1.
    - Datei `deployment/README.phase4.md` enthält "docker load", "vizpatch-webui-v1.1.0.tar", "install-autostart.sh", "8080", "WEBUI_PASSWORD", "openssl rand", "sha256sum".
    - Datei `deployment/kunde-env.example` enthält `WEBUI_USER=`, `WEBUI_PASSWORD=`, `AUTOSTART_ENABLED=`, `DOCKER_GID=`.
  </acceptance_criteria>
  <done>
    docker-compose.phase4.yml + README.phase4.md fertig (inkl. docker-compose.yml-Bind-Mount für subprocess-Fallback), kunde-env.example vollständig, Vor-Ort-Setup dokumentiert.
  </done>
</task>

<task type="auto">
  <name>Task 5: scripts/build-deployment-package.sh erweitern (zwei Images, zwei Tarballs, neue Compose, install-autostart, context-seed)</name>
  <files>scripts/build-deployment-package.sh</files>
  <read_first>
    - scripts/build-deployment-package.sh (aktueller Stand, 72 Zeilen, ein-Image-Builder)
    - .planning/phases/04-web-ui-multi-kunde/04-RESEARCH.md Sektion "Deployment Package — was ändert sich in `build-deployment-package.sh`" (kompletter Diff-Skizze Zeile ~831-903)
    - deployment/docker-compose.phase4.yml (aus Task 4)
    - deployment/README.phase4.md (aus Task 4)
    - scripts/install-autostart.sh (aus Task 3)
    - webui/prompts/context-seed.txt (aus 04.04 Task 1)
  </read_first>
  <action>
    Erweitere `scripts/build-deployment-package.sh` gemäß Diff-Skizze in RESEARCH.md Zeile 833-895. Konkrete Änderungen (kein fenced Code im Action-Text — Executor liest die Diff-Sektion aus RESEARCH.md):
    - VERSION default `v1.1.0` statt `v1.0.0` (Feature-Bump D-40).
    - Ersetze `IMAGE_TAG` durch zwei Variablen `AGENT_IMAGE_TAG="vizpatch:${VERSION}"` und `WEBUI_IMAGE_TAG="vizpatch-webui:${VERSION}"`.
    - Ersetze `TAR_NAME` durch `AGENT_TAR="vizpatch-${VERSION}.tar"` und `WEBUI_TAR="vizpatch-webui-${VERSION}.tar"`.
    - Neuer Build-Schritt für webui: `docker build -t "${WEBUI_IMAGE_TAG}" webui/` (nach dem bestehenden agent-Build).
    - Neuer `mkdir -p "${DIST_DIR}/scripts"` (für install-autostart.sh).
    - Zweiter `docker save` für webui-Tarball.
    - `cp` Zeile für docker-compose.yml ändern: Quelle ist nun `deployment/docker-compose.phase4.yml` (die neue Ziel-Compose für den Kundenserver mit beiden Services). Ziel bleibt `${DIST_DIR}/docker-compose.yml`.
    - Neuer `cp webui/prompts/context-seed.txt "${DIST_DIR}/prompts/"` (LLM-Seed-Prompt-Template mitliefern).
    - Neuer `cp scripts/install-autostart.sh "${DIST_DIR}/scripts/install-autostart.sh"` + `chmod +x`.
    - README-cp Quelle wechseln von `agent/README.md` auf `deployment/README.phase4.md` — Ziel bleibt `${DIST_DIR}/README.md`.
    - Zwei SHA256-Checksums statt einer: `sha256sum "${AGENT_TAR}" > "${AGENT_TAR}.sha256"` und `sha256sum "${WEBUI_TAR}" > "${WEBUI_TAR}.sha256"`.
    - Abschluss-`ls -la` und Echo bleibt strukturell; aktualisiere die Setup-Anleitung im Echo-Block: `docker load -i vizpatch-...tar` UND `docker load -i vizpatch-webui-...tar`.

    Das Skript muss idempotent bleiben (bestehender `rm -rf "${DIST_DIR}"` am Anfang wegen full-clean).
  </action>
  <verify>
    <automated>bash -n scripts/build-deployment-package.sh &amp;&amp; grep -c "vizpatch-webui" scripts/build-deployment-package.sh &amp;&amp; grep -c "docker save" scripts/build-deployment-package.sh &amp;&amp; grep -c "install-autostart.sh" scripts/build-deployment-package.sh &amp;&amp; grep -c "context-seed.txt" scripts/build-deployment-package.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/build-deployment-package.sh` exit 0 (Syntax valide).
    - Skript enthält `VERSION="${1:-v1.1.0}"`.
    - Skript enthält `vizpatch-webui` mindestens 3 mal (Image-Tag, Build, Save).
    - Skript enthält `docker save` mindestens 2 mal.
    - Skript enthält `install-autostart.sh` mindestens 1 mal (mit cp).
    - Skript enthält `context-seed.txt` mindestens 1 mal (mit cp).
    - Skript enthält `docker-compose.phase4.yml` (Quelle für Ziel-Compose).
    - Skript enthält `README.phase4.md` (Quelle für Ziel-README).
    - Skript enthält `sha256sum` und die Referenzen zu BEIDEN Tarballs (grep -c "sha256sum" >= 2, ODER ein sha256sum-Aufruf für "${AGENT_TAR}" UND ein weiterer für "${WEBUI_TAR}").
  </acceptance_criteria>
  <done>
    build-deployment-package.sh baut zwei Images, packt zwei Tarballs plus install-autostart.sh + context-seed.txt + neue Compose + neue README. Syntax valide.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Update-Flow (GHCR-Pull + Tarball-Upload) in der WebUI, Autostart-Post-Install-Skript mit systemd-Unit und idempotentem sed-Replace, erweiterter Deployment-Paket-Builder für v1.1.0.
  </what-built>
  <how-to-verify>
    Zwei Verifikations-Runden — lokale Ende-zu-ende und Deployment-Paket-Build:

    **Runde A: WebUI-Update-Flow lokal**
    1. Sicherstellen `.env` in `agent/` gültig (aus 04.02-Checkpoint übernommen).
    2. Aus `agent/`: `docker compose up -d webui`.
    3. Browser: http://localhost:8080/, Basic-Auth, scrollen zur Update-Sektion.
    4. Klick "Latest von GHCR pullen" → HTMX-Confirm-Dialog → Bestätigen. Warten. Update-Log-Panel zeigt Pull-Progress. **Erwartetes Ergebnis:** Wenn ghcr.io/EnverShala/vizpatch:latest im Registry nicht existiert (Public-GHCR erst später gepusht), Log zeigt "pull_error: 404" — das ist OK und beweist die Fehlerbehandlung. Wenn das Repo existiert und Pull klappt, restartet der agent automatisch.
    5. Alternativ: Aus 04.05-Build-Test einen kleinen dummy .tar erstellen (`docker save vizpatch:v1.0.0 -o /tmp/test.tar` falls das Image lokal existiert). Datei per Tarball-Upload-Form hochladen → Log zeigt "loaded" und Compose-Restart.
    6. `docker compose down`.

    **Runde B: Deployment-Paket-Build**
    7. Aus Root: `bash scripts/build-deployment-package.sh v1.1.0`. Erwartetes Ergebnis: beide Images werden gebaut (`vizpatch:v1.1.0` und `vizpatch-webui:v1.1.0`), Ordner `dist/deployment-paket-v1.1.0/` entsteht mit beiden Tarballs, `docker-compose.yml` (aus phase4-Template), `README.md` (aus phase4), `prompts/context-seed.txt`, `scripts/install-autostart.sh`, zwei `.sha256`-Files.
    8. `ls -la dist/deployment-paket-v1.1.0/` — verifiziere die 4-6 erwarteten Files/Ordner (vizpatch-v1.1.0.tar, vizpatch-webui-v1.1.0.tar, docker-compose.yml, README.md, prompts/, scripts/, deployment/, plus .sha256-Files).
    9. `sha256sum -c dist/deployment-paket-v1.1.0/vizpatch-v1.1.0.tar.sha256` (in dem dist-Ordner) — Integrität ok.
    10. Skript `install-autostart.sh` syntaktisch checken: `bash -n dist/deployment-paket-v1.1.0/scripts/install-autostart.sh` exit 0.
    11. Compose im Paket verifizieren: `docker compose -f dist/deployment-paket-v1.1.0/docker-compose.yml config --quiet` exit 0. **Zusätzlich:** `grep -q "docker-compose.yml:/config/docker-compose.yml:ro" dist/deployment-paket-v1.1.0/docker-compose.yml` — der B3-Bind-Mount muss auch im Ziel-Compose sein.

    **Runde C (optional, wenn Linux-VM verfügbar): systemd-Autostart-Test + sed-Idempotenz-Test**
    12. Deploy-Paket auf Ubuntu-Test-VM kopieren, `docker load` beider Tarballs, in `/opt/vizpatch` extrahieren, `.env` befüllen, `docker compose up -d`, dann `sudo bash /opt/vizpatch/scripts/install-autostart.sh enable`.
    13. `systemctl status vizpatch.service` → active (exited/running).
    14. **sed-Idempotenz-Test:** `sudo bash /opt/vizpatch/scripts/install-autostart.sh enable` ein zweites Mal ausführen. Prüfen: `grep -c "^DOCKER_GID=" /opt/vizpatch/.env` == 1 (nur eine Zeile, nicht doppelt).
    15. `sudo reboot`, 1 Min warten, `docker ps` → beide Container up.
    16. `sudo bash /opt/vizpatch/scripts/install-autostart.sh disable` → Unit weg, `systemctl status vizpatch.service` → nicht gefunden.

    Runde C ist optional wenn keine Linux-VM verfügbar; auf Windows-Dev-Env genügen Runde A + B.
  </how-to-verify>
  <resume-signal>Type "approved" wenn Update-Flow funktioniert, Paket-Build erfolgreich ist (inkl. docker-compose.yml-Bind-Mount in Ziel-Compose), und (optional) systemd-Test + sed-Idempotenz-Test bestanden sind. Beschreibe Abweichungen falls nicht.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| POST /update/pull → api-Aufruf an GHCR-Registry | Ausgehende HTTPS zu ghcr.io. Public-Registry, kein Auth. |
| POST /update/upload → Multipart-Stream zu Host-tmp | Betreiber uploaded arbiträre .tar-Datei. Wird docker.images.load ausgeführt — Registry-Injection möglich wenn Betreiber-Konto kompromittiert ist. |
| scripts/install-autostart.sh Ausführung mit sudo → Root-FS-Write | Legt systemd-Unit-Datei und schreibt in /opt/vizpatch/.env. Root-Vertrauen zwingend, verifiziert durch EUID-Check. |
| build-deployment-package.sh → docker daemon + FS-Write | Vizionists-Dev-Env. Nicht Runtime beim Kunden. Trust-Grenze OK. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.05-01 | Tampering | POST /update/upload — arbiträrer Tarball könnte Malware-Container enthalten | accept | Basic-Auth-Wall reduziert auf Betreiber-Persona. In v2: Signatur-Check via cosign/sigstore. |
| T-04.05-02 | Tampering | POST /update/pull — MITM auf ghcr.io? | mitigate | HTTPS zu ghcr.io mit Zertifikatsprüfung durch docker-CLI/SDK. Kein plain HTTP. |
| T-04.05-03 | Elevation of Privilege | install-autostart.sh wird mit sudo aufgerufen | mitigate | Root-Check via EUID; systemd-Unit-Content ist hard-coded (kein User-Input in ExecStart). WorkingDirectory nutzt `$DEPLOY_DIR` env var — potentiell manipulierbar. Mitigation: `${VIZPATCH_DIR:-/opt/vizpatch}` mit sensiblem Default. |
| T-04.05-04 | Denial of Service | Wiederholte Uploads großer Tarballs → /tmp voll | accept | Streaming statt Memory-Load; tmp_path.unlink im finally-Block. Kein Rate-Limit in v1. |
| T-04.05-05 | Information Disclosure | Update-Log zeigt Image-Layer-Details | accept | Nur an authentifizierten Betreiber (Basic-Auth). Kein PII-Leak. |
| T-04.05-06 | Denial of Service | `docker compose up -d agent` schlägt fehl, agent bleibt tot | mitigate | Update-Log zeigt stderr; Betreiber kann manuell via WebUI-Steuer-Panel starten (04.03). Rollback-Rezept im README.phase4.md. |
| T-04.05-SC | Tampering | Keine neuen Package-Installs in diesem Plan (nur Skripte + Templates) | mitigate | Keine slopcheck-Aktion nötig |
</threat_model>

<verification>
- Alle Tests aus 04.01-04.04 + neue Tests aus diesem Plan grün mit `cd webui && pytest tests/ -v`
- `bash tests/test_install_autostart.sh` grün mit 9 Checks (inkl. sed-Idempotenz)
- `bash scripts/build-deployment-package.sh v1.1.0` erfolgreich, `dist/deployment-paket-v1.1.0/` enthält beide Tarballs, alle Templates, install-autostart.sh
- Deployment-Paket-Inhalt validiert: SHA256-Checksums matchen, Compose-YAML valide, install-autostart.sh syntaktisch OK, docker-compose.yml-Bind-Mount im Ziel-Compose (B3)
- Manueller Update-Flow-Test in Checkpoint erfolgreich (mind. Runde A + B)
</verification>

<success_criteria>
- pull_and_restart + load_and_restart in docker_ctrl.py mit 6 grünen Tests
- POST /update/pull + POST /update/upload mit HTMX-Log-Panel, 5 grüne Endpoint-Tests
- install-autostart.sh idempotent via sed -i.bak (enable|disable|status), 9 Bash-Test-Checks grün (inkl. sed-Idempotenz-Test 9)
- deployment/docker-compose.phase4.yml + deployment/README.phase4.md existieren; docker-compose.phase4.yml enthält den docker-compose.yml-Bind-Mount (B3)
- scripts/build-deployment-package.sh baut zwei Images, packt zwei Tarballs + alle Extras
- Manueller Test aus Checkpoint erfolgreich (WebUI-Update-Flow + Paket-Build)
</success_criteria>

<output>
Create `.planning/phases/04-web-ui-multi-kunde/04.05-update-autostart-deployment-SUMMARY.md` when done.
</output>
</content>
</invoke>