---
phase: 06-schreibstil-adaption
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/src/config.py
  - agent/src/generate.py
  - agent/prompts/generate.txt
  - agent/tests/test_generate_with_style.py
  - agent/tests/conftest.py
autonomous: true
requirements: [STY-02]
must_haves:
  truths:
    - "Wenn style.md für einen Agenten existiert und ENABLE_STYLE_ADAPTION=true, enthält der Draft-Prompt einen Schreibstil-Block plus expliziten Hierarchie-Satz (context.md = WAS/Inhalt, style.md = nur WIE/Ton)"
    - "Fehlt oder leer ist style.md, ist der Draft-Prompt exakt wie heute — kein 'None'-String, kein Rest-Block zwischen den Markern"
    - "ENABLE_STYLE_ADAPTION=false unterdrückt die style.md-Injection auch wenn style.md existiert"
  artifacts:
    - path: "agent/src/config.py"
      provides: "Config-Feld style_md + style_file + Flag enable_style_adaption; Laden von agent_dir/style.md"
      contains: "enable_style_adaption"
    - path: "agent/src/generate.py"
      provides: "{style_md}-Format-Key im Prompt-Bau, konditional über enable_style_adaption"
      contains: "style_md"
    - path: "agent/prompts/generate.txt"
      provides: "# Schreibstil-Sektion mit Hierarchie-Formulierung"
      contains: "Schreibstil"
  key_links:
    - from: "agent/src/generate.py"
      to: "agent/prompts/generate.txt"
      via: "prompt_generate.format(style_md=...)"
      pattern: "\"style_md\""
    - from: "agent/src/config.py"
      to: "agent_dir/style.md"
      via: "style_file.read_text() in load_agent_config"
      pattern: "style\\.md"
---

<objective>
Der Agent liest pro Agent ein optionales `style.md` und injiziert es beim Draften zusätzlich zu `context.md` — mit fester Prompt-Hierarchie (context.md bestimmt den Inhalt, style.md nur Ton/Form). Fehlt das Profil, bleibt die Draft-Generierung byte-gleich zum heutigen Verhalten.

Purpose: Dies ist der Konsumenten-Kontrakt (STY-02). Der Draft-Pfad muss das style.md-Format erwarten, BEVOR die WebUI-Extraktion (Plan 06.02) es erzeugt — Interface-First. Definiert damit implizit das style.md-Zielformat (die 6 D-56-Abschnitte).
Output: geänderte `config.py`, `generate.py`, `generate.txt` + neue Testdatei.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-schreibstil-adaption/06-CONTEXT.md
@.planning/phases/06-schreibstil-adaption/06-PATTERNS.md

<interfaces>
<!-- Kern-Kontrakte, direkt aus dem Code extrahiert — Executor braucht keine weitere Codebase-Suche. -->

agent/src/generate.py — generate_draft_text baut den Prompt so:
  prompt = config.prompt_generate.format(**{
      "company_name": ..., "context_md_full": config.context_md,
      "conversation_history": history_block, "from": ..., "subject": ..., "body": ...,
  })
  -> ergänze Format-Key "style_md" analog zu "conversation_history"
     (leerer String bei fehlend/deaktiviert -> Platzhalter darf NIE "None" werden).

agent/src/config.py — Config ist ein @dataclass(frozen=True). Bestehende defaultete
  Trailing-Felder: agent_id: str = "" ; agent_enabled: bool = True.
  Feld-Muster context_md: context_file = agent_dir / "context.md";
              context_md = context_file.read_text(...) if context_file.exists() else ""
  Flag-Muster: enable_pii_redaction=(env.get("ENABLE_PII_REDACTION") or "true").lower() == "true"
  load_agent_config(agent_id, agent_dir) baut context_file = agent_dir / "context.md".
  _build_config(...) ist der gemeinsame Kern für load_config UND load_agent_config.

agent/tests/conftest.py — mock_config konstruiert Config(...) OHNE style_md/enable_style_adaption
  -> beide neuen Felder MÜSSEN defaultete Trailing-Felder sein, sonst bricht die Fixture.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing Test — style.md-Injection, Hierarchie, Leer-Fall, Flag-Off</name>
  <files>agent/tests/test_generate_with_style.py, agent/tests/conftest.py</files>
  <read_first>
    - agent/tests/test_generate_with_history.py (exaktes Analog: mock_config + mock_anthropic_generate, Prompt-Assertions via call_args.kwargs["prompt"], "None"-nicht-im-Abschnitt-Muster)
    - agent/tests/conftest.py (mock_config-Fixture Zeilen 19-55, mock_anthropic_generate Zeilen 69-77)
    - agent/src/generate.py (aktueller generate_draft_text-Prompt-Bau)
  </read_first>
  <behavior>
    - Test 1: config mit gesetztem style_md (6 Abschnitte, z.B. "## Anrede\nDu") + enable_style_adaption=True -> Prompt enthält den style_md-Text UND einen Hierarchie-Marker (Substring "Schreibstil")
    - Test 2: config mit leerem style_md ("") -> im Abschnitt zwischen "# Schreibstil" und "# Eingehende" steht kein "None"; Prompt-Verhalten wie heute
    - Test 3: config mit gesetztem style_md ABER enable_style_adaption=False -> style_md-Text NICHT im Prompt
    - Test 4 (Rückwärtskompat): generate_draft_text ohne style-bezogene Änderungen am Aufruf funktioniert weiter (bestehende History-Tests bleiben grün)
  </behavior>
  <action>
    Lege agent/tests/test_generate_with_style.py an, analog zu test_generate_with_history.py. Nutze dataclasses.replace(mock_config, style_md="## Anrede\nDu\n## Du/Sie\nDu\n## Grußformel\nViele Grüße\n## Satzlänge\nkurz\n## Formalität\nlocker\n## typische Wendungen\nMoin", enable_style_adaption=True) um Varianten zu bauen (mock_config ist frozen). Assertions gegen mock_anthropic_generate.call_args.kwargs["prompt"]. Für den Leer-Fall die "None"-nicht-im-Abschnitt-Technik aus test_generate_empty_history_no_none_in_prompt übernehmen (find("# Schreibstil") bis find("# Eingehende")). Ergänze in conftest.py die mock_config-Konstruktion NICHT zwingend um style-Werte — verlasse dich auf dataclasses.replace; falls die Config-Felder in Task 2 noch nicht existieren, schlägt der Test rot fehl (RED-Phase, gewollt). Test MUSS zu diesem Zeitpunkt fehlschlagen (Felder/Format-Key existieren noch nicht).
  </action>
  <verify>
    <automated>cd agent && python -m pytest tests/test_generate_with_style.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_generate_with_style.py` läuft und schlägt fehl mit AttributeError/TypeError (style_md-Feld bzw. Format-Key fehlt noch) — bestätigt gültige RED-Phase
    - Datei enthält 4 Testfunktionen (Injection, Leer-Fall-kein-None, Flag-off, Rückwärtskompat)
  </acceptance_criteria>
  <done>Failing Test existiert und beschreibt alle vier Verhaltensfälle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: config.py — style_md-Feld + style_file-Load + enable_style_adaption-Flag</name>
  <files>agent/src/config.py</files>
  <read_first>
    - agent/src/config.py (Config-dataclass Zeilen 84-129, _build_config Zeilen 131-209, load_agent_config Zeilen 259-284)
    - agent/tests/test_config_multi_agent.py (bestehende load_agent_config-Tests, dürfen nicht brechen)
  </read_first>
  <action>
    Ergänze die @dataclass(frozen=True) Config um zwei DEFAULTETE Trailing-Felder (nach agent_enabled, damit alle bestehenden Config(...)-Konstruktionsstellen inkl. mock_config unverändert bauen): `style_md: str = ""` und `enable_style_adaption: bool = True`. In _build_config: lade `style_md` analog zum context_md-Muster — style_file = context_file.parent / "style.md" (context_file ist bereits agent_dir/"context.md", also agent_dir), `style_md = style_file.read_text(encoding="utf-8") if style_file.exists() else ""`; das `if exists else ""`-Guard ist Pflicht (Fehler-Isolation: fehlendes style.md darf den Draft-Pfad nie brechen). Setze `enable_style_adaption=(env.get("ENABLE_STYLE_ADAPTION") or "true").lower() == "true"` (Default true, D-54), exakt nach dem enable_pii_redaction-Muster. Übergib beide neuen Werte im Config(...)-Konstruktor am Ende von _build_config. STYLE_SAMPLE_COUNT wird HIER NICHT benötigt (nur WebUI-seitig, Plan 06.02).
  </action>
  <verify>
    <automated>cd agent && python -m pytest tests/test_config_multi_agent.py tests/test_config_provider_override.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "enable_style_adaption" agent/src/config.py` zeigt Feld-Definition UND env-Auflösung
    - `grep -c "style.md" agent/src/config.py` >= 1
    - Bestehende Config-Tests (test_config_multi_agent, test_config_provider_override) bleiben grün
  </acceptance_criteria>
  <done>Config lädt style.md pro Agent (leer wenn fehlend) und exponiert enable_style_adaption.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: generate.py + generate.txt — {style_md}-Injection mit Hierarchie</name>
  <files>agent/src/generate.py, agent/prompts/generate.txt</files>
  <read_first>
    - agent/src/generate.py (generate_draft_text Zeilen 46-87, insb. der .format(**{...})-Block)
    - agent/prompts/generate.txt (aktuelles Template, 22 Zeilen)
    - agent/tests/test_generate_with_style.py (aus Task 1 — Zielverhalten)
  </read_first>
  <action>
    In generate.py: berechne vor dem .format(...) `style_block = config.style_md if config.enable_style_adaption else ""` und füge den Format-Key `"style_md": style_block` in das Dict ein (analog zu conversation_history -> history_block). Kein weiterer Aufruf-Parameter nötig; style kommt allein aus config. In generate.txt: füge NACH dem "# Firmen-Kontext"-Block und VOR "# Bisheriger Gesprächsverlauf" eine neue Sektion mit Überschrift "# Schreibstil (nur Ton/Form — wenn vorhanden)" ein, darunter der Platzhalter {style_md}, plus EINEN expliziten Hierarchie-Satz in der Kopfzeile des Templates (Zeile 3-Bereich), z.B. dass der Firmen-Kontext bestimmt WAS gesagt wird und der Schreibstil NUR das WIE (Ton, Anrede, Grußformel) — er darf fachliche Vorgaben aus dem Firmen-Kontext NIE übersteuern (Risiko: lockerer Ton bei Beschwerden). Der Platzhalter {style_md} muss im .format-Aufruf immer vorhanden sein (leerer String bei fehlend/deaktiviert -> keine "None").
  </action>
  <verify>
    <automated>cd agent && python -m pytest tests/test_generate_with_style.py tests/test_generate_with_history.py tests/test_generate.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Alle 4 Tests aus test_generate_with_style.py sind GRÜN
    - test_generate_with_history.py und test_generate.py bleiben grün (Rückwärtskompatibilität)
    - `grep -c "style_md" agent/prompts/generate.txt` == 1 (genau ein Platzhalter)
    - `grep -in "schreibstil" agent/prompts/generate.txt` zeigt die neue Sektion + Hierarchie-Formulierung
  </acceptance_criteria>
  <done>generate_draft_text injiziert style.md konditional; leerer/deaktivierter Fall ist byte-neutral zum heutigen Prompt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| style.md-Inhalt → Draft-Prompt | style.md ist aus Mail-Inhalten destilliert (Plan 06.02); der Text fließt ungefiltert in den LLM-Prompt |
| Firmen-Kontext vs. Stil-Hierarchie | style.md darf fachliche Vorgaben aus context.md nicht übersteuern |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering (Prompt-Injection) | generate.py {style_md}-Block | mitigate | Hierarchie-Satz im Template verankert style.md explizit als NUR Ton/Form, nachrangig zum Firmen-Kontext; A/B-Fixture-Prüfung (Beschwerde-Ton) in Plan 06.03-Verifikation |
| T-06-02 | Denial of Service | fehlendes/kaputtes style.md | mitigate | `if style_file.exists() else ""`-Guard in config.py — kein Crash im Draft-Pfad (STY-05-Fehler-Isolation) |
| T-06-SC | Tampering | keine neuen Package-Installs in diesem Plan | accept | Nur Änderungen an bestehenden agent-Modulen; keine Dependency-Änderung |
</threat_model>

<verification>
- `cd agent && python -m pytest tests/ -q` — Gesamt-Suite bleibt grün (keine Regression)
- Manuell: style.md mit lockerem Ton + Beschwerde-Fixture -> Draft übernimmt Ton, behält aber sachliche Beschwerde-Antwort (Hierarchie hält) — vollständige A/B-Abnahme erfolgt in 06.03
</verification>

<success_criteria>
STY-02 erfüllt: style.md wird bei jedem Draft zusätzlich zu context.md injiziert, mit dokumentierter Hierarchie; fehlendes style.md → Draft-Generierung unverändert wie heute.
</success_criteria>

<output>
Create `.planning/phases/06-schreibstil-adaption/06-01-SUMMARY.md` when done
</output>
