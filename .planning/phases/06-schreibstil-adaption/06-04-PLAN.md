---
phase: 06-schreibstil-adaption
plan: 04
type: execute
wave: 3
depends_on: [06-01, 06-03]
files_modified:
  - agent/tests/fixtures/style_ab
autonomous: false
requirements: [STY-02, STY-05]
must_haves:
  truths:
    - "Ein Draft MIT Stil-Profil unterscheidet sich sichtbar im TON von einem Draft OHNE Profil"
    - "Der Fach-Inhalt bleibt gleich — ein lockerer Stil übersteuert eine sachliche Beschwerde-Antwort NICHT (Hierarchie hält)"
    - "Der End-to-End-Klick-Pfad im WebUI funktioniert: Agent anlegen -> style.md automatisch/per Button -> editieren -> Re-Learn -> Draft im Agent nutzt den Stil"
    - "Ein migrierter Agent (Creds bereits vollständig, kein style.md) löst beim Speichern eines ANDEREN Fieldsets (context.md, Passwort) KEINE Auto-Extraktion aus (Esso-Guard, SC5) — style.md dort nur per Re-Learn-Button"
  artifacts:
    - path: "agent/tests/fixtures/style_ab"
      provides: "A/B-Fixture-Dokumentation (Prompt-Input mit/ohne style.md, Beschwerde-Fall)"
  key_links:
    - from: "style.md"
      to: "Draft-Ton"
      via: "generate.py-Injection (06.01) über echten LLM-Call"
      pattern: "style"
---

<objective>
Qualitative Abnahme der Stil-Adaption (SC2 der Roadmap): der A/B-Nachweis, dass ein Stil-Profil den TON sichtbar verändert, ohne den fachlichen Inhalt zu übersteuern — insbesondere im kritischen Beschwerde-Fall. Diese Prüfung ist bewusst als Checkpoint von der Implementierung getrennt (subjektive Ton-Bewertung braucht menschliche Augen auf echte Drafts).

Purpose: SC2 A/B-Nachweis + Hierarchie-Härtung (Risiko „lockerer Ton übersteuert Beschwerde-Antwort"); End-to-End-Klick-Abnahme.
Output: kurze A/B-Fixture-Notiz + Betreiber-Freigabe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-schreibstil-adaption/06-CONTEXT.md
@.planning/phases/06-schreibstil-adaption/06-01-PLAN.md
@.planning/phases/06-schreibstil-adaption/06-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: A/B-Fixture vorbereiten (mit/ohne style.md, Beschwerde-Fall)</name>
  <files>agent/tests/fixtures/style_ab</files>
  <read_first>
    - agent/src/generate.py (aktueller Injection-Pfad nach 06.01)
    - agent/tests/conftest.py (mock_config zum Nachstellen)
    - .planning/phases/06-schreibstil-adaption/06-CONTEXT.md (specifics: A/B-Nachweis, lockerer Ton darf Beschwerde nicht übersteuern)
  </read_first>
  <action>
    Lege unter agent/tests/fixtures/style_ab drei kleine Text-/Markdown-Dateien an: (1) ein Beispiel-style.md mit deutlich lockerem, du-basiertem Ton (6 D-56-Abschnitte); (2) eine sachlich-neutrale Kundenanfrage (Standard-Fall, z.B. Öffnungszeiten); (3) eine Beschwerde-Mail (verärgerter Kunde). Dokumentiere in einer README.md im selben Ordner den Abnahme-Ablauf: denselben Fall zweimal durch generate_draft_text laufen lassen — einmal mit style_md gesetzt, einmal leer — und die zwei Draft-Paare für den Checkpoint bereitstellen (echter LLM-Call, kein Mock). Ziel-Beobachtung: Standard-Fall Ton ändert sich; Beschwerde-Fall bleibt trotz lockerem style.md sachlich/deeskalierend (Hierarchie hält).
  </action>
  <verify>
    <automated>test -f agent/tests/fixtures/style_ab/README.md && ls agent/tests/fixtures/style_ab | grep -c . </automated>
  </verify>
  <acceptance_criteria>
    - Ordner agent/tests/fixtures/style_ab enthält >= 3 Dateien + README.md mit Abnahme-Ablauf
    - README beschreibt beide Fälle (Standard + Beschwerde) und die erwartete Ton-vs-Inhalt-Beobachtung
  </acceptance_criteria>
  <done>A/B-Material für den Checkpoint steht bereit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    style.md-Injection (06.01), WebUI-Extraktion + Endpoints + Fieldset (06.02/06.03). Für die Abnahme liegen A/B-Fixtures bereit (Task 1).
  </what-built>
  <how-to-verify>
    1. WebUI starten (docker compose up), Login (falls gesetzt).
    2. Neuen Agenten anlegen, gültige IMAP-Creds + LLM-Key eines Postfachs mit gesendeten Mails speichern.
    3. Prüfen: style.md erscheint automatisch im Fieldset (oder per „Schreibstil neu lernen"-Button). Freitext-Feld ausfüllen, Re-Learn klicken -> Bestätigung -> style.md wird überschrieben, Freitext bleibt erhalten.
    4. style.md editieren + „Diesen Abschnitt übernehmen" -> gespeichert.
    5. A/B (SC2): Standard-Anfrage (Fixture 2) draften lassen — einmal mit aktivem style.md, einmal mit ENABLE_STYLE_ADAPTION aus bzw. leerem style.md. Erwartung: sichtbar anderer TON, gleicher Fach-Inhalt.
    6. Beschwerde-Fall (Fixture 3) mit lockerem style.md draften: Erwartung — Ton bleibt trotz Profil sachlich/deeskalierend (Firmen-Kontext-Hierarchie hält, style.md übersteuert nicht).
    7a. Esso-Guard (style.md vorhanden): bestehenden Agenten mit vorhandenem style.md erneut speichern -> KEINE ungefragte Neu-Extraktion.
    7b. Esso-Guard (migriert, KRITISCH): Agent mit bereits vollständigen Creds ABER OHNE style.md (migrierter Esso-Zustand nachstellen) — nur ein anderes Fieldset speichern (context.md ODER Passwort-Rotation). Erwartung: KEINE Extraktion, style.md bleibt leer; erst der Re-Learn-Button erzeugt es. Dies ist das Blocker-1-Regressionsszenario.
    8. Leeres/kein Gesendet-Ordner-Postfach ohne Freitext -> klarer STY-05-Hinweis statt Crash.
  </how-to-verify>
  <resume-signal>„approved" wenn Ton-Unterschied sichtbar UND Beschwerde-Hierarchie hält UND Klick-Pfad + BEIDE Esso-Guards (7a vorhandenes style.md, 7b migriert-ohne-style.md) + STY-05-Hinweis stimmen; sonst konkrete Abweichungen beschreiben.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| style.md → Draft-Ton | Verifikation, dass die Hierarchie-Mitigation (T-06-01) real greift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering (Prompt-Injection) | style.md übersteuert Firmen-Kontext | mitigate | Menschliche A/B-Abnahme des Beschwerde-Falls bestätigt, dass style.md NUR den Ton beeinflusst (SC2) |
| T-06-SC | Tampering | keine Package-Installs | accept | Reine Fixture-/Verifikations-Arbeit |
</threat_model>

<verification>
Betreiber-Freigabe („approved") nach dem Klick-Pfad + A/B-Beobachtung.
</verification>

<success_criteria>
SC2 nachgewiesen: Draft mit vs. ohne Stil-Profil unterscheidet sich sichtbar im Ton, nicht im Fach-Inhalt; Beschwerde-Hierarchie hält; End-to-End-Klick-Pfad + Esso-Guard + STY-05-Hinweis verifiziert.
</success_criteria>

<output>
Create `.planning/phases/06-schreibstil-adaption/06-04-SUMMARY.md` when done
</output>
