---
phase: 06-schreibstil-adaption
plan: 03
type: execute
wave: 2
depends_on: [06-02]
files_modified:
  - webui/src/main.py
  - webui/src/templates/index.html
  - webui/tests/test_endpoints_style.py
autonomous: true
requirements: [STY-01, STY-03, STY-05]
must_haves:
  truths:
    - "Betreiber sieht pro Agent ein style.md-Fieldset, editiert es und speichert per Section-Save (HTMX)"
    - "Ein optionales Freitext-Feld (Stil-Beschreibung) wird gespeichert und überlebt einen Re-Learn (getrennte Datei style_note.md)"
    - "'Schreibstil neu lernen'-Button mit Bestätigung ruft die Extraktion, überschreibt style.md und zeigt das Ergebnis sofort im Fieldset"
    - "Beim ersten Speichern gültiger IMAP-Credentials entsteht style.md automatisch (best-effort, graceful) wenn ENABLE_STYLE_ADAPTION=true und noch kein style.md existiert"
    - "Zu wenig Material ohne Freitext -> klarer Hinweis statt schlechtem Profil (STY-05)"
    - "ENABLE_STYLE_ADAPTION-Schalter pro Agent im WebUI umschaltbar"
  artifacts:
    - path: "webui/src/main.py"
      provides: "/style/relearn-Endpoint + style-Felder im /save + Auto-Extraktion beim ersten Cred-Save"
      contains: "/style/relearn"
    - path: "webui/src/templates/index.html"
      provides: "style-Fieldset (style_md-Textarea + Freitext + Enable-Checkbox + Section-Save + Re-Learn-Button + JS)"
      contains: "save-msg-style"
    - path: "webui/tests/test_endpoints_style.py"
      provides: "Endpoint-Tests für /style/relearn + /save-style + Auto-Trigger"
  key_links:
    - from: "webui/src/main.py"
      to: "webui/src/style_extract.py"
      via: "extract_style(agent_id) in /style/relearn und Auto-Trigger"
      pattern: "extract_style\\("
    - from: "webui/src/main.py"
      to: "webui/src/agents_io.py"
      via: "write_style_md_atomic / write_style_note_atomic"
      pattern: "write_style_md_atomic|write_style_note_atomic"
    - from: "webui/src/templates/index.html"
      to: "/style/relearn"
      via: "fetch(...) im Re-Learn-JS"
      pattern: "/style/relearn"
---

<objective>
Der Betreiber steuert die Stil-Adaption vollständig im Browser: ein style.md-Fieldset pro Agent (sichtbar, editierbar, Section-Save), ein optionales Freitext-Feld, ein Enable/Disable-Schalter und ein „Schreibstil neu lernen"-Button mit Bestätigung. Zusätzlich entsteht style.md automatisch beim ersten Speichern gültiger IMAP-Zugangsdaten (best-effort, graceful) — der Esso-Guard bleibt erhalten (bestehende Agenten lernen nie ungefragt).

Purpose: STY-03 (editierbares Fieldset + Re-Learn-Button + Enable-Flag), STY-01 (automatische Extraktion beim Setup), STY-05 (Hinweis bei zu wenig Material / kein Learning-Loop).
Output: geänderte main.py + index.html + neue Endpoint-Testdatei. Vollständige, klickbare Feature-Slice.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-schreibstil-adaption/06-CONTEXT.md
@.planning/phases/06-schreibstil-adaption/06-PATTERNS.md
@.planning/phases/06-schreibstil-adaption/06-02-PLAN.md

<interfaces>
<!-- Kontrakte aus 06.02 + bestehendem Code — keine Codebase-Suche nötig. -->

webui/src/style_extract.py (aus 06.02):
  extract_style(agent_id: str) -> str        # style.md-Text
  StyleExtractionEmpty(RuntimeError)          # < MIN_USABLE_MAILS und leerer Freitext (STY-05)
  ValueError                                  # invalider agent_id
  RuntimeError                                # kein API-Key / LLM-/IMAP-Fehler

webui/src/agents_io.py (aus 06.02):
  read_style_md(agent_id) -> str ; write_style_md_atomic(agent_id, content)
  read_style_note(agent_id) -> str ; write_style_note_atomic(agent_id, content)
  read_env_raw(agent_id) -> dict ; write_env(agent_id, updates) ; MASKED = "****"

webui/src/main.py — bestehende Muster:
  /context/generate (Zeilen 218-254): agent_id-Guard->400, key-Guard->400,
    ValueError->400, RuntimeError->500, generisch->500; PlainTextResponse(text);
    @limiter.limit("10/minute"); Depends(auth.require_auth).
  /save (Zeilen 267-379): Section-Save via HX-Request-Header; _save_response(...)
    liefert HTML-Fragment (HTMX) oder Redirect. agent_fields_submitted-Block schreibt
    IMAP/LLM/context. autostart_enabled-Muster (Zeile 319) = Checkbox->"true"/"false".
  Re-Learn-Bestätigung: Zwei-Stufen-Muster /reset (Zeilen 382-402) bzw. clientseitig
    hx-confirm / confirm() wie /update/pull (index.html:268).

webui/src/templates/index.html:
  context.md-Fieldset (Zeilen 171-189) + generateContext(btn)-JS (Zeilen 220-248):
    Button disabled + „⏳ Generiere…" während fetch('/context/generate'); Ergebnis in Textarea.
  Section-Save-Button-Muster (Zeilen 136-139): hx-post="/save"
    hx-include="closest fieldset, #active-agent-id" hx-target="#save-msg-XXX".
  {{ agent_id }} / {{ env.* }} / {{ context_md }} sind im Template verfügbar.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing Endpoint-Tests — /style/relearn, /save-style, Auto-Trigger</name>
  <files>webui/tests/test_endpoints_style.py</files>
  <read_first>
    - webui/tests/test_endpoints_seed.py (exaktes Analog: TestClient, Auth-Mock, /context/generate-Mock von llm_seed.generate, Fehlercode-Assertions)
    - webui/tests/conftest.py (Fixtures: TestClient, WEBUI_CONFIG_ROOT/tmp, Agent-Anlage-Helper)
    - webui/src/main.py (/context/generate + /save als Signatur-Vorbild)
  </read_first>
  <behavior>
    - POST /style/relearn mit gültiger agent_id: mockt style_extract.extract_style -> "## Anrede\nDu…"; Antwort 200, PlainText enthält den Stil-Text; agents_io hat style.md geschrieben
    - POST /style/relearn wenn extract_style StyleExtractionEmpty wirft -> 400 mit STY-05-Hinweistext (kein 500)
    - POST /style/relearn persistiert das mitgesendete Freitext-Feld (style_note) VOR der Extraktion (überlebt Overwrite)
    - POST /save mit style_md-Feld -> write_style_md_atomic aufgerufen; Section-Save gibt HTMX-Fragment mit save-ok
    - POST /save mit enable_style_adaption-Checkbox -> ENABLE_STYLE_ADAPTION in agent-.env "true"/"false"
    - Auto-Trigger: POST /save mit erstmaligen gültigen IMAP-Creds+Key, kein style.md, ENABLE_STYLE_ADAPTION!=false -> extract_style wird (best-effort) aufgerufen; wirft es, wird der Save trotzdem als ok beantwortet (graceful)
    - Auto-Trigger unterbleibt, wenn style.md bereits existiert (Esso-Guard) — extract_style NICHT aufgerufen
  </behavior>
  <action>
    Lege webui/tests/test_endpoints_style.py an, analog test_endpoints_seed.py. Mocke webui.src.style_extract.extract_style (bzw. src.main.style_extract.extract_style je nach Import-Stil) statt echter IMAP/LLM. Nutze die bestehenden conftest-Fixtures für Agent-Anlage (schreibe eine agent-.env mit verschlüsselten Dummy-Creds oder nutze den vorhandenen Helper). Alle Tests MÜSSEN zunächst rot sein (Endpoint/Felder existieren noch nicht).
  </action>
  <verify>
    <automated>cd webui && python -m pytest tests/test_endpoints_style.py -x</automated>
  </verify>
  <acceptance_criteria>
    - test_endpoints_style.py existiert mit >= 6 Testfunktionen (relearn-ok, relearn-empty->400, style_note-persist, save-style, enable-flag, auto-trigger + auto-trigger-skip)
    - Läuft und schlägt fehl (404/422 für fehlenden Endpoint/Felder) — gültige RED-Phase
  </acceptance_criteria>
  <done>Failing Endpoint-Tests beschreiben das gesamte STY-03/STY-01/STY-05-Verhalten.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: main.py — /style/relearn + style-Felder im /save + Auto-Extraktion</name>
  <files>webui/src/main.py</files>
  <read_first>
    - webui/src/main.py (Import-Zeile 14; /context/generate Zeilen 218-254; /save Zeilen 267-379 inkl. _save_response + agent_fields_submitted-Block + autostart_enabled-Muster)
    - webui/src/style_extract.py (aus 06.02 — extract_style + StyleExtractionEmpty)
    - webui/src/agents_io.py (aus 06.02 — write_style_md_atomic/read_style_md/write_style_note_atomic; read_env_raw)
    - webui/tests/test_endpoints_style.py (aus Task 1 — Zielverhalten)
  </read_first>
  <action>
    Importiere style_extract in main.py (Zeile-14-Import-Gruppe erweitern). Neuer Endpoint POST /style/relearn (@limiter.limit("5/minute"), Depends(auth.require_auth)): Form-Params agent_id: str = Form(...), style_note: str = Form("", max_length=5000). Ablauf: read_env_raw(agent_id) -> ValueError->HTTPException 400 "invalid agent_id"; KEIN Anthropic-only-Gate (D-55 — Adapter ist provider-agnostisch); persistiere zuerst agents_io.write_style_note_atomic(agent_id, style_note) (überlebt Overwrite, D-54); rufe style_extract.extract_style(agent_id); Fehler-Kaskade: StyleExtractionEmpty -> HTTPException 400 mit STY-05-Hinweistext („Zu wenig verwertbares Mail-Material und kein Freitext — bitte Stil kurz im Feld beschreiben oder später erneut versuchen."), ValueError -> 400, RuntimeError -> 500, generisch -> 500 "Stil-Extraktion fehlgeschlagen"; bei Erfolg agents_io.write_style_md_atomic(agent_id, style_md) und return PlainTextResponse(style_md). Erweitere /save: neue Form-Params style_md: str | None = Form(None), style_note: str | None = Form(None), enable_style_adaption: str | None = Form(None). Style-Section-Handling: wenn style_md is not None -> write_style_md_atomic(agent_id, style_md); wenn style_note is not None -> write_style_note_atomic(agent_id, style_note); wenn enable_style_adaption is not None -> in global/agent updates ENABLE_STYLE_ADAPTION="true"/"false" nach dem autostart_enabled-Muster (Zeile 319) via agents_io.write_env(agent_id, {...}); erfordert agent_id (sonst _save_response error "Kein Agent ausgewählt"). Auto-Extraktion beim ersten Cred-Save: im bestehenden agent_fields_submitted-Block, NACH dem erfolgreichen write_env der IMAP/LLM-Updates, best-effort: wenn IMAP_USER+IMAP_PASSWORD+LLM_API_KEY nun vorhanden UND read_style_md(agent_id) leer UND (read_env_raw(agent_id).get("ENABLE_STYLE_ADAPTION") or "true").lower() != "false": versuche style_md = extract_style(agent_id); write_style_md_atomic(agent_id, style_md); ALLES in try/except (Exception -> nur loggen „auto_style_extract_failed", Save bleibt erfolgreich — graceful, STY-05). Der Esso-Guard folgt automatisch: existiert style.md schon, wird nicht neu gelernt.
  </action>
  <verify>
    <automated>cd webui && python -m pytest tests/test_endpoints_style.py tests/test_endpoints_seed.py tests/test_endpoints_config.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "/style/relearn" webui/src/main.py` >= 1
    - `grep -c "extract_style(" webui/src/main.py` >= 2 (relearn + auto-trigger)
    - `grep -c "write_style_md_atomic\|write_style_note_atomic" webui/src/main.py` >= 2
    - KEIN Anthropic-only-Gate im /style/relearn-Handler (kein `!= "anthropic"`-Block im relearn-Abschnitt)
    - Alle test_endpoints_style.py grün; test_endpoints_seed/config bleiben grün
  </acceptance_criteria>
  <done>Endpoints + Auto-Trigger + graceful Fehlerbehandlung implementiert.</done>
</task>

<task type="auto">
  <name>Task 3: index.html — style-Fieldset + Freitext + Enable-Checkbox + Re-Learn-Button + JS</name>
  <files>webui/src/templates/index.html</files>
  <read_first>
    - webui/src/templates/index.html (context.md-Fieldset Zeilen 171-189; Section-Save-Muster Zeilen 136-139; generateContext-JS Zeilen 220-248; Provider-Hint-JS; Zero-Reset-confirm Zeilen 252-264)
    - webui/src/main.py (aus Task 2 — /style/relearn erwartet agent_id + style_note; /save erwartet style_md/style_note/enable_style_adaption)
  </read_first>
  <action>
    Füge NACH dem context.md-Fieldset (nach Zeile 189, vor dem „Alles speichern"-Button) ein neues Fieldset „Schreibstil (style.md)" ein, nur im konfigurierten Agent-Kontext (gleicher {% if %}-Rahmen wie das context-Fieldset). Enthalten: (a) eine Enable-Checkbox name="enable_style_adaption" (checked wenn env.ENABLE_STYLE_ADAPTION != "false"), Label „Schreibstil-Adaption aktiv (Default an)"; (b) ein optionales Freitext-Feld <textarea name="style_note" rows="3" maxlength="5000"> vorbefüllt mit {{ style_note }} (neuer Template-Context aus main.py GET / — falls nicht vorhanden, Task 2/GET-Route liefert style_note; sonst leer), Hinweis „Optional: Schreibstil kurz beschreiben oder Beispiel einfügen — vor allem für neue/leere Postfächer"; (c) die style.md-Textarea name="style_md" rows="16" vorbefüllt mit {{ style_md }}; (d) einen Re-Learn-Button (type="button") der per JS relearnStyle(this) /style/relearn aufruft (FormData mit agent_id={{ agent_id }} und style_note aus dem Freitext-Feld), mit vorheriger confirm()-Bestätigung („Bestehendes Stil-Profil überschreiben und aus den aktuellen gesendeten Mails neu lernen?"), Button-Disable + „⏳ Lerne Stil (~30–60s)…" während des fetch, Ergebnis in die style_md-Textarea schreiben, Fehler via alert(status+text) (analog generateContext); (e) einen Section-Save-Button nach dem Muster Zeilen 136-139 mit hx-target="#save-msg-style" und span id="save-msg-style". Ergänze im <script>-Block die Funktion relearnStyle(btn) analog generateContext. WICHTIG: GET /-Route in main.py muss style_md + style_note in den Template-Context legen — falls Task 2 das nicht abgedeckt hat, hier NICHT Template-only lösen, sondern die Read-Funktionen (agents_io.read_style_md/read_style_note) im GET /-Handler ergänzen (siehe read_first main.py index-Route ~Zeile 85-106).
  </action>
  <verify>
    <automated>cd webui && python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "save-msg-style" webui/src/templates/index.html` >= 2 (Button-target + span)
    - `grep -c "/style/relearn" webui/src/templates/index.html` >= 1
    - `grep -c "name=\"style_md\"\|name=\"style_note\"\|name=\"enable_style_adaption\"" webui/src/templates/index.html` >= 3
    - `grep -c "relearnStyle" webui/src/templates/index.html` >= 2 (Definition + onclick)
    - GET /-Route liefert style_md/style_note in den Context (grep read_style_md webui/src/main.py >=1); Gesamt-Suite grün
  </acceptance_criteria>
  <done>Vollständiges, klickbares style-Fieldset mit Re-Learn, Freitext, Enable-Schalter und Section-Save.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /style/relearn | löst IMAP-Verbindung + LLM-Call mit entschlüsselten Agent-Credentials aus |
| Betreiber-Freitext (style_note) → style.md → Draft-Prompt | operator-kontrollierter Text, fließt später in den Draft-Prompt |
| /save Auto-Trigger → externer LLM | erstmaliger Cred-Save startet eine externe Extraktion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Elevation of Privilege | /style/relearn, /save | mitigate | Depends(auth.require_auth) auf allen Routen (bestehendes Muster); agent_id über agents_io._agent_dir-Guard |
| T-06-06 | Denial of Service (Kosten) | /style/relearn (teurer LLM+IMAP-Call) | mitigate | @limiter.limit("5/minute"); Auto-Trigger nur einmalig (nur wenn style.md fehlt); ~30–60s synchron akzeptiert (D-53) |
| T-06-03 | Tampering (Prompt-Injection) | style_note-Freitext | mitigate | style_note fließt als reiner Ton-Input in den Extraktions-Prompt; Konsum-Hierarchie (06.01) hält style.md nachrangig zum Firmen-Kontext; max_length=5000 |
| T-06-07 | Denial of Service | fehlgeschlagene Auto-Extraktion blockiert Save | mitigate | Auto-Trigger vollständig in try/except — Save-Erfolg unabhängig vom Extraktions-Ergebnis (graceful, STY-05) |
| T-06-SC | Tampering | keine neuen Package-Installs in diesem Plan | accept | Nur main.py/index.html/Tests; Deps kamen in 06.02 (pre-vetted) |
</threat_model>

<verification>
- `cd webui && python -m pytest tests/ -q` — Gesamt-Suite grün
- Manueller Klick-Pfad (Teil der 06.04-Abnahme): Agent anlegen -> Creds speichern -> style.md erscheint automatisch ODER via Button; editieren + Section-Save; Re-Learn überschreibt; Freitext überlebt Re-Learn; Enable-Schalter aus -> Agent ignoriert style.md beim Draften
</verification>

<success_criteria>
STY-03 (editierbares Fieldset + Re-Learn + Enable-Flag), STY-01 (automatische Extraktion beim Setup), STY-05 (Hinweis bei zu wenig Material) sind im WebUI vollständig bedienbar.
</success_criteria>

<output>
Create `.planning/phases/06-schreibstil-adaption/06-03-SUMMARY.md` when done
</output>
