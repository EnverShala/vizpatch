---
phase: 05-multi-llm-multi-agent-verschl-sselung-v1-2
plan: 04
type: execute
wave: 2
depends_on: ["05.01", "05.02"]
files_modified:
  - webui/src/agents_io.py
  - webui/src/migration.py
  - webui/docker-entrypoint.sh
  - .gitignore
  - .dockerignore
  - webui/tests/test_agents_io.py
  - webui/tests/test_migration.py
autonomous: true
requirements: [MA-01, SEC-02, SEC-03]
must_haves:
  truths:
    - "agents_io liest/schreibt pro agent_id die Dateien unter /config/agents/<id>/.env + context.md"
    - "IMAP_PASSWORD und LLM_API_KEY werden beim Schreiben Fernet-verschlüsselt (enc:-Prefix), Anzeige bleibt maskiert"
    - "Ein ungültiger agent_id (Path-Traversal) wird mit ValueError abgewiesen, bevor ein Pfad gebaut wird"
    - "list_agent_ids liefert vorhandene Agenten; delete_agent entfernt Config-Verzeichnis + Container + State"
    - "Beim WebUI-Start migriert ein vorhandenes Single-Agent-Layout idempotent und verlustfrei nach agents/default (inkl. ANTHROPIC_API_KEY→LLM_API_KEY + LLM_PROVIDER=anthropic)"
    - "Die Migration sichert vorher ein Backup und ist bei erneutem Start ein No-Op"
    - "Der Fernet-Key /config/.secret_key ist in .gitignore und .dockerignore ausgeschlossen"
  artifacts:
    - path: "webui/src/agents_io.py"
      provides: "Per-Agent read/write/list/delete mit Slug-Guard + Encrypt-Hook + get_missing_config(agent_id)"
      contains: "AGENT_ID_PATTERN"
    - path: "webui/src/migration.py"
      provides: "Idempotenter Single→agents/default-Migrationslauf mit Backup + Key-Rename"
      contains: "agents/default"
  key_links:
    - from: "webui/src/agents_io.py"
      to: "webui/src/crypto.py"
      via: "encrypt_value beim write_env"
      pattern: "encrypt_value\\("
    - from: "webui/src/migration.py"
      to: "webui/src/docker_ctrl.py"
      via: "create_or_replace_agent_container('default')"
      pattern: "create_or_replace_agent_container"
---

<objective>
Baut die per-Agent-Datenschicht der WebUI (`agents_io.py`) als agent_id-parametrisierte Variante
von `config_io.py` — mit striktem Slug-Whitelist-Guard (Path-Traversal-Abwehr), Fernet-Encrypt-Hook
beim Speichern (SEC-02, WebUI-Seite) und CRUD-Funktionen (`list_agent_ids`, `delete_agent`). Ergänzt
den idempotenten Startup-Migrationshook (`migration.py`), der ein bestehendes Single-Agent-Layout
verlustfrei nach `/config/agents/default/` überführt (inkl. Key-Rename `ANTHROPIC_API_KEY`→`LLM_API_KEY`
+ `LLM_PROVIDER=anthropic`), mit vorherigem Backup. Schließt den Fernet-Key aus Git/Docker-Kontext aus.

Purpose: MA-01 (Config-Layout + Migration), SEC-02 (Encrypt beim Save), SEC-03 (Key nie im Git + Reset).
Output: `agents_io.py`, `migration.py`, gehärtete `docker-entrypoint.sh`, `.gitignore`/`.dockerignore`, Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-multi-llm-multi-agent-verschl-sselung-v1-2/05-CONTEXT.md
@.planning/phases/05-multi-llm-multi-agent-verschl-sselung-v1-2/05-RESEARCH.md
@.planning/phases/05-multi-llm-multi-agent-verschl-sselung-v1-2/05-PATTERNS.md
@webui/src/config_io.py
@webui/src/crypto.py

<interfaces>
<!-- Slug-Guard + Zielsignaturen (05-PATTERNS.md Zeilen 143-174, 05-RESEARCH.md Zeilen 371-385, 490) -->
AGENT_ID_PATTERN = re.compile(r"^[a-z0-9-]{1,64}$")
SECRET_KEYS = {"IMAP_PASSWORD", "LLM_API_KEY"}   # WEBUI_PASSWORD bleibt in globaler config_io
def _agent_dir(agent_id: str) -> Path            # validiert, dann Path(f"/config/agents/{agent_id}")
def slugify(name_or_email: str) -> str           # re.sub(r"[^a-z0-9]+","-",v.lower()).strip("-"); Kollision → -2,-3
def read_env_masked(agent_id: str) -> dict
def write_env(agent_id: str, updates: dict) -> None   # encrypt_value auf SECRET_KEYS vor Line-Parser-Write + chmod 600
def get_missing_config(agent_id: str) -> list[str]
def list_agent_ids() -> list[str]                # Verzeichnisse unter /config/agents/
def delete_agent(agent_id: str) -> dict          # docker_ctrl.stop_and_remove_agent + rmtree config + state

<!-- Migration (05-RESEARCH.md Zeilen 371-385, Runtime-Inventory Zeilen 391-394) -->
# Idempotenz: wenn /config/agents/ existiert → No-Op
# Backup nach /config/.migration-backup-<ts>/ VOR dem Verschieben (rename, nicht copy)
# WEBUI_USER/WEBUI_PASSWORD/AUTOSTART_ENABLED bleiben in /config/.env (Root), ziehen NICHT um
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: agents_io.py (per-Agent CRUD + Slug-Guard + Encrypt-Hook)</name>
  <files>webui/src/agents_io.py</files>
  <read_first>
    - webui/src/config_io.py (komplett — read_env_masked, write_env-Line-Parser Z.35-67, get_missing_config, reset_all)
    - webui/src/crypto.py (encrypt_value-Signatur)
    - webui/src/docker_ctrl.py (stop_and_remove_agent(agent_id)-Signatur aus Plan 05.02)
    - .planning/phases/05-multi-llm-multi-agent-verschl-sselung-v1-2/05-PATTERNS.md (Zeilen 125-174: agents_io-Pattern; Zeilen 330-364: Shared Patterns)
  </read_first>
  <behavior>
    - write_env("info", {"LLM_API_KEY":"sk-x"}) schreibt einen Wert mit enc:-Prefix in /config/agents/info/.env
    - read_env_masked("info") gibt für gesetzte SECRET_KEYS "****" zurück, für Nicht-Secrets Klartext (kein Decrypt beim Anzeigen)
    - write_env mit ungültigem agent_id "../evil" wirft ValueError (kein Pfad wird gebaut)
    - slugify("Info@Esso.de") == "info-esso-de"; bei bestehender Kollision wird "-2" angehängt
    - list_agent_ids() listet nur gültige Verzeichnisnamen unter /config/agents/
    - delete_agent("info") ruft docker_ctrl.stop_and_remove_agent("info") und entfernt /config/agents/info + /data/agents/info
    - Line-Parser erhält Kommentare in der .env (kein dotenv.set_key)
  </behavior>
  <action>
    Erstelle `webui/src/agents_io.py` nach dem config_io.py-Vorbild, aber alle Pfade aus `agent_id` gebaut
    (kein Env-Var-Default mehr): `AGENT_ID_PATTERN`, `_agent_dir(agent_id)` mit Guard (ValueError bei Verstoß,
    Konvention wie docker_ctrl `raise ValueError(...)`). Übernimm den kommentar-erhaltenden Line-Parser aus
    config_io.write_env und ergänze VOR dem Schreiben den Encrypt-Hook: für Keys in `SECRET_KEYS =
    {"IMAP_PASSWORD","LLM_API_KEY"}` und nur wenn der Wert nicht leer/nicht "****" ist, den Wert durch
    `crypto.encrypt_value(...)` schicken. `read_env_masked(agent_id)` maskiert SECRET_KEYS immer als "****"
    unabhängig von enc:/Klartext (kein Decrypt beim Rendern, Pitfall-Vermeidung). Implementiere `slugify`
    (`re.sub(r"[^a-z0-9]+","-",v.lower()).strip("-")` + Kollisions-Suffix gegen bestehende list_agent_ids),
    `list_agent_ids()` (Verzeichnisse unter `/config/agents/`, per AGENT_ID_PATTERN gefiltert),
    `get_missing_config(agent_id)` (REQUIRED_ENV_KEYS `IMAP_USER`,`IMAP_PASSWORD`,`LLM_API_KEY` + context.md),
    `delete_agent(agent_id)` (ruft `docker_ctrl.stop_and_remove_agent(agent_id)`, dann `shutil.rmtree` auf
    `/config/agents/<id>` und `/data/agents/<id>`, Result-Dict-Muster wie config_io.reset_all). chmod-600 beim
    write_env (Idiom aus config_io Z.64-67). Erlaube Pfad-Override für Tests via env-Basis-Präfix
    (z. B. `os.getenv("WEBUI_CONFIG_ROOT","/config")`), damit tmp_path-Tests möglich sind.
  </action>
  <verify>
    <automated>cd webui && python -c "import ast; ast.parse(open('src/agents_io.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - `agents_io.py` enthält `AGENT_ID_PATTERN = re.compile(r"^[a-z0-9-]{1,64}$")` und `_agent_dir` mit Guard
    - `SECRET_KEYS` == {"IMAP_PASSWORD","LLM_API_KEY"} (kein ANTHROPIC_API_KEY)
    - write_env ruft `encrypt_value` für Secret-Keys, read_env_masked ruft KEIN decrypt_value
    - delete_agent ruft docker_ctrl.stop_and_remove_agent + rmtree beider Verzeichnisse
    - Syntax gültig
  </acceptance_criteria>
  <done>agents_io kapselt per-Agent-Config mit Slug-Guard, Encrypt-on-Save und CRUD.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: migration.py + WebUI-Startup-Hook + Key aus Git/Docker ausschließen</name>
  <files>webui/src/migration.py, webui/docker-entrypoint.sh, .gitignore, .dockerignore</files>
  <read_first>
    - webui/docker-entrypoint.sh (Ist-Zustand: Zero-Config-Seeding)
    - webui/src/agents_io.py (Task-1-Line-Parser/write_env für Key-Rename)
    - webui/src/docker_ctrl.py (create_or_replace_agent_container aus Plan 05.02)
    - .planning/phases/05-multi-llm-multi-agent-verschl-sselung-v1-2/05-RESEARCH.md (Zeilen 371-394: Migrationsablauf Schritt 1-7 + Backup-Empfehlung + WEBUI-Settings-Verbleib)
    - .gitignore + .dockerignore (aktueller Stand, prüfen ob .env/.secret_key erfasst)
  </read_first>
  <behavior>
    - migrate() bei vorhandenem /config/.env (mit Agent-Keys) und fehlendem /config/agents/ → legt agents/default an
    - ANTHROPIC_API_KEY wird zu LLM_API_KEY umbenannt (Wert unverändert, noch Klartext), LLM_PROVIDER=anthropic ergänzt
    - context.md, state.db, agent_status.json werden nach agents/default bzw. /data/agents/default verschoben (rename)
    - WEBUI_USER/WEBUI_PASSWORD/AUTOSTART_ENABLED bleiben in /config/.env (Root)
    - Vor dem Verschieben liegt ein Backup unter /config/.migration-backup-<ts>/
    - Zweiter migrate()-Aufruf ist No-Op (Idempotenz: /config/agents/ existiert bereits)
  </behavior>
  <action>
    Erstelle `webui/src/migration.py` mit `migrate()` nach 05-RESEARCH.md Zeilen 373-385: (1) Erkennung
    (Root-`.env` existiert UND `/config/agents/` existiert NICHT → sonst No-Op); (2) Backup nach
    `/config/.migration-backup-<timestamp>/`; (3) `mkdir -p /config/agents/default`, `.env` per Line-Parser
    (aus agents_io, Kommentar-erhaltend) nach `agents/default/.env` mit Key-Rename ANTHROPIC_API_KEY→LLM_API_KEY
    + Zeile `LLM_PROVIDER=anthropic`, dabei die Agent-Keys (IMAP_*, LLM_API_KEY, MODEL_*, POLL_*, BACKFILL_*)
    aus der Root-`.env` ENTFERNEN, WEBUI_*/AUTOSTART_ENABLED in Root-`.env` belassen; `context.md` per rename
    verschieben; (4) State `mkdir -p /data/agents/default`, `state.db`+`agent_status.json` per rename verschieben;
    (5) alten Container `vizpatch-agent` (falls vorhanden) stoppen+entfernen; (6)
    `docker_ctrl.create_or_replace_agent_container("default")`. Ver-/Entschlüsselung ist NICHT Teil der Migration
    (bewusst SEC-02-lazy). Rufe `migrate()` im WebUI-Startup auf — bevorzugt als Aufruf beim App-Import/Startup-Event
    in webui/src/main.py-Nähe ODER als zusätzlicher Schritt in docker-entrypoint.sh vor `exec "$@"` (wähle den
    Ansatz, der ohne laufende Requests garantiert vor der ersten Route läuft; dokumentiere die Wahl im Summary).
    Passe `docker-entrypoint.sh` an, falls dort der Hook platziert wird (Docker-Container-fähig, POSIX sh).
    Ergänze in `.gitignore` UND `.dockerignore`: `.secret_key`, `**/.secret_key`, `.migration-backup-*` (Key +
    Backups nie ins Git/Image).
  </action>
  <verify>
    <automated>cd webui && python -c "import ast; ast.parse(open('src/migration.py').read())" && grep -c "secret_key" ../.gitignore</automated>
  </verify>
  <acceptance_criteria>
    - migration.py: `migrate()` ist idempotent (No-Op wenn /config/agents/ existiert) und legt vor dem Verschieben ein Backup an
    - Key-Rename ANTHROPIC_API_KEY→LLM_API_KEY + LLM_PROVIDER=anthropic geschieht per Line-Parser (Kommentare bleiben)
    - `grep -c "secret_key" .gitignore` >= 1 und `grep -c "secret_key" .dockerignore` >= 1
    - migrate() ruft `create_or_replace_agent_container("default")` und den alten-Container-Cleanup
    - WEBUI_* verbleiben in Root-/config/.env (nicht nach agents/default verschoben)
  </acceptance_criteria>
  <done>Idempotenter, backup-gesicherter Migrationslauf verdrahtet; Key aus Git/Docker ausgeschlossen.</done>
</task>

<task type="auto">
  <name>Task 3: Tests für agents_io + migration</name>
  <files>webui/tests/test_agents_io.py, webui/tests/test_migration.py</files>
  <read_first>
    - webui/tests/test_config_io.py (komplett: tmp_path+monkeypatch+Re-Import-Idiom, Windows-skipif Z.61)
    - webui/src/agents_io.py + webui/src/migration.py (Task 1/2)
    - .planning/phases/05-multi-llm-multi-agent-verschl-sselung-v1-2/05-PATTERNS.md (Zeilen 312-323, 380-382: Test-Idiome)
  </read_first>
  <action>
    `test_agents_io.py` (Vorbild test_config_io.py, `WEBUI_CONFIG_ROOT`+`VIZPATCH_SECRET_KEY_FILE` auf tmp_path):
    Round-Trip write/read pro agent_id, Encrypt-on-Save (geschriebene .env enthält enc:-Prefix für LLM_API_KEY),
    Masking bei read_env_masked, Path-Traversal-ValueError bei "../x", slugify inkl. Kollision, list_agent_ids,
    delete_agent (mocke docker_ctrl.stop_and_remove_agent), Zwei-Agenten-Isolation (Werte von Agent A tauchen
    NICHT bei Agent B auf — MA-05-Cross-Kontaminations-Gate auf Datenschicht). `test_migration.py`: baue ein
    Single-Agent-Layout im tmp_path, rufe migrate() (mocke docker_ctrl.create_or_replace_agent_container +
    stop_and_remove_agent), prüfe Verschiebung, Key-Rename, LLM_PROVIDER=anthropic, Backup-Existenz,
    WEBUI_*-Verbleib und Idempotenz (zweiter Aufruf = No-Op).
  </action>
  <verify>
    <automated>cd webui && python -m pytest tests/test_agents_io.py tests/test_migration.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Beide Testdateien grün
    - test_agents_io beweist Zwei-Agenten-Isolation (kein Cross-Read) und Path-Traversal-Abwehr
    - test_migration beweist Idempotenz (zweiter migrate() ist No-Op) + Backup + Key-Rename
    - chmod-Assertions mit Windows-skipif gegatet
  </acceptance_criteria>
  <done>Datenschicht + Migration vollständig getestet inkl. Isolation und Idempotenz.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebUI-Formular → Dateisystem | agent_id + Feldwerte werden zu Pfaden und .env-Inhalten |
| Root-.env → agents/default | Migration verschiebt Live-Secrets |
| Config-Volume → Git/Image | Key + Backups dürfen nie ins Repo/Image |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Tampering | agent_id als Verzeichnispfad | mitigate | AGENT_ID_PATTERN-Whitelist in `_agent_dir` vor jeder Path()-Konstruktion; Test-abgedeckt |
| T-05-13 | Information Disclosure | Secrets im Klartext beim Save | mitigate | encrypt_value auf IMAP_PASSWORD/LLM_API_KEY vor Line-Parser-Write; read_env_masked zeigt nur "****" |
| T-05-14 | Information Disclosure | Cross-Agent-Datenleck | mitigate | agent_id ist Pflichtparameter, kein globaler Fallback-Pfad; Zwei-Agenten-Isolationstest (Pitfall 3) |
| T-05-15 | Information Disclosure | Fernet-Key/Backup im Git/Image | mitigate | `.secret_key` + `.migration-backup-*` in .gitignore + .dockerignore (SEC-03) |
| T-05-16 | Repudiation/Datenverlust | Migration bricht Live-Layout | mitigate | Backup vor rename; Idempotenz-Guard; Ausführung nur gegen Kopie des Esso-Layouts (ROADMAP) |
</threat_model>

<verification>
- `cd webui && python -m pytest tests/test_agents_io.py tests/test_migration.py -q` grün
- .gitignore + .dockerignore enthalten .secret_key
- migrate() idempotent + backup-gesichert
</verification>

<success_criteria>
Per-Agent-Config mit Slug-Guard und Encrypt-on-Save; idempotenter, verlustfreier Migrationslauf
mit Backup; Fernet-Key aus Git/Docker ausgeschlossen; Datenschicht-Isolation getestet.
</success_criteria>

<output>
Create `.planning/phases/05-multi-llm-multi-agent-verschl-sselung-v1-2/05.04-SUMMARY.md` when done
</output>
