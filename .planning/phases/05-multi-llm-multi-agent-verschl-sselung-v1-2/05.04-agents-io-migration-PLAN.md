---
phase: 05-multi-llm-multi-agent-verschl-sselung-v1-2
plan: 04
type: execute
wave: 2
depends_on: ["05.01"]
files_modified:
  - webui/src/agents_io.py
  - webui/src/migration.py
  - webui/docker-entrypoint.sh
  - .gitignore
  - .dockerignore
  - webui/tests/test_agents_io.py
  - webui/tests/test_migration.py
autonomous: true
requirements: [MA-01, SEC-02, SEC-03]
must_haves:
  truths:
    - "agents_io liest/schreibt pro agent_id die Dateien unter /config/agents/<id>/.env + context.md"
    - "read_context_md(agent_id) und write_context_md_atomic(agent_id, content) existieren als agent_id-parametrisierte Pendants der config_io-Funktionen"
    - "set_agent_enabled(agent_id, enabled) toggelt NUR das AGENT_ENABLED-Flag in der Agent-.env (Datenschicht für Start/Stop ohne Docker)"
    - "IMAP_PASSWORD und LLM_API_KEY werden beim Schreiben Fernet-verschlüsselt (enc:-Prefix), Anzeige bleibt maskiert"
    - "Ein ungültiger agent_id (Path-Traversal) wird mit ValueError abgewiesen, bevor ein Pfad gebaut wird"
    - "list_agent_ids liefert vorhandene Agenten; rename_agent verschiebt Config + State kollisionssicher; delete_agent entfernt NUR Config-Verzeichnis + State (kein Docker — Flag wird vorher deaktiviert)"
    - "Beim WebUI-Start migriert ein vorhandenes Single-Agent-Layout idempotent und verlustfrei nach agents/default (inkl. ANTHROPIC_API_KEY→LLM_API_KEY + LLM_PROVIDER=anthropic + AGENT_ENABLED=true)"
    - "Die Migration triggert NUR, wenn die Root-.env echte Agent-Keys enthält (ANTHROPIC_API_KEY oder IMAP_USER gesetzt) — die leere/WEBUI-only Seed-.env aus dem Zero-Config-Bootstrap erzeugt KEINEN Phantom-Agenten; frische Installation → Dropdown bleibt leer (D-50)"
    - "Die Migration sichert vorher ein Backup und ist bei erneutem Start ein No-Op"
    - "Der Fernet-Key /config/.secret_key ist in .gitignore und .dockerignore ausgeschlossen"
  artifacts:
    - path: "webui/src/agents_io.py"
      provides: "Per-Agent read/write/list/rename/delete + context.md-I/O + set_agent_enabled mit Slug-Guard + Encrypt-Hook + get_missing_config(agent_id)"
      contains: "AGENT_ID_PATTERN"
    - path: "webui/src/migration.py"
      provides: "Idempotenter Single→agents/default-Migrationslauf mit Backup + Key-Rename + Aktiv-Flag"
      contains: "agents/default"
  key_links:
    - from: "webui/src/agents_io.py"
      to: "webui/src/crypto.py"
      via: "encrypt_value beim write_env"
      pattern: "encrypt_value\\("
    - from: "webui/src/migration.py"
      to: "/config/agents/default/.env"
      via: "Line-Parser-Move mit Key-Rename + AGENT_ENABLED=true"
      pattern: "AGENT_ENABLED"
---

<objective>
Baut die per-Agent-Datenschicht der WebUI (`agents_io.py`) als agent_id-parametrisierte Variante
von `config_io.py` — mit striktem Slug-Whitelist-Guard (Path-Traversal-Abwehr), Fernet-Encrypt-Hook
beim Speichern (SEC-02, WebUI-Seite), context.md-I/O pro Agent (`read_context_md`/
`write_context_md_atomic`), dem Aktiv-Flag-Toggle `set_agent_enabled` (Datenschicht für Start/Stop
im neuen Ein-Container-Modell, D-46 revidiert) und vollständigem CRUD (`list_agent_ids`,
`rename_agent`, `delete_agent`). Ergänzt den idempotenten Startup-Migrationshook (`migration.py`),
der ein bestehendes Single-Agent-Layout verlustfrei nach `/config/agents/default/` überführt
(inkl. Key-Rename `ANTHROPIC_API_KEY`→`LLM_API_KEY`, `LLM_PROVIDER=anthropic` und
`AGENT_ENABLED=true`, damit der laufende Betrieb ohne Eingriff weitergeht), mit vorherigem Backup.
Schließt den Fernet-Key aus Git/Docker-Kontext aus.

WICHTIG (neues D-46): Diese Datenschicht ruft KEIN Docker-SDK — Start/Stop/Löschen wirken über
Dateien (Flag/Verzeichnisse), der eine Agent-Container liest sie pro Zyklus frisch (Plan 05.02).

Purpose: MA-01 (Config-Layout inkl. context.md pro Agent + Migration), SEC-02 (Encrypt beim Save),
SEC-03 (Key nie im Git + Reset). set_agent_enabled/rename_agent sind die Datenschicht für
MA-02/MA-04-Routen in 05.05.
Output: `agents_io.py`, `migration.py`, gehärtete `docker-entrypoint.sh`, `.gitignore`/`.dockerignore`, Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-multi-llm-multi-agent-verschl-sselung-v1-2/05-CONTEXT.md
@.planning/phases/05-multi-llm-multi-agent-verschl-sselung-v1-2/05-RESEARCH.md
@.planning/phases/05-multi-llm-multi-agent-verschl-sselung-v1-2/05-PATTERNS.md
@webui/src/config_io.py
@webui/src/crypto.py

<interfaces>
<!-- Slug-Guard + Zielsignaturen (05-PATTERNS.md, Sektion "webui/src/agents_io.py"; Aktiv-Flag: neues D-46) -->
AGENT_ID_PATTERN = re.compile(r"^[a-z0-9-]{1,64}$")
SECRET_KEYS = {"IMAP_PASSWORD", "LLM_API_KEY"}   # WEBUI_PASSWORD bleibt in globaler config_io
def _agent_dir(agent_id: str) -> Path            # validiert, dann Path(f"/config/agents/{agent_id}")
def slugify(name_or_email: str) -> str           # re.sub(r"[^a-z0-9]+","-",v.lower()).strip("-"); Kollision → -2,-3
def read_env_masked(agent_id: str) -> dict
def read_env_raw(agent_id: str) -> dict           # Werte wie gespeichert (inkl. enc:-Prefix) — Pendant zu config_io.read_env_raw; Konsument: /context/generate in 05.05 (llm_seed-Key-Quelle)
def write_env(agent_id: str, updates: dict) -> None   # encrypt_value auf SECRET_KEYS vor Line-Parser-Write + chmod 600
def read_context_md(agent_id: str) -> str             # Pendant zu config_io.read_context_md
def write_context_md_atomic(agent_id: str, content: str) -> None  # tmp-Datei + os.replace
def get_missing_config(agent_id: str) -> list[str]
def list_agent_ids() -> list[str]                # Verzeichnisse unter /config/agents/
def set_agent_enabled(agent_id: str, enabled: bool) -> None   # schreibt AGENT_ENABLED=true|false via write_env
def get_agent_enabled(agent_id: str) -> bool     # AGENT_ENABLED == "true" (fehlend => False)
def rename_agent(old_id: str, new_id: str) -> dict  # Kollisions-Check, Config- + State-Move (KEIN Docker)
def delete_agent(agent_id: str) -> dict          # set_agent_enabled(False) best-effort, dann rmtree config + state (KEIN Docker)

<!-- Aktiv-Flag-Kontrakt (gemeinsam mit 05.02/05.05) -->
AGENT_ENABLED=true|false   # fehlend => disabled; Agent-Loop wertet pro Zyklus aus

<!-- Migration (05-RESEARCH.md, Sektion "Migration: Single-Agent → agents/default (MA-01)" — Container-Schritte 5/6 daraus sind OBSOLET, siehe Addendum) -->
# Idempotenz: wenn /config/agents/ existiert → No-Op
# Backup nach /config/.migration-backup-<ts>/ VOR dem Verschieben (rename, nicht copy)
# WEBUI_USER/WEBUI_PASSWORD/AUTOSTART_ENABLED bleiben in /config/.env (Root), ziehen NICHT um
# KEIN Container-Stop/-Erzeugen: der bestehende agent-Service (v1.2.0-Image, Multi-Account-Loop)
# entdeckt agents/default ab dem nächsten Poll-Zyklus selbst
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: agents_io.py (per-Agent CRUD + context.md-I/O + Aktiv-Flag + rename + Slug-Guard + Encrypt-Hook)</name>
  <!-- Kein tdd-Flag: Behavior-Blöcke sind Spezifikation, die Tests folgen gesammelt in Task 3 (test-after) -->
  <files>webui/src/agents_io.py</files>
  <read_first>
    - webui/src/config_io.py (komplett — read_env_masked, write_env-Line-Parser Z.35-67, read_context_md Z.70-74, write_context_md_atomic Z.77-81, get_missing_config, reset_all)
    - webui/src/crypto.py (encrypt_value-Signatur)
    - .planning/phases/05-multi-llm-multi-agent-verschl-sselung-v1-2/05-CONTEXT.md (neues D-46 + D-50: Flag-Semantik, Löschen ohne Container)
    - .planning/phases/05-multi-llm-multi-agent-verschl-sselung-v1-2/05-PATTERNS.md (Sektion "webui/src/agents_io.py": Read-Masked/Line-Parser/Guard-Pattern; Sektion "Shared Patterns")
  </read_first>
  <behavior>
    - write_env("info", {"LLM_API_KEY":"sk-x"}) schreibt einen Wert mit enc:-Prefix in /config/agents/info/.env
    - read_env_masked("info") gibt für gesetzte SECRET_KEYS "****" zurück, für Nicht-Secrets Klartext (kein Decrypt beim Anzeigen)
    - write_context_md_atomic("info", "# Firma") + read_context_md("info") == "# Firma" (Round-Trip); Agent "other" sieht diesen Inhalt NICHT
    - set_agent_enabled("info", True) → .env enthält Zeile AGENT_ENABLED=true; set_agent_enabled("info", False) → AGENT_ENABLED=false; get_agent_enabled spiegelt das; fehlendes Flag → False
    - set_agent_enabled verändert KEINE anderen Zeilen der .env (Kommentare + Secrets bleiben byte-gleich)
    - write_env/read_context_md/set_agent_enabled mit ungültigem agent_id "../evil" wirft ValueError (kein Pfad wird gebaut)
    - slugify("Info@Esso.de") == "info-esso-de"; bei bestehender Kollision wird "-2" angehängt
    - rename_agent("alt", "neu") verschiebt /config/agents/alt → neu UND /data/agents/alt → neu; existierendes Ziel → ValueError; KEIN Docker-Aufruf
    - delete_agent("info") setzt zuerst AGENT_ENABLED=false (best-effort) und entfernt dann /config/agents/info + /data/agents/info; KEIN Docker-Aufruf
    - Line-Parser erhält Kommentare in der .env (kein dotenv.set_key)
  </behavior>
  <action>
    Erstelle `webui/src/agents_io.py` nach dem config_io.py-Vorbild, aber alle Pfade aus `agent_id` gebaut
    (kein Env-Var-Default mehr): `AGENT_ID_PATTERN`, `_agent_dir(agent_id)` mit Guard (ValueError bei Verstoß).
    Übernimm den kommentar-erhaltenden Line-Parser aus config_io.write_env und ergänze VOR dem Schreiben den
    Encrypt-Hook: für Keys in `SECRET_KEYS = {"IMAP_PASSWORD","LLM_API_KEY"}` und nur wenn der Wert nicht
    leer/nicht "****" ist, den Wert durch `crypto.encrypt_value(...)` schicken. `read_env_masked(agent_id)`
    maskiert SECRET_KEYS immer als "****" (kein Decrypt beim Rendern). Zusätzlich `read_env_raw(agent_id)`
    als per-Agent-Pendant zu `config_io.read_env_raw` (liefert Werte wie gespeichert, inkl. `enc:`-Prefix —
    Decrypt macht der Aufrufer; Konsument ist die /context/generate-Route in 05.05). Implementiere `read_context_md(agent_id)`
    und `write_context_md_atomic(agent_id, content)` als agent_id-Pendants zu config_io Z.70-81. Implementiere
    `set_agent_enabled(agent_id, enabled)` als dünnen Wrapper `write_env(agent_id, {"AGENT_ENABLED": "true" if
    enabled else "false"})` und `get_agent_enabled(agent_id)` (dotenv_values-Read, "true"-Vergleich,
    fehlend → False). Implementiere `slugify` (+ Kollisions-Suffix gegen list_agent_ids), `list_agent_ids()`
    (Verzeichnisse unter `/config/agents/`, AGENT_ID_PATTERN-gefiltert), `get_missing_config(agent_id)`
    (REQUIRED_ENV_KEYS `IMAP_USER`,`IMAP_PASSWORD`,`LLM_API_KEY` + context.md), `rename_agent(old_id, new_id)`
    (beide IDs validieren, ValueError bei existierendem Ziel, `Path.rename` von Config- UND State-Verzeichnis;
    KEIN docker_ctrl-Aufruf — der Agent-Loop entdeckt den neuen Namen ab dem nächsten Zyklus),
    `delete_agent(agent_id)` (erst `set_agent_enabled(agent_id, False)` in try/except als Best-Effort, dann
    `shutil.rmtree` auf `/config/agents/<id>` und `/data/agents/<id>`; Result-Dict-Muster wie
    config_io.reset_all; KEIN docker_ctrl-Aufruf). chmod-600 beim write_env (Idiom aus config_io Z.64-67).
    Pfad-Override für Tests via `os.getenv("WEBUI_CONFIG_ROOT","/config")` und
    `os.getenv("WEBUI_DATA_ROOT","/data")`.
  </action>
  <verify>
    <automated>cd webui && python -c "import ast; ast.parse(open('src/agents_io.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - `agents_io.py` enthält `AGENT_ID_PATTERN = re.compile(r"^[a-z0-9-]{1,64}$")` und `_agent_dir` mit Guard
    - `SECRET_KEYS` == {"IMAP_PASSWORD","LLM_API_KEY"} (kein ANTHROPIC_API_KEY)
    - `def set_agent_enabled(` und `def get_agent_enabled(` existieren; Flag-Wert exakt "true"/"false"
    - Funktionen `read_context_md(agent_id)`, `read_env_raw(agent_id)` und `write_context_md_atomic(agent_id, content)` existieren
    - `def rename_agent(old_id` mit Kollisions-Check (ValueError bei existierendem Ziel) und State-Move
    - `grep -c "import docker\|docker_ctrl" webui/src/agents_io.py` == 0 (Datenschicht ist Docker-frei)
    - write_env ruft `encrypt_value` für Secret-Keys, read_env_masked ruft KEIN decrypt_value
    - Syntax gültig
  </acceptance_criteria>
  <done>agents_io kapselt per-Agent-Config inkl. context.md, Aktiv-Flag und Umbenennen — komplett ohne Docker.</done>
</task>

<task type="auto">
  <name>Task 2: migration.py + WebUI-Startup-Hook + Key aus Git/Docker ausschließen</name>
  <!-- Kein tdd-Flag: Behavior-Blöcke sind Spezifikation, die Tests folgen gesammelt in Task 3 (test-after) -->
  <files>webui/src/migration.py, webui/docker-entrypoint.sh, .gitignore, .dockerignore</files>
  <read_first>
    - webui/docker-entrypoint.sh (Ist-Zustand: Zero-Config-Seeding)
    - webui/src/agents_io.py (Task-1-Line-Parser/write_env für Key-Rename)
    - .planning/phases/05-multi-llm-multi-agent-verschl-sselung-v1-2/05-RESEARCH.md (Sektion "Migration: Single-Agent → agents/default (MA-01)": Migrationsablauf — Schritte 5/6 (Container) sind laut Addendum OBSOLET; Backup-Empfehlung + WEBUI-Settings-Verbleib gelten weiter)
    - .gitignore + .dockerignore (aktueller Stand, prüfen ob .env/.secret_key erfasst)
  </read_first>
  <behavior>
    - migrate() bei vorhandenem /config/.env MIT Agent-Keys (ANTHROPIC_API_KEY oder IMAP_USER nicht-leer) und fehlendem /config/agents/ → legt agents/default an
    - migrate() bei leerer Root-.env oder Root-.env, die nur WEBUI_*/AUTOSTART_ENABLED enthält (Zero-Config-Seed via docker-entrypoint.sh `touch`) → No-Op, KEIN agents/default, kein Backup
    - ANTHROPIC_API_KEY wird zu LLM_API_KEY umbenannt (Wert unverändert, noch Klartext), LLM_PROVIDER=anthropic UND AGENT_ENABLED=true ergänzt
    - context.md, state.db, agent_status.json werden nach agents/default bzw. /data/agents/default verschoben (rename)
    - WEBUI_USER/WEBUI_PASSWORD/AUTOSTART_ENABLED bleiben in /config/.env (Root)
    - Vor dem Verschieben liegt ein Backup unter /config/.migration-backup-<ts>/
    - Zweiter migrate()-Aufruf ist No-Op (Idempotenz: /config/agents/ existiert bereits)
    - migrate() ruft KEIN Docker-SDK (kein Container-Stop, kein Container-Erzeugen)
  </behavior>
  <action>
    Erstelle `webui/src/migration.py` mit `migrate()`: (1) Erkennung — Migration NUR wenn ALLE drei
    Bedingungen gelten: Root-`.env` existiert, `/config/agents/` existiert NICHT, UND die Root-`.env`
    enthält echte Agent-Keys (`dotenv_values`: `ANTHROPIC_API_KEY` oder `IMAP_USER` mit nicht-leerem
    Wert) — sonst No-Op. Der Guard ist Pflicht: `docker-entrypoint.sh` legt beim Zero-Config-Bootstrap
    IMMER eine leere `/config/.env` per `touch` an; ohne Key-Check entstünde bei jeder frischen
    Installation ein Phantom-Agent `default` (verletzt D-50 „Dropdown leer bei frischer Installation");
    (2) Backup nach `/config/.migration-backup-<timestamp>/`; (3)
    `mkdir -p /config/agents/default`, `.env` per Line-Parser (aus agents_io, Kommentar-erhaltend) nach
    `agents/default/.env` mit Key-Rename ANTHROPIC_API_KEY→LLM_API_KEY + Zeilen `LLM_PROVIDER=anthropic` und
    `AGENT_ENABLED=true` (laufender Betrieb geht ohne Eingriff weiter — der Multi-Account-Loop des
    v1.2.0-Agent-Containers entdeckt den Agent `default` ab dem nächsten Zyklus), dabei die Agent-Keys
    (IMAP_*, LLM_API_KEY, MODEL_*, POLL_*, BACKFILL_*) aus der Root-`.env` ENTFERNEN, WEBUI_*/AUTOSTART_ENABLED
    in Root-`.env` belassen; `context.md` per rename verschieben; (4) State `mkdir -p /data/agents/default`,
    `state.db`+`agent_status.json` per rename verschieben. AUSDRÜCKLICH KEINE Docker-Schritte (die
    Container-Schritte des Migrationsablaufs in 05-RESEARCH.md sind durch das neue D-46 obsolet). Ver-/Entschlüsselung ist
    NICHT Teil der Migration (SEC-02-lazy). Rufe `migrate()` im WebUI-Startup auf — als Aufruf beim
    App-Import/Startup-Event in webui/src/main.py-Nähe ODER als Schritt in docker-entrypoint.sh vor `exec "$@"`
    (garantiert vor der ersten Route; Wahl im Summary dokumentieren). Passe `docker-entrypoint.sh` an, falls
    dort platziert (POSIX sh). Ergänze in `.gitignore` UND `.dockerignore`: `.secret_key`, `**/.secret_key`,
    `.migration-backup-*`.
  </action>
  <verify>
    <automated>cd webui && python -c "import ast; ast.parse(open('src/migration.py').read())" && grep -c "secret_key" ../.gitignore</automated>
  </verify>
  <acceptance_criteria>
    - migration.py: `migrate()` ist idempotent (No-Op wenn /config/agents/ existiert) und legt vor dem Verschieben ein Backup an
    - Agent-Key-Guard: leere oder WEBUI-only Root-.env → No-Op (kein agents/default, kein Backup); nur ANTHROPIC_API_KEY/IMAP_USER mit nicht-leerem Wert triggert die Migration
    - Key-Rename ANTHROPIC_API_KEY→LLM_API_KEY + LLM_PROVIDER=anthropic + AGENT_ENABLED=true per Line-Parser (Kommentare bleiben)
    - `grep -c "docker" webui/src/migration.py` == 0 (keine Container-Operationen in der Migration)
    - `grep -c "secret_key" .gitignore` >= 1 und `grep -c "secret_key" .dockerignore` >= 1
    - WEBUI_* verbleiben in Root-/config/.env (nicht nach agents/default verschoben)
  </acceptance_criteria>
  <done>Idempotenter, backup-gesicherter, docker-freier Migrationslauf verdrahtet; Key aus Git/Docker ausgeschlossen.</done>
</task>

<task type="auto">
  <name>Task 3: Tests für agents_io + migration</name>
  <files>webui/tests/test_agents_io.py, webui/tests/test_migration.py</files>
  <read_first>
    - webui/tests/test_config_io.py (komplett: tmp_path+monkeypatch+Re-Import-Idiom, Windows-skipif Z.61)
    - webui/src/agents_io.py + webui/src/migration.py (Task 1/2)
    - .planning/phases/05-multi-llm-multi-agent-verschl-sselung-v1-2/05-PATTERNS.md (Sektion "Tests" + Test-Idiome in "Shared Patterns": tmp_path/monkeypatch/Re-Import)
  </read_first>
  <action>
    `test_agents_io.py` (Vorbild test_config_io.py, `WEBUI_CONFIG_ROOT`+`WEBUI_DATA_ROOT`+
    `VIZPATCH_SECRET_KEY_FILE` auf tmp_path): Round-Trip write/read pro agent_id, Encrypt-on-Save
    (geschriebene .env enthält enc:-Prefix für LLM_API_KEY), Masking bei read_env_masked,
    context.md-Round-Trip pro Agent (Agent A sieht Inhalt von Agent B NICHT), set_agent_enabled/
    get_agent_enabled-Round-Trip (true/false/fehlend; andere .env-Zeilen bleiben unverändert),
    Path-Traversal-ValueError bei "../x" (auch für read_context_md + set_agent_enabled), slugify inkl.
    Kollision, list_agent_ids, rename_agent (Config- + State-Verzeichnis umgezogen; Kollision → ValueError),
    delete_agent (Verzeichnisse weg, kein Docker-Mock nötig — Test prüft, dass KEIN docker-Import existiert),
    Zwei-Agenten-Isolation (Werte von Agent A tauchen NICHT bei Agent B auf — MA-05-Gate auf Datenschicht).
    `test_migration.py`: baue ein Single-Agent-Layout im tmp_path, rufe migrate() (kein Docker-Mock nötig),
    prüfe Verschiebung, Key-Rename, LLM_PROVIDER=anthropic, AGENT_ENABLED=true, Backup-Existenz,
    WEBUI_*-Verbleib und Idempotenz (zweiter Aufruf = No-Op). Zusätzlich der Agent-Key-Guard (B2):
    (a) leere Root-.env (nur `touch`, wie Zero-Config-Seed) → migrate() ist No-Op, /config/agents/
    existiert danach NICHT, kein Backup-Verzeichnis; (b) Root-.env nur mit WEBUI_USER/WEBUI_PASSWORD/
    AUTOSTART_ENABLED → ebenfalls No-Op.
  </action>
  <verify>
    <automated>cd webui && python -m pytest tests/test_agents_io.py tests/test_migration.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Beide Testdateien grün
    - test_agents_io beweist Zwei-Agenten-Isolation (kein Cross-Read, auch für context.md) und Path-Traversal-Abwehr
    - test_agents_io enthält Flag-Round-Trip-Tests (true/false/fehlend) + mind. 2 rename_agent-Tests (Erfolg + Kollision)
    - test_migration beweist Idempotenz + Backup + Key-Rename + AGENT_ENABLED=true in agents/default/.env
    - test_migration beweist den Agent-Key-Guard: leere UND WEBUI-only Root-.env → No-Op ohne agents/default
    - chmod-Assertions mit Windows-skipif gegatet
  </acceptance_criteria>
  <done>Datenschicht + Migration vollständig getestet inkl. Flag, context.md-Isolation, rename und Idempotenz.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebUI-Formular → Dateisystem | agent_id + Feldwerte werden zu Pfaden, .env-/context.md-Inhalten und dem Aktiv-Flag |
| Root-.env → agents/default | Migration verschiebt Live-Secrets |
| Config-Volume → Git/Image | Key + Backups dürfen nie ins Repo/Image |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Tampering | agent_id als Verzeichnispfad (env + context.md + Flag + rename-Ziel) | mitigate | AGENT_ID_PATTERN-Whitelist in `_agent_dir` vor jeder Path()-Konstruktion; rename validiert BEIDE IDs; Test-abgedeckt |
| T-05-13 | Information Disclosure | Secrets im Klartext beim Save | mitigate | encrypt_value auf IMAP_PASSWORD/LLM_API_KEY vor Line-Parser-Write; read_env_masked zeigt nur "****" |
| T-05-14 | Information Disclosure | Cross-Agent-Datenleck | mitigate | agent_id ist Pflichtparameter, kein globaler Fallback-Pfad; Zwei-Agenten-Isolationstest inkl. context.md (Pitfall 3) |
| T-05-15 | Information Disclosure | Fernet-Key/Backup im Git/Image | mitigate | `.secret_key` + `.migration-backup-*` in .gitignore + .dockerignore (SEC-03) |
| T-05-16 | Repudiation/Datenverlust | Migration bricht Live-Layout | mitigate | Backup vor rename; Idempotenz-Guard; Live-Layout-Kopie-Test als Checkpoint in 05.06; keine Docker-Seiteneffekte mehr in der Migration |
| T-05-25 | Tampering/Datenverlust | rename_agent überschreibt bestehenden Agenten | mitigate | Kollisions-Check (ValueError bei existierendem new_id) vor jedem Move; Test-abgedeckt |
| T-05-31 | Tampering | Aktiv-Flag-Schreibzugriff (Start/Stop-Semantik über Datei) | mitigate | set_agent_enabled läuft durch denselben Slug-Guard + write_env-Line-Parser; nur auth-geschützte Routen (05.05) rufen es; Flag-Wert strikt "true"/"false" |
</threat_model>

<verification>
- `cd webui && python -m pytest tests/test_agents_io.py tests/test_migration.py -q` grün
- .gitignore + .dockerignore enthalten .secret_key
- migrate() idempotent + backup-gesichert + docker-frei
- read_context_md/write_context_md_atomic/set_agent_enabled/rename_agent vorhanden und getestet
</verification>

<success_criteria>
Per-Agent-Config (inkl. context.md und Aktiv-Flag) mit Slug-Guard und Encrypt-on-Save;
kollisionssicheres Umbenennen; idempotenter, verlustfreier, docker-freier Migrationslauf mit Backup
(migrierter Agent sofort aktiv); Fernet-Key aus Git/Docker ausgeschlossen; Datenschicht-Isolation getestet.
</success_criteria>

<output>
Create `.planning/phases/05-multi-llm-multi-agent-verschl-sselung-v1-2/05.04-SUMMARY.md` when done
</output>
