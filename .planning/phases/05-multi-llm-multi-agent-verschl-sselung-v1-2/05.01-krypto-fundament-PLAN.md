---
phase: 05-multi-llm-multi-agent-verschl-sselung-v1-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/src/crypto.py
  - webui/src/crypto.py
  - agent/pyproject.toml
  - webui/pyproject.toml
  - agent/tests/test_crypto.py
  - webui/tests/test_crypto.py
autonomous: true
requirements: [SEC-01]
must_haves:
  truths:
    - "Beim ersten Aufruf entsteht /config/.secret_key als chmod-600-Datei, bei Folgeaufrufen wird derselbe Key wiederverwendet"
    - "encrypt_value(x) liefert einen enc:-Wert, decrypt_value(encrypt_value(x)) == x (Round-Trip)"
    - "Klartext ohne enc:-Prefix wird von decrypt_value unverändert zurückgegeben (Legacy-Toleranz)"
    - "Ein falscher/fehlender Key führt bei decrypt_value zu einer klaren Fehler-Exception, nicht zu einem stillen Falschwert"
  artifacts:
    - path: "agent/src/crypto.py"
      provides: "Fernet encrypt/decrypt/is_encrypted + Key-Datei-Management für den Agent"
      contains: "ENC_PREFIX"
    - path: "webui/src/crypto.py"
      provides: "Identisches Fernet-Modul für die WebUI"
      contains: "ENC_PREFIX"
    - path: "agent/pyproject.toml"
      provides: "Neue Dependencies cryptography, openai, google-genai + Versionsbump 1.2.0"
      contains: "cryptography"
    - path: "webui/pyproject.toml"
      provides: "Neue Dependency cryptography + Versionsbump 1.2.0"
      contains: "cryptography"
  key_links:
    - from: "crypto.py"
      to: "/config/.secret_key"
      via: "_load_or_create_key()"
      pattern: "\\.secret_key"
---

<objective>
Fernet-Verschlüsselungs-Fundament in beiden Services (Agent + WebUI) und alle neuen
Python-Dependencies dieser Phase. Das `crypto.py`-Modul kapselt Key-Erzeugung
(`/config/.secret_key`, `chmod 600`), `encrypt_value`/`decrypt_value` mit `enc:`-Prefix
und tolerante Legacy-Klartext-Behandlung. Es ist die Grundlage für SEC-02 (transparente
Ver-/Entschlüsselung) und wird in beide unabhängige Python-Packages 1:1 dupliziert.

Purpose: SEC-01 — Secrets sollen at-rest verschlüsselt sein (Datei-/Backup-Leak-Schutz),
ohne Master-Passwort (D-48), Zero-Config-kompatibel.
Output: `agent/src/crypto.py`, `webui/src/crypto.py`, erweiterte `pyproject.toml` beider
Services, Unit-Tests mit Round-Trip- und Legacy-Toleranz-Abdeckung.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-multi-llm-multi-agent-verschl-sselung-v1-2/05-CONTEXT.md
@.planning/phases/05-multi-llm-multi-agent-verschl-sselung-v1-2/05-RESEARCH.md
@.planning/phases/05-multi-llm-multi-agent-verschl-sselung-v1-2/05-PATTERNS.md

<interfaces>
<!-- Zielsignatur crypto.py (identisch in beiden Services), vollständig vorformuliert in
     05-RESEARCH.md, Sektion "Fernet-Verschlüsselung → Empfohlenes Modul" (Code-Block
     {agent,webui}/src/crypto.py). Executor übernimmt diese Signatur exakt. -->
ENC_PREFIX = "enc:"
def encrypt_value(plaintext: str) -> str          # leer/bereits enc: → unverändert; sonst "enc:<token>"
def decrypt_value(value: str) -> str              # kein enc:-Prefix → unverändert; enc: → entschlüsselt; InvalidToken → RuntimeError
def is_encrypted(value: str) -> bool
def _load_or_create_key() -> bytes                # /config/.secret_key, chmod 600, lazy-create
# Key-Pfad überschreibbar via os.getenv("VIZPATCH_SECRET_KEY_FILE", "/config/.secret_key")

<!-- Bestehendes chmod-600-Idiom, aus webui/src/config_io.py Zeilen 64-67 -->
os.chmod(path, stat.S_IRUSR | stat.S_IWUSR)  # 0o600, in try/except PermissionError
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: agent/src/crypto.py + Agent-Dependencies + Versionsbump</name>
  <files>agent/src/crypto.py, agent/pyproject.toml, agent/tests/test_crypto.py</files>
  <read_first>
    - agent/pyproject.toml (aktuelle Dependency-Liste + Version)
    - webui/src/config_io.py (Zeilen 64-67: chmod-600-Idiom, wird übernommen)
    - webui/src/auth.py (Prefix-Erkennungs-/Legacy-Fallback-Muster als Vorbild)
    - .planning/phases/05-multi-llm-multi-agent-verschl-sselung-v1-2/05-RESEARCH.md (Sektion "Fernet-Verschlüsselung → Empfohlenes Modul": vollständige crypto.py-Vorlage; Sektion "Verifizierte SDK-Versionen": Dependency-Versionen)
  </read_first>
  <behavior>
    - encrypt_value("geheim") gibt String mit Prefix "enc:" zurück, ungleich "geheim"
    - decrypt_value(encrypt_value("geheim")) == "geheim" (Round-Trip)
    - decrypt_value("klartext-ohne-prefix") == "klartext-ohne-prefix" (Legacy unverändert)
    - encrypt_value("") == "" und encrypt_value("enc:xyz") == "enc:xyz" (idempotent, nicht doppelt verschlüsseln)
    - is_encrypted("enc:...") is True, is_encrypted("klartext") is False
    - Erster Aufruf ohne vorhandene Key-Datei legt sie an; zweiter Aufruf liest dieselbe (Key bleibt stabil)
    - decrypt_value eines enc:-Werts mit falschem/gelöschtem Key wirft RuntimeError mit erklärendem Text (nicht InvalidToken roh, nicht stiller Falschwert)
  </behavior>
  <action>
    Erstelle `agent/src/crypto.py` exakt nach der Vorlage in 05-RESEARCH.md, Sektion "Fernet-Verschlüsselung → Empfohlenes Modul":
    Konstante `ENC_PREFIX = "enc:"`, `_key_file()` liest `os.getenv("VIZPATCH_SECRET_KEY_FILE", "/config/.secret_key")`,
    `_load_or_create_key()` erzeugt bei Nichtexistenz via `Fernet.generate_key()` und setzt `chmod 600`
    (Idiom aus config_io.py, try/except PermissionError: pass), `encrypt_value`/`decrypt_value`/`is_encrypted`
    wie in der Interfaces-Sektion. `decrypt_value` fängt `cryptography.fernet.InvalidToken` und wirft stattdessen
    `RuntimeError` mit dem in der Vorlage formulierten Erklärungstext (Verweis auf SEC-03-Reset-Pfad).
    Ergänze in `agent/pyproject.toml` unter `dependencies`: `cryptography>=49,<50`, `openai>=2.45,<3.0`,
    `google-genai>=2.11,<3.0` (Bindestrich, NICHT google-generativeai — siehe Pitfall 2). Bumpe `version = "1.2.0"`.
    Schreibe `agent/tests/test_crypto.py` im tmp_path+monkeypatch-Stil (Vorbild webui/tests/test_config_io.py):
    setze `VIZPATCH_SECRET_KEY_FILE` auf `tmp_path/.secret_key`, decke alle Behavior-Punkte ab.
    chmod-Assertion mit `@pytest.mark.skipif(sys.platform == "win32", ...)` gaten (Windows-Dev-Umgebung).
  </action>
  <verify>
    <automated>cd agent && python -m pytest tests/test_crypto.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `agent/tests/test_crypto.py` läuft grün und deckt Round-Trip, Legacy-Passthrough, Idempotenz, Key-Stabilität und InvalidToken→RuntimeError ab
    - `agent/pyproject.toml` enthält `cryptography`, `openai`, `google-genai` (Bindestrich) und `version = "1.2.0"`
    - `grep -c "google-generativeai" agent/pyproject.toml` == 0 (altes SDK NICHT verwendet)
    - `agent/src/crypto.py` enthält `ENC_PREFIX = "enc:"` und keinen hartkodierten Fernet-Key im Quelltext
  </acceptance_criteria>
  <done>Agent-crypto-Modul + Dependencies vorhanden, Tests grün, kein Key im Code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: webui/src/crypto.py + WebUI-Dependency + Versionsbump</name>
  <files>webui/src/crypto.py, webui/pyproject.toml, webui/tests/test_crypto.py</files>
  <read_first>
    - webui/pyproject.toml (aktuelle Dependency-Liste + Version)
    - agent/src/crypto.py (das in Task 1 erstellte Modul — wird 1:1 dupliziert)
    - webui/tests/test_config_io.py (tmp_path+monkeypatch+Re-Import-Idiom + Windows-skipif-Guard Zeile 61)
  </read_first>
  <behavior>
    - Identische Behavior-Menge wie Task 1 (Round-Trip, Legacy-Passthrough, Idempotenz, Key-Stabilität, InvalidToken→RuntimeError)
    - Modul ist funktional identisch zum Agent-Modul (kein Shared-Import zwischen agent/ und webui/)
  </behavior>
  <action>
    Dupliziere `agent/src/crypto.py` nach `webui/src/crypto.py` (identischer Inhalt — kein Cross-Package-Import,
    konsistent mit bestehender Trennung provider_config.py/auth.py). Ergänze in `webui/pyproject.toml` unter
    `dependencies`: `cryptography>=49,<50`. Bumpe `version = "1.2.0"`. Schreibe `webui/tests/test_crypto.py`
    analog zu `agent/tests/test_crypto.py` (Vorbild-Stil aus test_config_io.py, Windows-skipif für chmod-Assertion).
    KEINE OpenAI/Google-Deps in webui/pyproject.toml — die WebUI ruft nie selbst einen LLM-Provider auf
    (Ausnahme llm_seed.py bleibt Anthropic, Pitfall 6).
  </action>
  <verify>
    <automated>cd webui && python -m pytest tests/test_crypto.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `webui/tests/test_crypto.py` grün mit identischer Behavior-Abdeckung wie Agent-Test
    - `webui/pyproject.toml` enthält `cryptography` und `version = "1.2.0"`
    - `grep -c "openai\|google-genai" webui/pyproject.toml` == 0 (WebUI ruft keine LLM-Provider)
    - Inhalt von `webui/src/crypto.py` ist funktional identisch zu `agent/src/crypto.py` (gleiche Funktionssignaturen, gleiches ENC_PREFIX)
  </acceptance_criteria>
  <done>WebUI-crypto-Modul + Dependency vorhanden, Tests grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dateisystem → Prozess | `/config/.secret_key` + `.env`-Dateien werden vom Prozess gelesen; Datei-Leak ist das primäre Angriffsmodell |
| PyPI → Build | Neue Dependencies (cryptography/openai/google-genai) werden installiert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | Fernet-Key-Datei | mitigate | `_load_or_create_key()` setzt `chmod 600`; Key nie im Quelltext, nie geloggt; Schutzumfang ehrlich dokumentiert (SEC-03 in 05.05/05.06) |
| T-05-02 | Cryptography-Fehlgebrauch | crypto.py | mitigate | Standard-Bibliothek `cryptography.fernet` statt Eigenbau (Don't-Hand-Roll); kein TTL; kein Hardcoded-Key |
| T-05-03 | Tampering / Fehlkonfiguration | decrypt bei falschem Key | mitigate | InvalidToken → klare RuntimeError mit Reset-Hinweis statt stillem Falschwert |
| T-05-SC | Tampering | npm/pip installs (cryptography, openai, google-genai) | mitigate | RESEARCH.md Package-Audit: alle 5 Pakete slopcheck [OK], offizielle PyPI-Pakete; `google-genai` (Bindestrich, offiziell) explizit statt Legacy `google-generativeai` |
</threat_model>

<verification>
- `cd agent && python -m pytest tests/test_crypto.py -q` grün
- `cd webui && python -m pytest tests/test_crypto.py -q` grün
- Beide pyproject.toml enthalten die neuen Deps + Version 1.2.0
- Kein `google-generativeai` (Legacy) in irgendeiner pyproject.toml
</verification>

<success_criteria>
Fernet-Modul existiert identisch in beiden Services, Round-Trip + Legacy-Toleranz + Key-Stabilität
per Test bewiesen, alle Phase-5-Dependencies deklariert, Version auf 1.2.0 gebumpt.
</success_criteria>

<output>
Create `.planning/phases/05-multi-llm-multi-agent-verschl-sselung-v1-2/05.01-SUMMARY.md` when done
</output>
