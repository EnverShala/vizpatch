---
phase: 05-multi-llm-multi-agent-verschl-sselung-v1-2
plan: 06
type: execute
wave: 4
depends_on: ["05.01", "05.02", "05.03", "05.04", "05.05"]
files_modified:
  - agent/src/llm.py
  - agent/src/config.py
  - scripts/build-deployment-package.sh
  - deployment/docker-compose.phase4.yml
  - deployment/README.phase4.md
  - agent/README.md
autonomous: false
requirements: [LLM-03, LLM-04, MA-01, MA-05, SEC-03]
user_setup:
  - service: openai
    why: "Modell-ID-Verifikation (client.models.list) + LLM-04-Fixture-Durchlauf für OpenAI-Provider"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI Dashboard → API keys (bezahlter Account für DPA)"
  - service: google
    why: "Modell-ID-Verifikation + LLM-04-Fixture-Durchlauf für Google-Gemini-Provider"
    env_vars:
      - name: GOOGLE_API_KEY
        source: "Google AI Studio → API key (ZWINGEND bezahlter API-Tier, nicht Free-Tier — Datennutzung)"
  - service: imap
    why: "MA-05 Parallelbetrieb-Verifikation mit 2 verschiedenen Test-Postfächern"
    dashboard_config:
      - task: "Zwei erreichbare Test-IMAP-Postfächer bereitstellen (z. B. IONOS + Gmail App-Password)"
        location: "Vizionists-eigene Testaccounts (Phase 1/2) + ein zweiter Account"
  - service: esso-layout-kopie
    why: "Checkpoint: migrate() wird gegen eine echte Kopie der Esso-/config- und /data-Verzeichnisse verifiziert (ROADMAP-Vorgabe)"
    dashboard_config:
      - task: "Kopie der Live-Verzeichnisse /opt/vizpatch/config + agent-data-Volume vom Esso-Server ziehen (nach Rollout)"
        location: "Esso-Kundenserver (scp/rsync, Secrets bleiben lokal bei Vizionists)"
must_haves:
  truths:
    - "Die produktiven OpenAI/Google-Modell-IDs sind gegen echte Keys via client.models.list() verifiziert; MODEL_DEFAULTS oder .env-Override stimmen"
    - "Der 14-.eml-Fixture-Durchlauf erreicht pro Provider ≥ 11/14 korrekt klassifiziert (≈ 80 %) und Ø Draft-Qualität ≥ 3.5/5"
    - "Zwei Agenten laufen parallel IM SELBEN Container gegen 2 Postfächer, jeder Draft landet im richtigen Postfach (keine Cross-Kontamination)"
    - "Fehler-Isolation real verifiziert: Agent A mit falschem Passwort erzeugt einen sichtbaren Fehler, Agent B draftet im selben Zyklus trotzdem"
    - "migrate() ist gegen eine echte Kopie des Esso-Live-Layouts (/config + /data) verifiziert: verlustfrei, idempotent, Backup vorhanden, Agent default aktiv"
    - "SEC-03: Key-Handling + Schutzumfang + Backup-Hinweis sind ehrlich dokumentiert"
    - "Das Deployment-Paket v1.2.0 baut mit Agent (llm/crypto/Multi-Account-Loop) + WebUI (crypto/agents_io/migration) + unveränderter 2-Service-Compose"
  artifacts:
    - path: "scripts/build-deployment-package.sh"
      provides: "v1.2.0-Paketbau inkl. neuer Module"
      contains: "1.2.0"
    - path: "deployment/README.phase4.md"
      provides: "SEC-03-Doku + Multi-Agent-(Ein-Container)-/Provider-Hinweise + AGENT_ENABLED-Erklärung"
      contains: "secret_key"
  key_links:
    - from: "agent/src/config.py MODEL_DEFAULTS"
      to: "verifizierte Provider-Modell-IDs"
      via: "client.models.list()-Check"
      pattern: "MODEL_DEFAULTS"
---

<objective>
Abnahme- und Ship-Plan der Phase. Verifiziert zuerst die LOW-Confidence-Modell-IDs für OpenAI/Google
gegen echte Keys (`client.models.list()`) und korrigiert `MODEL_DEFAULTS` bzw. dokumentiert den
`.env`-Override, BEVOR der Fixture-Durchlauf zählt. Führt danach den 14-`.eml`-Pre-Deployment-Test je
Provider durch (LLM-04, Gate ≥ 11/14) und den Parallelbetrieb-Test nach neuem D-46: 2 Agenten im
SELBEN Container gegen 2 Postfächer, inklusive explizitem Fehler-Isolations-Check (Agent A mit
falschem Passwort, Agent B draftet trotzdem). Verifiziert die Migration gegen eine ECHTE Kopie des
Esso-Live-Layouts (MA-01-Abnahme). Dokumentiert Fernet-Key-Handling (SEC-03) und das
Ein-Container-Multi-Agent-Modell (AGENT_ENABLED) und baut das Deployment-Paket v1.2.0 — die
Compose-Datei bleibt unverändert bei 2 Services (agent + webui), nur die Image-Versionen bumpen.

Purpose: LLM-03 (Modell-IDs final verifiziert), LLM-04 (Qualitäts-Gate je Provider), MA-01
(Migrations-Abnahme gegen Live-Layout-Kopie), MA-05 (Parallelbetrieb + Fehler-Isolation im
Ein-Container-Modell), SEC-03 (Doku), Ship.
Output: verifizierte Modell-Defaults, Testberichte, Migrations-Abnahme, SEC-03-Doku, Deployment-Paket v1.2.0.

Hinweis: Ausführung setzt echte OpenAI-/Google-Keys, 2 Test-Postfächer und eine Kopie des
Esso-Live-Layouts voraus (user_setup) sowie den abgeschlossenen Esso-Rollout (ROADMAP-Depends).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-multi-llm-multi-agent-verschl-sselung-v1-2/05-CONTEXT.md
@.planning/phases/05-multi-llm-multi-agent-verschl-sselung-v1-2/05-RESEARCH.md
@.planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST.md

<interfaces>
<!-- Modell-Defaults zum Verifizieren (05-RESEARCH.md Zeilen 146-164, Assumptions A1/A2) -->
openai:  classify gpt-5-mini (LOW),  draft gpt-5.4 (LOW)          → per client.models.list() prüfen
google:  classify gemini-2.5-flash-lite (MED), draft gemini-2.5-pro (MED) → prüfen
# Override-Netz ohne Code-Deploy: MODEL_CLASSIFY / MODEL_DRAFT in der Agent-.env
# LLM-04-Gate bei 14 Fixtures: ≥ 11/14 korrekt klassifiziert (≈ 80 %) + Ø Draft-Qualität ≥ 3.5/5 je Provider

<!-- MA-05 im Ein-Container-Modell (neues D-46): 2 Agenten = 2 Verzeichnisse unter /config/agents/,
     beide AGENT_ENABLED=true, EIN laufender vizpatch-agent-Container verarbeitet beide sequentiell -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Modell-ID-Verifikation gegen echte OpenAI/Google-Keys</name>
  <what-built>
    Ein einmaliger `client.models.list()`-Check (OpenAI + Google) bestätigt, ob die in
    `agent/src/config.py MODEL_DEFAULTS` hinterlegten IDs (`gpt-5-mini`/`gpt-5.4`,
    `gemini-2.5-flash-lite`/`gemini-2.5-pro`) real verfügbar sind. Bei Abweichung wird
    `MODEL_DEFAULTS` in config.py bzw. der zugehörige Kommentar in `agent/src/llm.py` auf die
    tatsächlich verfügbaren IDs korrigiert; alternativ wird der `.env`-Override (MODEL_CLASSIFY/
    MODEL_DRAFT) dokumentiert. Dies MUSS vor Task 2 (Fixture-Gate) abgeschlossen sein.
  </what-built>
  <read_first>
    - agent/src/config.py (MODEL_DEFAULTS)
    - agent/src/llm.py (Provider-Call-Pfade)
    - .planning/phases/05-multi-llm-multi-agent-verschl-sselung-v1-2/05-RESEARCH.md (Zeilen 144-164, 465-480: Assumptions A1/A2 + Open Question 1)
  </read_first>
  <action>
    Führe mit echten Keys aus: OpenAI `OpenAI(api_key=...).models.list()`, Google
    `genai.Client(api_key=...).models.list()`. Gleiche die Ausgaben mit den vier hinterlegten IDs ab.
    Bei Übereinstimmung: bestätigen. Bei Abweichung: die korrekten IDs in `agent/src/config.py MODEL_DEFAULTS`
    eintragen (Classify = schnell/billig, Draft = Qualität) und den LOW/MED-Confidence-Kommentar entfernen.
    Dokumentiere die verifizierten IDs im Summary.
  </action>
  <how-to-verify>
    1. OPENAI_API_KEY und GOOGLE_API_KEY (bezahlter Tier!) bereitstellen.
    2. `client.models.list()` je Provider ausführen (Ad-hoc-Skript im scratchpad, nicht ins Repo committen).
    3. Prüfen: sind gpt-5-mini/gpt-5.4 bzw. gemini-2.5-flash-lite/gemini-2.5-pro in der Liste?
    4. Falls nein: passende verfügbare IDs wählen, in MODEL_DEFAULTS eintragen.
  </how-to-verify>
  <acceptance_criteria>
    - Für beide Provider ist dokumentiert, welche classify/draft-Modell-ID real verfügbar und gewählt ist
    - `agent/src/config.py MODEL_DEFAULTS` enthält NUR verifizierte IDs (oder .env-Override ist dokumentiert)
    - Kein LOW-Confidence-Platzhalter bleibt unbestätigt in Produktion
  </acceptance_criteria>
  <resume-signal>"verifiziert" tippen oder die korrigierten Modell-IDs nennen</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: LLM-04 Fixture-Durchlauf je Provider (≥ 11/14) + MA-05 Parallelbetrieb + Fehler-Isolation im Ein-Container-Modell</name>
  <what-built>
    Der bestehende 14-`.eml`-Pre-Deployment-Test wird je Provider (Anthropic/OpenAI/Google) durchlaufen
    und muss **≥ 11/14 korrekt klassifiziert (≈ 80 %)** + Ø Draft-Qualität ≥ 3.5/5 erreichen. Danach
    MA-05 nach neuem D-46: zwei Agenten IM SELBEN Container (Multi-Account-Loop) gegen zwei verschiedene
    Test-Postfächer; jede Testmail landet im jeweils richtigen Postfach. Zusätzlich der explizite
    Fehler-Isolations-Check: Agent A bekommt absichtlich ein falsches IMAP-Passwort — sein Fehler
    erscheint in der Status-Übersicht, Agent B draftet im selben Zyklus trotzdem.
  </what-built>
  <read_first>
    - .planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST.md (Testablauf + Fixtures)
    - agent/tests/fixtures (14 .eml)
    - .planning/phases/05-multi-llm-multi-agent-verschl-sselung-v1-2/05-CONTEXT.md (neues D-46: Multi-Account-Loop + Fehler-Isolation)
  </read_first>
  <action>
    (a) LLM-04: Fixtures je Provider durchlaufen (Anthropic bereits Referenz aus Phase 2; OpenAI + Google mit
    den in Task 1 verifizierten Keys/Modellen). Ergebnisse (korrekt/14 + Ø-Qualität) je Provider protokollieren;
    Gate: ≥ 11/14 korrekt UND Ø ≥ 3.5/5.
    (b) MA-05: über die WebUI zwei Agenten anlegen (2 Verzeichnisse unter /config/agents/), gegen zwei
    Test-Postfächer konfigurieren, beide per Start-Button aktivieren (AGENT_ENABLED=true); verifizieren, dass
    der EINE vizpatch-agent-Container beide im selben Zyklus verarbeitet (Logs mit agent_id), je eine Testmail
    einschicken und die Drafts im jeweils richtigen Entwürfe-Ordner prüfen; State-DBs unter
    /data/agents/<id>/ getrennt.
    (c) Fehler-Isolation: IMAP-Passwort von Agent A absichtlich invalidieren → nächster Zyklus: Status-Übersicht
    zeigt Fehler bei A (error aus agent_status.json), Agent B erzeugt trotzdem einen Draft; danach A korrigieren
    und Erholung verifizieren. Bei Unterschreiten des LLM-04-Gates: context.md/Prompt oder Modellwahl
    nachschärfen und erneut laufen.
  </action>
  <how-to-verify>
    1. Zwei Test-Postfächer + drei Provider-Keys bereitstellen.
    2. WebUI öffnen, zwei Agenten mit unterschiedlichen Providern/Postfächern anlegen, beide starten (Flag).
    3. `docker ps` zeigt genau EINEN vizpatch-agent-Container; Logs zeigen beide agent_ids im selben Zyklus.
    4. Fixtures je Provider durchspielen: Anzahl korrekt (von 14) + Ø-Qualität notieren.
    5. Testmails einschicken, Drafts im jeweils richtigen Entwürfe-Ordner prüfen (keine Cross-Drafts).
    6. Agent A: falsches Passwort setzen → Fehler in Status-Zeile A, Draft bei B trotzdem erzeugt.
  </how-to-verify>
  <acceptance_criteria>
    - Pro Provider dokumentiert: ≥ 11/14 korrekt klassifiziert UND Ø Draft-Qualität ≥ 3.5/5
    - Parallelbetrieb: genau EIN Agent-Container, 2 Agenten im selben Zyklus verarbeitet (Log-Beleg mit agent_id), je Draft im richtigen Postfach, getrennte State-DBs bestätigt
    - Fehler-Isolation belegt: Fehler bei A sichtbar (Status-Übersicht), B draftet im selben Zyklus; Erholung nach Korrektur
    - Keine Cross-Kontamination beobachtet
  </acceptance_criteria>
  <resume-signal>Testergebnisse je Provider (x/14 + Ø-Qualität) + Parallelbetrieb-/Isolations-Ergebnis nennen, dann "bestanden"</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Migrations-Abnahme gegen echte Kopie des Esso-Live-Layouts</name>
  <what-built>
    Der in 05.04 gebaute `migrate()`-Lauf wird — zusätzlich zu den synthetischen tmp_path-Tests — gegen
    eine ECHTE Kopie der Esso-Live-Verzeichnisse (`/config` mit befüllter `.env` + `context.md`, `/data`
    mit gewachsener `state.db` + `agent_status.json`) verifiziert, wie in der ROADMAP gefordert. Erst nach
    dieser Abnahme darf das v1.2.0-Update auf dem Kundenserver eingespielt werden.
  </what-built>
  <read_first>
    - webui/src/migration.py (der zu verifizierende Migrationslauf)
    - .planning/phases/05-multi-llm-multi-agent-verschl-sselung-v1-2/05-CONTEXT.md (D-47: Migration; neues D-46: kein Container-Handling)
  </read_first>
  <action>
    Kopie der Esso-Verzeichnisse in eine lokale Test-Compose-Umgebung einspielen (Bind-Mount `./config`,
    Volume-Inhalt nach `agent-data`), beide Services mit v1.2.0-Images starten und den automatischen
    `migrate()`-Lauf beobachten. Danach prüfen: Layout, Werte, State und Verhalten (siehe how-to-verify).
    Anschließend WebUI erneut starten und Idempotenz bestätigen (kein zweiter Migrationslauf, keine Veränderung).
  </action>
  <how-to-verify>
    1. `/config/agents/default/.env` existiert, enthält `LLM_API_KEY=<alter ANTHROPIC-Wert>` + `LLM_PROVIDER=anthropic` + `AGENT_ENABLED=true`, KEIN `ANTHROPIC_API_KEY` mehr; Kommentare der Original-.env erhalten.
    2. `/config/agents/default/context.md` ist byte-identisch zur Esso-Kopie; `/data/agents/default/state.db` enthält die gewachsenen processed_emails-Zeilen (Stichprobe per sqlite3-Query).
    3. Root-`/config/.env` enthält nur noch WEBUI_*/AUTOSTART_ENABLED; Backup-Verzeichnis `/config/.migration-backup-<ts>/` existiert mit Original-Dateien.
    4. Der EINE agent-Container (Compose-Service, unverändert) verarbeitet Agent `default` ab dem nächsten Zyklus weiter (Log-Beleg, keine Doppel-Drafts auf bereits verarbeitete Mails — state.db greift); WebUI zeigt Agent "default" im Dropdown als Läuft.
    5. WebUI-Neustart: kein erneuter Migrationslauf, Dateien unverändert (Idempotenz).
  </how-to-verify>
  <acceptance_criteria>
    - Alle 5 how-to-verify-Punkte bestätigt und im Summary protokolliert
    - Kein Datenverlust: state.db-Zeilenzahl vor == nach Migration; context.md unverändert
    - Kein Doppel-Draft auf historische Mails nach der Migration (processed_emails wirkt weiter)
    - Idempotenz gegen das echte Layout bestätigt (zweiter Start = No-Op)
  </acceptance_criteria>
  <resume-signal>"migration bestanden" tippen oder Abweichungen beschreiben</resume-signal>
</task>

<task type="auto">
  <name>Task 4: SEC-03- + Multi-Agent-Doku + Deployment-Paket v1.2.0</name>
  <files>scripts/build-deployment-package.sh, deployment/docker-compose.phase4.yml, deployment/README.phase4.md, agent/README.md</files>
  <read_first>
    - scripts/build-deployment-package.sh (Ist-Paketbau, Versionslogik)
    - deployment/docker-compose.phase4.yml + deployment/README.phase4.md (Ist-Templates)
    - agent/README.md (Ist-Setup-/Update-Doku)
    - .planning/phases/05-multi-llm-multi-agent-verschl-sselung-v1-2/05-RESEARCH.md (Zeilen 251-255: SEC-03-Schutzumfang-Text)
  </read_first>
  <action>
    SEC-03-Doku: ergänze in `deployment/README.phase4.md` (und ggf. `agent/README.md`) einen Abschnitt zum
    Fernet-Key-Handling — Key liegt als `/config/.secret_key` (chmod 600) im Config-Bind-Mount; Backup umfasst
    Key + verschlüsselte `.env`s zusammen; Key nie ins Git/Image (.gitignore/.dockerignore); Zero-Reset löscht
    den Key mit; ehrlicher Schutzumfang (schützt gegen Datei-/Backup-Leak OHNE vollen Host-Zugriff, NICHT
    gegen Root/Volume-Backup). Multi-Agent-Doku (neues D-46): EIN Agent-Container verarbeitet alle Agenten
    unter /config/agents/ sequentiell pro Zyklus; Start/Stop pro Agent = AGENT_ENABLED-Flag (wirkt ab nächstem
    Zyklus); Zyklus-Dauer wächst linear mit Agenten-Zahl (bei 5-Min-Intervall unkritisch); provider-abhängiger
    AVV-Hinweis; Autostart/Reboot unverändert wie Phase 4 (Compose `restart: unless-stopped` + vizpatch.service).
    `deployment/docker-compose.phase4.yml` bleibt strukturell UNVERÄNDERT bei 2 Services (agent + webui) —
    nur die Image-Referenzen auf v1.2.0 bumpen. Passe `scripts/build-deployment-package.sh` auf v1.2.0 an:
    die neuen Agent-Module (llm.py, crypto.py) und WebUI-Module (crypto.py, agents_io.py, migration.py)
    müssen im Tarball landen.
  </action>
  <verify>
    <automated>bash -n scripts/build-deployment-package.sh && grep -c "secret_key" deployment/README.phase4.md && python -c "import yaml; d=yaml.safe_load(open('deployment/docker-compose.phase4.yml')); assert 'agent' in d['services'] and 'webui' in d['services'], 'beide Services muessen bleiben'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `deployment/README.phase4.md` enthält den SEC-03-Abschnitt (`grep -c "secret_key"` >= 1) UND eine AGENT_ENABLED-Erklärung (`grep -c "AGENT_ENABLED" deployment/README.phase4.md` >= 1)
    - `deployment/docker-compose.phase4.yml` enthält weiterhin BEIDE Services agent + webui (YAML-Assertion grün), Images auf v1.2.0
    - `scripts/build-deployment-package.sh` referenziert Version 1.2.0 und schließt die neuen Module ein
    - `bash -n scripts/build-deployment-package.sh` fehlerfrei (Syntax)
  </acceptance_criteria>
  <done>SEC-03 + Ein-Container-Multi-Agent-Modell dokumentiert, Deployment-Paket v1.2.0 baubereit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Echte Provider-Keys → Testlauf | Keys werden für Verifikation/Fixtures verwendet |
| Zwei Postfächer → ein Container | Trennung muss im Ein-Prozess-Modell real halten (kein Cross-Draft) |
| Esso-Live-Kopie → Test-Umgebung | Kopie enthält echte Kunden-Secrets + Mail-Metadaten |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-22 | Information Disclosure | echte Keys im Verifikations-Skript | mitigate | Ad-hoc-Skript nur im scratchpad, nie ins Repo committen; Keys nur via Umgebungsvariable |
| T-05-23 | Information Disclosure | Cross-Agent-Draft-Leak im Ein-Container-Modell | mitigate | MA-05-Test verifiziert getrennte State-DBs + richtiges Zielpostfach + Log-Zuordnung per agent_id explizit |
| T-05-32 | Denial of Service | Fehler eines Accounts legt alle lahm | mitigate | Expliziter Fehler-Isolations-Check (falsches Passwort bei A, B draftet trotzdem) als Abnahme-Kriterium |
| T-05-24 | Information Disclosure | SEC-03 verschweigt Schutzgrenzen | mitigate | Doku benennt ausdrücklich, wogegen Fernet NICHT schützt (Root/Volume-Backup) |
| T-05-27 | Information Disclosure | Esso-Live-Kopie (Secrets + Mail-Metadaten) auf Dev-Rechner | mitigate | Kopie bleibt lokal bei Vizionists, wird nach der Abnahme gelöscht, nie ins Repo/Scratchpad-Sync; Task-3-Summary vermerkt die Löschung |
| T-05-SC | Tampering | Deployment-Tarball-Inhalt | mitigate | Build-Skript bündelt nur die verifizierten Repo-Module; Key/Backup durch .dockerignore ausgeschlossen |
</threat_model>

<verification>
- Modell-IDs gegen echte Keys verifiziert (Checkpoint 1)
- LLM-04-Gate ≥ 11/14 + Ø ≥ 3.5/5 je Provider; MA-05 im Ein-Container-Modell inkl. Fehler-Isolation (Checkpoint 2)
- Migration gegen echte Esso-Layout-Kopie verlustfrei + idempotent, Agent default läuft weiter (Checkpoint 3)
- SEC-03- + AGENT_ENABLED-Doku vorhanden, `bash -n` grün, Compose weiterhin 2 Services
</verification>

<success_criteria>
Modell-Defaults produktiv verifiziert, Qualitäts-Gate (≥ 11/14 + Ø ≥ 3.5/5) je Provider bestanden,
Parallelbetrieb im Ein-Container-Modell cross-kontaminationsfrei mit belegter Fehler-Isolation,
Migration gegen echte Live-Layout-Kopie abgenommen, Key-Handling + Multi-Agent-Modell ehrlich
dokumentiert, Deployment-Paket v1.2.0 baubereit.
</success_criteria>

<output>
Create `.planning/phases/05-multi-llm-multi-agent-verschl-sselung-v1-2/05.06-SUMMARY.md` when done
</output>
