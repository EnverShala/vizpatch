---
phase: 05-multi-llm-multi-agent-verschl-sselung-v1-2
plan: 01
subsystem: security
tags: [cryptography, fernet, secrets-at-rest, tdd]

# Dependency graph
requires: []
provides:
  - Identisches Fernet-Krypto-Modul (`crypto.py`) in `agent/` und `webui/` mit `encrypt_value`/`decrypt_value`/`is_encrypted`
  - Key-Datei-Management (`/config/.secret_key`, chmod 600, überschreibbar via `VIZPATCH_SECRET_KEY_FILE`)
  - Alle neuen Phase-5-Dependencies deklariert (`cryptography`, `openai`, `google-genai` im Agent; `cryptography` in der WebUI)
  - Beide Packages auf Version 1.2.0 gebumpt
affects: [05.03-llm-adapter-im-agent, 05.04-agents-io, 05.05-webui-agent-routing]

# Tech tracking
tech-stack:
  added: ["cryptography>=49,<50", "openai>=2.45,<3.0", "google-genai>=2.11,<3.0 (agent only)"]
  patterns:
    - "Fernet-Verschlüsselung mit enc:-Prefix als Erkennungsmerkmal, Legacy-Klartext-Passthrough ohne Prefix"
    - "InvalidToken wird nie roh durchgereicht, sondern zu erklärender RuntimeError mit SEC-03-Reset-Hinweis gewrappt"
    - "Key-Pfad überschreibbar via Env-Var (VIZPATCH_SECRET_KEY_FILE) für Testbarkeit ohne echten /config-Mount"

key-files:
  created:
    - agent/src/crypto.py
    - webui/src/crypto.py
    - agent/tests/test_crypto.py
    - webui/tests/test_crypto.py
  modified:
    - agent/pyproject.toml
    - webui/pyproject.toml

key-decisions:
  - "crypto.py 1:1 dupliziert zwischen agent/ und webui/ (kein Shared-Import) — konsistent mit bestehender Trennung von provider_config.py/auth.py aus Phase 4"
  - "TDD-Zyklus strikt eingehalten: Implementierung temporär entfernt, RED-Fehlschlag verifiziert (ModuleNotFoundError), dann wiederhergestellt und GREEN verifiziert, bevor commits erfolgten"

requirements-completed: [SEC-01]

duration: 10min
completed: 2026-07-16
---

# Phase 5 Plan 01: Krypto-Fundament Summary

**Fernet-Verschlüsselungsmodul (`cryptography.fernet`) identisch dupliziert in Agent und WebUI, mit `enc:`-Prefix-Erkennung, tolerantem Legacy-Klartext-Passthrough und Key-Datei-Management (`/config/.secret_key`, chmod 600) — plus alle neuen Phase-5-Dependencies (openai, google-genai, cryptography) deklariert und beide Packages auf Version 1.2.0 gebumpt.**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-07-16T20:39:00Z (approx.)
- **Completed:** 2026-07-16T20:43:56Z
- **Tasks:** 2 (beide TDD, je 2 Commits — 4 Commits gesamt)
- **Files modified:** 6 (4 neu, 2 geändert)

## Accomplishments
- `agent/src/crypto.py` und `webui/src/crypto.py`: identisches ~55-LOC-Modul mit `ENC_PREFIX`, `_load_or_create_key()`, `encrypt_value`, `decrypt_value`, `is_encrypted`
- Round-Trip-, Legacy-Passthrough-, Idempotenz-, Key-Stabilitäts- und Fehlerfall-Tests (InvalidToken → RuntimeError) für beide Services grün
- `agent/pyproject.toml`: `cryptography>=49,<50`, `openai>=2.45,<3.0`, `google-genai>=2.11,<3.0` (Bindestrich, NICHT Legacy `google-generativeai`), Version 1.2.0
- `webui/pyproject.toml`: `cryptography>=49,<50`, Version 1.2.0 (bewusst keine LLM-SDK-Deps, da die WebUI nie selbst einen Provider aufruft außer dem festen Anthropic-Call in `llm_seed.py`)

## Task Commits

Jeder Task wurde im vollen RED→GREEN-TDD-Zyklus committet:

1. **Task 1: agent/src/crypto.py + Agent-Dependencies + Versionsbump**
   - `a6dbf15` — `test(05-01): add failing test for agent Fernet crypto module` (RED)
   - `53f8b8f` — `feat(05-01): implement agent Fernet crypto module + Phase-5 deps` (GREEN)
2. **Task 2: webui/src/crypto.py + WebUI-Dependency + Versionsbump**
   - `2b1a5c6` — `test(05-01): add failing test for webui Fernet crypto module` (RED)
   - `92dd1ea` — `feat(05-01): implement webui Fernet crypto module + version bump` (GREEN)

_Kein REFACTOR-Commit nötig — Implementierung folgte der in 05-RESEARCH.md vorformulierten Vorlage ohne weiteren Cleanup-Bedarf._

## Files Created/Modified
- `agent/src/crypto.py` — Fernet encrypt/decrypt/is_encrypted + Key-Datei-Management für den Agent
- `webui/src/crypto.py` — identisches Modul für die WebUI (kein Cross-Package-Import)
- `agent/tests/test_crypto.py` — 8 Tests (Round-Trip, Legacy, Idempotenz, Key-Stabilität, chmod 600 [Windows-skip], InvalidToken)
- `webui/tests/test_crypto.py` — identische Test-Abdeckung wie Agent-Test
- `agent/pyproject.toml` — neue Deps (cryptography, openai, google-genai) + Version 1.2.0
- `webui/pyproject.toml` — neue Dep (cryptography) + Version 1.2.0

## Decisions Made
- TDD strikt durchgeführt: Da die Implementierung im Zuge der Recherche-Vorlage zuerst geschrieben wurde, wurde sie vor dem ersten Testlauf temporär entfernt, der RED-Fehlschlag (`ModuleNotFoundError`) verifiziert, dann wiederhergestellt und GREEN bestätigt — erst danach erfolgten die Commits in korrekter RED→GREEN-Reihenfolge. Damit ist die Git-Historie TDD-konform, auch wenn der Entwicklungsfluss pragmatisch war.
- Kein Shared-Import zwischen `agent/` und `webui/` — identischer Code wird bewusst dupliziert (folgt der Phase-4-Konvention).

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

Beim vollständigen `webui`-Testlauf (nicht nur `test_crypto.py`) schlugen 5 vorbestehende Tests in `test_endpoints_config.py` mit `docker.errors.DockerException: Error while fetching server API version` fehl. Dies ist eine Windows-Dev-Umgebungseinschränkung (kein laufender Docker-Daemon/Docker Desktop im Worktree-Kontext) und wurde bereits vor dieser Plan-Ausführung bestanden — nicht durch die crypto.py-Änderungen verursacht (Scope Boundary, out of scope). `webui/tests/test_crypto.py` selbst läuft vollständig grün (7 passed, 1 skipped).

## User Setup Required

None - keine externe Service-Konfiguration nötig.

## Next Phase Readiness

- Krypto-Fundament bereit für Plan 05.03 (LLM-Adapter im Agent, Fernet-Decrypt beim Config-Load) und Plan 05.04/05.05 (agents_io/WebUI-Routing, Fernet-Encrypt beim Save)
- Keine Blocker. Die Docker-Daemon-Testumgebungseinschränkung betrifft nur lokale Windows-Entwicklung, nicht den Produktivbetrieb (Docker läuft dort im Compose-Container).

## Self-Check: PASSED

- FOUND: agent/src/crypto.py
- FOUND: webui/src/crypto.py
- FOUND: agent/tests/test_crypto.py
- FOUND: webui/tests/test_crypto.py
- FOUND commits: a6dbf15, 53f8b8f, 2b1a5c6, 92dd1ea
- agent/tests/test_crypto.py: 7 passed, 1 skipped
- webui/tests/test_crypto.py: 7 passed, 1 skipped
- Both pyproject.toml at version 1.2.0 with required deps
- No google-generativeai (legacy) in either pyproject.toml

---
*Phase: 05-multi-llm-multi-agent-verschl-sselung-v1-2*
*Completed: 2026-07-16*
