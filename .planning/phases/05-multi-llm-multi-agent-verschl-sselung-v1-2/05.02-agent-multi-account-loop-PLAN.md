---
phase: 05-multi-llm-multi-agent-verschl-sselung-v1-2
plan: 02
type: execute
wave: 3
depends_on: ["05.03"]
files_modified:
  - agent/src/config.py
  - agent/src/main.py
  - agent/src/imap_client.py
  - agent/src/status_writer.py
  - agent/tests/test_config_multi_agent.py
  - agent/tests/test_main_multi_account.py
autonomous: true
requirements: [MA-03, MA-04]
must_haves:
  truths:
    - "Der EINE bestehende Agent-Container verarbeitet pro Poll-Zyklus sequentiell alle Agenten aus /config/agents/*/ mit gesetztem Aktiv-Flag (AGENT_ENABLED=true)"
    - "Agenten-Discovery liest /config/agents/*/ bei JEDEM Zyklus frisch ein — ein neuer/aktivierter Agent wird ohne Container-Restart ab dem nächsten Zyklus verarbeitet"
    - "Ein Fehler eines Agenten (Auth/IMAP/LLM/Decrypt, inkl. Timeout) wird geloggt und isoliert — die übrigen Agenten laufen im selben Zyklus weiter"
    - "Jeder Agent hat eigenen State (/data/agents/<id>/state.db) und eigene agent_status.json mit last_cycle-Heartbeat"
    - "Config-Load pro Agent mutiert NICHT os.environ (kein Cross-Agent-Leak zwischen Agenten im selben Prozess)"
    - "Bei 0 konfigurierten/aktiven Agenten wartet der Container idle (generalisierter Wait-for-Config, kein Crash/Restart-Loop)"
    - "Jeder Log-Record während der Agent-Verarbeitung trägt die agent_id"
  artifacts:
    - path: "agent/src/main.py"
      provides: "Multi-Account-Poll-Loop: Discovery + Flag-Filter + per-Agent-Fehler-Isolation + Idle-Wait"
      contains: "discover_agents"
    - path: "agent/src/config.py"
      provides: "load_agent_config(agent_dir) via dotenv_values ohne os.environ-Mutation + agent_enabled-Feld"
      contains: "load_agent_config"
    - path: "agent/src/status_writer.py"
      provides: "Per-Agent-Status-Datei mit last_cycle-Heartbeat"
      contains: "last_cycle"
  key_links:
    - from: "agent/src/main.py"
      to: "/config/agents/*/"
      via: "discover_agents() pro Zyklus"
      pattern: "discover_agents\\("
    - from: "agent/src/main.py"
      to: "/data/agents/<id>/agent_status.json"
      via: "status_writer mit per-Agent-Pfad + last_cycle"
      pattern: "last_cycle"
---

<objective>
Baut den Agent-Prozess auf das neue D-46 (revidiert 2026-07-16) um: Es gibt weiterhin genau EINEN
Agent-Container (bestehender Compose-Service, unverändert). `main.py` wird Multi-Account — pro
Poll-Zyklus wird `/config/agents/*/` frisch eingelesen und sequentiell jeder Agent mit gesetztem
Aktiv-Flag (`AGENT_ENABLED=true`) verarbeitet: eigene IMAP-Verbindung, eigener State, eigener
LLM-Provider je Durchlauf. Fehler eines Agenten werden geloggt und isoliert; ein hängender Agent
wird per IMAP-Timeout begrenzt. Der Wait-for-Config-Loop generalisiert auf "0 konfigurierte/aktive
Agenten = idle warten". Config-Load pro Agent erfolgt OHNE `os.environ`-Mutation (dotenv_values),
sonst würden sich Agenten im selben Prozess gegenseitig Werte unterschieben.

Ausdrücklich NICHT in diesem Plan (obsolet durch neues D-46): Docker-Self-Inspection,
`create_or_replace_agent_container`, SDK-Update-Loop, Compose-Umbau. `docker_ctrl.py` und
`agent/docker-compose.yml` bleiben auf Phase-4-Stand.

Purpose: MA-03 (Ein Container, Multi-Account-Loop, Aktiv-Flag, Fehler-Isolation), MA-04
(getrennter State + Heartbeat pro Agent, Agent-Seite).
Output: umgebaute `main.py`/`config.py`/`status_writer.py`, neue Multi-Account-Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-multi-llm-multi-agent-verschl-sselung-v1-2/05-CONTEXT.md
@.planning/phases/05-multi-llm-multi-agent-verschl-sselung-v1-2/05-PATTERNS.md

<interfaces>
<!-- Neue Config-API (ersetzt Env-Var-Routing pro Container aus dem obsoleten Multi-Container-Design) -->
def discover_agents() -> list[tuple[str, Path]]
# Verzeichnisse unter os.getenv("AGENTS_CONFIG_ROOT", "/config/agents"), gefiltert per
# Slug-Whitelist ^[a-z0-9-]{1,64}$; sortiert; KEIN Cache — pro Zyklus frisch

def load_agent_config(agent_id: str, agent_dir: Path) -> Config
# Liest agent_dir/".env" via dotenv_values (dict) — NIEMALS load_dotenv/os.environ-Mutation.
# Pfade abgeleitet: context_file = agent_dir/"context.md",
#   state_db = DATA_ROOT/agents/<agent_id>/"state.db",
#   status_file → DATA_ROOT/agents/<agent_id>/"agent_status.json" (DATA_ROOT = os.getenv("AGENT_DATA_ROOT","/data"))
# Neues Config-Feld: agent_enabled: bool  (AGENT_ENABLED == "true", fehlend/anders → False)
# Decrypt (crypto.decrypt_value) auf IMAP_PASSWORD/LLM_API_KEY bleibt wie in 05.03, wirft DecryptionError

<!-- Aktiv-Flag-Kontrakt (gemeinsam mit 05.04/05.05) -->
AGENT_ENABLED=true|false   # in der Agent-.env; fehlend => disabled; WebUI-Start/Stop toggelt NUR dieses Flag

<!-- agent_status.json-Kontrakt (gemeinsam mit 05.05 — WebUI-Läuft-Heuristik) -->
{"drafts_folder": str|null, "detection_source": str|null, "error": str|null,
 "last_updated": iso8601, "last_cycle": iso8601}
# last_cycle wird am Ende JEDER Agent-Verarbeitung (auch bei Fehler) gesetzt;
# WebUI zeigt "Läuft" wenn AGENT_ENABLED=true UND last_cycle jünger als 2*POLL_INTERVAL_SECONDS

<!-- Bestehender Zyklus (main.py Z.118-144 _poll_once, Z.236-249 Hauptschleife) bleibt als
     innere Verarbeitung EINES Agenten erhalten; außen kommt die Agenten-Schleife dazu -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: config.py — discover_agents + load_agent_config ohne os.environ-Mutation + AGENT_ENABLED</name>
  <files>agent/src/config.py, agent/tests/test_config_multi_agent.py</files>
  <read_first>
    - agent/src/config.py (Stand nach 05.03: LLM_API_KEY/LLM_PROVIDER/MODEL_DEFAULTS/DecryptionError, load_config Z.66-133)
    - agent/src/crypto.py (decrypt_value)
    - agent/tests/test_config_provider_override.py (bestehendes Config-Test-Idiom)
    - .planning/phases/05-multi-llm-multi-agent-verschl-sselung-v1-2/05-CONTEXT.md (neues D-46 + Claude's Discretion "Aktiv-Flag-Mechanik")
  </read_first>
  <behavior>
    - discover_agents() liefert nur Slug-valide Verzeichnisse unter AGENTS_CONFIG_ROOT, sortiert; "../x"/Großbuchstaben-Ordner werden ignoriert
    - load_agent_config("a", dir_a) und danach load_agent_config("b", dir_b): Config b enthält AUSSCHLIESSLICH Werte aus dir_b/.env — kein Wert aus dir_a sickert durch (os.environ unangetastet, per Test bewiesen)
    - AGENT_ENABLED=true → config.agent_enabled True; fehlend oder "false"/"0" → False
    - IMAP_PASSWORD/LLM_API_KEY mit enc:-Prefix werden entschlüsselt; kaputter Token → DecryptionError
    - state_db/context_file/status-Pfad werden aus agent_id/agent_dir abgeleitet (Interfaces-Kontrakt), nicht aus Prozess-Env-Vars
  </behavior>
  <action>
    Ergänze in `config.py`: `discover_agents()` (Verzeichnis-Scan unter `os.getenv("AGENTS_CONFIG_ROOT",
    "/config/agents")`, Filter `re.compile(r"^[a-z0-9-]{1,64}$")`, Rückgabe sortierte Liste (agent_id, Path)).
    Baue `load_agent_config(agent_id, agent_dir)` als per-Agent-Variante von `load_config`: Werte via
    `dotenv_values(agent_dir/".env")` in ein lokales dict lesen (KEIN `load_dotenv`, KEIN `os.environ`-Zugriff
    für Agent-Werte — das ist die zentrale Isolation im Ein-Prozess-Modell); Pflichtfeld-Check
    (IMAP_USER/IMAP_PASSWORD/LLM_API_KEY, OWN_EMAIL_ADDRESS Default = IMAP_USER) → RuntimeError mit agent_id im
    Text; Decrypt via `decrypt_value` → `DecryptionError` (aus 05.03) mit agent_id im Text; Provider-Resolution
    (`resolve_imap_config`), MODEL_DEFAULTS-Logik und alle bestehenden Default-Werte aus `load_config`
    wiederverwenden (gemeinsame private Helper-Funktion extrahieren statt Code-Dopplung); neues Dataclass-Feld
    `agent_enabled: bool` (AGENT_ENABLED == "true", case-insensitive; fehlend → False) und `agent_id: str`.
    Pfade: `context_file = agent_dir/"context.md"`, `state_db = Path(os.getenv("AGENT_DATA_ROOT","/data"))/
    "agents"/agent_id/"state.db"`. Das alte `load_config()` bleibt für Alt-Tests/Übergang erhalten.
    Schreibe `agent/tests/test_config_multi_agent.py` (tmp_path, VIZPATCH_SECRET_KEY_FILE + AGENTS_CONFIG_ROOT +
    AGENT_DATA_ROOT via monkeypatch): alle Behavior-Punkte, insbesondere den Zwei-Agenten-Isolationstest
    (Agent A: user-a@x.de, Agent B: user-b@y.de → beide Configs korrekt, `os.environ` enthält danach KEINEN
    der Werte) und den Slug-Filter in discover_agents.
  </action>
  <verify>
    <automated>cd agent && python -m pytest tests/test_config_multi_agent.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `def discover_agents(` und `def load_agent_config(` existieren in agent/src/config.py
    - `grep -c "load_dotenv" agent/src/config.py` zeigt load_dotenv NUR im alten load_config-Pfad, nicht in load_agent_config (Source-Assertion im Review)
    - Config-Dataclass hat Felder `agent_enabled: bool` und `agent_id: str`
    - Test beweist: nach zwei load_agent_config-Aufrufen enthält os.environ keine Agent-Werte (kein Cross-Leak)
    - Test beweist: discover_agents ignoriert Verzeichnisnamen, die nicht ^[a-z0-9-]{1,64}$ matchen
    - test_config_multi_agent.py grün
  </acceptance_criteria>
  <done>Per-Agent-Config-Load isoliert, flag-fähig und traversal-sicher.</done>
</task>

<task type="auto">
  <name>Task 2: main.py Multi-Account-Loop + Fehler-Isolation + Idle-Wait; status_writer per-Agent + Heartbeat</name>
  <files>agent/src/main.py, agent/src/imap_client.py, agent/src/status_writer.py</files>
  <read_first>
    - agent/src/main.py (komplett, Stand nach 05.03 — _poll_once Z.118-144, _resolve_drafts_folder Z.150-184, _wait_for_config Z.187-205, Hauptschleife Z.236-249)
    - agent/src/status_writer.py (write_status-Signatur + Pfad-Logik)
    - agent/src/imap_client.py (Konstruktor/Timeout-Möglichkeiten der imap-tools-Verbindung)
    - agent/src/config.py (Task-1-API: discover_agents/load_agent_config)
    - .planning/ROADMAP.md (Phase-5-Risiko: "hängender Agent verzögert Zyklus → Timeouts pro Agent Pflicht")
  </read_first>
  <action>
    `status_writer.py`: `write_status` bekommt einen expliziten `status_file: Path`-Parameter (statt globalem
    AGENT_STATUS_FILE-Env-Default; Alt-Default als Fallback für Übergang ok) und ein neues Feld `last_cycle`
    (UTC-ISO-8601) im Payload — Kontrakt exakt wie in der Interfaces-Sektion (05.05 liest dieses Format).
    `main.py`: Ersetze die Single-Config-Hauptschleife durch den Multi-Account-Zyklus:
    (1) `_wait_for_agents(logger)` ersetzt `_wait_for_config`: Schleife, die `discover_agents()` ruft und
    idle wartet (CONFIG_WAIT_SECONDS-Rhythmus, Log `waiting_for_agents` mit Anzahl gefunden/aktiv), solange
    KEIN Agent existiert, dessen Config ladbar UND `agent_enabled` ist; kein Crash, kein Restart-Loop.
    (2) Haupt-Zyklus pro Poll-Intervall: `for agent_id, agent_dir in discover_agents():` → try:
    `cfg = load_agent_config(agent_id, agent_dir)`; skip wenn `not cfg.agent_enabled` (Debug-Log);
    `state.init_db(cfg.state_db)` (mkdir parents); Drafts-Resolution `_resolve_drafts_folder` pro Agent (Ergebnis
    in DESSEN status_file; Resolution-Ergebnis pro agent_id im Prozess cachen, damit nicht jeder Zyklus neu
    probt — Cache-Invalidierung bei geänderter .env-mtime reicht); `_poll_once(cfg, logger)`;
    except `DecryptionError`/`RuntimeError`/`Exception` als drei Stufen: Fehler loggen (`agent_cycle_failed`,
    extra mit agent_id + error), `status_writer.write_status(status_file, ..., error=str(e), last_cycle=now)`,
    dann `continue` zum nächsten Agenten — NIEMALS den Gesamt-Zyklus abbrechen. Nach erfolgreicher Verarbeitung
    `write_status(..., error=None, last_cycle=now)`. (3) Timeout-Begrenzung gegen hängende Agenten:
    erweitere den `ImapClient`-Konstruktor in `agent/src/imap_client.py` um einen `timeout`-Parameter
    und reiche ihn an `MailBox(...)` durch (imap-tools unterstützt `timeout=` im Konstruktor); main.py
    setzt ihn aus `IMAP_TIMEOUT_SECONDS`-Env (Default 60) — dokumentiere im Code-Kommentar, dass die
    LLM-SDKs eigene HTTP-Timeouts mitbringen. (4) Logging-Kontext: verwende `logging.LoggerAdapter` (oder extra={"agent_id":...}
    an allen Aufrufstellen des Zyklus), sodass jeder Record der Agent-Verarbeitung die agent_id trägt.
    (5) Shutdown-Verhalten unverändert (SIGTERM-Flag wird zwischen Agenten geprüft). Die per-Zyklus-Discovery
    ersetzt Container-Restarts: WebUI-Save/Flag-Toggle wirkt ab dem nächsten Zyklus automatisch.
  </action>
  <verify>
    <automated>cd agent && python -c "import ast; ast.parse(open('src/main.py').read()); ast.parse(open('src/status_writer.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - main.py ruft `discover_agents()` INNERHALB der Hauptschleife (pro Zyklus frisch) — `grep -c "discover_agents(" agent/src/main.py` >= 2 (Wait + Zyklus)
    - Agenten mit `agent_enabled == False` werden übersprungen, ohne Fehler
    - Fehler-Isolation: try/except um die EINZEL-Agent-Verarbeitung mit `continue`, nicht um den Gesamt-Zyklus — DecryptionError bricht NICHT den Prozess ab, sondern landet in der status_file des Agenten
    - `status_writer.write_status` akzeptiert `status_file: Path` und schreibt `last_cycle` — `grep -c "last_cycle" agent/src/status_writer.py` >= 1
    - `grep -c "agent_id" agent/src/main.py` >= 5 (Logging-Kontext + Loop)
    - IMAP-Timeout konfigurierbar (`IMAP_TIMEOUT_SECONDS`, Default 60) und an ImapClient durchgereicht
  </acceptance_criteria>
  <done>Ein Container verarbeitet N Agenten sequentiell, fehler-isoliert, mit Heartbeat je Agent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Multi-Account-Loop-Tests (Isolation, Flag, Idle, Heartbeat)</name>
  <files>agent/tests/test_main_multi_account.py</files>
  <read_first>
    - agent/tests/test_main_history.py (bestehendes main.py-Test-Idiom, Mocking-Stil)
    - agent/tests/conftest.py (mock_config-Fixture, Stand nach 05.03)
    - agent/src/main.py + agent/src/status_writer.py (Task-2-Implementierung)
  </read_first>
  <behavior>
    - Zwei Agenten konfiguriert, beide enabled: beide werden im selben Zyklus verarbeitet (in Discovery-Reihenfolge)
    - Agent A enabled, Agent B disabled: nur A wird verarbeitet
    - Agent A wirft in _poll_once eine Exception (gemockt): A bekommt error + last_cycle in seine status_file, B wird trotzdem vollständig verarbeitet (Fehler-Isolations-Beweis, MA-03)
    - Agent A wirft DecryptionError beim Config-Load: gleiches Isolationsverhalten, Prozess läuft weiter
    - 0 Agenten (leeres /config/agents): _wait_for_agents wartet idle (kein Crash; per Shutdown-Flag beendbar)
    - Nach erfolgreichem Durchlauf steht in beiden status_files ein frisches last_cycle
  </behavior>
  <action>
    Schreibe `agent/tests/test_main_multi_account.py`: baue mit tmp_path + monkeypatch
    (AGENTS_CONFIG_ROOT/AGENT_DATA_ROOT/VIZPATCH_SECRET_KEY_FILE) Agenten-Verzeichnisse mit minimaler `.env`
    (IMAP_USER, IMAP_PASSWORD, LLM_API_KEY, AGENT_ENABLED, IMAP_HOST-Override zur Vermeidung von MX-Lookups);
    mocke `_poll_once` und `_resolve_drafts_folder` via mocker.patch, um IMAP/LLM zu vermeiden; rufe die
    Zyklus-Funktion (den in Task 2 extrahierten Ein-Zyklus-Baustein, z. B. `_run_cycle(logger)`) direkt auf.
    Decke alle Behavior-Punkte ab; prüfe status_file-JSON-Inhalte (error-Feld, last_cycle vorhanden und
    ISO-parsebar). Für den Idle-Test: setze das Shutdown-Flag über einen Side-Effect, damit der Wait-Loop
    terminiert. Danach die GESAMTE Agent-Suite laufen lassen und Alt-Tests, die die entfernte
    Single-Config-Hauptschleife referenzieren, an die neue Struktur anpassen.
  </action>
  <verify>
    <automated>cd agent && python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - test_main_multi_account.py grün mit allen 6 Behavior-Punkten
    - Fehler-Isolations-Test beweist: Exception bei Agent A → B wird trotzdem verarbeitet (Mock-Aufruf für B erfolgt)
    - Idle-Test terminiert (kein Hänger) und beweist "0 Agenten = warten statt Crash"
    - Gesamte Agent-Suite grün (`python -m pytest -q` → 0 failed)
  </acceptance_criteria>
  <done>Multi-Account-Verhalten inkl. Fehler-Isolation und Idle-Wait testbeweisbar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /config/agents/*-Verzeichnisnamen → Prozess | Verzeichnisnamen (via WebUI erzeugt, aber auch manuell manipulierbar) werden zu Pfaden/IDs |
| Agent-.env → gemeinsamer Prozess | N Credentials-Sätze leben im selben Python-Prozess |
| Ein Agent → Gesamt-Zyklus | Fehler/Hänger eines Accounts darf andere nicht beeinträchtigen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | Verzeichnisname als agent_id/Pfad | mitigate | discover_agents filtert per Slug-Whitelist ^[a-z0-9-]{1,64}$; kein ungefilterter Name wird zu Pfad/Log-ID |
| T-05-28 | Information Disclosure | Cross-Agent-Credential-Leak via os.environ im Ein-Prozess-Modell | mitigate | load_agent_config nutzt dotenv_values (lokales dict), nie load_dotenv/os.environ; Isolationstest mit 2 Agenten |
| T-05-29 | Denial of Service | Hängender Agent (IMAP-Timeout) blockiert Zyklus für alle | mitigate | IMAP-Socket-Timeout (IMAP_TIMEOUT_SECONDS, Default 60 s) + Fehler-Isolation mit continue; Risiko in ROADMAP dokumentiert |
| T-05-30 | Repudiation | Log-Vermischung mehrerer Accounts (Fehler nicht zuordenbar) | mitigate | agent_id in jedem Log-Record der Agent-Verarbeitung (LoggerAdapter/extra); error zusätzlich in per-Agent status_file |
| T-05-10 | Denial of Service | DecryptionError → stille Endlosschleife | mitigate | Im Multi-Account-Modell: DecryptionError wird pro Agent isoliert, geloggt und in dessen agent_status.json als error sichtbar — WebUI-Status zeigt es an; Prozess läuft weiter (ersetzt die Prozess-Abbruch-Übergangslösung aus 05.03) |
</threat_model>

<verification>
- `cd agent && python -m pytest -q` — gesamte Suite grün
- Fehler-Isolation, Flag-Filter, Idle-Wait, os.environ-Isolation und last_cycle-Heartbeat testbewiesen
- docker_ctrl.py und agent/docker-compose.yml unverändert (kein Diff in diesem Plan)
</verification>

<success_criteria>
Ein einziger Agent-Container verarbeitet pro Zyklus alle aktiven Agenten sequentiell mit getrenntem
State und Heartbeat; Fehler und Hänger einzelner Accounts sind isoliert; Start/Stop wirkt über das
Aktiv-Flag ab dem nächsten Zyklus — ohne Docker-Operationen.
</success_criteria>

<output>
Create `.planning/phases/05-multi-llm-multi-agent-verschl-sselung-v1-2/05.02-SUMMARY.md` when done
</output>
