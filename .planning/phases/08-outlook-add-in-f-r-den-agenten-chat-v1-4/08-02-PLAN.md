---
phase: 08-outlook-add-in-f-r-den-agenten-chat-v1-4
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - outlook-addin/VizpatchOutlookAddin.sln
  - outlook-addin/VizpatchAddin/VizpatchAddin.csproj
  - outlook-addin/VizpatchAddin/ThisAddIn.cs
  - outlook-addin/VizpatchAddin/Ribbon/ChatRibbon.cs
  - outlook-addin/VizpatchAddin/Ribbon/ChatRibbon.xml
  - outlook-addin/VizpatchAddin/TaskPane/ChatTaskPaneHost.cs
  - outlook-addin/VizpatchAddin/TaskPane/ChatView.cs
  - outlook-addin/README.addin-dev.md
autonomous: false
requirements: [OUT-05, OUT-06, OUT-07]
user_setup: []

must_haves:
  truths:
    - "Ein Ribbon-Toggle-Button in Outlook classic blendet eine CustomTaskPane 'Vizpatch-Chat' ein/aus"
    - "Der Chat-Bereich nimmt eine Nachricht entgegen, ruft ChatClient.StreamChatAsync auf und rendert Text-Frames inkrementell sowie event:tool-Labels als Werkzeug-Hinweise"
    - "Pro Task-Pane-Sitzung wird eine session_id (GUID) erzeugt und bei jedem Send unveraendert mitgeschickt; ein Reset erzeugt eine neue"
    - "Der thinnest-slice-End-to-End-Weg (getippte Nachricht -> SSE-Antwort erscheint) funktioniert gegen die reale Backend-Instanz im LAN"
  artifacts:
    - path: "outlook-addin/VizpatchAddin/ThisAddIn.cs"
      provides: "CustomTaskPane-Registrierung + uebergebenes Application-Objekt"
      contains: "CustomTaskPanes.Add"
    - path: "outlook-addin/VizpatchAddin/Ribbon/ChatRibbon.xml"
      provides: "Ribbon-Toggle-Button"
      contains: "toggleButton"
    - path: "outlook-addin/VizpatchAddin/TaskPane/ChatView.cs"
      provides: "Chat-UI, ruft VizpatchAddin.Core.ChatClient, rendert SSE inkrementell"
      contains: "StreamChatAsync"
  key_links:
    - from: "ChatRibbon toggleButton"
      to: "ThisAddIn.ChatPane.Visible"
      via: "Click-Handler + VisibleChanged-Sync"
      pattern: "ChatPane"
    - from: "ChatView"
      to: "VizpatchAddin.Core.ChatClient"
      via: "await foreach über StreamChatAsync-Frames"
      pattern: "StreamChatAsync"
---

<objective>
Baue die VSTO-Outlook-classic-Add-in-Huelle (.NET Framework 4.8): Ribbon-Toggle-Button + `CustomTaskPane` mit einem nativen Chat-Bereich, der die in Plan 08-01 gebaute `VizpatchAddin.Core.ChatClient` aufruft und den SSE-Stream (Text + `event: tool`-Labels) inkrementell rendert. Dies ist die duennste End-to-End-Scheibe (MVP-Slice 1): eine getippte Nachricht geht raus, die Antwort erscheint live — beweist Origin/Auth/SSE gegen die echte Backend-API.

Purpose: Erste nutzbare Scheibe in Outlook classic; validiert den kritischen CSRF/Origin-Weg und den SSE-Client unter realen Bedingungen.

Output: `VizpatchAddin`-VSTO-Projekt in der Solution + Entwickler-README. Draft-Erzeugung + Werkzeuge bleiben serverseitig (D-82/D-87).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-outlook-add-in-f-r-den-agenten-chat-v1-4/08-CONTEXT.md
@.planning/phases/08-outlook-add-in-f-r-den-agenten-chat-v1-4/08-RESEARCH.md
@.planning/phases/08-outlook-add-in-f-r-den-agenten-chat-v1-4/08-01-PLAN.md

<interfaces>
<!-- Aus VizpatchAddin.Core (Plan 08-01): -->
<!--   ChatClient(AddinSettings settings); IAsyncEnumerable<(string EventType,string Data)> StreamChatAsync(message, history, mailContext, sessionId, ct) -->
<!--   SessionIdGenerator.NewId() -> string (GUID); SecureSettingsStore.Load()/Save(AddinSettings) -->
<!-- SSE-EventTypes: "message" (Text-Chunk anhaengen), "tool" (Werkzeug-Label), "done" (Turn Ende), "error" (Fehler anzeigen) -->
<!-- VSTO-Ribbon/CTP-Muster: siehe 08-RESEARCH.md Pattern 1 (ThisAddIn.cs + ChatRibbon) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: VSTO-Projekt + Ribbon-Toggle + CustomTaskPane-Host (Object-Model-sicher)</name>
  <files>outlook-addin/VizpatchOutlookAddin.sln, outlook-addin/VizpatchAddin/VizpatchAddin.csproj, outlook-addin/VizpatchAddin/ThisAddIn.cs, outlook-addin/VizpatchAddin/Ribbon/ChatRibbon.cs, outlook-addin/VizpatchAddin/Ribbon/ChatRibbon.xml, outlook-addin/VizpatchAddin/TaskPane/ChatTaskPaneHost.cs, outlook-addin/README.addin-dev.md</files>
  <read_first>
    - .planning/phases/08-outlook-add-in-f-r-den-agenten-chat-v1-4/08-RESEARCH.md (Pattern 1 CTP+Ribbon, Package Legitimacy Audit: Interop NICHT per NuGet, Anti-Patterns)
    - .planning/phases/08-outlook-add-in-f-r-den-agenten-chat-v1-4/08-CONTEXT.md (D-83 Plattform, D-87 Kein-Auto-Send)
  </read_first>
  <action>
    Erzeuge das VSTO-Outlook-Add-in-Projekt `VizpatchAddin` (net48) und fuege es der bestehenden `VizpatchOutlookAddin.sln` hinzu, mit Projektreferenz auf `VizpatchAddin.Core`. Die Outlook-PIA (`Microsoft.Office.Interop.Outlook`) wird ueber die VS-VSTO-Projektvorlage referenziert (Embed Interop Types = true) — NICHT als NuGet-Paket (siehe Package Legitimacy Audit, [WARNING]). In `ThisAddIn.cs`: registriere im `ThisAddIn_Startup` einen `ChatTaskPaneHost` via `this.CustomTaskPanes.Add(..., "Vizpatch-Chat")`, Default `Visible=false`, mit `VisibleChanged`-Handler, der den Ribbon-Toggle synchronisiert; exponiere eine `ChatPane`-Property. Nutze ausschliesslich das von Outlook uebergebene `Application`-Objekt (kein `new Outlook.Application()`/`CreateObject` — sonst Object Model Guard, Pitfall 3). Lege `Ribbon/ChatRibbon.xml` (Ribbon-XML mit einem `toggleButton` "Vizpatch-Chat") + `Ribbon/ChatRibbon.cs` (Click-Handler setzt `Globals.ThisAddIn.ChatPane.Visible`) und einen `ChatTaskPaneHost.cs` (WinForms-UserControl, hostet spaeter ChatView) an. Schreibe eine knappe `README.addin-dev.md` mit den Build-Voraussetzungen (Visual Studio 2022 + Workload "Office/SharePoint development", installiertes Outlook classic fuer die PIA-Referenz, Ziel-Framework net48).
  </action>
  <acceptance_criteria>
    - CustomTaskPane-Registrierung + Ribbon-Sync vorhanden (CustomTaskPanes.Add, VisibleChanged, ChatPane-Property)
    - Ribbon-XML enthaelt einen toggleButton
    - Kein NuGet-PackageReference auf Microsoft.Office.Interop.Outlook (nur COMReference/embedded PIA)
    - Kein eigenes Application-Objekt (kein `new Outlook.Application`, kein CreateObject)
    - VizpatchAddin.csproj ist in der .sln referenziert; README.addin-dev.md nennt VS-2022-Office-Workload + net48
  </acceptance_criteria>
  <verify>
    <automated>grep -q "CustomTaskPanes.Add" outlook-addin/VizpatchAddin/ThisAddIn.cs && grep -q "VisibleChanged" outlook-addin/VizpatchAddin/ThisAddIn.cs && grep -q "toggleButton" outlook-addin/VizpatchAddin/Ribbon/ChatRibbon.xml && ! grep -q "Microsoft.Office.Interop.Outlook" outlook-addin/VizpatchAddin/VizpatchAddin.csproj && ! grep -q "new Outlook.Application" outlook-addin/VizpatchAddin/ThisAddIn.cs && grep -q "VizpatchAddin.csproj" outlook-addin/VizpatchOutlookAddin.sln</automated>
  </verify>
  <done>VSTO-Projekt in der Solution, Ribbon-Toggle + CTP-Registrierung vorhanden, Interop nur via PIA (kein NuGet), kein eigenes Application-Objekt, README.addin-dev.md vorhanden.</done>
</task>

<task type="auto">
  <name>Task 2: ChatView — SSE-Rendering + session_id-Durchreichung + Settings-Bootstrap</name>
  <files>outlook-addin/VizpatchAddin/TaskPane/ChatView.cs, outlook-addin/VizpatchAddin/TaskPane/ChatTaskPaneHost.cs</files>
  <read_first>
    - outlook-addin/VizpatchAddin.Core/ChatClient.cs (StreamChatAsync-Signatur)
    - outlook-addin/VizpatchAddin.Core/SessionIdGenerator.cs, outlook-addin/VizpatchAddin.Core/SecureSettingsStore.cs
    - .planning/phases/08-outlook-add-in-f-r-den-agenten-chat-v1-4/08-RESEARCH.md (Pattern 2 Aufrufer-Beispiel: switch über EventType, KEIN ConfigureAwait(false))
    - webui/static/chat.js (Verlauf in-memory + Reset = neue session_id — Referenzverhalten)
  </read_first>
  <action>
    Baue `ChatView.cs` (WinForms-UserControl oder WPF via ElementHost — Executor-Wahl, D-83): Eingabefeld + Send-Button + scrollbares Chat-Log + Reset-Button. Beim Laden `SecureSettingsStore.Load()`; erzeuge beim Sitzungsstart eine session_id via `SessionIdGenerator` (Reset erzeugt neue + leert den in-memory-Verlauf, analog chat.js). Der Send-Handler baut einen `ChatClient` aus den Settings und iteriert `await foreach (var (evt,data) in client.StreamChatAsync(message, history, mailContext:null, sessionId, ct))` mit `switch(evt)`: `"tool"` -> Werkzeug-Label anzeigen, `"done"` -> Turn abschliessen, `"error"` -> Fehlermeldung, default (`"message"`) -> Text-Chunk anhaengen. KEIN `ConfigureAwait(false)` (UI-Thread-Marshalling, Anti-Pattern). Haenge User-Turn und Assistant-Antwort an den in-memory-Verlauf (role/content) an, der beim naechsten Send als `history` mitgeht. `mail_context` bleibt in diesem Plan `null` (kommt in Plan 08-03). Hoste ChatView in `ChatTaskPaneHost`. Das Add-in ruft KEINE Outlook-Send-/Write-/Move-/Delete-APIs auf (D-87).
  </action>
  <acceptance_criteria>
    - ChatView ruft VizpatchAddin.Core.ChatClient.StreamChatAsync auf
    - EventType-Dispatch fuer "tool", "done", "error" (und default = Text) vorhanden
    - session_id via SessionIdGenerator erzeugt; Reset-Pfad erzeugt eine neue
    - Kein ConfigureAwait(false) in ChatView
    - Kein Outlook-Send/Write/Move/Delete-Aufruf und kein CreateItem in ChatView (Kein-Auto-Send)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "StreamChatAsync" outlook-addin/VizpatchAddin/TaskPane/ChatView.cs && grep -q '"tool"' outlook-addin/VizpatchAddin/TaskPane/ChatView.cs && grep -q '"done"' outlook-addin/VizpatchAddin/TaskPane/ChatView.cs && grep -q '"error"' outlook-addin/VizpatchAddin/TaskPane/ChatView.cs && grep -qi "SessionIdGenerator" outlook-addin/VizpatchAddin/TaskPane/ChatView.cs && ! grep -q "ConfigureAwait(false)" outlook-addin/VizpatchAddin/TaskPane/ChatView.cs && ! grep -qE "\.(Send|Save|Move|Delete)\(|CreateItem" outlook-addin/VizpatchAddin/TaskPane/ChatView.cs</automated>
  </verify>
  <done>ChatView rendert SSE inkrementell (tool/done/error/text), reicht eine stabile session_id durch, kein ConfigureAwait(false), keine Outlook-Schreib-APIs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live-Abnahme thinnest slice (Build + Sideload in echtem Outlook classic)</name>
  <files>outlook-addin/VizpatchOutlookAddin.sln</files>
  <action>Menschlicher Checkpoint — keine Code-Aenderung. Der baubare Teil (Tasks 1-2) ist fertig; hier wird auf einer Windows-Maschine mit Visual Studio 2022 + Outlook classic gebaut, sideloaded und der thinnest-slice-Chat-Weg gegen die echte Backend-Instanz gegengeprueft (Origin/Auth/SSE). Diese Session-Maschine hat kein VS/Office (Pitfall 4), daher NICHT hier ausfuehrbar.</action>
  <what-built>VSTO-Add-in-Huelle mit Ribbon-Toggle + CustomTaskPane, Chat-Bereich der die Backend-Chat-API per SSE aufruft (Text + Werkzeug-Labels), session_id pro Sitzung. Draft-/Werkzeug-Logik bleibt serverseitig.</what-built>
  <how-to-verify>
    Auf einer Windows-Maschine mit Visual Studio 2022 (Office/SharePoint-Workload) + installiertem Outlook classic + erreichbarer Backend-Instanz im LAN:
    1. Solution `outlook-addin/VizpatchOutlookAddin.sln` in VS oeffnen, `VizpatchAddin` bauen (net48). Erwartung: Build ohne Fehler (PIA wird aus der lokalen Office-Installation referenziert).
    2. F5/Debug startet Outlook classic mit geladenem Add-in. Pruefe: Ribbon zeigt den "Vizpatch-Chat"-Toggle-Button (Vorab-Check: laeuft Outlook classic, NICHT "neues Outlook"? — Pitfall 2).
    3. Toggle klicken -> CustomTaskPane "Vizpatch-Chat" erscheint rechts; erneut -> verschwindet (VisibleChanged-Sync).
    4. Backend-URL/Agent-ID/User/Passwort provisorisch in `%AppData%\Vizpatch\OutlookAddin\settings.json` hinterlegen (Settings-Dialog kommt in Plan 08-03). Eine einfache Frage tippen (z. B. "Welche Agenten sind konfiguriert?") und senden. Erwartung: Antwort erscheint inkrementell (Text streamt), KEIN HTTP-403-Fehler (Origin-Workaround wirkt), ggf. `event: tool`-Labels sichtbar.
    5. Reset klicken -> Verlauf leert, neue session_id.
  </how-to-verify>
  <resume-signal>Schreibe "approved" oder beschreibe, was nicht funktioniert (403? kein Toggle? kein Stream?)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Add-in-Client -> Backend-API (LAN) | CSRF/Auth/TLS-Grenze; native HttpClient-POST |
| Outlook-Prozess -> Add-in | In-Process-COM; das Add-in nutzt nur das uebergebene Application-Objekt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Spoofing | ChatView-POST vs. enforce_same_origin | mitigate | ChatClient (08-01) sendet Origin-Header; hier nur konsumiert — Live-Checkpoint verifiziert kein 403 |
| T-08-05 | Elevation of Privilege | Outlook Object Model Guard | accept | In-Process-VSTO-Add-in nutzt nur das uebergebene Application-Objekt -> Guard greift nicht (Pitfall 3); Verifikation: kein `new Outlook.Application`/CreateObject |
| T-08-06 | Tampering | Kein-Auto-Send-Invariante | mitigate | ChatView ruft keine Send/Save/Move/Delete/CreateItem-APIs (grep-Gate); Drafts entstehen nur serverseitig (D-87) |
| T-08-SC | Tampering | Microsoft.Office.Interop.Outlook via NuGet | mitigate | PIA-Referenz ausschliesslich ueber VSTO-Projektvorlage; NuGet-Install verboten ([WARNING] im Audit) — grep-Gate im csproj |
</threat_model>

<verification>
- Ribbon-Toggle steuert CTP-Sichtbarkeit (Live-Checkpoint)
- Chat-Turn laeuft ohne 403 end-to-end gegen echte Backend-Instanz (Live-Checkpoint) — beweist Origin/Auth/SSE
- ChatView enthaelt keine Outlook-Schreib-APIs
</verification>

<success_criteria>
- Add-in installiert Ribbon + CustomTaskPane in Outlook classic (Success Criterion 1)
- Task Pane ruft /chat/{agent_id}/send auf und rendert SSE inkrementell (Success Criterion 2)
- session_id wird pro Sitzung erzeugt und durchgereicht (Success Criterion 3, Grundlage)
- Kein-Auto-Send strukturell im UI-Code (Success Criterion 5, Teil)
</success_criteria>

<output>
Create `.planning/phases/08-outlook-add-in-f-r-den-agenten-chat-v1-4/08-02-SUMMARY.md` when done
</output>
