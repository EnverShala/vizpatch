---
phase: 08-outlook-add-in-f-r-den-agenten-chat-v1-4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - outlook-addin/VizpatchOutlookAddin.sln
  - outlook-addin/VizpatchAddin.Core/VizpatchAddin.Core.csproj
  - outlook-addin/VizpatchAddin.Core/MailContext.cs
  - outlook-addin/VizpatchAddin.Core/SessionIdGenerator.cs
  - outlook-addin/VizpatchAddin.Core/AddinSettings.cs
  - outlook-addin/VizpatchAddin.Core/SecureSettingsStore.cs
  - outlook-addin/VizpatchAddin.Core/SseLineParser.cs
  - outlook-addin/VizpatchAddin.Core/ChatClient.cs
  - outlook-addin/VizpatchAddin.Tests/VizpatchAddin.Tests.csproj
  - outlook-addin/VizpatchAddin.Tests/MailContextTests.cs
  - outlook-addin/VizpatchAddin.Tests/AddinSettingsTests.cs
  - outlook-addin/VizpatchAddin.Tests/SseLineParserTests.cs
  - outlook-addin/VizpatchAddin.Tests/ChatClientRequestTests.cs
autonomous: true
requirements: [OUT-06, OUT-07, OUT-09]
user_setup: []

must_haves:
  truths:
    - "Der SSE-Zeilenparser zerlegt den exakten Server-Framing-Stil (event: tool / mehrzeilige data: / event: done / event: error) in (EventType, Data)-Frames"
    - "Der ChatClient baut einen Form-encoded POST an {BackendUrl}/chat/{AgentId}/send mit message/history/mail_context/session_id"
    - "Der ChatClient setzt einen Origin-Header (aus Settings) und einen Basic-Auth-Header, sodass die enforce_same_origin-Middleware den nativen Request NICHT mit 403 abweist"
    - "Das Basic-Auth-Passwort wird lokal ausschliesslich DPAPI-verschluesselt (CurrentUser) gespeichert, nie im Klartext"
    - "Pro Chat-Sitzung wird eine GUID als session_id erzeugt (analog chat.js), Reset erzeugt eine neue"
  artifacts:
    - path: "outlook-addin/VizpatchAddin.Core/SseLineParser.cs"
      provides: "SSE-Frame-Zustandsmaschine (event:/data:/Leerzeile-Frame-Grenze)"
      contains: "class SseLineParser"
    - path: "outlook-addin/VizpatchAddin.Core/ChatClient.cs"
      provides: "HttpClient-POST + inkrementelles SSE-Streaming gegen /chat/{id}/send"
      contains: "class ChatClient"
    - path: "outlook-addin/VizpatchAddin.Core/SecureSettingsStore.cs"
      provides: "DPAPI-verschluesselte Settings-Persistenz (%AppData%\\Vizpatch\\OutlookAddin\\settings.json)"
      contains: "ProtectedData"
    - path: "outlook-addin/VizpatchAddin.Tests/SseLineParserTests.cs"
      provides: "xUnit-Tests fuer alle vier Frame-Typen + Multi-Line-data"
      contains: "[Fact]"
  key_links:
    - from: "ChatClient"
      to: "SseLineParser"
      via: "zeilenweises Einspeisen des Response-Streams"
      pattern: "SseLineParser"
    - from: "ChatClient-POST"
      to: "enforce_same_origin (Backend)"
      via: "Origin-Header mit in ADDIN_FRAME_ANCESTORS gelistetem Wert"
      pattern: "Origin"
---

<objective>
Baue die COM-freie Kernbibliothek `VizpatchAddin.Core` (.NET Framework 4.8) samt xUnit-Testprojekt: den SSE-Zeilenparser (hoechstes Risiko), den `ChatClient` (Form-POST + Origin/Basic-Auth + inkrementelles SSE-Lesen), die Settings-DTOs mit DPAPI-Persistenz und den Session-ID-Generator. Diese Schicht enthaelt KEINE Outlook-COM-Referenzen und ist damit unabhaengig von Outlook auf einer Windows-Build-Maschine testbar.

Purpose: Transport- und Sicherheits-Klempnerei (SSE-Framing, TLS-Trust, Passwort-Verschluesselung, CSRF-Origin) frueh und unabhaengig absichern, bevor die COM-Huelle (Plan 08-02) darauf aufsetzt. Der kritische CSRF/Origin-Befund und der SSE-Parser werden hier deterministisch verifizierbar gemacht.

Output: `outlook-addin/`-Verzeichnis mit `.sln`, Core-Klassenbibliothek + xUnit-Testprojekt.

Hinweis MVP-Slice: Diese Schicht ist die testbare Transport-Grundlage unter MVP-Slice 1 (die erste end-user-sichtbare Scheibe ist Plan 08-02). Sie wird per xUnit auf der Windows-Build-Maschine verifiziert; auf der aktuellen Session-Maschine (kein VS/Office/.NET-Framework-SDK) erfolgt nur Quell-Autoring + Struktur-Assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-outlook-add-in-f-r-den-agenten-chat-v1-4/08-CONTEXT.md
@.planning/phases/08-outlook-add-in-f-r-den-agenten-chat-v1-4/08-RESEARCH.md

<interfaces>
<!-- Server-seitiges SSE-Framing (Quelle der Wahrheit), aus webui/src/main.py: -->
<!-- Text-Chunk:  "data: {line}\n"  je Zeile, Frame endet mit zusaetzlichem "\n"  (_sse_data_frame) -->
<!-- Werkzeug:    "event: tool\ndata: {label}\n\n" -->
<!-- Abschluss:   "event: done\ndata: \n\n" -->
<!-- Fehler:      "event: error\ndata: {line}\n\n" -->
<!-- Chat-Endpoint (webui/src/main.py::chat_send): POST /chat/{agent_id}/send, Form-Felder: -->
<!--   message (max 8000), history (JSON-Array [{role,content}]), mail_context (JSON {subject,sender,body} ODER ""), session_id -->
<!-- CSRF (webui/src/main.py::enforce_same_origin + _origin_allowed_for_addin): -->
<!--   Nicht-sichere Methoden brauchen Origin/Referer == Host; Ausnahme fuer /chat/{id}/(send|embed) NUR wenn Origin gesetzt UND in ADDIN_FRAME_ANCESTORS gelistet. -->
<!--   DEFAULT_ADDIN_FRAME_ANCESTORS enthaelt u.a. https://outlook.office.com -> als Default-Origin-Token nutzbar (Zero-Config). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Solution-Geruest + Settings-/Kontext-DTOs mit DPAPI-Persistenz</name>
  <read_first>
    - .planning/phases/08-outlook-add-in-f-r-den-agenten-chat-v1-4/08-RESEARCH.md (Recommended Project Structure, Pattern 4 Settings-Persistenz, Package Legitimacy Audit)
    - webui/static/chat.js (generateSessionId — Referenzmuster fuer session_id)
    - webui/src/main.py (_parse_mail_context — Feldnamen subject/sender/body, _parse_chat_history — role/content)
  </read_first>
  <behavior>
    - MailContext serialisiert exakt die Felder subject/sender/body als JSON (Newtonsoft.Json)
    - AddinSettings haelt BackendUrl, AgentId, Username, Password, AddinOriginToken (Default "https://outlook.office.com"), CertThumbprint, TrustAnyCertificate (Default false)
    - SecureSettingsStore.Save schreibt Password NUR DPAPI-verschluesselt (DataProtectionScope.CurrentUser) als Base64; Load entschluesselt round-trip-treu
    - SessionIdGenerator liefert einen neuen GUID-String pro Aufruf
  </behavior>
  <action>
    Lege `outlook-addin/VizpatchOutlookAddin.sln` an und darin die beiden Projekte `VizpatchAddin.Core` (Class Library, TargetFramework `net48`) und `VizpatchAddin.Tests` (net48, xUnit). Referenziere in Core `Newtonsoft.Json` (13.0.x, per NuGet PackageReference — Approved laut Audit) und die BCL-Assembly `System.Security` (fuer `System.Security.Cryptography.ProtectedData`). In Tests: `xunit` + `xunit.runner.visualstudio` (Approved). Erstelle `MailContext.cs` (Properties Subject/Sender/Body + `ToJson()` via `JsonConvert.SerializeObject`), `AddinSettings.cs` (die oben genannten Felder, AddinOriginToken-Default aus ADDIN_FRAME_ANCESTORS-Default abgeleitet und im XML-Doc-Kommentar auf den CSRF-Origin-Befund verweisend, D-84/D-85), `SessionIdGenerator.cs` (`Guid.NewGuid().ToString()`, analog chat.js), `SecureSettingsStore.cs` (Save/Load nach `%AppData%\Vizpatch\OutlookAddin\settings.json`, Passwort ueber `ProtectedData.Protect/Unprotect` mit `DataProtectionScope.CurrentUser`, Klartext-Felder direkt, per D-85/Pattern 4 — NICHT `Properties.Settings.Default`). Schreibe `MailContextTests.cs` (JSON-Feldnamen) und `AddinSettingsTests.cs` (DPAPI-Round-Trip: gespeichertes settings.json enthaelt das Klartext-Passwort NICHT).
  </action>
  <acceptance_criteria>
    - Solution + beide Projekte (VizpatchAddin.Core, VizpatchAddin.Tests) existieren, beide TargetFramework net48
    - MailContext serialisiert die Felder subject/sender/body; AddinSettings enthaelt alle acht Felder inkl. AddinOriginToken-Default und TrustAnyCertificate=false
    - SecureSettingsStore nutzt ProtectedData mit DataProtectionScope.CurrentUser
    - AddinSettingsTests behauptet: das gespeicherte settings.json enthaelt das Klartext-Passwort nicht (DPAPI-Round-Trip)
    - Core enthaelt keine Outlook-COM-Referenz
  </acceptance_criteria>
  <verify>
    <automated>grep -q "class SecureSettingsStore" outlook-addin/VizpatchAddin.Core/SecureSettingsStore.cs && grep -q "ProtectedData" outlook-addin/VizpatchAddin.Core/SecureSettingsStore.cs && grep -q "DataProtectionScope.CurrentUser" outlook-addin/VizpatchAddin.Core/SecureSettingsStore.cs && grep -q "net48" outlook-addin/VizpatchAddin.Core/VizpatchAddin.Core.csproj && ! grep -rq "Office.Interop" outlook-addin/VizpatchAddin.Core/</automated>
    <human-check>Auf der Windows-Build-Maschine: `dotnet test` bzw. Test-Explorer laesst MailContextTests + AddinSettingsTests gruen laufen (kein Outlook noetig)</human-check>
  </verify>
  <done>Solution + beide Projekte existieren (net48); DTOs + SecureSettingsStore vorhanden; settings.json enthaelt kein Klartext-Passwort; Tests-Datei behauptet den DPAPI-Round-Trip.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SseLineParser (hoechstes Risiko) + xUnit-Tests</name>
  <read_first>
    - webui/src/main.py (_sse_data_frame + der _stream-Generator in chat_send — exaktes Frame-Format fuer tool/text/done/error)
    - .planning/phases/08-outlook-add-in-f-r-den-agenten-chat-v1-4/08-RESEARCH.md (Architecture Pattern 2, Don't Hand-Roll: SSE-Frame-Erkennung)
  </read_first>
  <behavior>
    - Eine Leerzeile beendet einen Frame und liefert (EventType, JoinedData) mit "\n" zwischen mehreren data:-Zeilen
    - Ohne vorheriges "event:" ist der EventType "message" (Text-Chunk)
    - "event: tool" + "data: X" liefert ("tool", "X")
    - "event: done" + "data: " liefert ("done", "")
    - "event: error" + "data: Y" liefert ("error", "Y")
    - Mehrzeiliger Text (zwei data:-Zeilen desselben Frames) wird mit einem "\n" verbunden
    - Fuehrendes Leerzeichen nach "data:" / "event:" wird per TrimStart entfernt
  </behavior>
  <action>
    Erstelle `SseLineParser.cs`: eine Zustandsmaschine, die Zeilen einzeln entgegennimmt (`Feed(string line)`) und beim Leerzeilen-Frame-Ende ein `(string EventType, string Data)` ausgibt (z. B. via `TryTakeFrame`/Callback/IEnumerable). Felder: `event:`-Prefix setzt EventType (TrimStart), `data:`-Prefix haengt an eine `data`-Zeilenliste an (TrimStart), sonstige SSE-Felder (`id:`,`retry:`) werden ignoriert. Kein Netzwerk-, kein HttpClient-Bezug (rein string-basiert, damit ohne Backend testbar). Schreibe `SseLineParserTests.cs` mit je einem `[Fact]`/`[Theory]` pro Behavior-Zeile oben (mind. Text-Single, Text-Multiline, tool, done, error).
  </action>
  <acceptance_criteria>
    - SseLineParser existiert, string-basiert, ohne HttpClient-/Netzwerk-Bezug
    - SseLineParserTests enthaelt >=5 [Fact]/[Theory] und deckt explizit tool, done, error sowie mehrzeiligen Text ab
    - Fuehrende Leerzeichen nach event:/data: werden entfernt (im Test belegt)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "class SseLineParser" outlook-addin/VizpatchAddin.Core/SseLineParser.cs && ! grep -q "HttpClient" outlook-addin/VizpatchAddin.Core/SseLineParser.cs && grep -cE "\[(Fact|Theory)\]" outlook-addin/VizpatchAddin.Tests/SseLineParserTests.cs | awk '{ if ($1 < 5) exit 1 }' && grep -qi "tool" outlook-addin/VizpatchAddin.Tests/SseLineParserTests.cs && grep -qi "done" outlook-addin/VizpatchAddin.Tests/SseLineParserTests.cs && grep -qi "error" outlook-addin/VizpatchAddin.Tests/SseLineParserTests.cs</automated>
    <human-check>Auf der Windows-Build-Maschine: SseLineParserTests laufen gruen (RED->GREEN vor Task 3)</human-check>
  </verify>
  <done>SseLineParser existiert, kein HttpClient-Bezug; >=5 Tests decken Text-Single/Text-Multiline/tool/done/error ab.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ChatClient — Form-POST + Origin/Basic-Auth + inkrementelles SSE-Streaming</name>
  <read_first>
    - webui/src/main.py (chat_send Form-Felder + enforce_same_origin + _origin_allowed_for_addin + DEFAULT_ADDIN_FRAME_ANCESTORS)
    - .planning/phases/08-outlook-add-in-f-r-den-agenten-chat-v1-4/08-RESEARCH.md (Pattern 2, Code Examples: Basic-Auth-Header, Origin-Header, scoped Self-signed-Cert-Trust; Common Pitfall 1 + 5)
    - outlook-addin/VizpatchAddin.Core/SseLineParser.cs (aus Task 2)
    - outlook-addin/VizpatchAddin.Core/AddinSettings.cs (aus Task 1)
  </read_first>
  <behavior>
    - StreamChatAsync sendet POST {BackendUrl}/chat/{AgentId}/send als FormUrlEncodedContent mit message, history (JSON), mail_context (JSON oder ""), session_id
    - Der Request traegt einen Origin-Header == settings.AddinOriginToken und einen Basic-Auth-Header (base64 user:pass) — ohne Origin wuerde enforce_same_origin mit 403 abweisen (Common Pitfall 1)
    - HttpCompletionOption.ResponseHeadersRead + Timeout.InfiniteTimeSpan; der Body wird zeilenweise gelesen und durch SseLineParser gefuehrt; jeder Frame wird als (EventType, Data) an den Aufrufer weitergegeben
    - Zertifikatspruefung ist auf DEN einen HttpClientHandler gescoped (ServerCertificateCustomValidationCallback): Thumbprint-Pinning wenn CertThumbprint gesetzt, Blanket-Trust NUR wenn TrustAnyCertificate==true; NIEMALS ServicePointManager global (Common Pitfall 5)
  </behavior>
  <action>
    Erstelle `ChatClient.cs`: Konstruktor nimmt `AddinSettings` und baut einen `HttpClientHandler` mit gescopetem `ServerCertificateCustomValidationCallback` (Pinning per `CertThumbprint` bzw. Blanket-Trust nur bei `TrustAnyCertificate`, jeweils mit Sicherheits-Kommentar). Methode `StreamChatAsync(message, history, mailContext, sessionId, ct)` baut `FormUrlEncodedContent` mit den Feldern `message`/`history` (`JsonConvert.SerializeObject(history)`)/`mail_context` (serialisiert oder "")/`session_id`, setzt `request.Headers.Add("Origin", settings.AddinOriginToken)` und `DefaultRequestHeaders.Authorization = new AuthenticationHeaderValue("Basic", base64(user:pass))`, sendet mit `HttpCompletionOption.ResponseHeadersRead`, liest `StreamReader.ReadLineAsync()` in einer Schleife und speist jede Zeile in den `SseLineParser`; gibt Frames als `IAsyncEnumerable<(string,string)>` oder via Callback zurueck. KEIN `ConfigureAwait(false)` (Anti-Pattern, UI-Thread-Marshalling). Schreibe `ChatClientRequestTests.cs`, die (ohne echtes Netzwerk, z. B. via injizierbarem HttpMessageHandler-Stub) verifizieren: (a) URL == {BackendUrl}/chat/{AgentId}/send, (b) Origin-Header-Wert == AddinOriginToken, (c) Authorization-Header ist Basic mit korrektem base64, (d) alle vier Form-Felder gesetzt.
  </action>
  <acceptance_criteria>
    - ChatClient sendet POST an {BackendUrl}/chat/{AgentId}/send mit allen vier Form-Feldern (message/history/mail_context/session_id)
    - Origin-Header == AddinOriginToken und Basic-Auth-Header werden gesetzt (im Request-Test belegt)
    - HttpCompletionOption.ResponseHeadersRead + zeilenweises Streaming ueber SseLineParser
    - Cert-Trust ist auf den HttpClientHandler gescoped (kein ServicePointManager); kein ConfigureAwait(false)
    - ChatClientRequestTests behauptet URL, Origin-Wert, Basic-Auth-base64 und die vier Felder
  </acceptance_criteria>
  <verify>
    <automated>grep -q "class ChatClient" outlook-addin/VizpatchAddin.Core/ChatClient.cs && grep -q "ResponseHeadersRead" outlook-addin/VizpatchAddin.Core/ChatClient.cs && grep -q '"Origin"' outlook-addin/VizpatchAddin.Core/ChatClient.cs && grep -q "AuthenticationHeaderValue" outlook-addin/VizpatchAddin.Core/ChatClient.cs && grep -q "ServerCertificateCustomValidationCallback" outlook-addin/VizpatchAddin.Core/ChatClient.cs && ! grep -q "ServicePointManager" outlook-addin/VizpatchAddin.Core/ChatClient.cs && ! grep -q "ConfigureAwait(false)" outlook-addin/VizpatchAddin.Core/ChatClient.cs && grep -qi "Origin" outlook-addin/VizpatchAddin.Tests/ChatClientRequestTests.cs</automated>
    <human-check>Auf der Windows-Build-Maschine: ChatClientRequestTests gruen; optional Live-POST gegen eine Test-WebUI-Instanz liefert 200 statt 403 (Origin-Workaround wirkt)</human-check>
  </verify>
  <done>ChatClient baut den exakten Request (URL/Felder/Origin/Basic-Auth), streamt via ResponseHeadersRead+SseLineParser, Cert-Trust ist gescoped (kein ServicePointManager), kein ConfigureAwait(false); Request-Tests behaupten URL/Origin/Auth/Felder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Add-in-Client -> Backend-API (LAN) | Untrusted-Netzwerk zwischen Windows-Rechner und LAN-Server; CSRF-/Auth-/TLS-Grenze |
| Lokale Platte (settings.json) -> Add-in | Gespeicherte Credentials at-rest auf dem Windows-Profil |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Spoofing | ChatClient-POST vs. enforce_same_origin | mitigate | Origin-Header == in ADDIN_FRAME_ANCESTORS gelisteter Wert (Default https://outlook.office.com); nutzt bestehende _origin_allowed_for_addin-Ausnahme, keine Backend-Codeaenderung (Common Pitfall 1) |
| T-08-02 | Information Disclosure | Basic-Auth-Passwort at-rest (settings.json) | mitigate | DPAPI ProtectedData (DataProtectionScope.CurrentUser); Klartext-Passwort erscheint nie in der Datei (AddinSettingsTests belegt das) |
| T-08-03 | Tampering/Spoofing | TLS gegen selbstsigniertes Backend-Zertifikat | mitigate | Thumbprint-Pinning als Default; Blanket-Trust nur bei explizitem TrustAnyCertificate, gescoped auf den einen HttpClientHandler (kein globaler ServicePointManager, Common Pitfall 5) |
| T-08-SC | Tampering | NuGet-Installs (Newtonsoft.Json, xunit) | accept | Beide laut RESEARCH.md Package Legitimacy Audit als Approved verifiziert (nuget.org, etablierte Publisher); Microsoft.Office.Interop.Outlook wird NICHT per NuGet gezogen (erst Plan 08-02, VSTO-Projektvorlage) |
</threat_model>

<verification>
- Core-Bibliothek enthaelt keinerlei `Microsoft.Office.Interop.Outlook`- oder `Microsoft.Office.Tools`-Referenz (COM-frei, ohne Outlook testbar): `! grep -rq "Office.Interop" outlook-addin/VizpatchAddin.Core/`
- Alle vier SSE-Frame-Typen sind in SseLineParserTests abgedeckt
- ChatClient sendet Origin + Basic-Auth und nutzt kein global gepatchtes TLS
</verification>

<success_criteria>
- SseLineParser deckt das exakte Server-Framing ab (Success Criterion 2, Transport-Anteil)
- ChatClient umschifft den CSRF/Origin-403-Befund ohne Backend-Aenderung (Success Criterion 2)
- Passwort wird DPAPI-verschluesselt persistiert (Success Criterion 6, Sicherheits-Anteil)
- session_id-Generator vorhanden (Success Criterion 3, Client-Anteil)
</success_criteria>

<output>
Create `.planning/phases/08-outlook-add-in-f-r-den-agenten-chat-v1-4/08-01-SUMMARY.md` when done
</output>
