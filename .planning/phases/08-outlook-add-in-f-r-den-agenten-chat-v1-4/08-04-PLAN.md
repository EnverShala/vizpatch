---
phase: 08-outlook-add-in-f-r-den-agenten-chat-v1-4
plan: 04
type: execute
wave: 4
depends_on: ["08-02", "08-03"]
files_modified:
  - scripts/check-addin-no-autosend.sh
  - deployment/README.addin-outlook.md
autonomous: false
requirements: [OUT-05, OUT-07, OUT-09]
user_setup: []

must_haves:
  truths:
    - "Ein automatisierter Wächter belegt, dass der gesamte outlook-addin-Quellbaum KEINE Outlook-Send-/Write-/Move-/Delete-/CreateItem-APIs aufruft (Kein-Auto-Send strukturell)"
    - "Ein Runbook-Kapitel beschreibt ClickOnce-Installation + Voraussetzungen (.NET Framework 4.8 / VSTO-Runtime), LAN-Erreichbarkeit, optional HTTPS/selbstsigniertes Zertifikat inkl. Trade-off, und den 'Outlook classic vs neues Outlook'-Vorabcheck"
    - "Die agentischen Postfach-Werkzeuge (Phase 9) inkl. Bestätigungs-Gate (session_id) laufen im Live-Test end-to-end; ein resultierender Draft erscheint via IMAP-Sync in Outlooks Drafts-Ordner"
  artifacts:
    - path: "scripts/check-addin-no-autosend.sh"
      provides: "Struktureller Kein-Auto-Send-Quellwächter über outlook-addin/"
      contains: "Send"
    - path: "deployment/README.addin-outlook.md"
      provides: "ClickOnce-/LAN-/HTTPS-Runbook-Kapitel"
      contains: "ClickOnce"
  key_links:
    - from: "check-addin-no-autosend.sh"
      to: "outlook-addin/ Quellbaum"
      via: "grep über *.cs auf verbotene Write-APIs"
      pattern: "outlook-addin"
---

<objective>
Schliesse die Phase ab (MVP-Slice 5): (1) ein automatisierter, im Repo lauffaehiger Kein-Auto-Send-Quellwächter (analog zum Phase-9-AST-Wächter) belegt strukturell, dass das Add-in keine Outlook-Schreib-APIs aufruft; (2) ein Runbook-Kapitel dokumentiert ClickOnce-Verteilung, Voraussetzungen, LAN-Erreichbarkeit und optional HTTPS/Zertifikat mit dokumentiertem Trade-off; (3) ein menschlicher Live-Abnahme-Checkpoint in echtem Outlook classic (Installation, Werkzeug-Lauf inkl. Bestätigungs-Gate, Draft erscheint via IMAP-Sync, Kein-Auto-Send).

Purpose: Verteilbarkeit (OUT-05), End-to-End-Nachweis der Werkzeuge inkl. Gate (OUT-07) und die strukturelle Kein-Auto-Send-Garantie + Betriebsdoku (OUT-09).

Output: Wächter-Skript, Runbook-Kapitel, abgenommenes Add-in.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-outlook-add-in-f-r-den-agenten-chat-v1-4/08-CONTEXT.md
@.planning/phases/08-outlook-add-in-f-r-den-agenten-chat-v1-4/08-RESEARCH.md
@.planning/phases/02-deployment-beim-kunden/RUNBOOK.md
@.planning/phases/08-outlook-add-in-f-r-den-agenten-chat-v1-4/08-02-PLAN.md
@.planning/phases/08-outlook-add-in-f-r-den-agenten-chat-v1-4/08-03-PLAN.md

<interfaces>
<!-- Bestehendes Backend: /chat/{agent_id}/send (SSE), session_id-HMAC-Gate fuer Papierkorb-Werkzeuge (chat_tools.py, 12h TTL) -->
<!-- ADDIN_FRAME_ANCESTORS (webui/src/main.py): Origin-Token des Add-ins muss hier gelistet sein (Default enthaelt https://outlook.office.com) -->
<!-- ClickOnce/Prereqs: 08-RESEARCH.md Deployment + Environment Availability (VS-Publish-Wizard, Bootstrapper) -->
<!-- Draft-Erzeugung: serverseitig via IMAP APPEND -> erscheint via IMAP-Sync in Outlooks Drafts-Ordner (D-87) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Struktureller Kein-Auto-Send-Quellwächter über den Add-in-Quellbaum</name>
  <files>scripts/check-addin-no-autosend.sh</files>
  <read_first>
    - .planning/phases/08-outlook-add-in-f-r-den-agenten-chat-v1-4/08-CONTEXT.md (D-87 Kein-Auto-Send strukturell)
    - outlook-addin/VizpatchAddin/TaskPane/ChatView.cs, outlook-addin/VizpatchAddin/MailContextReader.cs (bestehende lesende Zugriffe)
  </read_first>
  <action>
    Schreibe `scripts/check-addin-no-autosend.sh` (POSIX/Git-Bash): durchsuche alle `*.cs` unter `outlook-addin/` (Kommentarzeilen mit `//` vorher herausfiltern, damit erklaerende Prosa das Gate nicht faelschlich ausloest — z. B. `grep -v '^[[:space:]]*//'`) auf verbotene Outlook-Schreib-/Sende-Aufrufe: die Methodenaufruf-Muster `.Send(`, `.Save(`, `.SaveAs(`, `.Move(`, `.Delete(`, `.CreateItem(`, `.Reply(`, `.ReplyAll(`, `.Forward(` sowie `new Outlook.Application`/`CreateObject`. Bei einem Treffer: nicht-null Exit-Code + Ausgabe der Fundstelle; bei sauberem Baum Exit 0 mit Bestaetigung. Das Skript ist das strukturelle Gegenstueck zum Phase-9-AST-Wächter und laeuft ohne Windows/VS. Lesende Zugriffe (`.Subject`, `.SenderEmailAddress`, `.Body`, `.CurrentItem`, `.Selection`) sind ausdruecklich erlaubt und duerfen NICHT matchen.
  </action>
  <acceptance_criteria>
    - Skript existiert, ist ausfuehrbar und scannt outlook-addin/ rekursiv nach *.cs
    - Kommentarzeilen werden vor dem Gate herausgefiltert (keine Selbst-Invalidierung durch Prosa)
    - Gegen den aktuellen (lesenden) Quellbaum liefert das Skript Exit 0
    - Ein testweise eingefuegter `.Send(`-Aufruf wuerde einen nicht-null Exit erzeugen (im Skript-Kommentar dokumentiertes Verhalten)
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/check-addin-no-autosend.sh; test $? -eq 0</automated>
  </verify>
  <done>Kein-Auto-Send-Wächter läuft grün gegen den Add-in-Quellbaum und würde Schreib-/Sende-APIs blockieren.</done>
</task>

<task type="auto">
  <name>Task 2: Runbook-Kapitel — ClickOnce-Verteilung + LAN/HTTPS + Vorabchecks</name>
  <files>deployment/README.addin-outlook.md</files>
  <read_first>
    - .planning/phases/08-outlook-add-in-f-r-den-agenten-chat-v1-4/08-RESEARCH.md (Deployment/ClickOnce, Environment Availability, Common Pitfalls 2/4/5, Security Domain, Open Questions 1-3)
    - .planning/phases/02-deployment-beim-kunden/RUNBOOK.md (Stil/Struktur des bestehenden Runbooks)
    - deployment/README.phase4.md (Backend-Deployment/Ports als Referenz)
  </read_first>
  <action>
    Erstelle `deployment/README.addin-outlook.md` (deutsch): (a) Voraussetzungen auf der Betreiber-Maschine — Outlook classic (NICHT "neues Outlook", mit Umschalt-Hinweis oben rechts, Pitfall 2), .NET Framework 4.8 + VSTO-Runtime (werden vom ClickOnce-Bootstrapper bei Bedarf nachinstalliert, ggf. Adminrechte fuer die VSTO-Runtime), 32-/64-Bit-Office-Bitness-Check (`Datei > Office-Konto > Info`, Open Question 3). (b) ClickOnce-Publish auf der Windows-Build-Maschine (VS-Publish-Wizard, Prerequisites aktivieren) + Per-User-Installation beim Betreiber; Hinweis auf Code-Signing/„Herausgeber nicht verifiziert" (Selbstsigniert vertretbar fuer Einzel-Kunde, im Runbook als bewusste Abwaegung, Security Domain). (c) Backend-Erreichbarkeit im LAN: Settings-Dialog-Werte fuer beide Faelle — direkt `http://<lan-ip>:8080` UND Reverse-Proxy/HTTPS mit selbstsigniertem Zertifikat (Caddyfile.example wiederverwendbar, Open Question 1); Basic-Auth ueber Klartext-HTTP NUR im vertrauenswuerdigen LAN, HTTPS empfohlen — Trade-off explizit (D-85). (d) Origin-Token: Standard `https://outlook.office.com` funktioniert ohne Backend-Aenderung, weil in `ADDIN_FRAME_ANCESTORS` gelistet; optional dedizierter Marker `https://vizpatch-addin.local` durch Ergaenzung von `ADDIN_FRAME_ANCESTORS` (Runbook zeigt die Env-Zeile, kein Code-Change), Open Question 2.
  </action>
  <acceptance_criteria>
    - Datei nennt ClickOnce, .NET Framework 4.8, VSTO-Runtime, Outlook-classic-vs-neues-Outlook-Check
    - LAN-HTTP- UND HTTPS-/Zertifikats-Fall dokumentiert inkl. Basic-Auth-Klartext-Trade-off
    - Origin-Token/ADDIN_FRAME_ANCESTORS-Zusammenhang erklaert (zero-config-Default + optionaler Marker)
    - Bitness-Check + Code-Signing-Abwaegung erwaehnt
  </acceptance_criteria>
  <verify>
    <automated>grep -qi "ClickOnce" deployment/README.addin-outlook.md && grep -qi "4.8" deployment/README.addin-outlook.md && grep -qi "neues Outlook" deployment/README.addin-outlook.md && grep -qi "ADDIN_FRAME_ANCESTORS" deployment/README.addin-outlook.md && grep -qi "HTTPS" deployment/README.addin-outlook.md</automated>
  </verify>
  <done>Runbook-Kapitel deckt Voraussetzungen, ClickOnce, LAN/HTTPS-Trade-off, Origin-Token und Vorabchecks ab.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live-Abnahme in echtem Outlook classic (Installation + Werkzeuge + Gate + Draft)</name>
  <files>outlook-addin/VizpatchOutlookAddin.sln</files>
  <action>Menschlicher Checkpoint — keine Code-Aenderung. Der baubare Teil ist mit Plan 08-01…08-03 fertig; hier erfolgt ClickOnce-Publish + Installation + End-to-End-Abnahme in echtem Outlook classic. Nicht auf dieser Session-Maschine ausfuehrbar (kein VS/Office, Pitfall 4).</action>
  <what-built>Vollstaendiges VSTO-Add-in (Ribbon + Task Pane + SSE-Chat + Mail-Kontext + Settings-Dialog), ClickOnce-verteilbar, mit strukturell nachgewiesenem Kein-Auto-Send.</what-built>
  <how-to-verify>
    Auf der Windows-Build-Maschine (VS 2022 + Office) publishen, auf dem Betreiber-Rechner (Outlook classic) installieren, Backend im LAN erreichbar:
    1. ClickOnce-Publish (Prerequisites aktiv) -> Setup auf dem Betreiber-Rechner ausfuehren; Add-in laedt in Outlook classic (Ribbon-Toggle sichtbar).
    2. Settings-Dialog: Backend-URL/Agent-ID/Zugangsdaten (+ ggf. HTTPS-Zertifikats-Thumbprint) setzen und speichern.
    3. Eine Mail oeffnen, "aktuelle Mail einbeziehen" aktiv, fragen "Fasse diese Mail zusammen" -> Antwort bezieht sich auf die Mail (mail_context, OUT-08).
    4. Werkzeug-Lauf inkl. Bestätigungs-Gate: "Suche die Mail zu Thema X" (mails_suchen) und dann eine bestätigungspflichtige Aktion (z. B. "verschiebe das in den Papierkorb") -> Add-in zeigt die Rueckfrage, erst nach ausdruecklicher Bestätigung fuehrt der Server die Aktion aus (session_id-Gate greift ueber den nativen Client, OUT-07).
    5. Entwurf erzeugen lassen -> ein Draft erscheint via IMAP-Sync in Outlooks Drafts-Ordner (KEIN Auto-Send, OUT-07/OUT-09).
    6. Kein-Auto-Send-Sichtpruefung: es wird zu keinem Zeitpunkt eine Mail gesendet oder direkt im Store veraendert; nur Drafts erscheinen.
  </how-to-verify>
  <resume-signal>Schreibe "approved" oder liste die abweichenden Punkte (z. B. Gate greift nicht, Draft fehlt, Add-in laedt nicht)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Add-in-Quellbaum -> Outlook-Store | Kein-Auto-Send: keine Schreib-/Sende-APIs erlaubt |
| ClickOnce-Paket -> Betreiber-Maschine | Supply-Chain beim Erst-Setup |
| Add-in -> Backend (session_id-Gate) | Autorisierung destruktiver Werkzeuge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Tampering | Kein-Auto-Send-Invariante (gesamter Add-in-Baum) | mitigate | Automatischer Quellwächter (scripts/check-addin-no-autosend.sh) blockiert Send/Save/Move/Delete/CreateItem — strukturell, nicht per Konvention (D-87) |
| T-08-12 | Elevation of Privilege | Bestätigungs-Gate Papierkorb-Werkzeuge über nativen Client | mitigate | session_id wird stabil pro Sitzung durchgereicht (08-02); Server-HMAC-Gate (chat_tools.py) verlangt explizite Bestätigung; Live-Checkpoint prüft, dass ohne Bestätigung nichts verschoben wird |
| T-08-13 | Tampering | ClickOnce-Installationspaket (Supply-Chain) | accept | Code-Signing (auch selbstsigniert) empfohlen; fuer Einzel-Kunde vertretbar mit muendlicher Bestätigung beim Vor-Ort-Setup, im Runbook als bewusste Abwaegung dokumentiert |
| T-08-14 | Information Disclosure | Basic-Auth über Klartext-HTTP im LAN | mitigate | HTTPS empfohlen (selbstsigniert + Client-Trust/Pinning), Trade-off im Runbook dokumentiert (D-85) |
</threat_model>

<verification>
- scripts/check-addin-no-autosend.sh Exit 0 gegen den finalen Quellbaum
- Runbook-Kapitel deckt ClickOnce/LAN/HTTPS/Vorabchecks ab
- Live-Checkpoint: Werkzeuge + Gate + Draft-via-IMAP-Sync + Kein-Auto-Send bestaetigt
</verification>

<success_criteria>
- ClickOnce-Installer + Voraussetzungen dokumentiert (Success Criterion 1)
- Werkzeuge inkl. Bestätigungs-Gate laufen end-to-end, Draft erscheint via IMAP-Sync (Success Criterion 3)
- Kein-Auto-Send strukturell nachgewiesen (Success Criterion 5)
- LAN-Erreichbarkeit + optional HTTPS als Runbook-Kapitel (Success Criterion 6)
</success_criteria>

<output>
Create `.planning/phases/08-outlook-add-in-f-r-den-agenten-chat-v1-4/08-04-SUMMARY.md` when done
</output>
