---
phase: 09-agentischer-chat-mit-postfach-werkzeugen-v1-5
plan: 05
type: execute
wave: 5
depends_on: ["09-04"]
files_modified:
  - webui/tests/test_chat_tools.py
  - webui/src/templates/_datenschutz.html
  - .planning/phases/02-deployment-beim-kunden/AVV-CHECKLIST.md
autonomous: true
requirements: [CTOOL-05]
must_haves:
  truths:
    - "Der Werkzeugsatz enthält strukturell KEIN Sende-/SMTP-Werkzeug (durch einen Test bewiesen)."
    - "chat_tools.py importiert/nutzt keine SMTP-/Send-API (smtplib, sendmail, o. Ä.)."
    - "Die Datenschutzerklärung (Ziffer 6) beschreibt die tatsächlichen Fähigkeiten: suchen/lesen, Entwurf bearbeiten, Löschen=Papierkorb nach Bestätigung, kein Senden."
    - "Die AVV-Checkliste (§6.2) nennt den Phase-9-Verarbeitungszweck (Postfach-Werkzeuge, Papierkorb-Move nach Bestätigung)."
    - "Die volle webui-Test-Suite ist grün, keine Regression."
  artifacts:
    - path: "webui/tests/test_chat_tools.py"
      provides: "struktureller Kein-Auto-Send-Wächter (kein Sende-Tool, keine SMTP-API)"
      contains: "smtplib"
    - path: "webui/src/templates/_datenschutz.html"
      provides: "Ziffer 6 auf tatsächliche Fähigkeiten angeglichen (D-81)"
      contains: "Papierkorb"
    - path: ".planning/phases/02-deployment-beim-kunden/AVV-CHECKLIST.md"
      provides: "§6.2 Phase-9-Verarbeitungszweck ergänzt (D-81)"
      contains: "Phase 9"
  key_links:
    - from: "webui/tests/test_chat_tools.py"
      to: "webui/src/chat_tools.py Werkzeugsatz"
      via: "Scan: kein Sende-Tool in TOOL_HANDLERS, keine SMTP-API im Quelltext"
      pattern: "smtplib|sendmail|SMTP"
---

<objective>
Struktureller Kein-Auto-Send-Nachweis (CTOOL-05, D-77) und Angleichung der rechtlichen Texte an die
tatsächlichen Fähigkeiten (D-81): Datenschutzerklärung (`_datenschutz.html` Ziffer 6) und
AVV-Verarbeitungszwecke (`AVV-CHECKLIST.md` §6.2). Plus Abschluss-Verifikation der ganzen Phase.

Purpose: Kein-Auto-Send ist nicht nur Konvention, sondern strukturell (kein Sende-Werkzeug existiert) und
durch einen Test dauerhaft abgesichert; die rechtlichen Muster-Texte beschreiben ehrlich, was der Chat
jetzt kann (suchen/lesen, Entwurf bearbeiten, Löschen=Papierkorb nach Bestätigung).

Output: Kein-Auto-Send-Wächter-Test, aktualisierte `_datenschutz.html` + `AVV-CHECKLIST.md`, grüne Gesamt-Suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-agentischer-chat-mit-postfach-werkzeugen-v1-5/09-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Bestehende Muster. -->
webui/tests/test_addin_readonly.py  # Phase-8-Muster: Kein-Auto-Send-Quelltext-Scan mit Kommentar-Filterung
webui/tests/test_chat_tools.py      # TOOL_HANDLERS-Keys aus 09-01..09-04
webui/src/chat_tools.py             # der zu prüfende Werkzeugsatz
webui/src/templates/_datenschutz.html  # Ziffer 6 (Agenten-Chat) — anzupassen
.planning/phases/02-deployment-beim-kunden/AVV-CHECKLIST.md  # §6.2 Verarbeitungszwecke — zu ergänzen
</interfaces>

@webui/src/chat_tools.py
@webui/src/templates/_datenschutz.html
@.planning/phases/02-deployment-beim-kunden/AVV-CHECKLIST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Struktureller Kein-Auto-Send-Wächter-Test (CTOOL-05 / D-77)</name>
  <read_first>
    - webui/tests/test_addin_readonly.py (Quelltext-Scan-Muster mit Kommentar-Filterung, Positiv-/Negativ-Fall)
    - webui/src/chat_tools.py (finaler Werkzeugsatz aus 09-01..09-04)
    - webui/tests/test_chat_tools.py (bestehende TOOL_HANDLERS-Assertions)
  </read_first>
  <action>
    Ergänze in `webui/tests/test_chat_tools.py` einen strukturellen Kein-Auto-Send-Wächter analog
    `test_addin_readonly.py`: (a) `TOOL_HANDLERS` enthält ausschließlich die erlaubten Werkzeuge
    (mails_suchen, mail_lesen, entwuerfe_auflisten, entwurf_lesen, entwurf_bearbeiten, mail_in_papierkorb,
    entwurf_in_papierkorb) und KEIN Werkzeug, dessen Name/Beschreibung auf Senden/Reply/SMTP hindeutet
    (Scan der TOOL_SCHEMAS-Namen + descriptions auf verbotene Muster: "send", "senden", "versend", "smtp",
    "reply", "verschick"); (b) der `chat_tools.py`-Quelltext importiert/nutzt keine SMTP-/Send-API
    (`smtplib`, `SMTP`, `sendmail`, `.send_message(`) — Kommentare via Zeilenfilter (`lstrip().startswith('#')`)
    ausschließen, damit erklärende Kommentare den Test nicht selbst-invalidieren. Positiv-/Negativ-Fall
    dokumentieren (der Test würde anschlagen, wenn jemand ein Sende-Tool hinzufügt).
  </action>
  <acceptance_criteria>
    - `grep -n "smtplib\|send_message\|TOOL_HANDLERS" webui/tests/test_chat_tools.py` belegt den Wächter.
    - Test: `set(TOOL_HANDLERS)` == exakt die sieben erlaubten Werkzeuge; kein zusätzlicher Name.
    - Test: kein TOOL_SCHEMAS-Name/keine description matcht die verbotenen Sende-Muster.
    - Test: chat_tools.py-Quelltext (ohne Kommentarzeilen) enthält keine SMTP-/Send-API.
  </acceptance_criteria>
  <verify>
    <automated>cd webui && python -m pytest tests/test_chat_tools.py -x -q</automated>
  </verify>
  <done>Kein-Auto-Send ist strukturell durch einen dauerhaften Test abgesichert; Tests grün.</done>
</task>

<task type="auto">
  <name>Task 2: Datenschutzerklärung Ziffer 6 + AVV-Checkliste §6.2 an tatsächliche Fähigkeiten angleichen (D-81)</name>
  <read_first>
    - webui/src/templates/_datenschutz.html (Ziffer 6 „Agenten-Chat" — aktuell allgemein „lesende oder verändernde Aktionen … auf ausdrückliche Anweisung")
    - .planning/phases/02-deployment-beim-kunden/AVV-CHECKLIST.md (§6.2 „Neue Verarbeitungszwecke", listet bisher Phase 6/7/8)
    - .planning/phases/09-agentischer-chat-mit-postfach-werkzeugen-v1-5/09-CONTEXT.md (D-74..D-77 exakte Fähigkeiten)
  </read_first>
  <action>
    Aktualisiere `_datenschutz.html` Ziffer 6: benenne die konkreten Chat-Fähigkeiten der v1.5 — Mails/Entwürfe
    suchen und lesen (read-only, PII-redigiert vor KI-Übermittlung), bestehende Entwürfe umformulieren und im
    Entwürfe-Ordner ablegen, Mails/Entwürfe **in den Papierkorb verschieben (kein endgültiges Löschen,
    reversibel) und nur nach ausdrücklicher Bestätigung**; **Kein-Auto-Send bleibt** (kein Sende-Werkzeug).
    Behalte den Muster-/Prüf-Hinweis-Stil des Dokuments bei. Ergänze in `AVV-CHECKLIST.md` §6.2 einen neuen
    Checklisten-Punkt „**Agentische Postfach-Werkzeuge (Phase 9):**" — Verarbeitungszweck: auf Betreiber-
    Anweisung Mails/Entwürfe suchen/lesen/bearbeiten sowie in den Papierkorb verschieben (Move, kein Expunge,
    Bestätigungspflicht); an den KI-Anbieter gehen die dafür nötigen (PII-redigierten) Inhalte; Kein-Auto-Send.
    Halte beide Änderungen als Muster/Entwurf gekennzeichnet (rechtliche Prüfung bleibt Betreiber-Pflicht).
  </action>
  <acceptance_criteria>
    - `grep -ni "Papierkorb\|Bestätigung\|umformul\|kein.*sende" webui/src/templates/_datenschutz.html` belegt die konkretisierte Ziffer 6.
    - `grep -n "Phase 9\|Papierkorb\|Bestätigung" .planning/phases/02-deployment-beim-kunden/AVV-CHECKLIST.md` belegt den neuen §6.2-Punkt.
    - Datenschutz-Endpoint-Test bleibt grün: `python -m pytest tests/test_endpoints_datenschutz.py -q` (das Fragment rendert weiterhin fehlerfrei).
    - Kein-Auto-Send bleibt in Ziffer 6 ausdrücklich erwähnt (Wortlaut „Kein-Auto-Send"/„versendet keine").
  </acceptance_criteria>
  <verify>
    <automated>cd webui && python -m pytest tests/test_endpoints_datenschutz.py -q</automated>
  </verify>
  <done>Datenschutzerklärung Ziffer 6 + AVV §6.2 spiegeln die tatsächlichen v1.5-Fähigkeiten (D-81); Datenschutz-Rendering grün.</done>
</task>

<task type="auto">
  <name>Task 3: Phasen-Abschluss-Verifikation</name>
  <read_first>
    - webui/tests/ (gesamte Suite)
    - webui/tests/test_llm_sync.py, test_pii_sync.py, test_crypto_sync.py, test_provider_config_sync.py, test_model_defaults_sync.py (Drift-Guards)
  </read_first>
  <action>
    Führe die volle webui-Suite aus und stelle sicher, dass keine Regression vorliegt (Baseline vor Phase 9:
    296 passed / 3 skipped, plus die in 09-01..09-05 hinzugekommenen Tests). Verifiziere separat, dass die fünf
    Drift-Guard-Sync-Suiten grün sind (die Zwillinge llm/pii/crypto/provider_config wurden nur aufgerufen, nie
    geändert — D-73). Prüfe per `git diff --name-only`, dass keine Änderung an
    webui/src/{llm.py,pii.py,crypto.py,provider_config.py} oder an agent/ erfolgt ist. Halte im SUMMARY fest,
    welche CTOOL-Requirements durch welche Plans abgedeckt sind und dass der Anthropic-Tool-Use-Pfad end-to-end
    (mit gemocktem Client) bewiesen ist, OpenAI/Google über den Fallback sauber laufen.
  </action>
  <acceptance_criteria>
    - `cd webui && python -m pytest -q` → 0 failed; passed-Zahl ≥ 296 + neue Tests; skipped == 3.
    - `python -m pytest tests/test_llm_sync.py tests/test_pii_sync.py tests/test_crypto_sync.py tests/test_provider_config_sync.py tests/test_model_defaults_sync.py -q` → alle grün.
    - `git diff --name-only` enthält NICHT webui/src/llm.py, webui/src/pii.py, webui/src/crypto.py, webui/src/provider_config.py oder Pfade unter agent/.
  </acceptance_criteria>
  <verify>
    <automated>cd webui && python -m pytest -q</automated>
  </verify>
  <done>Volle Suite grün, Drift-Guards intakt, keine Änderung an geschützten Zwillingen/agent; Phase 9 code-komplett.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Werkzeug-Registry → externe Wirkung | ein hinzugefügtes Sende-Tool würde Kein-Auto-Send strukturell brechen |
| Rechtstexte → tatsächliches Verhalten | Datenschutz/AVV müssen die realen Fähigkeiten korrekt beschreiben (Compliance) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-20 | Elevation | künftiges Hinzufügen eines Sende-/SMTP-Werkzeugs | mitigate | dauerhafter struktureller Wächter-Test (TOOL_HANDLERS-Whitelist + SMTP-API-Scan) — CI-Rückfall bei Regression |
| T-09-21 | Repudiation/Compliance | Rechtstexte beschreiben Fähigkeiten falsch/zu vage | mitigate | Ziffer 6 + AVV §6.2 auf konkrete Fähigkeiten (Papierkorb-Move + Bestätigung, kein Senden) angeglichen (D-81) |
| T-09-22 | Tampering | Drift-Guard-Zwillinge unbemerkt verändert | mitigate | Sync-Suiten grün + `git diff`-Check auf geschützte Pfade |
| T-09-SC | Tampering | keine neuen Package-Installs | accept | keine neuen Dependencies |
</threat_model>

<verification>
- `cd webui && python -m pytest -q` → 0 failed, skipped == 3.
- Drift-Guard-Sync-Suiten grün; `git diff --name-only` ohne geschützte Zwillinge/agent.
- `grep`-Belege für angeglichene Datenschutz-Ziffer-6 + AVV-§6.2.
</verification>

<success_criteria>
- CTOOL-05 erfüllt: struktureller Kein-Auto-Send-Nachweis (Test) + Datenschutz/AVV auf tatsächliche Fähigkeiten angeglichen (D-81).
- Gesamte Phase 9 code-komplett und verifiziert; keine Regression, Drift-Guards intakt.
</success_criteria>

<output>
Create `.planning/phases/09-agentischer-chat-mit-postfach-werkzeugen-v1-5/09-05-SUMMARY.md` when done
</output>
