---
phase: 09-agentischer-chat-mit-postfach-werkzeugen-v1-5
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - webui/src/chat_tools.py
  - webui/tests/test_chat_tools.py
autonomous: true
requirements: [CTOOL-03]
must_haves:
  truths:
    - "Der Chat kann einen bestehenden Entwurf umformulieren: die neue Fassung landet im Entwürfe-Ordner."
    - "Die Threading-Header (In-Reply-To/References) des Originals bleiben in der neuen Fassung erhalten."
    - "Der alte Entwurf wird in den Papierkorb verschoben (kein Expunge)."
    - "Es gibt keinerlei Sende-/SMTP-Pfad in entwurf_bearbeiten."
  artifacts:
    - path: "webui/src/chat_tools.py"
      provides: "Papierkorb-Ordner-Erkennung (\\Trash + Fallback), IMAP-Move-Helfer, entwurf_bearbeiten-Tool"
      contains: "def entwurf_bearbeiten"
  key_links:
    - from: "webui/src/chat_tools.py::entwurf_bearbeiten"
      to: "RFC-5322-Rebuild mit In-Reply-To/References"
      via: "draft.py-Threading-Muster (EmailMessage + Header aus Original)"
      pattern: "In-Reply-To|References"
    - from: "webui/src/chat_tools.py::entwurf_bearbeiten"
      to: "IMAP APPEND(Drafts) + MOVE(alt→Trash)"
      via: "append + _move_to_trash, kein expunge"
      pattern: "append|_move_to_trash"
---

<objective>
`entwurf_bearbeiten(uid, neuer_text[, neuer_betreff])` (CTOOL-03, D-75): legt die neue Fassung eines
bestehenden Entwurfs per IMAP-APPEND im Entwürfe-Ordner ab und verschiebt den alten Entwurf in den
Papierkorb — **Threading-Header (In-Reply-To/References) bleiben erhalten**, **kein Senden**. Führt
zugleich die für 09-04 gemeinsam benötigte Papierkorb-Erkennung + den Move-Helfer ein.

Purpose: Der Betreiber kann Formulierungen im Chat anpassen lassen, ohne das Mailprogramm zu wechseln;
die reversible Papierkorb-Mechanik (kein Expunge) wird hier zentral etabliert und in 09-04 wiederverwendet.

Output: erweitertes `webui/src/chat_tools.py` (Trash-Detection, Move-Helfer, entwurf_bearbeiten) + Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-agentischer-chat-mit-postfach-werkzeugen-v1-5/09-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Aus 09-01/09-02. -->
webui/src/chat_tools.py:
  contextmanager open_agent_mailbox(agent_id)
  def _detect_drafts_folder(mailbox, fallback) -> str
  def entwurf_lesen(agent_id, uid) -> dict   # liefert Body + In-Reply-To/References (09-02)
  TOOL_SCHEMAS / TOOL_HANDLERS / wrap_tool_result

agent/src/draft.py (RFC-5322 + Threading — NUR Referenz, agent/ nicht ändern):
  build_reply_draft(original, draft_text, own_email, own_display_name) -> bytes
  # Muster: EmailMessage(); msg["In-Reply-To"]=original_msg_id; msg["References"]=refs+" "+id;
  # imap-tools headers sind lowercase-Tuple; MailMessageFlags.DRAFT beim APPEND

imap-tools: mailbox.append(bytes, folder=..., flag_set=[MailMessageFlags.DRAFT]);
            mailbox.move(uid_list, destination_folder)   # MOVE = kein expunge
webui/src/provider_config.py::resolve_imap_config  # kein trash-Key → eigene Kandidatenliste nötig
</interfaces>

@webui/src/chat_tools.py
@agent/src/draft.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Papierkorb-Ordner-Erkennung + IMAP-Move-Helfer (kein Expunge)</name>
  <read_first>
    - webui/src/chat_tools.py (open_agent_mailbox, _detect_drafts_folder)
    - webui/src/style_extract.py (`_detect_sent_folder` als SPECIAL-USE-Vorlage)
    - agent/src/imap_client.py (SPECIAL-USE-Flag-Iteration, nur Referenz)
    - webui/tests/test_chat_tools.py (Mock-Muster)
  </read_first>
  <behavior>
    - `_detect_trash_folder(mailbox, fallback=None)` erkennt \Trash via SPECIAL-USE; sonst probiert eine
      Kandidatenliste ("Trash","Papierkorb","Deleted Items","[Gmail]/Trash","INBOX.Trash") gegen die
      Ordnerliste; findet er keinen, `TrashFolderNotFound` (typisiert) — es wird NIE geraten/erstellt.
    - `_move_to_trash(mailbox, uid, source_folder)` nutzt IMAP MOVE in den erkannten Papierkorb; niemals
      `expunge`/`delete`. Kein Treffer → typisierter Fehler (kein stiller Datenverlust).
  </behavior>
  <action>
    Ergänze `_detect_trash_folder(mailbox, fallback=None)` analog `_detect_sent_folder` (Flag-Match "Trash");
    bei fehlender SPECIAL-USE-Ankündigung eine feste Kandidatenliste gegen `mailbox.folder.list()`-Namen matchen;
    kein Treffer → `TrashFolderNotFound` (neue typisierte Exception in chat_tools.py). Implementiere
    `_move_to_trash(mailbox, uid, source_folder)`: Ordner setzen, `mailbox.move([uid], trash_folder)` — MOVE,
    kein `expunge`, kein `delete`. Kommentiere D-76 (reversibel, kein Expunge) am Move-Helfer.
  </action>
  <acceptance_criteria>
    - `grep -n "def _detect_trash_folder\|def _move_to_trash\|class TrashFolderNotFound" webui/src/chat_tools.py` trifft alle drei.
    - `grep -c "expunge\|\.delete(" webui/src/chat_tools.py` (Kommentare via `grep -v '^#'` gefiltert) == 0 — kein Expunge/Delete im Modul.
    - Test: `_detect_trash_folder` liefert bei Fake-Folder mit `\Trash`-Flag dessen Namen; ohne Flag aber mit Ordner "Papierkorb" in der Liste dessen Namen; ohne beides → `TrashFolderNotFound`.
    - Test: `_move_to_trash` ruft `mailbox.move` mit der uid + Trash-Ziel auf (MagicMock-assert), niemals expunge.
  </acceptance_criteria>
  <verify>
    <automated>cd webui && python -m pytest tests/test_chat_tools.py -x -q</automated>
  </verify>
  <done>Papierkorb-Erkennung (\Trash + Fallback) + reversibler Move-Helfer ohne Expunge; Tests grün.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: entwurf_bearbeiten — neue Fassung APPEND + Original in den Papierkorb, Threading erhalten</name>
  <read_first>
    - webui/src/chat_tools.py (entwurf_lesen, _detect_drafts_folder, _detect_trash_folder, _move_to_trash, TOOL_SCHEMAS/HANDLERS)
    - agent/src/draft.py (`build_reply_draft` — EmailMessage-Rebuild, In-Reply-To/References-Übernahme, MailMessageFlags.DRAFT)
    - webui/tests/test_chat_tools.py
  </read_first>
  <behavior>
    - `entwurf_bearbeiten(agent_id, uid, neuer_text, neuer_betreff=None)`: liest den bestehenden Entwurf,
      baut eine neue RFC-5322-Message mit `neuer_text` (und optional `neuer_betreff`), übernimmt From/To sowie
      **In-Reply-To/References unverändert aus dem Original** (D-75), APPEND in den Drafts-Ordner mit
      `\Draft`-Flag, danach `_move_to_trash` des alten Entwurfs. Kein Senden.
    - Original nicht gefunden / kein Drafts- oder Trash-Ordner → `{"fehler":..}`, kein Teil-Zustand ohne Meldung.
    - Registriert in TOOL_SCHEMAS + TOOL_HANDLERS.
  </behavior>
  <action>
    Implementiere `entwurf_bearbeiten(agent_id, uid, neuer_text, neuer_betreff=None)`. Hol den Original-Entwurf
    (Header + Adressen) über den Drafts-Ordner. Baue die neue Message nach dem `agent/src/draft.py`-Muster:
    `EmailMessage()`, From/To aus dem Original, Subject = `neuer_betreff or original.subject`, neues Message-ID,
    **In-Reply-To/References aus dem Original übernehmen** (imap-tools lowercase-Tuple-Header wie in draft.py),
    Body = `neuer_text`, `set_content(..., charset="utf-8")`. `mailbox.append(bytes, folder=drafts,
    flag_set=[MailMessageFlags.DRAFT])`, dann `_move_to_trash(mailbox, uid, drafts_folder)` für den alten
    Entwurf. Registriere in `TOOL_SCHEMAS` (deutsche description; input_schema uid + neuer_text + optional
    neuer_betreff) + `TOOL_HANDLERS`. Kein SMTP/Send. Protokolliere die Bearbeitung (structured log, ohne Body-
    Inhalt/Secrets).
  </action>
  <acceptance_criteria>
    - `grep -n "def entwurf_bearbeiten" webui/src/chat_tools.py` trifft; `"entwurf_bearbeiten" in TOOL_HANDLERS` (Test).
    - Test (gemocktes MailBox): entwurf_bearbeiten APPENDet eine neue Message, deren Bytes `neuer_text` enthalten UND die In-Reply-To/References des Original-Entwurfs (Assertion auf den an `append` übergebenen Bytes-Blob).
    - Test: der alte Entwurf wird per `_move_to_trash`/`mailbox.move` in den Papierkorb verschoben (kein expunge).
    - Test: kein Trash-Ordner (`TrashFolderNotFound`) → `{"fehler":..}`, keine unbehandelte Exception; der APPEND-Pfad hat keinen SMTP-/Send-Aufruf (Quelltext-Scan auf smtplib/send == 0).
    - Test: Original-uid nicht gefunden → `{"fehler":..}`.
  </acceptance_criteria>
  <verify>
    <automated>cd webui && python -m pytest tests/test_chat_tools.py -x -q</automated>
  </verify>
  <done>entwurf_bearbeiten legt die neue Fassung im Drafts-Ordner ab (Threading erhalten) und verschiebt das Original in den Papierkorb; kein Senden; Tests grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Chat-Anweisung → IMAP-Mutation (APPEND/MOVE) | erste verändernde Aktion am Postfach |
| WebUI-Container → IMAP (Drafts/Trash) | Schreib-/Move-Zugriff mit per-Agent-Creds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11 | Tampering | Threading-Header gehen bei Rebuild verloren → Draft als eigener Thread | mitigate | In-Reply-To/References explizit aus Original übernommen (draft.py-Muster); Test auf APPEND-Bytes |
| T-09-12 | Repudiation | Bearbeitung nicht nachvollziehbar | mitigate | structured log der Bearbeitung (ohne Body/Secrets) |
| T-09-13 | Denial of Service / Data Loss | alter Entwurf gelöscht bevor neuer sicher liegt, oder Expunge | mitigate | Reihenfolge APPEND→MOVE; MOVE statt Expunge (reversibel, D-76); TrashFolderNotFound bricht sauber ab statt zu raten |
| T-09-14 | Elevation | LLM formuliert eigenmächtig einen Sende-Versuch | mitigate | strukturell kein Sende-Werkzeug/SMTP im Modul (D-77); Quelltext-Scan-Test |
| T-09-SC | Tampering | keine neuen Package-Installs | accept | keine neuen Dependencies |
</threat_model>

<verification>
- `cd webui && python -m pytest tests/test_chat_tools.py -q` grün.
- `cd webui && python -m pytest -q` → keine Regression.
- `git diff --name-only` zeigt keine Änderung an agent/ oder an den Drift-Guard-Zwillingen.
</verification>

<success_criteria>
- CTOOL-03 erfüllt: Entwurf-Bearbeitung mit erhaltenem Threading, neue Fassung im Drafts-Ordner, Original im Papierkorb, kein Senden.
- Papierkorb-Mechanik (\Trash + Fallback, MOVE ohne Expunge) zentral etabliert und für 09-04 bereit.
</success_criteria>

<output>
Create `.planning/phases/09-agentischer-chat-mit-postfach-werkzeugen-v1-5/09-03-SUMMARY.md` when done
</output>
