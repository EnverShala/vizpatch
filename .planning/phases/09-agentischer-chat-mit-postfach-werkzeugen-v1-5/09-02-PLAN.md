---
phase: 09-agentischer-chat-mit-postfach-werkzeugen-v1-5
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - webui/src/chat_tools.py
  - webui/tests/test_chat_tools.py
autonomous: true
requirements: [CTOOL-02]
must_haves:
  truths:
    - "Der Chat kann eine einzelne Mail per uid vollständig lesen (PII-redigiert)."
    - "Der Chat kann die vorhandenen Entwürfe auflisten und einen einzelnen Entwurf per uid lesen."
    - "Der Entwürfe-Ordner wird per SPECIAL-USE \\Drafts erkannt, mit provider_config-Fallback."
    - "Alle read-only-Tool-Ergebnisse sind PII-redigiert und als untrusted DATEN markiert."
  artifacts:
    - path: "webui/src/chat_tools.py"
      provides: "mail_lesen, entwuerfe_auflisten, entwurf_lesen + Drafts-Ordner-Erkennung, registriert in TOOL_SCHEMAS/TOOL_HANDLERS"
      contains: "def entwurf_lesen"
  key_links:
    - from: "webui/src/chat_tools.py::TOOL_HANDLERS"
      to: "mail_lesen/entwuerfe_auflisten/entwurf_lesen"
      via: "Registry-Erweiterung ohne Änderung von run_agentic_chat"
      pattern: "\"mail_lesen\"|\"entwuerfe_auflisten\"|\"entwurf_lesen\""
    - from: "webui/src/chat_tools.py"
      to: "SPECIAL-USE \\Drafts + provider_config"
      via: "detect_drafts_folder-Muster wie style_extract._detect_sent_folder"
      pattern: "Drafts"
---

<objective>
Vervollständigt den read-only-Werkzeugsatz (CTOOL-02): `mail_lesen(uid)`, `entwuerfe_auflisten()`,
`entwurf_lesen(uid)`. Alle drei nutzen den in 09-01 gebauten IMAP-Helfer und Registry-Kontrakt und
hängen sich ohne Änderung an `run_agentic_chat` ein.

Purpose: Der Betreiber kann eine konkrete Mail/einen Entwurf im Chat lesen lassen — Grundlage für
die Bearbeitung (09-03) und das gezielte Löschen (09-04), die per uid auf diese Lese-Tools aufbauen.

Output: erweitertes `webui/src/chat_tools.py` + Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-agentischer-chat-mit-postfach-werkzeugen-v1-5/09-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Aus 09-01, gegen das dieser Plan baut. -->
webui/src/chat_tools.py:
  contextmanager open_agent_mailbox(agent_id)   # loggt Fernet-decrypted MailBox mit timeout
  def mails_suchen(agent_id, query, folder="INBOX", limit=...) -> dict
  TOOL_SCHEMAS: list[dict]     # Anthropic-tools-Format — hier ANHÄNGEN
  TOOL_HANDLERS: dict[str, callable]  # name → handler — hier ANHÄNGEN
  def wrap_tool_result(name, payload) -> str   # Untrusted-DATEN-Anker (D-78)
  MAX_TOOL_RESULT_BODY_CHARS

webui/src/pii.py (DRIFT-GUARD — nur redact aufrufen)
agent/src/imap_client.py::detect_drafts_folder  # SPECIAL-USE \Drafts-Muster (nur lesen, nicht ändern)
webui/src/provider_config.py::resolve_imap_config -> {"drafts": ...}  # Fallback-Ordnername
</interfaces>

@webui/src/chat_tools.py
@webui/src/style_extract.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: mail_lesen + Drafts-Ordner-Erkennung</name>
  <read_first>
    - webui/src/chat_tools.py (open_agent_mailbox, mails_suchen, wrap_tool_result, TOOL_SCHEMAS/TOOL_HANDLERS)
    - webui/src/style_extract.py (`_detect_sent_folder` als SPECIAL-USE-Vorlage — hier für \Drafts adaptieren)
    - agent/src/imap_client.py (`detect_drafts_folder` — SPECIAL-USE \Drafts, nur Referenz)
    - webui/tests/test_chat_tools.py (Mock-Muster aus 09-01)
  </read_first>
  <behavior>
    - `_detect_drafts_folder(mailbox, fallback)` erkennt \Drafts via SPECIAL-USE, sonst `provider_config`-Fallback bzw. "Drafts".
    - `mail_lesen(agent_id, uid, folder="INBOX")` fetcht genau die Mail mit der uid, liefert from/to/subject/date + `pii.redact`-Body (truncatet); nicht gefunden → dict mit `fehler`.
    - Registrierung von `mail_lesen` in TOOL_SCHEMAS + TOOL_HANDLERS.
  </behavior>
  <action>
    Ergänze in `chat_tools.py` `_detect_drafts_folder(mailbox, fallback)` analog `style_extract._detect_sent_folder`
    (Flag-Match auf "Drafts" statt "Sent"). Implementiere `mail_lesen(agent_id, uid, folder="INBOX")`: über
    `open_agent_mailbox` den Ordner setzen, per uid-Kriterium fetchen (imap-tools `AND(uid=...)` bzw. `fetch`
    mit uid-Query — konsistent mit imap_client.py), Body via `pii.redact` redigieren und auf
    `MAX_TOOL_RESULT_BODY_CHARS` truncaten. Registriere `mail_lesen` in `TOOL_SCHEMAS` (deutsche description,
    input_schema mit uid + optionalem folder) und `TOOL_HANDLERS`. Fehler/Not-found → `{"fehler": ...}`.
  </action>
  <acceptance_criteria>
    - `grep -n "def mail_lesen\|def _detect_drafts_folder" webui/src/chat_tools.py` trifft beide.
    - Test (gemocktes MailBox): `mail_lesen` liefert den redigierten Body einer Ziel-uid; eine eingeschleuste Kreditkartennummer im Fake-Body erscheint NICHT im Ergebnis.
    - Test: unbekannte uid → `{"fehler": ...}`, keine Exception.
    - Test: `_detect_drafts_folder` liefert bei einem Fake-Folder mit `\Drafts`-Flag dessen Namen, sonst den Fallback.
    - `"mail_lesen" in TOOL_HANDLERS` und in einem TOOL_SCHEMAS-Eintrag (Test).
  </acceptance_criteria>
  <verify>
    <automated>cd webui && python -m pytest tests/test_chat_tools.py -x -q</automated>
  </verify>
  <done>mail_lesen liest eine einzelne Mail PII-redigiert; Drafts-Ordner-Erkennung vorhanden; registriert; Tests grün.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: entwuerfe_auflisten + entwurf_lesen</name>
  <read_first>
    - webui/src/chat_tools.py (mail_lesen, _detect_drafts_folder aus Task 1)
    - webui/tests/test_chat_tools.py (Mock-Muster)
  </read_first>
  <behavior>
    - `entwuerfe_auflisten(agent_id)` öffnet den (erkannten) Drafts-Ordner, liefert eine kompakte Liste (uid, subject, date, to) — Bodys NICHT im List-Result (Datenminimierung), Ordner-fehlt/leer → leere Liste ohne Crash.
    - `entwurf_lesen(agent_id, uid)` liest genau einen Entwurf aus dem Drafts-Ordner (from/to/subject/date + `pii.redact`-Body), inklusive der Threading-Header (In-Reply-To/References) für spätere Bearbeitung.
    - Beide in TOOL_SCHEMAS + TOOL_HANDLERS registriert.
  </behavior>
  <action>
    Implementiere `entwuerfe_auflisten(agent_id)`: `open_agent_mailbox` → `_detect_drafts_folder` → Ordner
    setzen → `fetch(mark_seen=False, reverse=True, limit=...)`, pro Entwurf nur Metadaten (uid, subject, date,
    to) sammeln. Implementiere `entwurf_lesen(agent_id, uid)`: Entwurf per uid aus dem Drafts-Ordner holen,
    `pii.redact`-Body + die Threading-Header (In-Reply-To/References aus `msg.headers`, imap-tools-lowercase-
    Tuple-Muster wie agent/src/draft.py) mitliefern (die braucht 09-03 für `entwurf_bearbeiten`). Registriere
    beide in `TOOL_SCHEMAS` + `TOOL_HANDLERS`. Fehlender Drafts-Ordner → `entwuerfe_auflisten` = leere Liste,
    `entwurf_lesen` = `{"fehler": ...}`.
  </action>
  <acceptance_criteria>
    - `grep -n "def entwuerfe_auflisten\|def entwurf_lesen" webui/src/chat_tools.py` trifft beide.
    - Test: `entwuerfe_auflisten` liefert Metadaten-Liste ohne Body-Feld; Test: fehlender Drafts-Ordner → leere Liste, kein Crash.
    - Test: `entwurf_lesen` liefert redigierten Body UND die Threading-Header (In-Reply-To/References) der Ziel-uid.
    - `set(TOOL_HANDLERS) >= {"mails_suchen","mail_lesen","entwuerfe_auflisten","entwurf_lesen"}` (Test).
    - Jedes read-only-Tool-Result läuft durch `wrap_tool_result` bzw. wird PII-redigiert (Test prüft Redaction für entwurf_lesen-Body).
  </acceptance_criteria>
  <verify>
    <automated>cd webui && python -m pytest tests/test_chat_tools.py -x -q</automated>
  </verify>
  <done>Alle vier read-only-Tools (CTOOL-02) existieren, registriert, PII-redigiert, Threading-Header für 09-03 verfügbar; Tests grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mail-/Entwurf-Inhalt (IMAP) → LLM | untrusted Text als Tool-Ergebnis |
| WebUI-Container → IMAP (Drafts/INBOX) | Lesezugriff mit per-Agent-Creds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Info Disclosure | PII in gelesener Mail/Entwurf geht ans LLM | mitigate | `pii.redact` in mail_lesen/entwurf_lesen VOR Rückgabe; Test mit IBAN/Kreditkarte |
| T-09-08 | Info Disclosure | entwuerfe_auflisten leakt komplette Bodys unnötig | mitigate | Liste liefert nur Metadaten (uid/subject/date/to), kein Body (Datenminimierung) |
| T-09-09 | Tampering | Injection über gelesenen Mail-Text | mitigate | Ergebnisse via `wrap_tool_result` als untrusted DATEN markiert (D-78) |
| T-09-10 | Denial of Service | fehlender/leerer Drafts-Ordner crasht Handler | mitigate | graceful: leere Liste / `{"fehler":..}`, IMAP-Fehler gefangen |
| T-09-SC | Tampering | keine neuen Package-Installs | accept | keine neuen Dependencies |
</threat_model>

<verification>
- `cd webui && python -m pytest tests/test_chat_tools.py -q` grün.
- `cd webui && python -m pytest -q` → keine Regression (≥ 296 passed baseline + neue Tests).
- Drift-Guard-Zwillinge (llm/pii/crypto/provider_config) unverändert.
</verification>

<success_criteria>
- Alle vier read-only-Werkzeuge (CTOOL-02) vorhanden, registriert und ohne Änderung von `run_agentic_chat` aufrufbar.
- PII-Redaction vor jeder LLM-Übergabe; Datenminimierung in der Liste; Threading-Header für 09-03 bereitgestellt.
</success_criteria>

<output>
Create `.planning/phases/09-agentischer-chat-mit-postfach-werkzeugen-v1-5/09-02-SUMMARY.md` when done
</output>
