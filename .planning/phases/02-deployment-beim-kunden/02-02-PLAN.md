---
phase: 02-deployment-beim-kunden
plan: 02
plan_id: 02.02-auto-create-drafts-folder
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/src/imap_client.py
  - agent/tests/test_imap_client_auto_create.py
autonomous: true
requirements:
  - DEP-06
must_haves:
  truths:
    - "Bei fehlender Ordner-Existenz erzeugt append_to_drafts() den Drafts-Ordner via CREATE und wiederholt den APPEND — der Kunde muss den Ordner nicht vorher im Mail-Programm anlegen."
    - "Bei APPEND-Fehler mit anderer Ursache (Auth, Quota, Netzwerk) propagiert die Exception nach oben und wird NICHT als processed markiert."
    - "Ein Log-Event `drafts_folder_created` mit extra={folder: ...} wird bei erfolgreichem CREATE geschrieben."
    - "Alle Phase-1-Tests (26) laufen weiterhin grün."
    - "Der neue Test test_imap_client_auto_create.py ist grün (≥ 4 Testfälle)."
  artifacts:
    - path: "agent/src/imap_client.py"
      provides: "append_to_drafts() mit Auto-CREATE-Fallback bei fehlendem Ordner (D-25)"
      contains: "MailboxAppendError"
    - path: "agent/tests/test_imap_client_auto_create.py"
      provides: "Unit-Tests für Auto-CREATE-Fallback + Fehlerprotokoll"
      min_lines: 60
  key_links:
    - from: "agent/src/imap_client.py"
      to: "imap_tools.errors.MailboxAppendError"
      via: "except MailboxAppendError als Trigger für CREATE + retry"
      pattern: "except MailboxAppendError"
    - from: "agent/src/imap_client.py::append_to_drafts"
      to: "self._mailbox.folder.create()"
      via: "Ordner-Anlage auf spezifische Fehlerstrings hin"
      pattern: "folder\\.create\\("
---

<objective>
D-25 umsetzen: Der Agent legt den in `IMAP_DRAFTS_FOLDER` konfigurierten Ordner automatisch via `CREATE` an, wenn er noch nicht existiert. Der Kunde muss den Ordner NICHT vorher im Mail-Programm anlegen — er tippt einfach `IMAP_DRAFTS_FOLDER=KI-Entwürfe` in die `.env` und der Agent erzeugt den Ordner beim ersten Draft.

Purpose: Setup-Vereinfachung beim Kundentermin (siehe RUNBOOK-Vereinfachung in CONTEXT.md D-25). Selbstheilend bei versehentlicher Löschung des Ordners. Kein zusätzlicher Setup-Schritt "Ordner anlegen" im Vor-Ort-Runbook mehr nötig.

Output: Angepasste `append_to_drafts()`-Methode in `agent/src/imap_client.py` mit try/except gegen `MailboxAppendError`, Fehlerstring-Match auf 5 gängige "does not exist"-Varianten, `folder.create()` + retry-APPEND, Logging-Events `drafts_folder_missing_creating` (WARN) und `drafts_folder_created` (INFO). Neue Test-Datei `agent/tests/test_imap_client_auto_create.py` mit ≥ 4 Tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deployment-beim-kunden/02-CONTEXT.md
@.planning/phases/02-deployment-beim-kunden/02-RESEARCH.md
@.planning/phases/02-deployment-beim-kunden/02-PATTERNS.md
@agent/src/imap_client.py
@agent/tests/test_draft.py
@agent/tests/conftest.py

<interfaces>
Aktuelle append_to_drafts() (agent/src/imap_client.py Zeilen 46-50):

```python
def append_to_drafts(self, raw_msg_bytes: bytes) -> None:
    """APPEND a raw RFC5322 message to the Drafts folder with \\Draft flag."""
    assert self._mailbox is not None, "Use inside 'with' block"
    self._mailbox.append(raw_msg_bytes, folder=self.config.imap_drafts_folder, flag_set=[MailMessageFlags.DRAFT])
    self.logger.info("draft_appended", extra={"folder": self.config.imap_drafts_folder})
```

imap-tools Fehlertypen (verifiziert in RESEARCH.md § D-25):

```python
from imap_tools.errors import MailboxAppendError, MailboxFolderCreateError
# mailbox.folder.create(folder_name: str) -> tuple  — wirft MailboxFolderCreateError bei Berechtigungsfehler
```

Fehler-String-Patterns (server-abhängig, aus RESEARCH.md verifiziert):
- `[trycreate]` (RFC 3501, universell)
- `does not exist` (Dovecot: IONOS, mailbox.org, Hetzner)
- `no such mailbox` (generisch)
- `non-existent` (diverse)
- `trying to append to non-existent mailbox` (Dovecot)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: append_to_drafts() um Auto-CREATE-Fallback + neuen Test test_imap_client_auto_create.py erweitern</name>
  <files>agent/src/imap_client.py, agent/tests/test_imap_client_auto_create.py</files>
  <read_first>
    - agent/src/imap_client.py (aktuelle Struktur; Zeilen 1-51 komplett)
    - agent/tests/test_draft.py (Analog-Muster: MagicMock für imap_tools-Objekte, `_make_original`-artiges Fabrik-Pattern, direkte Assertions ohne Fixtures wenn möglich)
    - agent/tests/conftest.py (mock_config-Fixture — noch OHNE imap_sent_folder; Task 2 von Plan 02.01 hat das Feld hinzugefügt, aber dieser Plan hat kein depends_on auf 02.01 — Fallback: das neue Feld darf nicht verwendet werden. `Config(imap_drafts_folder="Drafts", ...)` reicht.)
    - .planning/phases/02-deployment-beim-kunden/02-PATTERNS.md (§ `agent/src/imap_client.py` — vollständiger Zielcode für D-25 Zeilen 155-189, MagicMock-Test-Pattern Zeilen 800-856)
    - .planning/phases/02-deployment-beim-kunden/02-RESEARCH.md (§ D-25 — verifizierte Fehlerstrings + `MailboxAppendError` Import + Implementierungsmuster)
  </read_first>
  <behavior>
    - Test 1 (Auto-CREATE bei "does not exist"): Erster `append()`-Aufruf wirft `MailboxAppendError("does not exist", b"")`, zweiter Aufruf ist OK → `folder.create()` wurde aufgerufen, `append` wurde 2× aufgerufen, Log-Event `drafts_folder_created` mit `extra={"folder": "Drafts"}` erschien.
    - Test 2 (Auto-CREATE bei "[TRYCREATE]"): Erster `append()` wirft `MailboxAppendError("[TRYCREATE] unknown mailbox", b"")` (Gmail-Muster) → CREATE + retry passiert, keine Exception nach außen.
    - Test 3 (Auto-CREATE bei "no such mailbox"): Erster `append()` wirft `MailboxAppendError("NO No such mailbox", b"")` → CREATE + retry.
    - Test 4 (Nicht-fehlender-Ordner-Fehler propagiert): `append()` wirft `MailboxAppendError("quota exceeded", b"")` → `pytest.raises(MailboxAppendError)`, `folder.create()` wurde NIE aufgerufen.
    - Test 5 (Case-Insensitivity): `append()` wirft `MailboxAppendError("Does Not Exist", b"")` (Gross-Klein-gemischt) → CREATE + retry passiert (der Fehlerstring-Match ist case-insensitive via `.lower()`).
    - Test 6 (Non-missing errors mit "non-existent"-Substring): `append()` wirft `MailboxAppendError("mailbox does not exist", b"")` → CREATE passiert (Substring-Match findet "does not exist").
  </behavior>
  <action>
    Modul `agent/src/imap_client.py` erweitern:

    1. Imports (Zeile 8) ergänzen: `from imap_tools.errors import MailboxAppendError`. Bestehende `from imap_tools import ...`-Zeile bleibt unverändert (aber Task 3 in Plan 02.03 fügt ggf. `OR` hinzu — hier NUR `MailboxAppendError` hinzufügen um Merge-Konflikte zu vermeiden).

    2. `append_to_drafts()` (Zeilen 46-50) komplett ersetzen durch die Version aus 02-PATTERNS.md § `agent/src/imap_client.py` (Zeilen 155-189):
       ```
       def append_to_drafts(self, raw_msg_bytes: bytes) -> None:
           """APPEND mit Auto-CREATE-Fallback bei fehlendem Drafts-Ordner (D-25)."""
           assert self._mailbox is not None, "Use inside 'with' block"
           try:
               self._mailbox.append(
                   raw_msg_bytes,
                   folder=self.config.imap_drafts_folder,
                   flag_set=[MailMessageFlags.DRAFT],
               )
               self.logger.info("draft_appended", extra={"folder": self.config.imap_drafts_folder})
           except MailboxAppendError as err:
               err_lower = str(err).lower()
               is_missing = any(p in err_lower for p in (
                   "[trycreate]", "does not exist", "no such mailbox",
                   "non-existent", "trying to append to non-existent mailbox",
               ))
               if not is_missing:
                   raise  # anderer Fehler (Auth, Quota) — nicht self-heilen
               self.logger.warning("drafts_folder_missing_creating",
                                   extra={"folder": self.config.imap_drafts_folder})
               self._mailbox.folder.create(self.config.imap_drafts_folder)
               self.logger.info("drafts_folder_created",
                                extra={"folder": self.config.imap_drafts_folder})
               # Retry — MailboxFolderCreateError propagiert unbehandelt (Auto-Recovery in main.py)
               self._mailbox.append(
                   raw_msg_bytes,
                   folder=self.config.imap_drafts_folder,
                   flag_set=[MailMessageFlags.DRAFT],
               )
               self.logger.info("draft_appended", extra={"folder": self.config.imap_drafts_folder})
       ```
       Wichtig: KEINE eigene Exception-Behandlung für den 2. append-Aufruf — wenn CREATE erfolgreich war, sollte der 2. APPEND auch klappen; sonst propagiert die Exception, `_process_one()` in main.py fängt sie im `except Exception`-Block, Mail wird NICHT als processed markiert → nächster Poll-Zyklus versucht erneut (D-25-Auto-Recovery-Rationale).

    3. Neue Datei `agent/tests/test_imap_client_auto_create.py` mit folgender Struktur (Analog: `agent/tests/test_draft.py`, MagicMock-Pattern aus 02-PATTERNS.md Zeilen 800-856):
       - Imports: `from __future__ import annotations`, `from unittest.mock import MagicMock`, `import pytest`, `from imap_tools.errors import MailboxAppendError`, `from src.imap_client import ImapClient`.
       - Helper-Funktion `_make_client_with_mock_mailbox(config)`: `ImapClient.__new__(ImapClient)` (umgeht `__init__`), setzt `client.config = config`, `client.logger = MagicMock()`, `client._mailbox = MagicMock()`. Rückgabe: ImapClient-Instanz ohne echten IMAP-Login.
       - 6 Tests aus `<behavior>` — jede mit `side_effect`-Liste für `client._mailbox.append` (erste Exception, zweiter Call OK), Assertions auf `client._mailbox.folder.create.called`, `client._mailbox.append.call_count == 2`, `client.logger.info.assert_any_call("drafts_folder_created", extra={"folder": ...})`.
       - Nutzt `mock_config`-Fixture aus `conftest.py` — aktuell hat mock_config `imap_drafts_folder="Drafts"`. KEINE Änderung an conftest.py in diesem Plan (Plan 02.01 fügt `imap_sent_folder` hinzu — dieser Plan berührt das Feld nicht).

    4. Wichtig: `MailboxAppendError(msg, b"")` — das zweite Arg ist der `data`-Bytestring (siehe imap-tools-Source); im Test darf leer sein.
  </action>
  <verify>
    <automated>cd agent && grep -c "MailboxAppendError" src/imap_client.py && grep -c "folder.create" src/imap_client.py && grep -c "drafts_folder_created" src/imap_client.py && pytest tests/test_imap_client_auto_create.py -v -x && pytest tests/ -x</automated>
  </verify>
  <acceptance_criteria>
    - `agent/src/imap_client.py` importiert `MailboxAppendError` aus `imap_tools.errors`. Prüfbar via `grep "from imap_tools.errors import MailboxAppendError" agent/src/imap_client.py`.
    - `append_to_drafts()` enthält try/except-Block gegen `MailboxAppendError`. Prüfbar via `grep -A3 "except MailboxAppendError" agent/src/imap_client.py`.
    - `append_to_drafts()` prüft mindestens 5 verschiedene "missing folder"-Fehlerstrings. Prüfbar via `python -c "import re; s=open('agent/src/imap_client.py').read(); assert all(p in s.lower() for p in ('[trycreate]', 'does not exist', 'no such mailbox', 'non-existent'))"`.
    - `agent/tests/test_imap_client_auto_create.py` existiert und enthält ≥ 6 pytest-Testfunktionen. Prüfbar via `grep -c "^def test_" agent/tests/test_imap_client_auto_create.py` liefert ≥ 6.
    - `pytest tests/test_imap_client_auto_create.py -v` läuft, alle Tests grün.
    - Alle Phase-1-Tests bleiben grün: `pytest tests/ -x` insgesamt ≥ 32 Tests, alle grün (26 Phase-1 + 6 neu).
    - Kein Import-Bruch: `python -c "from src.imap_client import ImapClient"` läuft ohne Fehler.
  </acceptance_criteria>
  <done>
    append_to_drafts() hat Auto-CREATE-Fallback mit 5 Fehlerstring-Mustern, propagiert nicht-fehlender-Ordner-Fehler unverändert, loggt drafts_folder_missing_creating (WARN) + drafts_folder_created (INFO); test_imap_client_auto_create.py mit 6 Tests grün; alle Phase-1-Tests weiterhin grün.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IMAP-Server → Agent | Server-Antworten (Fehlerstrings) sind semi-vertrauenswürdig; können Attacker-manipuliert sein bei MITM |
| Agent → IMAP-Server | CREATE-Kommando wird nur bei erkanntem "missing folder"-Fehler geschickt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.02-01 | Tampering | IMAP-Server-Fehlerstring | mitigate | Substring-Match nur gegen 5 spezifische "missing folder"-Patterns; alle anderen Fehler propagieren (Fail-Safe). Attacker-manipulierter Fehlertext kann höchstens CREATE-Versuch auslösen — der scheitert bei Auth/Quota-Problemen und wird geloggt. |
| T-02.02-02 | Denial of Service | Endless-Loop bei CREATE-Failure | mitigate | Kein try/except um den 2. append-Aufruf — wenn CREATE fehlschlägt, propagiert Exception; `main.py` fängt sie, Mail bleibt unprocessed, nächster Poll-Zyklus retried. Kein Endlosloop. |
| T-02.02-03 | Information Disclosure | Log-Event `drafts_folder_created` mit Foldername | accept | Foldername ist Konfig-Wert (nicht Credential), Log ist json-file-lokaler Container-Log — akzeptabel. |
| T-02.02-04 | Elevation of Privilege | CREATE erzeugt Ordner ohne User-Autorisierung | accept | Der IMAP-User hat per definitionem CREATE-Recht (sonst würde CREATE server-seitig fehlschlagen). Kein Rechte-Escalation-Vektor. |
</threat_model>

<verification>
Automatisierte Verifikation für den gesamten Plan:

```bash
cd agent && pytest tests/ -v -x
```

Erwartetes Ergebnis:
- Alle 26 Phase-1-Tests weiterhin grün
- 6 neue Tests in test_imap_client_auto_create.py grün
- Gesamt ≥ 32 Tests grün

Import-Sanity-Check:
```bash
cd agent && python -c "from src.imap_client import ImapClient; print('OK')"
```
Erwartetes Ergebnis: "OK", kein ImportError.

Manuelle Verifikation entfällt — Plan ist reines Code + Test. Praxistest folgt in Plan 02.05 (Pre-Deployment-Test bei Vizionists gegen `shala@vizionists.com`, dort mit `IMAP_DRAFTS_FOLDER=KEA-Test-Entwürfe` als noch-nicht-existierendem Ordner).
</verification>

<success_criteria>
1. `append_to_drafts()` fängt `MailboxAppendError` und prüft Fehlerstring auf 5 "missing folder"-Muster.
2. Bei erkanntem Muster: `folder.create()` + retry-`append` in einem Aufruf.
3. Bei anderen Fehlern: Exception propagiert unverändert (nicht self-heilen bei Auth/Quota/Netzwerk).
4. Log-Events `drafts_folder_missing_creating` (WARN) und `drafts_folder_created` (INFO) geschrieben.
5. `test_imap_client_auto_create.py` mit ≥ 6 Tests, alle grün, deckt alle Fehlerstring-Muster + Non-Missing-Fehler + Case-Insensitivity ab.
6. Alle Phase-1-Tests bleiben grün.
</success_criteria>

<output>
Create `.planning/phases/02-deployment-beim-kunden/02.02-SUMMARY.md` when done — dokumentiere welche Fehlerstrings verifiziert wurden und welche Tests hinzugekommen sind.
</output>
