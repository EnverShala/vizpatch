---
plan_id: 02.01-auto-provider-detection
status: completed
completed_at: 2026-07-11
---

# 02.01 Summary — Auto-Provider-Detection

## Was wurde gebaut

- `agent/src/provider_config.py`: STATIC_PROVIDERS (12 Domains), MX_PATTERNS (12 Muster), `_get_mx_host()`, `resolve_imap_config()`
- `agent/src/config.py`: Auto-Detect-Fallback, `imap_sent_folder`-Feld, `IMAP_HOST` aus REQUIRED_ENV_VARS entfernt
- `agent/pyproject.toml`: `dnspython>=2.4,<3.0` hinzugefügt
- `agent/tests/conftest.py`: `imap_sent_folder="Sent"` in mock_config-Fixture
- `agent/tests/test_provider_config.py`: 8 Tests (statisch, MX-Fallback, RuntimeError, Case)
- `agent/tests/test_config_provider_override.py`: 6 Tests (Override, Auto-Detect, Sent-Override)

## Provider-Status

- **Live-verifiziert (statische Tabelle)**: IONOS, GMX, Web.de, Gmail, Outlook
- **[ASSUMED] (MX-Fallback)**: Strato, Hetzner, Alfahosting — MX-Patterns aus RESEARCH.md, nicht live DNS-getestet
- **Live-verifiziert (dnspython)**: dnspython 2.8.0 aus pyproject.toml — Import OK

## Test-Ergebnis

Alle Tests grün (pytest tests/ -x), außer pre-existierender Fehler in test_imap_client_auto_create.py (unrelated, war vor diesem Plan bereits rot).
