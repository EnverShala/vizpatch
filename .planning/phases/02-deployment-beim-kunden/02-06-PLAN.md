---
phase: 02-deployment-beim-kunden
plan: 06
plan_id: 02.06-preflight-avv-kunden-interview
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-deployment-beim-kunden/PREFLIGHT.md
  - .planning/phases/02-deployment-beim-kunden/AVV-CHECKLIST.md
  - .planning/phases/02-deployment-beim-kunden/KUNDEN-INTERVIEW.md
autonomous: true
requirements:
  - PRE-02
  - PRE-04
  - PRE-05
must_haves:
  truths:
    - "PREFLIGHT.md enthaelt einen ausfuellbaren Server-Check mit verifizierten Bash-Kommandos fuer Docker-Version, RAM, Disk, IMAP-Erreichbarkeit und Anthropic-API-Erreichbarkeit."
    - "AVV-CHECKLIST.md enthaelt eine DSGVO-Konformitaets-Checkliste fuer PRE-04: Anthropic-AVV/DPA Bezug (Commercial Terms), ZDR-Entscheidung, Records-of-Processing nach Art. 30, DSGVO Art. 5 (Datenminimierung durch D-26 Live-Fetch), Art. 17 (Recht auf Loeschung). BLOCKER-Status ist explizit dokumentiert: ohne unterschriebene AVV/DPA-Bestaetigung darf der Vor-Ort-Termin nicht starten."
    - "KUNDEN-INTERVIEW.md enthaelt einen ~30-Min-Fragenkatalog fuer PRE-05: About-Text, Oeffnungszeiten, Angebote/Preise, FAQ, Ton, Signatur, Reklamations-Standardformulierung. Fragen sind so gestellt, dass der Betreiber sie in <30 Min beantworten kann."
    - "Alle drei Dokumente sind auf DEUTSCH und fuer den Kunden lesbar (kein Entwickler-Jargon)."
    - "D-08: KUNDEN-INTERVIEW.md dokumentiert den OSINT-Vorlauf explizit — Vizionists sammelt vor dem Termin oeffentliche Infos (Website, Google-My-Business, Impressum, ggf. Facebook) und baut damit die Erstversion von `context.md.tankstelle-erstversion.md`."
    - "D-09: KUNDEN-INTERVIEW.md ist so strukturiert, dass der Fragenkatalog die im Vor-Ort-Termin fehlenden Inhalte (Ton, Signatur, Reklamations-Standardformulierung, ggf. nicht-oeffentliche Preise/FAQ) gezielt beim Betreiber ergaenzt."
  artifacts:
    - path: ".planning/phases/02-deployment-beim-kunden/PREFLIGHT.md"
      provides: "Ausfuellbare Server-Check-Liste mit verifizierten Kommandos"
      min_lines: 80
    - path: ".planning/phases/02-deployment-beim-kunden/AVV-CHECKLIST.md"
      provides: "DSGVO/AVV-Nachweis-Sammlung fuer PRE-04 mit expliziter Blocker-Kennzeichnung"
      min_lines: 60
    - path: ".planning/phases/02-deployment-beim-kunden/KUNDEN-INTERVIEW.md"
      provides: "Fragenkatalog fuer 30-Min-Interview zur context.md-Erstellung"
      min_lines: 60
  key_links:
    - from: ".planning/phases/02-deployment-beim-kunden/PREFLIGHT.md"
      to: "Docker + IMAP-Host + api.anthropic.com"
      via: "Bash-Kommandos zur Erreichbarkeitspruefung"
      pattern: "openssl s_client|docker version|free -m|df -h"
    - from: ".planning/phases/02-deployment-beim-kunden/AVV-CHECKLIST.md"
      to: "Anthropic Commercial Terms + Art. 30 DSGVO"
      via: "Nachweise verlinkt und dokumentiert (nicht als HTTP-Header-Task)"
      pattern: "DSGVO|AVV|Art\\.|Commercial Terms"
    - from: ".planning/phases/02-deployment-beim-kunden/KUNDEN-INTERVIEW.md"
      to: "agent/context.md.example"
      via: "Fragen orientieren sich an context.md-Sektionsstruktur (About/Oeffnungszeiten/Angebote/FAQ/Ton/Signatur)"
      pattern: "About|Oeffnungszeiten|Angebote|FAQ|Ton|Signatur"
---

<objective>
Drei Vorbereitungs-Dokumente fuer den Kundentermin anlegen, die PRE-02/PRE-04/PRE-05 abdecken. Alle drei sind reine Doku-Arbeit (Wave 1, parallel zu 02.02 und 02.03), sind auf Deutsch und richten sich teilweise an den Kunden (nicht an Entwickler).

Purpose:
- **PREFLIGHT.md**: Vor dem Vor-Ort-Termin (oder als erster Schritt im Termin) prueft der Techniker mit einer Ampel-Liste, ob der Kundenserver deploy-tauglich ist. Kein Skript, sondern Kommando-fuer-Kommando Checkliste mit erwarteter Antwort und Aktion bei Fehlschlag.
- **AVV-CHECKLIST.md**: Bevor der Vor-Ort-Termin startet, muss die AVV-Situation geklaert sein (PRE-04). Das Dokument dokumentiert den Anthropic-AVV-Bezug (in Commercial Terms eingebettet, nicht separates PDF), die ZDR-Entscheidung (Vertrag, KEIN Header), und das Records-of-Processing gemaess Art. 30 DSGVO. **PRE-04 ist Blocker fuer den Vor-Ort-Termin** - explizit markiert.
- **KUNDEN-INTERVIEW.md**: Vor dem Vor-Ort-Termin (oder im ersten 30-Min-Slot des Termins) sammelt Vizionists die Firmen-Inhalte vom Betreiber (PRE-05). Fragenkatalog ist so strukturiert, dass er in <=30 Min durchsprechbar ist, und die Antworten direkt in `deployment/context.md.tankstelle-erstversion.md` einfliessen koennen.

Output: 3 Markdown-Dokumente unter `.planning/phases/02-deployment-beim-kunden/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-deployment-beim-kunden/02-CONTEXT.md
@.planning/phases/02-deployment-beim-kunden/02-RESEARCH.md
@agent/context.md.example

<interfaces>
Aus RESEARCH.md verfuegbar (§ Preflight-Checks, verifiziert):

```bash
docker version --format '{{.Server.Version}}'     # Erwartung: 26.x
docker compose version                             # Erwartung: v2.x
free -m | awk '/^Mem:/{print $7}'                 # Erwartung: >= 512
df -h /opt | awk 'NR==2{print $4}'                # Erwartung: >= 5 GB
echo | openssl s_client -connect imap.ionos.de:993 -servername imap.ionos.de 2>&1 | grep CONNECTED
echo | openssl s_client -connect api.anthropic.com:443 -servername api.anthropic.com 2>&1 | grep CONNECTED
systemctl is-enabled docker                        # Erwartung: enabled
```

Aus RESEARCH.md § DSGVO/AVV verfuegbar (2026-07-11 verifiziert):
- ZDR ist KEIN HTTP-Header - Sales-Vertrag, per Antrag bei sales@anthropic.com
- Anthropic DPA/AVV ist in Commercial Terms eingebettet, gilt automatisch fuer paid-API-Accounts
- Standard-Retention seit Sep 2025: 7 Tage (war 30)
- EU-SCCs Module 2+3 automatisch im DPA

Aus CONTEXT.md § context.md-Workflow (D-08/D-09):
- Vizionists sammelt OSINT vor Termin (Website, Google-My-Business, Impressum)
- Im Termin ergaenzt der Betreiber: Ton-Vorgaben, Signatur, Reklamations-Standard, nicht-oeffentliche Preise, spezielle FAQ
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PREFLIGHT.md - Server-Check-Liste anlegen</name>
  <files>.planning/phases/02-deployment-beim-kunden/PREFLIGHT.md</files>
  <read_first>
    - .planning/phases/02-deployment-beim-kunden/02-RESEARCH.md (§ Preflight-Checks - alle verifizierten Kommandos)
    - .planning/phases/02-deployment-beim-kunden/02-CONTEXT.md (§ Claude's Discretion - Preflight-Skript-Details, Threshold-Vorgaben)
    - .planning/REQUIREMENTS.md (PRE-02 Spec: Ubuntu 22.04+/Debian 12+, Docker 26+, min. 512 MB RAM, 5 GB SSD)
  </read_first>
  <action>
    `.planning/phases/02-deployment-beim-kunden/PREFLIGHT.md` neu anlegen. Struktur (>= 80 Zeilen):

    ```markdown
    # Preflight-Check - Kundenserver

    **Zweck:** Vor dem Vor-Ort-Termin (oder als erster Schritt im Termin) verifizieren, dass der Kundenserver alle Voraussetzungen (PRE-02) erfuellt.

    **Ausfuehrung:** Alle Kommandos werden als Login-User auf dem Kundenserver ausgefuehrt (nicht als root, ausser wo mit `sudo` markiert).

    **Zeit-Estimate:** ~10 Minuten wenn alles gruen ist, 20-40 Minuten wenn Docker installiert werden muss.

    ## Ampel-Legende

    - 🟢 Gruen: Wert erfuellt Erwartung. Naechster Schritt.
    - 🟡 Gelb: Wert grenzwertig oder Aktion moeglich. Notieren, weitermachen.
    - 🔴 Rot: BLOCKER. Termin verschieben oder problem beheben bevor weitermachen.

    ## 1. Betriebssystem

    ```bash
    lsb_release -d
    uname -a
    ```

    | Ergebnis | Ampel | Aktion |
    |----------|-------|--------|
    | Ubuntu 22.04 LTS oder neuer | 🟢 | weiter |
    | Ubuntu 24.04 LTS | 🟢 | weiter |
    | Debian 12 (bookworm) oder neuer | 🟢 | weiter |
    | AElter (Ubuntu 20.04, Debian 11) | 🟡 | Docker-Compose-Plugin-Version pruefen, meist OK |
    | CentOS/Alma/Rocky Linux | 🟡 | Docker installierbar, aber Kommandos ggf. anpassen |
    | Etwas ganz Anderes | 🔴 | Vor Ort abklaeren |

    ## 2. Docker + Compose Plugin

    ```bash
    docker version --format '{{.Server.Version}}'
    docker compose version
    systemctl is-enabled docker
    ```

    | Ergebnis | Ampel | Aktion |
    |----------|-------|--------|
    | Docker-Version 26.x oder neuer + Compose v2.x + `enabled` | 🟢 | weiter |
    | Docker 24.x/25.x + Compose v2.x + `enabled` | 🟡 | funktioniert vermutlich, notieren |
    | Docker < 24 | 🔴 | Docker aktualisieren (~10 Min: `curl -fsSL https://get.docker.com \| sh`) |
    | Docker nicht installiert | 🔴 | Docker installieren (~10 Min). Kunde vorab bitten oder im Termin durchfuehren. |
    | `docker.service` nicht enabled | 🟡 | `sudo systemctl enable docker` (30 Sek) |

    ## 3. Speicher (RAM)

    ```bash
    free -m
    ```

    | Freier Speicher | Ampel | Aktion |
    |-----------------|-------|--------|
    | >= 512 MB (bei buff/cache oder 'available') | 🟢 | weiter |
    | 256-512 MB | 🟡 | wird funktionieren, aber knapp - Kunde informieren |
    | < 256 MB | 🔴 | RAM zu klein, andere Prozesse beenden oder Server upgraden |

    ## 4. Disk (SSD)

    ```bash
    df -h /opt 2>/dev/null || df -h /
    ```

    | Freier Speicher | Ampel | Aktion |
    |-----------------|-------|--------|
    | >= 5 GB | 🟢 | weiter |
    | 2-5 GB | 🟡 | reicht fuer Docker-Image (~200 MB) + Logs, aber knapp |
    | < 2 GB | 🔴 | Speicher freimachen (Logs rotieren, alte Docker-Images `docker system prune -a`) |

    ## 5. IMAP-Erreichbarkeit

    Ersetze `imap.ionos.de` durch den tatsaechlichen IMAP-Host des Kunden (aus Auto-Detect via `IMAP_USER`-Domain - falls unbekannt, siehe `agent/src/provider_config.py`).

    ```bash
    IMAP_HOST=imap.ionos.de   # z.B. imap.gmx.net, imap.web.de, ...
    echo | openssl s_client -connect ${IMAP_HOST}:993 -servername ${IMAP_HOST} 2>&1 | grep -E "CONNECTED|Verify return code"
    ```

    | Ergebnis | Ampel | Aktion |
    |----------|-------|--------|
    | `CONNECTED(...)` + `Verify return code: 0 (ok)` | 🟢 | weiter |
    | `CONNECTED` aber Zertifikatsfehler | 🟡 | Zertifikat pruefen, meist harmlos |
    | Keine `CONNECTED`-Zeile, Timeout | 🔴 | Firewall/Netzwerk blockiert IMAP - mit Kunden-Admin klaeren |

    ## 6. Anthropic-API-Erreichbarkeit

    ```bash
    echo | openssl s_client -connect api.anthropic.com:443 -servername api.anthropic.com 2>&1 | grep -E "CONNECTED|Verify return code"
    ```

    | Ergebnis | Ampel | Aktion |
    |----------|-------|--------|
    | `CONNECTED` + Verify OK | 🟢 | weiter |
    | Timeout / Blocked | 🔴 | Ausgehende HTTPS/443 wird blockiert - Firewall-Regel noetig |

    ## 7. `.env`-Berechtigungen (nach Setup)

    Nach dem Anlegen der `.env` muss die Datei `chmod 600` haben.

    ```bash
    ls -la /opt/kea/.env
    ```

    | Berechtigungen | Ampel | Aktion |
    |----------------|-------|--------|
    | `-rw-------` (600) | 🟢 | weiter |
    | Alles anderes | 🔴 | `sudo chmod 600 /opt/kea/.env` |

    ## Zusammenfassung / Freigabe fuer Deployment

    - [ ] OS gruen oder grenzwertig-gelb
    - [ ] Docker + Compose ausgefuehrt, `docker.service` enabled
    - [ ] RAM >= 512 MB frei
    - [ ] Disk >= 5 GB frei
    - [ ] IMAP-Host erreichbar
    - [ ] Anthropic-API erreichbar
    - [ ] `.env chmod 600` (nach Setup)

    **Wenn alle 🟢 oder 🟡 mit dokumentierter Notiz: Deployment kann starten.**
    **Wenn ein 🔴 uebrig ist: Termin verschieben oder das Problem im Termin beheben.**

    ## Anmerkung: Auto-Docker-Install (Fallback)

    Falls Docker nicht installiert ist und Kunde stimmt zu:

    ```bash
    curl -fsSL https://get.docker.com | sh
    sudo systemctl enable --now docker
    sudo usermod -aG docker $USER
    # Neu einloggen damit Gruppen-Membership wirkt:
    exit
    # dann neu SSH-Session
    ```

    ~10 Min Installation. Bei Ubuntu/Debian standardmaessig unterstuetzt.
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/02-deployment-beim-kunden/PREFLIGHT.md && wc -l < .planning/phases/02-deployment-beim-kunden/PREFLIGHT.md | awk '{if ($1 < 80) exit 1}' && grep -c "openssl s_client" .planning/phases/02-deployment-beim-kunden/PREFLIGHT.md && grep -c "docker version\|docker compose version" .planning/phases/02-deployment-beim-kunden/PREFLIGHT.md</automated>
  </verify>
  <acceptance_criteria>
    - Datei existiert, mindestens 80 Zeilen. Pruefbar via `wc -l < .planning/phases/02-deployment-beim-kunden/PREFLIGHT.md` >= 80.
    - Enthaelt `openssl s_client` fuer IMAP UND fuer Anthropic. Pruefbar via `grep -c "openssl s_client" .planning/phases/02-deployment-beim-kunden/PREFLIGHT.md` >= 2.
    - Enthaelt Docker-Version-Check und Compose-Version-Check. Pruefbar via `grep -c "docker version\|docker compose version" .planning/phases/02-deployment-beim-kunden/PREFLIGHT.md` >= 2.
    - Enthaelt RAM-Check (`free -m`) und Disk-Check (`df -h`). Pruefbar via `grep -c "free -m\|df -h" .planning/phases/02-deployment-beim-kunden/PREFLIGHT.md` >= 2.
    - Enthaelt Ampel-Legende (🟢, 🟡, 🔴). Pruefbar via `grep -c "🟢\|🟡\|🔴" .planning/phases/02-deployment-beim-kunden/PREFLIGHT.md` >= 3.
    - Enthaelt Zusammenfassungs-Checkbox-Block. Pruefbar via `grep -c "^\- \[ \]" .planning/phases/02-deployment-beim-kunden/PREFLIGHT.md` >= 5.
  </acceptance_criteria>
  <done>
    PREFLIGHT.md hat 7 Check-Sektionen (OS, Docker, RAM, Disk, IMAP, Anthropic, .env), Ampel-Bewertung pro Sektion, Zusammenfassungs-Checkboxen und Auto-Docker-Install-Fallback.
  </done>
</task>

<task type="auto">
  <name>Task 2: AVV-CHECKLIST.md - DSGVO/AVV-Nachweis-Sammlung anlegen (PRE-04 als BLOCKER)</name>
  <files>.planning/phases/02-deployment-beim-kunden/AVV-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/02-deployment-beim-kunden/02-RESEARCH.md (§ DSGVO/AVV - Anthropic, verifizierte Erkenntnisse zu ZDR/DPA/Commercial Terms/EU-SCCs/7-Tage-Retention)
    - .planning/phases/02-deployment-beim-kunden/02-CONTEXT.md (§ Delivery-Mechanismus D-05: keine github-Zugang noetig - relevant fuer AVV-Doku)
    - .planning/REQUIREMENTS.md (PRE-04 Spec)
    - .planning/PROJECT.md (§ Constraints: AVV mit Anthropic vor Live-Verarbeitung, chmod 600)
  </read_first>
  <action>
    `.planning/phases/02-deployment-beim-kunden/AVV-CHECKLIST.md` neu anlegen. Struktur (>= 60 Zeilen):

    ```markdown
    # AVV / DSGVO-Checklist (PRE-04)

    > ⚠ **BLOCKER-Status:** Dieser Punkt MUSS abgeschlossen sein BEVOR der Vor-Ort-Termin startet.
    > Ohne wirksamen AVV/DPA-Rahmen darf der Bot keine echten Kunden-Mails verarbeiten.

    ## Zielsetzung

    Der Betreiber (Auftraggeber der Verarbeitung) muss vor der ersten Live-Verarbeitung nachweisen koennen:

    1. Ein wirksamer AVV/DPA mit Anthropic besteht.
    2. Die Uebermittlung in die USA ist DSGVO-konform abgesichert (EU-SCCs).
    3. Es gibt eine Records-of-Processing-Notiz gemaess Art. 30 DSGVO.
    4. Recht auf Loeschung (Art. 17) ist technisch umgesetzt.
    5. Datenminimierung (Art. 5) ist gegeben.

    ## 1. Anthropic-Vertragsrahmen

    ### Fakten (Recherche 2026-07-11)

    - **AVV / DPA:** Der Anthropic Data Processing Agreement (DPA) ist automatisch in den **Anthropic Commercial Terms** eingebettet. Er gilt fuer alle bezahlten API-Accounts. **Kein separater PDF-Download noetig.**
    - **EU-SCCs:** Modul 2 + 3 (Controller-to-Processor + Processor-to-Sub-Processor) sind automatisch im DPA enthalten.
    - **Standard-Retention:** 7 Tage seit September 2025 (war vorher 30 Tage). Daten werden NICHT zum Model-Training genutzt.
    - **ZDR (Zero Data Retention):** ⚠ **ZDR ist KEIN HTTP-Header oder API-Parameter.** ZDR wird per Vertragsanhang aktiviert - Antrag bei `sales@anthropic.com` oder `https://claude.com/contact-sales`.

    ### Checkliste

    - [ ] Anthropic-Account existiert und ist als "Commercial Account" markiert. Verifikation: Anthropic-Console -> Settings -> Billing.
    - [ ] Commercial Terms wurden bei Account-Setup akzeptiert (elektronische Zustimmung). Verifikation: Anthropic-Console zeigt keinen "Terms not accepted"-Banner mehr.
    - [ ] Zustimmung ist im Namen des Betreibers erfolgt (nicht als Privatperson des Vizionists-Mitarbeiters).
    - [ ] ZDR-Entscheidung getroffen:
        - Option A: 7-Tage-Retention akzeptabel -> **KEINE Aktion noetig, Standard gilt.**
        - Option B: ZDR gewuenscht -> Antrag bei `sales@anthropic.com` mit Firmen-Details, DSGVO-Sensitivitaet begruenden. Wartezeit typischerweise 1-4 Wochen. **BLOCKER falls Option B.**
    - [ ] Entscheidung dokumentieren: [ ] Option A (Standard 7 Tage) [ ] Option B (ZDR, Wartezeit __)

    ## 2. Records of Processing (Art. 30 DSGVO)

    Der Betreiber (nicht Vizionists) fuehrt das Verzeichnis der Verarbeitungstaetigkeiten. Fuer die KI-Email-Verarbeitung ist folgender Eintrag anzulegen:

    ```
    Verarbeitungstaetigkeit: Automatisierte Erstellung von Antwort-Entwuerfen auf Kunden-E-Mails via LLM
    Verantwortlicher:        <Tankstelle GmbH, Adresse, USt-ID>
    Auftragsverarbeiter:     Anthropic PBC / Anthropic Ireland Limited
    Kategorien betroffener
    Personen:               Kunden der Tankstelle (Absender eingehender E-Mails)
    Kategorien Daten:       E-Mail-Inhalt (Text), Absender-E-Mail-Adresse,
                             Betreff, ggf. E-Mail-Signatur/Kontaktdaten
    Zweck:                   Vorbereitung von Antwortvorschlaegen fuer den Betreiber;
                             kein automatisches Versenden
    Rechtsgrundlage:         Art. 6 Abs. 1 lit. f DSGVO (berechtigtes Interesse an
                             effizienter Kundenkommunikation) UND Art. 6 Abs. 1 lit. b
                             (Vertragsanbahnung/-erfuellung)
    Datenuebermittlung:      USA (Anthropic PBC), abgesichert durch EU-SCCs
                             Modul 2+3 (im DPA eingebettet)
    Speicherdauer bei
    Anthropic:              max. 7 Tage (Standard) oder 0 Tage (ZDR falls aktiviert)
    Speicherdauer im
    Postfach des Betreibers:  Postfach-Retention gemaess IMAP-Provider,
                             keine zusaetzliche Speicherung durch den Bot
    Technisch-organisatorische
    Massnahmen:             - PII-Redaction fuer IBAN/Kreditkarten vor LLM-Call
                             - .env chmod 600 (Secrets-Schutz)
                             - Non-root Container-User (uid 1000)
                             - Kein Auto-Send (Draft-only, Betreiber-Freigabe zwingend)
                             - Live-Fetch statt Bot-DB-Kopie (D-26)
    ```

    - [ ] Records-of-Processing-Eintrag ist im Betreiber-Verzeichnis angelegt.

    ## 3. Recht auf Loeschung (Art. 17)

    **Umsetzung:** Der Bot legt KEINE eigene Kopie der Mails an (D-26 Live-Fetch aus IMAP). Wenn der Betreiber eine Mail aus dem Postfach loescht, ist sie automatisch aus dem Kontext des Bots verschwunden.

    - [ ] Betreiber ist informiert, dass Loeschen einer Mail im Postfach die Bot-Sicht sofort mit-loescht (kein separater Loesch-Prozess noetig).
    - [ ] Loesch-Anfragen von Kunden werden vom Betreiber im Postfach umgesetzt (keine Vizionists-Aktion noetig).

    ## 4. Datenminimierung (Art. 5)

    **Umsetzung:** Der Bot laedt bei jedem Draft-Zyklus nur die JETZT relevanten Mails aus IMAP (Thread-Suche + max 30-Tage-Absender-Fallback, max. 6 Nachrichten, Body-Truncation 800 Zeichen). Kein "Alle Mails lesen und speichern".

    - [ ] Konfiguration `ENABLE_PII_REDACTION=true` ist gesetzt (regex-Redaction fuer IBAN + Kreditkarten vor LLM-Call).

    ## 5. `.env`-Sicherheit

    - [ ] `.env` auf Kundenserver hat `chmod 600` (nur owner lesbar).
    - [ ] `.env` enthaelt keine Passwoerter im Terminal-History (mit `nano` bearbeiten, nicht `export`).
    - [ ] `.env` ist NICHT in einem `git`-Repo (Kundenserver hat kein Git nach D-05).

    ## Freigabe

    - [ ] Alle Punkte in Abschnitt 1-5 sind ✅
    - [ ] Optional: Datenschutzbeauftragter der Tankstelle (falls vorhanden) hat den Vorgang zur Kenntnis genommen

    **AVV/DSGVO-Bereit fuer Live-Verarbeitung?** [ ] Ja [ ] Nein - Blocker: ______________

    **Bestaetigt durch:** ______________ (Betreiber) und ______________ (Vizionists) am ______________
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/02-deployment-beim-kunden/AVV-CHECKLIST.md && wc -l < .planning/phases/02-deployment-beim-kunden/AVV-CHECKLIST.md | awk '{if ($1 < 60) exit 1}' && grep -c "BLOCKER" .planning/phases/02-deployment-beim-kunden/AVV-CHECKLIST.md && grep -c "Art\. 5\|Art\. 17\|Art\. 30\|Art\. 6" .planning/phases/02-deployment-beim-kunden/AVV-CHECKLIST.md && grep -c "ZDR" .planning/phases/02-deployment-beim-kunden/AVV-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - Datei existiert, mindestens 60 Zeilen. Pruefbar via `wc -l < .planning/phases/02-deployment-beim-kunden/AVV-CHECKLIST.md` >= 60.
    - Enthaelt explizite BLOCKER-Kennzeichnung. Pruefbar via `grep -c "BLOCKER" .planning/phases/02-deployment-beim-kunden/AVV-CHECKLIST.md` >= 1.
    - Referenziert DSGVO-Artikel 5, 17, 30 UND 6. Pruefbar via `grep -c "Art\. 5\|Art\. 17\|Art\. 30\|Art\. 6" .planning/phases/02-deployment-beim-kunden/AVV-CHECKLIST.md` >= 4.
    - Dokumentiert explizit, dass ZDR KEIN HTTP-Header ist. Pruefbar via `grep -c "kein.*Header\|KEIN.*Header\|nicht.*Header" .planning/phases/02-deployment-beim-kunden/AVV-CHECKLIST.md` >= 1.
    - Dokumentiert Anthropic Commercial Terms als AVV-Rahmen. Pruefbar via `grep -c "Commercial Terms" .planning/phases/02-deployment-beim-kunden/AVV-CHECKLIST.md` >= 1.
    - Enthaelt Records-of-Processing-Vorlage nach Art. 30. Pruefbar via `grep -c "Verantwortlicher\|Auftragsverarbeiter" .planning/phases/02-deployment-beim-kunden/AVV-CHECKLIST.md` >= 2.
    - Hat 5+ Checkboxen. Pruefbar via `grep -c "^\s*- \[ \]" .planning/phases/02-deployment-beim-kunden/AVV-CHECKLIST.md` >= 5.
  </acceptance_criteria>
  <done>
    AVV-CHECKLIST.md hat 5 Abschnitte (Anthropic-Vertrag, Records-of-Processing, Art. 17, Art. 5, .env-Sicherheit), BLOCKER-Kennzeichnung, ZDR-KEIN-Header-Klarstellung, DSGVO-Artikel-Referenzen, Freigabe-Block.
  </done>
</task>

<task type="auto">
  <name>Task 3: KUNDEN-INTERVIEW.md - Fragenkatalog fuer 30-Min-Interview anlegen</name>
  <files>.planning/phases/02-deployment-beim-kunden/KUNDEN-INTERVIEW.md</files>
  <read_first>
    - .planning/phases/02-deployment-beim-kunden/02-CONTEXT.md (§ context.md-Workflow D-08/D-09/D-10, § Specifics OSINT fuer context.md)
    - agent/context.md.example (die 6 Sektionen: About/Oeffnungszeiten/Angebote/FAQ/Ton/Signatur)
    - .planning/REQUIREMENTS.md (PRE-05 Spec)
  </read_first>
  <action>
    `.planning/phases/02-deployment-beim-kunden/KUNDEN-INTERVIEW.md` neu anlegen. Struktur (>= 60 Zeilen). Der Ton richtet sich AN DEN BETREIBER (nicht an Entwickler).

    ```markdown
    # Kunden-Interview fuer `context.md`-Erstellung

    **Zweck:** In ~30 Minuten die Firmen-Inhalte sammeln, die der KI-Agent braucht, um Ihre Kunden-Antworten realistisch draften zu koennen.

    **Format:** Vor dem Vor-Ort-Termin per Video-Call oder Telefon (bevorzugt), oder als erster Termin-Slot vor Ort (wenn Zeit knapp).

    **Vorbereitung:**
    - Sie haben Ihre Oeffnungszeiten parat.
    - Sie haben eine Preisliste (oder wissen wo im Aushang / Website).
    - Sie haben 2-3 Beispiele fuer typische Kunden-Mails der letzten Wochen im Kopf.
    - Vizionists hat vorab aus Ihrer Website / Google-My-Business bereits eine Rohversion vorbereitet - wir gleichen ab und ergaenzen.

    ---

    ## Teil 1: About (2-3 Minuten)

    **Frage 1.1:** Wie heisst Ihre Tankstelle offiziell? (Auf dem Rechnungsformular, im Impressum.)

    **Frage 1.2:** Wo ist der Standort? (Strasse, PLZ, Ort.)

    **Frage 1.3:** Wie lange gibt es Sie schon? Seit wann fuehren Sie die Tankstelle?

    **Frage 1.4:** Gibt es etwas, das Sie besonders auszeichnet? (Familienbetrieb, spezielle Werkstatt, Wohnmobil-freundlich, 24/7 Automat, etc.)

    ---

    ## Teil 2: Oeffnungszeiten (2 Minuten)

    **Frage 2.1:** Was sind Ihre normalen Oeffnungszeiten?
    - Montag bis Freitag: ___________
    - Samstag: ___________
    - Sonntag: ___________

    **Frage 2.2:** Gibt es Abweichungen an Feiertagen?

    **Frage 2.3:** Hat der Shop / das Bistro andere Zeiten als die Tankstelle?

    **Frage 2.4:** Gibt es einen SB-24-Automaten der auch nachts laeuft? (Fuer die Antwort auf "geht das nachts?")

    ---

    ## Teil 3: Angebote / Preise / Produkte (5-8 Minuten)

    **Frage 3.1:** Welche Kraftstoffe fuehren Sie? (Diesel, Super, E5, E10, AdBlue, LPG, ...)

    **Frage 3.2:** Haben Sie eine Auto-Waschanlage?
    - Wenn ja: welche Programme? Preise?
    - Ist es eine SB-Waschstrasse oder Portalwaschanlage?

    **Frage 3.3:** Haben Sie einen Shop? Bistro? Was fuehren Sie im Sortiment?

    **Frage 3.4:** Haben Sie eine Werkstatt?
    - Wenn ja: welche Leistungen? (TUeV, Oelwechsel, Reifenwechsel, ...)
    - Terminvereinbarung noetig? Wie?

    **Frage 3.5:** Sonstiges: DHL-Paketshop? Reifenluft? Staubsauger? Wohnmobil-Entsorgung? Ladesaeule fuer E-Autos?

    ---

    ## Teil 4: FAQ (5-8 Minuten)

    **Frage 4.1:** Was sind die 3-5 Fragen, die Kunden Ihnen am haeufigsten stellen?

    Beispiele um zu triggern:
    - "Was kostet ...?"
    - "Habt ihr ... auf Lager?"
    - "Ist am Sonntag / Feiertag geoeffnet?"
    - "Nehmt ihr ... als Zahlungsmittel an?"

    **Frage 4.2:** Wenn ein Kunde eine Reklamation hat (z.B. Waschanlage hat Kratzer verursacht, falscher Kraftstoff getankt): Wie gehen Sie damit standard-maessig um? Was ist Ihre erste Antwort?

    **Frage 4.3:** Gibt es Sachen die Sie NICHT machen? (z.B. keine Bezahlung mit AmEx, keine Reifen fuer LKW, keine Werkstattarbeit ohne Termin)

    ---

    ## Teil 5: Ton und Kommunikation (3-5 Minuten)

    **Frage 5.1:** Wie sprechen Sie Kunden an? Sie oder Du?

    **Frage 5.2:** Wenn Sie eine Antwort auf eine Kunden-Anfrage schreiben, wie klingt das typischerweise? (Beispiel: Sind Sie eher kurz-und-direkt, oder eher ausfuehrlich-freundlich?)

    **Frage 5.3:** Gibt es Formulierungen die Sie IMMER verwenden? (z.B. "Vielen Dank fuer Ihre Nachricht", "Mit freundlichen Gruessen aus <Ort>")

    **Frage 5.4:** Gibt es Formulierungen die Sie AUF KEINEN FALL wollen? (z.B. keine Marketing-Sprache, keine Emojis, keine "wir freuen uns auf Sie"-Floskeln)

    ---

    ## Teil 6: Signatur (2 Minuten)

    **Frage 6.1:** Wie soll die Signatur unter jeder Draft-Antwort aussehen?

    Sammeln:
    - Ihr Name (Vorname + Nachname wie Sie in E-Mails auftreten)
    - Firmenname
    - Adresse
    - Telefonnummer
    - E-Mail-Adresse
    - Website (falls vorhanden)
    - Ggf. Zusatz wie "Ihr Team der Tankstelle Mustermann"

    ---

    ## Teil 7: Reklamations-Standardformulierung (2-3 Minuten)

    Diese Frage ist wichtig, weil Reklamationen der haeufigste Fall sind, in dem ein Standard-Text hilfreich waere.

    **Frage 7.1:** Wenn ein Kunde eine Reklamation schickt (Waschanlage-Kratzer, falscher Kraftstoff, verdorbene Ware im Shop): Was ist Ihre erste "wir kuemmern uns"-Antwort? Bitte 2-3 Saetze diktieren.

    **Beispiel-Antwort** (nur zur Anregung, nicht copy-pasten):
    "Sehr geehrter <Name>, vielen Dank fuer Ihre Nachricht. Es tut uns leid, dass es zu einem Problem gekommen ist. Ich werde mich persoenlich um Ihre Anfrage kuemmern und melde mich innerhalb der naechsten Werktage bei Ihnen. Wenn moeglich, bitte ich Sie noch um <Details>. Mit freundlichen Gruessen, <Name>"

    ---

    ## Naechster Schritt

    - Vizionists uebertraegt Ihre Antworten in `context.md`.
    - Beim Vor-Ort-Termin (~30 Min) machen wir gemeinsam eine 5-Min-Review der `context.md` und passen an, was Ihnen beim Lesen auffaellt.
    - In Phase 3 (~1 Woche nach Deployment): Feedback-Runde mit realen Drafts - wir schleifen `context.md` noch mal nach.

    **Interview durchgefuehrt am:** ______________ **durch:** ______________
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/02-deployment-beim-kunden/KUNDEN-INTERVIEW.md && wc -l < .planning/phases/02-deployment-beim-kunden/KUNDEN-INTERVIEW.md | awk '{if ($1 < 60) exit 1}' && grep -c "About\|Oeffnungszeiten\|Angebote\|FAQ\|Ton\|Signatur" .planning/phases/02-deployment-beim-kunden/KUNDEN-INTERVIEW.md && grep -c "^\*\*Frage" .planning/phases/02-deployment-beim-kunden/KUNDEN-INTERVIEW.md</automated>
  </verify>
  <acceptance_criteria>
    - Datei existiert, mindestens 60 Zeilen. Pruefbar via `wc -l < .planning/phases/02-deployment-beim-kunden/KUNDEN-INTERVIEW.md` >= 60.
    - Deckt alle 6 context.md-Sektionen (About, Oeffnungszeiten, Angebote, FAQ, Ton, Signatur) UND Reklamations-Standardformulierung. Pruefbar via `grep -c "About\|Oeffnungszeiten\|Angebote\|FAQ\|Ton\|Signatur" .planning/phases/02-deployment-beim-kunden/KUNDEN-INTERVIEW.md` >= 6.
    - Enthaelt mindestens 15 nummerierte Fragen (Frage X.Y). Pruefbar via `grep -c "^\*\*Frage" .planning/phases/02-deployment-beim-kunden/KUNDEN-INTERVIEW.md` >= 15.
    - Ist auf Deutsch verfasst (keine englischen Fach-Begriffe im Fliesstext). Pruefbar via `grep -c "Ihre\|Sie\|Ihnen" .planning/phases/02-deployment-beim-kunden/KUNDEN-INTERVIEW.md` >= 10 (deutet auf durchgaengige Anrede).
    - Zeit-Estimate pro Teil ist explizit. Pruefbar via `grep -c "Minuten\|Minute" .planning/phases/02-deployment-beim-kunden/KUNDEN-INTERVIEW.md` >= 6.
  </acceptance_criteria>
  <done>
    KUNDEN-INTERVIEW.md hat 7 Teile (About, Oeffnungszeiten, Angebote, FAQ, Ton, Signatur, Reklamation) mit >=15 nummerierten Fragen, Zeit-Estimates pro Teil, auf Deutsch.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Preflight-Kommandos -> Kundenserver | Diagnose-Kommandos, keine State-Aenderung ausser dokumentierte Aktionen (Docker-Install) |
| AVV-Checklist -> Betreiber | Nachweise werden vom Betreiber unterzeichnet - Vizionists ist nur Facilitator |
| Interview-Antworten -> context.md | Firmen-Inhalte werden vertrauenswuerdig direkt vom Betreiber uebernommen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.06-01 | Repudiation | Betreiber bestreitet spaeter, AVV zu kennen | mitigate | AVV-CHECKLIST.md endet mit Freigabe-Signatur-Block (Datum + Namen Betreiber + Vizionists). Zwei-Personen-Bestaetigung reduziert Repudiation-Risiko. |
| T-02.06-02 | Information Disclosure | Kunden-Interview-Antworten enthalten sensible Preise / Kalkulationen | accept | Antworten fliessen NUR in context.md ein, die als read-only Bind-Mount am Kundenserver liegt. Kein Backup ausserhalb der Betreiber-Kontrolle. |
| T-02.06-03 | Denial of Service | Preflight-Check zeigt Blocker (rot), Termin muss verschoben werden | accept | Genau der Sinn des Preflight - VOR dem Termin klaeren, dass alle Voraussetzungen erfuellt sind. Verschieben ist besser als scheitern vor Ort. |
| T-02.06-04 | Compliance-Fehler | AVV nicht abgeschlossen, aber Bot startet trotzdem | mitigate | BLOCKER-Kennzeichnung im AVV-CHECKLIST.md explizit; RUNBOOK.md (Plan 02.07) verweist auf AVV-Checklist als Pflicht-Vorarbeit. |
</threat_model>

<verification>
Alle drei Dokumente pruefen:

```bash
for f in PREFLIGHT.md AVV-CHECKLIST.md KUNDEN-INTERVIEW.md; do
    p=".planning/phases/02-deployment-beim-kunden/$f"
    test -f "$p" && echo "OK: $f exists, $(wc -l < "$p") lines"
done
```

Erwartetes Ergebnis: 3 Dateien, jede >= 60 Zeilen.

Kein Runtime-Verify - alles reine Doku, die im Termin oder vorab genutzt wird.
</verification>

<success_criteria>
1. PREFLIGHT.md hat 7 Check-Sektionen (OS/Docker/RAM/Disk/IMAP/Anthropic/.env) mit verifizierten Bash-Kommandos.
2. AVV-CHECKLIST.md dokumentiert AVV/DPA-Bezug (Commercial Terms), ZDR-KEIN-Header-Klarstellung, Art. 5/17/30/6 DSGVO-Referenzen, BLOCKER-Kennzeichnung, Freigabe-Signatur.
3. KUNDEN-INTERVIEW.md hat 7 Interview-Teile mit >=15 nummerierten Fragen, deckt alle 6 context.md-Sektionen + Reklamations-Standardformulierung.
4. Alle 3 Dokumente auf Deutsch.
5. Alle 3 Dokumente sind bereit fuer den Termin und werden von Plan 02.07 (RUNBOOK.md) referenziert.
</success_criteria>

<output>
Create `.planning/phases/02-deployment-beim-kunden/02.06-SUMMARY.md` when done - dokumentiere alle drei Dokumente und ihre Rolle im Termin-Flow.
</output>
