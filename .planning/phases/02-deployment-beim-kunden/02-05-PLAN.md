---
phase: 02-deployment-beim-kunden
plan: 05
plan_id: 02.05-pre-deployment-test-vizionists
type: execute
wave: 3
depends_on:
  - 02.01-auto-provider-detection
  - 02.02-auto-create-drafts-folder
  - 02.03-conversation-history-live-imap
  - 02.04-docker-bindmount-deployment-package
files_modified:
  - agent/tests/fixtures/pre-deployment/01-oeffnungszeiten-frage.eml
  - agent/tests/fixtures/pre-deployment/02-preis-anfrage.eml
  - agent/tests/fixtures/pre-deployment/03-termin-anfrage.eml
  - agent/tests/fixtures/pre-deployment/04-reklamation.eml
  - agent/tests/fixtures/pre-deployment/05-newsletter.eml
  - agent/tests/fixtures/pre-deployment/06-amazon-bestellbestaetigung.eml
  - agent/tests/fixtures/pre-deployment/07-cold-sales.eml
  - agent/tests/fixtures/pre-deployment/08-delivery-failure.eml
  - agent/tests/fixtures/pre-deployment/09-umlaut-frage.eml
  - agent/tests/fixtures/pre-deployment/10-long-mail.eml
  - agent/tests/fixtures/pre-deployment/11-multi-turn-1-frage.eml
  - agent/tests/fixtures/pre-deployment/12-multi-turn-2-rueckfrage.eml
  - agent/tests/fixtures/pre-deployment/13-multi-turn-3-detail.eml
  - agent/tests/fixtures/pre-deployment/14-multi-turn-4-bestaetigung.eml
  - agent/tests/fixtures/pre-deployment/README.md
  - .planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST.md
  - .planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST-REPORT.md
autonomous: false
requirements:
  - DEP-02
  - DEP-04
  - DEP-05
  - DEP-06
must_haves:
  truths:
    - "10 Einzel-Test-Mails als `.eml`-Fixtures existieren, decken die Kategorien Öffnungszeiten, Preis, Termin, Reklamation, Newsletter, Amazon, Cold-Sales, Delivery-Failure, UTF-8-Umlaute, Long-Mail (>2000 Zeichen) ab."
    - "4 Multi-Turn-Konversations-Mails existieren mit korrekt gesetzten References/In-Reply-To-Headern (Waschanlage-Termin: Frage → Rückfrage → Detail → Bestätigung)."
    - "`PRE-DEPLOYMENT-TEST.md` beschreibt den kompletten Test-Ablauf (Setup, 10-Mails-Batch, Multi-Turn-Batch, Reboot-Test, Prompt-Iteration, Docker-Image-Verify) mit Zeit-Estimates."
    - "`PRE-DEPLOYMENT-TEST-REPORT.md` ist eine Template-Datei mit Checkbox-Struktur, in der Vizionists nach jedem Test-Batch das Ergebnis dokumentiert (welche Draft-Qualität pro Kategorie, welche Prompt-Iterationen nötig waren, welche Bugs gefunden)."
    - "Der Vor-Test verifiziert alle 4 neuen Features aus Wave 1+2: D-16 (Bind-Mount), D-23 (Auto-Detect IONOS), D-25 (Auto-CREATE für KEA-Test-Entwürfe), D-26 (Multi-Turn-Konversation) — jedes Feature hat eine explizite Verify-Sektion im Test-Ablauf."
    - "`PRE-DEPLOYMENT-TEST-REPORT.md` enthält Sektion 'D-21 Provider-Kompatibilitäts-Check' mit Test-Account-Anleitung und Report-Schema (nur auszufüllen wenn PRE-01-Ergebnis nicht IONOS ist)."
    - "D-18: Der Vor-Test ist als Halb-Tag-End-to-End-Test (~2-4 h) bei Vizionists strukturiert und dokumentiert BEVOR das Deployment-Paket zum Kunden geht — verkuerzt den Kundentermin von 'Setup + Debug' (~2 h) auf 'Setup + Verify' (~30-45 Min)."
    - "D-19: Test-Postfach ist `shala@vizionists.com` ueber IONOS (Vizionists' eigenes Business-Mail-Konto) — `vizionists-test-env.example` enthaelt die IONOS-Werte, PRE-DEPLOYMENT-TEST.md nennt das Konto explizit im Setup-Abschnitt."
    - "D-20: Der Test-Ablauf-Katalog (10 Kategorien-Mails + Multi-Turn-Konversation) ist vollstaendig in PRE-DEPLOYMENT-TEST.md dokumentiert, inklusive Poll-Zyklus-Check, Prompt-Iteration via Bind-Mount-Restart, und Docker-Reboot-Test."
  artifacts:
    - path: "agent/tests/fixtures/pre-deployment/"
      provides: "14 .eml-Fixture-Files (10 Kategorien + 4 Multi-Turn) + README"
    - path: ".planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST.md"
      provides: "Test-Ablauf-Anleitung mit Setup, Batches, Reboot-Test, Prompt-Iteration"
      min_lines: 120
    - path: ".planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST-REPORT.md"
      provides: "Report-Template mit Checkbox-Struktur pro Kategorie + Iteration-Log + D-21 Provider-Fallback-Sektion"
      min_lines: 60
  key_links:
    - from: ".planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST.md"
      to: "agent/tests/fixtures/pre-deployment/"
      via: "Test-Ablauf referenziert die 14 .eml-Fixtures als Eingabe"
      pattern: "fixtures/pre-deployment"
    - from: ".planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST.md"
      to: "scripts/build-deployment-package.sh"
      via: "Docker-Image-Verify (DEP-02) triggert Build-Skript und misst Tarball-Größe"
      pattern: "build-deployment-package"
    - from: ".planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST.md"
      to: "deployment/vizionists-test-env.example"
      via: "Setup nutzt Vizionists-Test-Env-Template mit shala@vizionists.com + KEA-Test-Entwürfe"
      pattern: "vizionists-test-env"
---

<objective>
D-18/D-19/D-20 umsetzen: Vollständiges Pre-Deployment-Test-Paket für den halb-Tag-End-to-End-Test bei Vizionists gegen `shala@vizionists.com` über IONOS. Zweck: Alle 4 neuen Phase-2-Features (D-16 Bind-Mount, D-23 Auto-Detect, D-25 Auto-CREATE, D-26 Multi-Turn-Kontext) und die gesamte Draft-Qualität VOR dem Kundentermin verifizieren, damit der Vor-Ort-Termin von "Setup + Debug" (~2 h) auf "Setup + Verify" (~30-45 Min) verkürzt werden kann.

Purpose: Der Vor-Test ersetzt echte Kunden-Mails durch 14 sorgfältig konstruierte `.eml`-Fixtures (10 Einzelmails + 4 Multi-Turn-Konversations-Mails). Die Fixtures sind CI-freundlich versioniert (im Repo, nicht ad-hoc), und der Test-Ablauf ist so dokumentiert dass jeder in der Zukunft ihn reproduzieren kann.

Scope-Notiz: 17 files_modified sind bewusst akzeptiert — 14 davon sind zusammenhängende `.eml`-Fixtures ohne Code-Logik. Kein Split vorgenommen.

Output:
- 14 `.eml`-Fixture-Files unter `agent/tests/fixtures/pre-deployment/`
- 1 Fixtures-README das Charakter der Fixtures dokumentiert
- 1 Test-Ablauf-Dokument `PRE-DEPLOYMENT-TEST.md` mit Schritt-für-Schritt-Anleitung
- 1 Report-Template `PRE-DEPLOYMENT-TEST-REPORT.md` (inkl. D-21-Sektion)

Der Plan enthält bewusst KEINE Ausführung des Tests — die Ausführung erfolgt danach durch Vizionists (halb-Tag-Termin gemäß D-18-Aufwand). Der Plan liefert nur das Test-Artefakt-Paket.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deployment-beim-kunden/02-CONTEXT.md
@.planning/phases/02-deployment-beim-kunden/02-RESEARCH.md
@.planning/phases/02-deployment-beim-kunden/02-PATTERNS.md
@deployment/vizionists-test-env.example
@deployment/context.md.vizionists-test.md
@scripts/build-deployment-package.sh

<interfaces>
Nach Wave 2 (Plans 02.01-02.04) verfügbare Assets:

- **`scripts/build-deployment-package.sh`** — erzeugt Deployment-Paket per `bash scripts/build-deployment-package.sh v1.0.0` (siehe 02-04).
- **`deployment/vizionists-test-env.example`** — Vizionists-Test-Env mit `IMAP_USER=shala@vizionists.com`, `IMAP_DRAFTS_FOLDER=KEA-Test-Entwürfe` (bewusst neuer Ordner für Auto-CREATE-Test), `ANTHROPIC_API_KEY=<...>`.
- **`deployment/context.md.vizionists-test.md`** — Test-Kontext mit Vizionists-Firmen-Infos.
- **Auto-Detect (D-23)** verhält sich für `shala@vizionists.com`: Domain `vizionists.com` NICHT in statischer Tabelle → MX-Lookup. Falls MX auf IONOS zeigt → IONOS-Config (imap.ionos.de:993 SSL, Sent="Sent"). Falls MX auf anderen Hoster zeigt: entsprechende Config. Falls beide nicht: RuntimeError, dann muss manuell `IMAP_HOST=imap.ionos.de` in `.env` gesetzt werden.

**Wichtig für Fixtures:** Die 4 Multi-Turn-Mails müssen korrekte RFC 5322 `Message-ID`, `In-Reply-To`, `References`-Header haben, damit `fetch_thread_history()` in D-26 den Thread korrekt aufbaut. Der Thread-Root hat Message-ID `<multi-turn-root-2026-07-11@vizionists.com>`. Jede Antwort referenziert alle vorherigen Message-IDs in `References`.

**Wichtig für den Multi-Turn-Test:** Damit der Bot beim Draft-Zyklus die vorherigen Mails findet, müssen die Mails im IMAP-Postfach REIHENFOLGE-KORREKT liegen. Die Ausführung im Test-Ablauf muss die Mails NACHEINANDER senden und zwischen jeder Runde einen Poll-Zyklus abwarten.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 14 .eml-Fixtures + Fixtures-README für Pre-Deployment-Test anlegen</name>
  <files>agent/tests/fixtures/pre-deployment/01-oeffnungszeiten-frage.eml, agent/tests/fixtures/pre-deployment/02-preis-anfrage.eml, agent/tests/fixtures/pre-deployment/03-termin-anfrage.eml, agent/tests/fixtures/pre-deployment/04-reklamation.eml, agent/tests/fixtures/pre-deployment/05-newsletter.eml, agent/tests/fixtures/pre-deployment/06-amazon-bestellbestaetigung.eml, agent/tests/fixtures/pre-deployment/07-cold-sales.eml, agent/tests/fixtures/pre-deployment/08-delivery-failure.eml, agent/tests/fixtures/pre-deployment/09-umlaut-frage.eml, agent/tests/fixtures/pre-deployment/10-long-mail.eml, agent/tests/fixtures/pre-deployment/11-multi-turn-1-frage.eml, agent/tests/fixtures/pre-deployment/12-multi-turn-2-rueckfrage.eml, agent/tests/fixtures/pre-deployment/13-multi-turn-3-detail.eml, agent/tests/fixtures/pre-deployment/14-multi-turn-4-bestaetigung.eml, agent/tests/fixtures/pre-deployment/README.md</files>
  <read_first>
    - .planning/phases/02-deployment-beim-kunden/02-CONTEXT.md (§ D-20 Test-Ablauf konkret — die 10 Kategorien-Liste + Multi-Turn-Erweiterung)
    - .planning/phases/02-deployment-beim-kunden/02-RESEARCH.md (§ D-26 References/In-Reply-To-Header-Struktur für Thread-Erkennung)
    - agent/context.md.example (Referenz für was der Bot in .eml als Fragen findet — Öffnungszeiten, Preise, FAQ)
    - agent/src/draft.py (falls existiert — RFC-5322-Header-Struktur; wenn nicht: siehe RFC 5322 § 3.6.4 für References-Format)
  </read_first>
  <action>
    Jede `.eml`-Datei muss ein syntaktisch valides RFC-5322-Format haben: Header-Block (getrennt durch Leerzeile vom Body), Zeilenumbrüche als `\n` (LF reicht für imap-tools 1.13 in fetch-Modus).

    **Grundmuster jeder Einzel-Mail (Beispiel `01-oeffnungszeiten-frage.eml`):**
    ```
    From: kunde@web.de
    To: shala@vizionists.com
    Subject: Öffnungszeiten Sonntag
    Date: Sat, 11 Jul 2026 09:15:00 +0200
    Message-ID: <fixture-01-2026-07-11@web.de>
    Content-Type: text/plain; charset=UTF-8
    Content-Transfer-Encoding: 8bit

    Guten Tag,

    ab wann haben Sie am Sonntag geöffnet? Ich müsste ein paar Sachen einkaufen.

    Viele Grüße
    Max Mustermann
    ```

    **Die 10 Einzel-Fixtures** — bitte jede EIGENSTÄNDIG und realistisch schreiben (keine trivialen Copy-Paste-Varianten):

    1. **`01-oeffnungszeiten-frage.eml`** — Freundliche Öffnungszeiten-Frage für Sonntag (siehe oben). Erwartung: REPLY_NEEDED.
    2. **`02-preis-anfrage.eml`** — Frage nach Preis für Autowäsche (Waschanlage-Stufen). From: `kunde2@gmail.com`. Erwartung: REPLY_NEEDED.
    3. **`03-termin-anfrage.eml`** — Terminfrage für Werkstatt / TÜV / Reifenwechsel. From: `interessent@t-online.de`. Erwartung: REPLY_NEEDED.
    4. **`04-reklamation.eml`** — Reklamation über beschädigte Auto-Wäsche. Ton: sachlich, aber unzufrieden. From: `enttaeuscht@gmx.de`. Erwartung: REPLY_NEEDED — deeskalierender Ton erwartet.
    5. **`05-newsletter.eml`** — Werbe-Newsletter von "Deutsche Tankstellen Werbe GmbH". `List-Unsubscribe`-Header setzen, Betreff "Sonderaktion Frühling 2026". From: `newsletter@dtwg-marketing.de`. Erwartung: IGNORE.
    6. **`06-amazon-bestellbestaetigung.eml`** — Standardisierte Amazon-Bestellbestätigung "Ihre Bestellung wurde versandt". From: `auto-confirm@amazon.de`. Erwartung: IGNORE.
    7. **`07-cold-sales.eml`** — Cold-Sales-Mail von einem Software-Anbieter "Digitalisieren Sie jetzt Ihre Tankstelle". From: `sales@digital-solutions-24.de`. Erwartung: IGNORE.
    8. **`08-delivery-failure.eml`** — MTA-Bounce/Delivery-Failure-Notice. From: `MAILER-DAEMON@mail.example.com`. Betreff "Undelivered Mail Returned to Sender". Erwartung: IGNORE.
    9. **`09-umlaut-frage.eml`** — Anfrage mit vielen Umlauten und Sonderzeichen: "Können Sie mir sagen, ob Sie Ölwechsel für Fahrzeuge über 3,5 t anbieten?". Content-Type UTF-8. From: `öhler@gmx.net`. Erwartung: REPLY_NEEDED, Draft muss Umlaute korrekt encoden.
    10. **`10-long-mail.eml`** — Sehr lange Anfrage (Body > 2000 Zeichen — z. B. detaillierte Schilderung eines Reklamations-Falls mit vielen Details, Datums-Angaben, konkreten Preisen, zwei Fragen am Ende). Zweck: Truncation im Klassifikations-Prompt testen. Erwartung: REPLY_NEEDED, Draft geht auf die zwei Fragen ein.

    **Die 4 Multi-Turn-Konversations-Fixtures** — Thread "Waschanlage-Termin buchen":

    11. **`11-multi-turn-1-frage.eml`** — Erste Frage. Message-ID: `<multi-turn-2026-07-11-t1@web.de>`, KEIN In-Reply-To. Body: "Guten Tag, ich möchte am nächsten Montag Vormittag zur SB-Waschanlage kommen. Wie lange dauert eine Standard-Wäsche? Und muss ich vorher reservieren? Viele Grüße Klaus Fischer".
    12. **`12-multi-turn-2-rueckfrage.eml`** — Vom Kunden: Rückfrage NACH der (imaginären) Bot-Antwort. Message-ID: `<multi-turn-2026-07-11-t2@web.de>`, In-Reply-To: `<multi-turn-2026-07-11-t1@web.de>`, References: `<multi-turn-2026-07-11-t1@web.de>`. Body: "Danke für die Info! Eine weitere Frage: Nehmt ihr auch Karten? Ich habe leider selten Bargeld dabei. Und gibt es einen Waschgang für SUVs?"
    13. **`13-multi-turn-3-detail.eml`** — Vom Kunden: Detail-Frage. Message-ID: `<multi-turn-2026-07-11-t3@web.de>`, In-Reply-To: `<multi-turn-2026-07-11-t2@web.de>`, References: `<multi-turn-2026-07-11-t1@web.de> <multi-turn-2026-07-11-t2@web.de>`. Body: "Perfekt, dann komme ich Montag um 9:30 Uhr. Kann ich meinen Hund im Auto lassen während der Wäsche oder muss ich raussteigen mit ihm?"
    14. **`14-multi-turn-4-bestaetigung.eml`** — Vom Kunden: Bestätigung + weitere Detail-Frage. Message-ID: `<multi-turn-2026-07-11-t4@web.de>`, In-Reply-To: `<multi-turn-2026-07-11-t3@web.de>`, References: `<multi-turn-2026-07-11-t1@web.de> <multi-turn-2026-07-11-t2@web.de> <multi-turn-2026-07-11-t3@web.de>`. Body: "Alles klar, dann bis Montag 9:30. Noch eine Sache: Habt ihr auch Innenraum-Staubsauger für 2 EUR wie früher?"

    **Bei Multi-Turn:** ALLE Mails haben `From: kunde-fischer@web.de` (durchgehend derselbe Absender, sonst funktioniert die References-Kette nicht).

    **`agent/tests/fixtures/pre-deployment/README.md`** — Dokumentiert die Fixtures:
    ```markdown
    # Pre-Deployment-Test-Fixtures

    14 `.eml`-Fixtures für den Pre-Deployment-Test bei Vizionists (D-18).
    Ziel: Vor dem Vor-Ort-Termin verifizieren dass Klassifikator und Generator
    für die typischen Tankstellen-Anfrage-Muster solide arbeiten.

    ## Struktur

    | Fixture | Kategorie | Erwartete Klassifikation | Test-Fokus |
    |---------|-----------|-------------------------|------------|
    | 01-oeffnungszeiten-frage.eml | Öffnungszeiten | REPLY_NEEDED | Draft nennt Öffnungszeiten aus context.md |
    | 02-preis-anfrage.eml | Preis (Autowäsche) | REPLY_NEEDED | Draft verweist auf Preis-Aushang |
    | 03-termin-anfrage.eml | Termin (Werkstatt) | REPLY_NEEDED | Draft mit Terminvorschlag oder Anruf-Aufforderung |
    | 04-reklamation.eml | Reklamation | REPLY_NEEDED | Ton deeskalierend, Kontakt-Angebot |
    | 05-newsletter.eml | Newsletter | IGNORE | Kein Draft |
    | 06-amazon-bestellbestaetigung.eml | System-Mail | IGNORE | Kein Draft |
    | 07-cold-sales.eml | Cold-Sales | IGNORE | Kein Draft |
    | 08-delivery-failure.eml | MTA-Bounce | IGNORE | Kein Draft |
    | 09-umlaut-frage.eml | UTF-8-Encoding | REPLY_NEEDED | Umlaute im Draft korrekt |
    | 10-long-mail.eml | Long-Mail | REPLY_NEEDED | Truncation OK, Draft geht auf Kernfragen ein |
    | 11..14 | Multi-Turn-Thread | REPLY_NEEDED (alle 4) | Draft 12/13/14 kennt Kontext aus Vorgängern |

    ## Multi-Turn-Thread-Struktur (Fixtures 11-14)

    Thread-Root: `<multi-turn-2026-07-11-t1@web.de>` (Fixture 11 — ohne In-Reply-To)

    Jede Folge-Mail (Fixtures 12, 13, 14) enthält:
    - `In-Reply-To: <vorherige-message-id>`
    - `References: <alle vorherigen message-ids, chronologisch>`

    Damit die Thread-Erkennung in `imap_client.fetch_thread_history()` funktioniert,
    müssen die 4 Mails NACHEINANDER ins Test-Postfach gesendet werden, mit einem
    Poll-Zyklus dazwischen. Der Bot erzeugt zwischen jeder Runde einen Draft, der
    (ab Fixture 12) den bisherigen Verlauf berücksichtigt.

    ## Nutzung im Test

    Siehe `.planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST.md`.

    Die Mails werden am Test-Tag manuell an `shala@vizionists.com` gesendet (z. B. via
    Zweit-Account oder mit `swaks` / `msmtp` per Skript). Der Bot pollt automatisch
    und legt Drafts in `KEA-Test-Entwürfe` an.
    ```

    **Wichtig:** Alle `.eml`-Dateien nutzen `Content-Type: text/plain; charset=UTF-8` und `Content-Transfer-Encoding: 8bit` (oder `quoted-printable` bei Umlaut-Mails 09/14 falls Zeichen sonst falsch encodiert werden — 8bit ist einfacher und funktioniert auf IONOS). Zeilen sollen mit LF getrennt sein (Standard-Editor-Verhalten auf Linux/Mac; auf Windows: Editor auf "LF-only" stellen).

    **Kein automatisierter Test** — Fixtures sind Text-Assets, valid ist "existiert und ist parsable durch `email.message_from_bytes()`".
  </action>
  <verify>
    <automated>ls agent/tests/fixtures/pre-deployment/*.eml | wc -l | grep -q "^14$" && test -f agent/tests/fixtures/pre-deployment/README.md && python -c "import email; [email.message_from_file(open(f'agent/tests/fixtures/pre-deployment/{n:02d}-'+n2+'.eml')) for n,n2 in [(1,'oeffnungszeiten-frage'),(2,'preis-anfrage'),(3,'termin-anfrage'),(4,'reklamation'),(5,'newsletter'),(6,'amazon-bestellbestaetigung'),(7,'cold-sales'),(8,'delivery-failure'),(9,'umlaut-frage'),(10,'long-mail'),(11,'multi-turn-1-frage'),(12,'multi-turn-2-rueckfrage'),(13,'multi-turn-3-detail'),(14,'multi-turn-4-bestaetigung')]]; print('all_parse_ok')" | grep -q all_parse_ok && python -c "import email; msg=email.message_from_file(open('agent/tests/fixtures/pre-deployment/13-multi-turn-3-detail.eml')); assert 'multi-turn-2026-07-11-t1@web.de' in msg['References']"</automated>
  </verify>
  <acceptance_criteria>
    - Genau 14 `.eml`-Files existieren in `agent/tests/fixtures/pre-deployment/`.
    - Jede Datei ist ein syntaktisch valides RFC-5322-Message (parsable durch `email.message_from_file()`).
    - `10-long-mail.eml` hat Body-Länge > 2000 Zeichen. Prüfbar via `python -c "import email; msg=email.message_from_file(open('agent/tests/fixtures/pre-deployment/10-long-mail.eml')); body=msg.get_payload(); assert len(body) > 2000"`.
    - Fixtures 11-14 haben durchgehend denselben Absender. Prüfbar via `python -c "import email; froms=[email.message_from_file(open(f'agent/tests/fixtures/pre-deployment/{i:02d}-multi-turn-{j}-{k}.eml'))['From'] for i,j,k in [(11,1,'frage'),(12,2,'rueckfrage'),(13,3,'detail'),(14,4,'bestaetigung')]]; assert len(set(froms)) == 1, froms"`.
    - Fixture 13 (`multi-turn-3-detail`) referenziert Fixture 11 (`t1`) in References. Prüfbar via `grep -c "multi-turn-2026-07-11-t1@web.de" agent/tests/fixtures/pre-deployment/13-multi-turn-3-detail.eml` liefert ≥ 1.
    - `README.md` existiert mit Tabelle die alle 14 Fixtures listet. Prüfbar via `grep -c "REPLY_NEEDED\|IGNORE" agent/tests/fixtures/pre-deployment/README.md` liefert ≥ 12.
    - UTF-8-Umlaute in Fixture 09 sind korrekt kodiert. Prüfbar via `grep "Ölwechsel" agent/tests/fixtures/pre-deployment/09-umlaut-frage.eml`.
  </acceptance_criteria>
  <done>
    14 .eml-Fixtures existieren, alle parsable, Multi-Turn-Thread (11-14) mit korrekten References/In-Reply-To-Ketten, README dokumentiert Kategorien und Erwartungen.
  </done>
</task>

<task type="auto">
  <name>Task 2: PRE-DEPLOYMENT-TEST.md (Ablauf-Anleitung) und PRE-DEPLOYMENT-TEST-REPORT.md (Report-Template inkl. D-21-Sektion) anlegen</name>
  <files>.planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST.md, .planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST-REPORT.md</files>
  <read_first>
    - .planning/phases/02-deployment-beim-kunden/02-CONTEXT.md (§ D-18/D-19/D-20 — Halb-Tag-Aufwand, Test-Postfach, konkreter Test-Ablauf mit 9 Schritten; § D-21 — Provider-Fallback-Check-Anforderung)
    - .planning/phases/02-deployment-beim-kunden/02-RESEARCH.md (§ Preflight-Checks — verifizierte Kommandos für Docker-Version/RAM/Disk/IMAP-Erreichbarkeit)
    - deployment/vizionists-test-env.example (nach Plan 02.04 — Test-Config die genutzt wird)
    - deployment/context.md.vizionists-test.md (nach Plan 02.04 — Test-Kontext den der Bot nutzt)
    - agent/tests/fixtures/pre-deployment/README.md (aus Task 1 — welche Fixtures wie eingesetzt werden)
  </read_first>
  <action>
    Zwei Dokumente anlegen:

    **1. `.planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST.md`** — Schritt-für-Schritt-Anleitung (≥ 120 Zeilen). Struktur:

    ```markdown
    # Pre-Deployment-Test bei Vizionists

    **Zweck:** Alle Phase-2-Features (D-16 Bind-Mount, D-23 Auto-Detect, D-25 Auto-CREATE, D-26 Multi-Turn) und die grundsätzliche Draft-Qualität VOR dem Kundentermin gegen ein reales IMAP-Postfach verifizieren.

    **Umgebung:** Vizionists-Laptop (Docker installiert) → `shala@vizionists.com` (IONOS-Business-Mail-Konto).

    **Aufwand:** ~2–4 h (D-18).

    **Voraussetzungen:**
    - Docker + Docker Compose Plugin installiert.
    - IONOS-App-Password für `shala@vizionists.com` bereit (aus IONOS Kundencenter → E-Mail → App-Passwörter).
    - Anthropic-API-Key mit Guthaben.
    - Ein Zweit-Postfach (z. B. Handy-Mail, Gmail-Testaccount) für Test-Mails.
    - Optional: `swaks` oder `msmtp` installiert, um `.eml`-Fixtures per Skript zu senden (Alternative: Fixtures manuell öffnen, Inhalt kopieren, aus Zweit-Postfach senden).

    ## Zeitplan (halb-Tag)

    | Phase | Dauer | Aktivität |
    |-------|-------|-----------|
    | 1. Build + Setup | 20 Min | Docker-Image bauen, Deployment-Paket, `.env` + `context.md` anlegen |
    | 2. Einzel-Mails (Fixtures 1-10) | 60-90 Min | Nacheinander senden, Drafts prüfen, ggf. Prompts iterieren |
    | 3. Multi-Turn-Konversation (Fixtures 11-14) | 40-60 Min | 4 Runden mit Poll-Zyklen dazwischen |
    | 4. Reboot-Test + Docker-Verify | 15 Min | Container-Neustart, State-DB-Persistenz, `sudo reboot`-Simulation |
    | 5. Report ausfüllen | 15 Min | `PRE-DEPLOYMENT-TEST-REPORT.md` finalisieren, Bugs dokumentieren |

    ## Setup (Phase 1)

    ### Docker-Image bauen (DEP-02-Verify)

    ```bash
    cd /path/to/kiemailagent
    bash scripts/build-deployment-package.sh v1.0.0
    ls -lh dist/deployment-paket-v1.0.0/
    ```

    Erwartung:
    - Tarball `kea-tankstelle-v1.0.0.tar` existiert, Größe zwischen 180 und 250 MB.
    - `.sha256`-Datei daneben.
    - `deployment/`-Ordner mit 4 Templates.
    - `prompts/`-Ordner mit `classify.txt`, `generate.txt`.

    Report-Feld ausfüllen: Image-Größe [_____], Build-Dauer [_____].

    ### Test-Verzeichnis vorbereiten

    ```bash
    mkdir -p /tmp/kea-test
    cd /tmp/kea-test
    cp /path/to/kiemailagent/dist/deployment-paket-v1.0.0/* .
    cp -r /path/to/kiemailagent/dist/deployment-paket-v1.0.0/deployment ./deployment
    cp -r /path/to/kiemailagent/dist/deployment-paket-v1.0.0/prompts ./prompts
    docker load -i kea-tankstelle-v1.0.0.tar
    docker images | grep kea-tankstelle    # Verify Image ist geladen
    ```

    ### `.env` befüllen

    ```bash
    cp deployment/vizionists-test-env.example .env
    chmod 600 .env
    nano .env
    # IMAP_PASSWORD und ANTHROPIC_API_KEY eintragen. Rest bleibt wie im Template.
    # IMAP_DRAFTS_FOLDER=KEA-Test-Entwürfe (bewusst NEUER Ordner — Auto-CREATE-Test)
    ```

    ### `context.md` erstellen

    ```bash
    cp deployment/context.md.vizionists-test.md context.md
    ```

    ### Container starten

    ```bash
    docker compose up -d
    docker compose logs -f agent
    ```

    Erwartung:
    - `startup`-Log-Event mit `imap_host=imap.ionos.de` (D-23 Auto-Detect verifiziert).
    - `imap_connected`-Event ohne Auth-Fehler.
    - `poll_start` innerhalb 5 Sekunden.
    - `poll_done processed=0` (noch keine Test-Mails geschickt).

    ## Einzel-Mails (Phase 2)

    Für jede der 10 Einzel-Fixtures (`01-oeffnungszeiten-frage.eml` bis `10-long-mail.eml`):

    1. Fixture-Inhalt aus `agent/tests/fixtures/pre-deployment/NN-<name>.eml` extrahieren (Body-Text).
    2. Vom Zweit-Postfach eine Mail an `shala@vizionists.com` schicken mit Subject + Body aus der Fixture.
    3. Warten (max. 5 Min = 1 Poll-Zyklus).
    4. IONOS-Webmail öffnen → Ordner `KEA-Test-Entwürfe` prüfen:
       - **Erste Fixture:** Ordner muss automatisch angelegt worden sein (D-25 Auto-CREATE). Wenn `docker compose logs` das Event `drafts_folder_created` zeigt: ✅.
       - Erwartete Draft-Existenz gemäß README-Tabelle (REPLY_NEEDED oder IGNORE).
       - Bei REPLY_NEEDED: Threading prüfen (Draft ist Reply auf Original), Ton prüfen, Fakten prüfen (Öffnungszeiten aus context.md).
       - Bei IGNORE: KEIN Draft im Ordner.
    5. Report-Zeile pro Fixture ausfüllen (siehe REPORT-Template).

    **Bei schlechter Draft-Qualität:** `nano prompts/generate.txt` (oder `prompts/classify.txt`), dann `docker compose restart agent`. Rebuild NICHT nötig (Bind-Mount, D-16 verifiziert). Erneut Test-Mail schicken, neue Draft-Qualität prüfen. **Iterations-Zyklus:** je ~5 Sek Restart + 5 Min Poll = ~5 Min pro Prompt-Iteration.

    ## Multi-Turn-Konversation (Phase 3)

    Fixtures 11-14 nacheinander senden, mit ≥ 6 Min Warte-Zeit zwischen jeder (damit der Bot Zeit hat den vorherigen Draft anzulegen, den nächster Fetch-Zyklus als Sent-Kontext lesen kann):

    1. Fixture 11 senden → Poll-Zyklus abwarten → Draft in `KEA-Test-Entwürfe` prüfen. **Manuell den Draft absenden** (aus IONOS-Webmail, "Draft öffnen → Senden") ODER Draft ins Sent-Postfach kopieren. → das macht den Bot-Antwort-Kontext für Runde 2 verfügbar.
    2. Fixture 12 (Rückfrage) senden. Wichtig: der Client (Handy/Zweit-Account) muss die Mail als **Antwort auf Fixture 11** schicken — mit korrekten In-Reply-To/References-Headern. Das erreicht man am einfachsten durch "Antworten"-Button auf die vom Bot gesendete Antwort auf Fixture 11.
    3. Poll-Zyklus abwarten → Draft für Fixture 12 in `KEA-Test-Entwürfe` prüfen. **Kritischer Verify (D-26):** enthält der Draft Bezug auf Fixture-11-Inhalt? Erwähnt der Bot Bezug zur ursprünglichen Waschanlage-Frage? Wenn ja: ✅ Multi-Turn-Kontext funktioniert. Bot-Draft senden.
    4. Fixture 13 (Detail) senden als Antwort → Draft prüfen. Bot sollte jetzt Kontext aus Runden 1+2 haben.
    5. Fixture 14 (Bestätigung) senden als Antwort → Draft prüfen. Bot sollte Kontext aus Runden 1+2+3 haben.

    **Erwartung:** Ab Draft 12 nimmt der Bot Bezug auf frühere Runden ("wie besprochen", "zusätzlich zu Ihrer vorherigen Frage", etc.). Der Draft-Text darf NICHT den kompletten Verlauf wiederholen (Prompt-Anweisung), aber die inhaltliche Kontinuität muss sichtbar sein.

    ## Reboot-Test (Phase 4)

    ```bash
    # State-DB-Persistenz verifizieren:
    docker compose stop
    docker compose up -d
    docker compose logs -f agent | head -20
    # Erwartung: keine erneute Draft-Erzeugung auf bereits verarbeitete Test-Mails (Dedup via message_id in state.db).

    # Vollständiger Docker-Restart (simuliert sudo reboot):
    docker compose down       # Container weg, Volume bleibt
    docker compose up -d      # Kein --build nötig (D-16 verifiziert)
    docker compose logs -f agent | head -30
    # Erwartung: startup + imap_connected + poll_start ohne Fehler, keine Doppel-Drafts.
    ```

    Report-Feld: State-DB überlebt Container-Restart? [ ] Ja  [ ] Nein
    Report-Feld: State-DB überlebt `docker compose down`? [ ] Ja  [ ] Nein
    Report-Feld: `docker compose up -d` ohne `--build` funktioniert? [ ] Ja  [ ] Nein

    ## Cleanup (nach Test)

    ```bash
    docker compose down -v         # Volumes löschen (State-DB weg)
    docker rmi kea-tankstelle:v1.0.0  # Image entfernen wenn nicht mehr gebraucht
    rm -rf /tmp/kea-test
    # IONOS-Postfach: KEA-Test-Entwürfe-Ordner leeren oder löschen
    # State-DB (in Docker-Volume) wird durch `down -v` entfernt
    ```

    ## Abbruch-Kriterien

    Wenn im Test eines der folgenden auftritt, den Test STOPPEN und Bug fixen bevor der Vor-Ort-Termin stattfindet:

    - Auth-Fehler beim IMAP-Login (`imap_connected` erscheint nicht) → App-Password prüfen
    - `resolve_imap_config` wirft RuntimeError für `vizionists.com` → statische Provider-Tabelle oder MX-Patterns erweitern (vizionists.com nutzt IONOS)
    - `drafts_folder_created` erscheint NICHT beim ersten Draft → D-25 nicht funktional, imap_client.py-Fix nötig
    - Multi-Turn-Draft 12+ hat KEINEN Kontext aus Runde 1 → D-26 nicht funktional, `fetch_thread_history` prüfen
    - Draft-Qualität nach 3 Prompt-Iterationen immer noch < 60% brauchbar → Prompt-Rewrite nötig
    ```

    **2. `.planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST-REPORT.md`** — Report-Template (Vizionists füllt das nach Test aus). Das Template enthält ALLE Sektionen inklusive D-21-Sektion am Ende:

    ```markdown
    # Pre-Deployment-Test-Report

    **Datum:** _______________
    **Tester:** _______________
    **Docker-Version:** _______________
    **Image-Version:** kea-tankstelle:v1.0.0
    **Test-Postfach:** shala@vizionists.com (IONOS)
    **Test-Drafts-Ordner:** KEA-Test-Entwürfe

    ## Setup-Ergebnisse (Phase 1)

    - [ ] `docker build` erfolgreich, Image-Größe: _______ MB
    - [ ] `docker save`-Tarball erzeugt, Größe: _______ MB
    - [ ] SHA256-Checksum-Datei existiert
    - [ ] `.env` mit Vizionists-Test-Werten befüllt, `chmod 600`
    - [ ] `docker compose up -d` erfolgreich
    - [ ] Log-Event `startup` mit `imap_host=imap.ionos.de` sichtbar (D-23 Auto-Detect ✅)
    - [ ] Log-Event `imap_connected` ohne Auth-Fehler
    - [ ] `poll_start` innerhalb 5 Sek nach Container-Start

    ## Einzel-Mails-Ergebnisse (Phase 2)

    | # | Fixture | Erwartet | Draft entstanden? | Qualität (1-5) | Prompt-Iteration nötig? | Anmerkung |
    |---|---------|----------|-------------------|----------------|-------------------------|-----------|
    | 01 | oeffnungszeiten-frage | REPLY_NEEDED | [ ] Ja [ ] Nein | ___/5 | [ ] Ja [ ] Nein | |
    | 02 | preis-anfrage | REPLY_NEEDED | [ ] Ja [ ] Nein | ___/5 | [ ] Ja [ ] Nein | |
    | 03 | termin-anfrage | REPLY_NEEDED | [ ] Ja [ ] Nein | ___/5 | [ ] Ja [ ] Nein | |
    | 04 | reklamation | REPLY_NEEDED | [ ] Ja [ ] Nein | ___/5 | [ ] Ja [ ] Nein | Deeskalation OK? |
    | 05 | newsletter | IGNORE | [ ] Nein (korrekt) [ ] Ja (Fehler) | — | — | |
    | 06 | amazon-bestellbestaetigung | IGNORE | [ ] Nein (korrekt) [ ] Ja (Fehler) | — | — | |
    | 07 | cold-sales | IGNORE | [ ] Nein (korrekt) [ ] Ja (Fehler) | — | — | |
    | 08 | delivery-failure | IGNORE | [ ] Nein (korrekt) [ ] Ja (Fehler) | — | — | |
    | 09 | umlaut-frage | REPLY_NEEDED | [ ] Ja [ ] Nein | ___/5 | [ ] Ja [ ] Nein | Umlaute im Draft OK? |
    | 10 | long-mail | REPLY_NEEDED | [ ] Ja [ ] Nein | ___/5 | [ ] Ja [ ] Nein | Beide Kern-Fragen adressiert? |

    **Zusammenfassung Einzel-Mails:**
    - REPLY_NEEDED korrekt: __/6
    - IGNORE korrekt: __/4
    - Draft-Qualität ⌀: ___/5
    - Prompt-Iterationen gesamt: _____

    ## Multi-Turn-Ergebnisse (Phase 3)

    | # | Fixture | Kontext aus Vorgängern erkannt? | Draft-Qualität | Anmerkung |
    |---|---------|---------------------------------|----------------|-----------|
    | 11 | multi-turn-1-frage | (n/a — erste Mail) | ___/5 | |
    | 12 | multi-turn-2-rueckfrage | [ ] Ja (Kontext F11) [ ] Nein | ___/5 | Bezug auf Waschanlage-Frage aus F11? |
    | 13 | multi-turn-3-detail | [ ] Ja (Kontext F11+F12) [ ] Nein | ___/5 | Kennt Bot bereits Karten-Zahlung + SUV? |
    | 14 | multi-turn-4-bestaetigung | [ ] Ja (Kontext F11+F12+F13) [ ] Nein | ___/5 | Kennt Bot Hunde-Thema? |

    **D-26-Verify:** Multi-Turn-Kontext funktioniert wie spezifiziert? [ ] Ja [ ] Nein

    ## Reboot-Test (Phase 4)

    - [ ] `docker compose stop && docker compose up -d`: keine Doppel-Drafts auf bereits processed Mails
    - [ ] `docker compose down && docker compose up -d`: State-DB überlebt (agent-data-Volume)
    - [ ] Kein `--build` bei `up -d` nötig (D-16 verifiziert)
    - [ ] Container-`Up`-Status wieder gegeben

    ## Prompt-Iterationen-Log

    | Iteration | Datei | Änderungs-Grund | Ergebnis |
    |-----------|-------|-----------------|----------|
    | 1 | prompts/generate.txt | z. B. Ton zu formell | verbessert um X |
    | 2 | prompts/classify.txt | z. B. FN auf Reklamation | fixed |
    | ... | | | |

    ## Bugs / Auffälligkeiten

    - _______________________
    - _______________________

    ## D-21 Provider-Kompatibilitäts-Check

    **Wann ausfüllen:** Nur wenn das PRE-01-Ergebnis (Kunden-Provider-Klärung) ergeben hat, dass der Kunden-Provider NICHT IONOS ist. Wenn IONOS: diese Sektion überspringen (der IONOS-Test oben deckt das ab).

    **Ziel (per D-21):** Vor dem Vor-Ort-Termin einen kostenlosen Test-Account beim tatsächlichen Kunden-Provider anlegen und folgendes verifizieren: IMAP-Login, 1 Test-Mail-Zyklus (Klassifikation + Draft), korrekter Drafts-Ordner-Name.

    **Schritt-für-Schritt:**

    1. Kostenlosen Test-Account beim Kunden-Provider anlegen (z. B. Gmail, GMX, Outlook.com — je nach PRE-01-Ergebnis).
    2. Test-`.env` anlegen (Kopie der `deployment/vizionists-test-env.example` mit Test-Account-Credentials):
       ```
       IMAP_USER=<test-account@provider.de>
       IMAP_PASSWORD=<test-password>
       IMAP_DRAFTS_FOLDER=KEA-Test-Entwuerfe
       # IMAP_HOST, IMAP_PORT, IMAP_USE_SSL weglassen → Auto-Detect (D-23) testen
       ```
    3. Container gegen Test-Account starten:
       ```bash
       docker compose up -d
       docker compose logs -f agent | head -30
       # Erwartung: startup mit korrekt auto-detektierten Host/Port/SSL für Provider
       ```
    4. 1 Test-Mail vom Zweit-Account an Test-Account schicken (Öffnungszeiten-Frage).
    5. Poll-Zyklus abwarten, Draft in `KEA-Test-Entwuerfe` prüfen.
    6. Drafts-Ordner-Name protokollieren: Welchen Namen hat der Ordner tatsächlich im Webmail des Providers?
       (z. B. Gmail = `[Gmail]/Drafts`, GMX = `Entwürfe`, Outlook = `Drafts`). Falls abweichend von `KEA-Test-Entwuerfe` → `IMAP_DRAFTS_FOLDER` im RUNBOOK für diesen Provider dokumentieren.

    **Report-Felder:**

    - Kunden-Provider laut PRE-01: _______________
    - [ ] Test-Account angelegt
    - [ ] Auto-Detect (D-23) hat korrekt erkannt: `imap_host=` _______________, `imap_port=` ___
    - [ ] IMAP-Login erfolgreich (kein `auth_failed`)
    - [ ] 1 Test-Mail-Zyklus: Draft in `KEA-Test-Entwuerfe` entstanden
    - [ ] Drafts-Ordner-Name im Provider-Webmail: _______________
    - [ ] Drafts-Ordner-Name stimmt mit `IMAP_DRAFTS_FOLDER`-Konfig überein (oder Abweichung dokumentiert)
    - Anmerkungen / Abweichungen: _______________

    **D-21-Status:** [ ] Bestanden  [ ] Nicht bestanden — Blocker: _______________

    ## Freigabe für Vor-Ort-Termin

    - [ ] Alle Setup-Punkte ✅
    - [ ] Einzel-Mails: ≥ 8/10 korrekt klassifiziert, Draft-Qualität ⌀ ≥ 3.5/5
    - [ ] Multi-Turn: mindestens 3 von 3 Folgemails (F12/F13/F14) mit korrektem Kontextbezug
    - [ ] Reboot-Test: alle 4 Punkte ✅
    - [ ] D-21 Provider-Check: bestanden (oder N/A weil IONOS)
    - [ ] Keine offenen Bugs

    **Bereit für Vor-Ort-Termin?** [ ] Ja  [ ] Nein — Blocker: __________

    **Freigegeben durch:** _______________ am _______________
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST.md && test -f .planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST-REPORT.md && wc -l .planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST.md | awk '{if ($1 < 100) exit 1}' && grep -c "D-16\|D-23\|D-25\|D-26" .planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST.md && grep -c "REPLY_NEEDED\|IGNORE" .planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST-REPORT.md && grep -c "D-21" .planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST.md` existiert, > 100 Zeilen. Prüfbar via `wc -l < .planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST.md` > 100.
    - Das Dokument referenziert alle 4 Phase-2-Decisions (D-16, D-23, D-25, D-26). Prüfbar via `grep -c "D-16\|D-23\|D-25\|D-26" .planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST.md` liefert >= 4.
    - Das Dokument enthält die 5 Phasen (Setup, Einzel-Mails, Multi-Turn, Reboot-Test, Cleanup) als Überschriften. Prüfbar via `grep -c "^## " .planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST.md` liefert >= 5.
    - Der Test-Ablauf referenziert `bash scripts/build-deployment-package.sh` und `vizionists-test-env.example` und `KEA-Test-Entwürfe`. Prüfbar via `grep -c "build-deployment-package\|vizionists-test-env\|KEA-Test-Entwürfe" .planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST.md` liefert >= 3.
    - `.planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST-REPORT.md` existiert mit Checkbox-Struktur (Zeilen mit `- [ ]`). Prüfbar via `grep -c "^\s*- \[ \]" .planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST-REPORT.md` liefert >= 10.
    - Report referenziert Fixture-Namen (oeffnungszeiten-frage etc.). Prüfbar via `grep -c "oeffnungszeiten\|preis-anfrage\|multi-turn" .planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST-REPORT.md` liefert >= 5.
    - Report enthält Freigabe-Kriterien mit numerischen Schwellenwerten (>= 8/10, >= 3.5/5). Prüfbar via `grep -c "8/10\|3.5/5" .planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST-REPORT.md` liefert >= 1.
    - Report enthält D-21-Sektion mit Anleitung zum Provider-Kompatibilitäts-Check. Prüfbar via `grep -c "D-21" .planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST-REPORT.md` liefert >= 3.
  </acceptance_criteria>
  <done>
    PRE-DEPLOYMENT-TEST.md dokumentiert Setup (Build + Load + Env + Container-Start), 10 Einzel-Mails-Ablauf, 4-Runden-Multi-Turn-Ablauf, Reboot-Test und Cleanup mit expliziten D-16/D-23/D-25/D-26-Verify-Punkten; PRE-DEPLOYMENT-TEST-REPORT.md hat Checkbox-Struktur, Fixture-Tabelle, Prompt-Iterationen-Log, D-21-Provider-Kompatibilitäts-Check-Sektion und Freigabe-Kriterien.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vizionists-Laptop -> IONOS-Postfach | IMAP-Verbindung mit App-Password ueberquert Provider-Boundary |
| Zweit-Postfach -> shala@vizionists.com | Test-Mails koennen absichtlich Prompt-Injection versuchen (Test-Vektor) |
| Bot -> Anthropic-API | Test-Mail-Content wird an LLM uebermittelt (in AVV/DSGVO-Rahmen) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.05-01 | Information Disclosure | Test-Mail-Content wird an Anthropic uebermittelt | accept | Test-Mails enthalten keine echten Kunden-PII. Vizionists-Test-Kontext hat generische Firmen-Infos. |
| T-02.05-02 | Spoofing | Zweit-Postfach koennte Fake-References-Header setzen (Prompt-Injection-Vektor) | mitigate | Multi-Turn-Fixtures 11-14 haben KONTROLLIERTE Message-IDs. Test-Ablauf empfiehlt Nutzung des "Antworten"-Buttons - Client setzt References automatisch korrekt. |
| T-02.05-03 | Tampering | IONOS-App-Password Leak im Test-Terminal-History | mitigate | Runbook empfiehlt `.env` per `nano` zu befuellen, nicht per Kommandozeilen-`export`. `.env` mit `chmod 600`. Terminal-History enthaelt keine Secrets. |
| T-02.05-SC | Tampering | Docker-Image-Tarball vor Test | mitigate | SHA256 aus scripts/build-deployment-package.sh (Plan 02.04) wird im Test-Setup validiert. |
</threat_model>

<verification>
Automatisierte Verifikation nur der Plan-Artefakte:

```bash
# Alle 14 Fixtures existieren und sind parsable:
python -c "import email; [email.message_from_file(open(f'agent/tests/fixtures/pre-deployment/{f}')) for f in __import__('os').listdir('agent/tests/fixtures/pre-deployment') if f.endswith('.eml')]"

# Multi-Turn-Thread-Struktur:
python -c "import email; msg=email.message_from_file(open('agent/tests/fixtures/pre-deployment/14-multi-turn-4-bestaetigung.eml')); assert msg['References'].count('multi-turn-2026-07-11-t') >= 3"

# Test-Dokumente existieren:
test -f .planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST.md
test -f .planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST-REPORT.md

# D-21-Sektion vorhanden:
grep -c "D-21" .planning/phases/02-deployment-beim-kunden/PRE-DEPLOYMENT-TEST-REPORT.md
```

**Manuelle Verifikation:** Der Test selbst wird VON VIZIONISTS AUSGEFUEHRT nach Abschluss dieses Plans. Ergebnisse werden in `PRE-DEPLOYMENT-TEST-REPORT.md` dokumentiert. Der Plan liefert nur die Vorbereitung (Fixtures + Doku + Template).
</verification>

<success_criteria>
1. 14 `.eml`-Fixtures existieren, alle parsable, alle mit realistischen Kunden-Anfragen bzw. Newsletter-/System-Mails.
2. Multi-Turn-Thread (11-14) hat korrekte In-Reply-To/References-Header, alle vom gleichen Absender.
3. `PRE-DEPLOYMENT-TEST.md` beschreibt vollstaendigen Test-Ablauf mit Setup, 10-Mails-Batch, Multi-Turn-Batch, Reboot-Test, Cleanup und Abbruch-Kriterien.
4. `PRE-DEPLOYMENT-TEST-REPORT.md` ist als ausfuellbares Template mit Fixture-Tabelle, Multi-Turn-Bewertung, Prompt-Iterationen-Log, D-21-Provider-Kompatibilitaets-Check-Sektion und Freigabe-Kriterien angelegt.
5. Alle 4 neuen Phase-2-Features (D-16/D-23/D-25/D-26) werden im Test explizit verifiziert.
6. D-21-Sektion in Report-Template erklaert wann und wie der Provider-Fallback-Check durchzufuehren ist (nur wenn PRE-01-Ergebnis nicht IONOS ist).
</success_criteria>

<output>
Create `.planning/phases/02-deployment-beim-kunden/02.05-SUMMARY.md` when done - dokumentiere, dass die Test-Artefakte fertig sind und die Ausfuehrung ein separater manueller Halb-Tag-Termin durch Vizionists ist.
</output>
