---
phase: 02-deployment-beim-kunden
plan: 04
plan_id: 02.04-docker-bindmount-deployment-package
type: execute
wave: 2
depends_on:
  - 02.01-auto-provider-detection
  - 02.02-auto-create-drafts-folder
  - 02.03-conversation-history-live-imap
files_modified:
  - agent/Dockerfile
  - agent/docker-compose.yml
  - agent/.env.example
  - agent/README.md
  - scripts/build-deployment-package.sh
  - deployment/kunde-env.example
  - deployment/vizionists-test-env.example
  - deployment/context.md.tankstelle-erstversion.md
  - deployment/context.md.vizionists-test.md
autonomous: true
requirements:
  - DEP-01
  - DEP-02
  - DEP-03
must_haves:
  truths:
    - "Der Docker-Build erzeugt ein Image ohne einkopierte prompts/ — prompts werden zur Laufzeit via Bind-Mount hereingegeben."
    - "docker-compose.yml nutzt `image: kea-tankstelle:v1.0.0` statt `build: .` — kein Rebuild am Kundenserver."
    - "docker-compose.yml mountet `./prompts:/app/prompts:ro` als Bind-Mount — Prompt-Iteration ohne Rebuild möglich."
    - "`.env.example` hat IMAP_HOST/PORT/USE_SSL/SENT_FOLDER als optional-Overrides auskommentiert; Pflichtfelder sind reduziert auf IMAP_USER, IMAP_PASSWORD, IMAP_DRAFTS_FOLDER, OWN_EMAIL_ADDRESS, ANTHROPIC_API_KEY."
    - "Ein POSIX-Shell-Skript `scripts/build-deployment-package.sh` erzeugt einen Deployment-Ordner mit Tarball + Compose + Templates + 2× env.example + 2× context.md-Vorlage + Prompts + README."
    - "`agent/README.md` beschreibt Tarball-Delivery statt `git clone` (`docker load` + `.env` + `context.md` + `docker compose up -d`)."
    - "Zwei Deployment-Config-Trennung: `kunde-env.example` (Platzhalter) und `vizionists-test-env.example` (IONOS-konkret) existieren."
    - "Zwei context.md-Trennung: `context.md.tankstelle-erstversion.md` (OSINT-Rohbau) und `context.md.vizionists-test.md` (Vizionists-Test-Kontext) existieren."
    - "D-04: Delivery erfolgt per `docker save`/`docker load` Tarball statt `git clone` — Kundenserver braucht keinen github.com-Zugriff, nur ausgehende HTTPS zu api.anthropic.com und IMAP-Host. `scripts/build-deployment-package.sh` erzeugt das Tarball reproduzierbar auf dem Vizionists-Laptop."
    - "D-22: Deployment-Paket enthaelt Zwei-Konfig-Trennung — `deployment/vizionists-test-env.example` und `deployment/kunde-env.example` fuer die zwei Env-Rollen; `deployment/context.md.vizionists-test.md` und `deployment/context.md.tankstelle-erstversion.md` fuer die zwei context.md-Rollen."
    - "D-03: [informational] GitHub-Repo `vizionists/kea-tankstelle` bleibt oeffentlich als Vizionists-Build-Quelle — nicht Delivery-Kanal an den Kunden (Kundenserver braucht keinen github.com-Zugriff, siehe D-04). Kein Phase-2-Task."
    - "D-06: [informational] Der Phase-1-REQUIREMENT-Update-Vermerk fuer `DEL-08` (privates → oeffentliches Repo) ist bereits in `.planning/REQUIREMENTS.md` verankert; kein Phase-2-Task."
    - "D-07: [informational] Update-Workflow-Details (Prompt-Aenderung via SSH-Restart, context.md-Aenderung via Bind-Mount, Code-Aenderung via Tarball) sind Phase-3-Content — Phase 2 legt nur die Bind-Mount-Fundamente (D-16) und das Tarball-Build-Skript (D-04)."
  artifacts:
    - path: "agent/Dockerfile"
      provides: "Image-Build ohne COPY prompts/"
      contains: "python:3.13-slim"
    - path: "agent/docker-compose.yml"
      provides: "image: kea-tankstelle:v1.0.0 + prompts-Bind-Mount"
      contains: "image: kea-tankstelle:v1.0.0"
    - path: "agent/.env.example"
      provides: "IMAP_HOST/PORT/USE_SSL/SENT_FOLDER als auskommentierte Optional-Overrides; 5 Pflichtfelder prominent"
    - path: "scripts/build-deployment-package.sh"
      provides: "One-Command Deployment-Paket-Builder (docker save + Ordner-Struktur)"
      min_lines: 40
    - path: "deployment/kunde-env.example"
      provides: "Kunden-Template mit Platzhaltern"
    - path: "deployment/vizionists-test-env.example"
      provides: "Vizionists-Test-Config mit IONOS-Werten für shala@vizionists.com"
    - path: "deployment/context.md.tankstelle-erstversion.md"
      provides: "OSINT-Rohbau für Tankstelle mit Sektionsstruktur"
    - path: "deployment/context.md.vizionists-test.md"
      provides: "Vizionists-Test-Kontext für Pre-Deployment-Test"
    - path: "agent/README.md"
      provides: "Setup-Anleitung basiert auf Tarball-Delivery (docker load), nicht mehr auf git clone"
      contains: "docker load"
  key_links:
    - from: "agent/docker-compose.yml"
      to: "agent/prompts/"
      via: "Bind-Mount ./prompts:/app/prompts:ro — Prompt-Iteration ohne Rebuild"
      pattern: "\\./prompts:/app/prompts:ro"
    - from: "scripts/build-deployment-package.sh"
      to: "docker save kea-tankstelle:v1.0.0"
      via: "docker save erzeugt Tarball für USB-Delivery"
      pattern: "docker save"
    - from: "agent/.env.example"
      to: "agent/src/provider_config.py"
      via: "IMAP_HOST wird optional; Auto-Detect aus IMAP_USER-Domain per D-23"
      pattern: "IMAP_HOST=.*# Optional|# IMAP_HOST"
---

<objective>
Zwei Ziele in einem Plan (Wave 2 nach 02.01+02.02+02.03):

1. **D-16 umsetzen**: Docker-Image-Modus umstellen — `prompts/` wird zur Laufzeit gemountet statt eingebaut, `docker-compose.yml` nutzt `image:` statt `build:`. Ergebnis: Prompt-Iteration in Phase 3 braucht keinen Rebuild.

2. **D-04 + D-22 umsetzen**: Deployment-Paket-Builder-Skript und die zwei-fache Konfig-Trennung (Kunde vs. Vizionists-Test) anlegen. Ergebnis: `bash scripts/build-deployment-package.sh` erzeugt ein USB-taugliches Verzeichnis mit Tarball + Compose + Templates + Prompts + README. Zusätzlich: `agent/.env.example` und `agent/README.md` reflektieren die neue Struktur.

Purpose: Vor-Ort-Setup beim Kunden wird auf `docker load` + `.env` + `context.md` + `docker compose up -d` reduziert. Kein Build, kein Git, kein PyPI. Prompt-Iteration in Phase 3 dauert Sekunden statt Minuten.

Output: 3 modifizierte Agent-Dateien (Dockerfile, docker-compose.yml, .env.example, README.md), 1 neues Shell-Skript `scripts/build-deployment-package.sh`, 4 neue Deployment-Vorlagen unter `deployment/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deployment-beim-kunden/02-CONTEXT.md
@.planning/phases/02-deployment-beim-kunden/02-RESEARCH.md
@.planning/phases/02-deployment-beim-kunden/02-PATTERNS.md
@agent/Dockerfile
@agent/docker-compose.yml
@agent/.env.example
@agent/README.md
@agent/context.md.example

<interfaces>
Aktuelle Dockerfile (agent/Dockerfile):
- Zeile 19: `COPY prompts/ ./prompts/` — DIESE ZEILE ENTFERNEN.
- Rest unverändert (python:3.13-slim, non-root uid 1000, WORKDIR /app, pyproject-install, VOLUMEs, CMD).

Aktuelle docker-compose.yml (agent/docker-compose.yml):
- Zeile 3: `build: .` — durch `image: kea-tankstelle:v1.0.0` ersetzen (bzw. Zeile 4 `image: kea-tankstelle:latest` durch v1.0.0-Tag).
- Zeilen 8-10 volumes: `./context.md:/config/context.md:ro` + `agent-data:/data` bleiben; NEUE Zeile `./prompts:/app/prompts:ro` einfügen.
- Rest unverändert (container_name, restart, env_file, logging).

Aktuelle .env.example (agent/.env.example) — nach Plan 02.01 sind IMAP_HOST, IMAP_PORT, IMAP_USE_SSL bereits **funktional optional** (config.py-Auto-Detect); ABER die .env.example listet sie noch als Pflicht-Werte. In diesem Plan die .env.example umformatieren: alle Auto-Detect-fähigen Felder als `# IMAP_HOST=...` auskommentierte Optional-Overrides. Ziel-Format aus 02-PATTERNS.md § `agent/.env.example`:

```env
# ==== IMAP ====
# Pflichtfelder (Kunde füllt diese aus):
IMAP_USER=info@tankstelle-mustermann.de
IMAP_PASSWORD=xxx-app-password-xxx
IMAP_DRAFTS_FOLDER=KI-Entwürfe   # Dedizierter KI-Ordner ODER Standard (z. B. "Entwürfe"/"Drafts"), wird bei Bedarf automatisch angelegt.

# Optional-Overrides (nur setzen wenn Auto-Detect fehlschlägt):
# Unterstützte Provider: GMX, Web.de, IONOS, T-Online, Gmail, Outlook/M365, Mailbox.org, Strato, Hetzner.
# IMAP_HOST=imap.mein-eigener-server.de
# IMAP_PORT=993
# IMAP_USE_SSL=true
# IMAP_SENT_FOLDER=Sent
IMAP_INBOX_FOLDER=INBOX

# ==== Verhalten ====
POLL_INTERVAL_SECONDS=300
BACKFILL_DAYS=1
OWN_EMAIL_ADDRESS=info@tankstelle-mustermann.de
OWN_DISPLAY_NAME=Tankstelle Mustermann

# ==== LLM (Anthropic) ====
ANTHROPIC_API_KEY=sk-ant-xxx
MODEL_CLASSIFY=claude-haiku-4-5
MODEL_DRAFT=claude-sonnet-4-6
LLM_MAX_TOKENS_DRAFT=600
LLM_TEMPERATURE_DRAFT=0.3

# ==== Feature-Flags ====
ENABLE_PII_REDACTION=true
LOG_LEVEL=INFO

# ==== Pfade (nicht ändern, Container-intern) ====
CONTEXT_FILE=/config/context.md
STATE_DB=/data/state.db
PROMPTS_DIR=/app/prompts
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: agent/Dockerfile + docker-compose.yml + .env.example + README.md auf Bind-Mount-Modell umstellen (D-16)</name>
  <files>agent/Dockerfile, agent/docker-compose.yml, agent/.env.example, agent/README.md</files>
  <read_first>
    - agent/Dockerfile (aktueller Stand — Zeile 19 `COPY prompts/ ./prompts/` ist die einzige Änderung)
    - agent/docker-compose.yml (aktueller Stand — build: → image: v1.0.0, prompts-Volume hinzufügen)
    - agent/.env.example (aktuelle 37 Zeilen — Pflichtfelder werden reduziert)
    - agent/README.md (aktuelle 72 Zeilen — Setup-Sektion Zeilen 12-41 komplett ersetzen)
    - .planning/phases/02-deployment-beim-kunden/02-PATTERNS.md (§ `agent/Dockerfile`, § `agent/docker-compose.yml`, § `agent/.env.example`, § `agent/README.md` — vollständige Zieldateien)
    - .planning/phases/02-deployment-beim-kunden/02-CONTEXT.md (§ D-16 Rationale + Phase-2-Prerequisite)
  </read_first>
  <action>
    Vier Dateien anpassen:

    **1. `agent/Dockerfile`** — Zeile 19 (`COPY prompts/ ./prompts/`) entfernen. Alle anderen 28 Zeilen bleiben unverändert. Sanity-Check: Nach Build sollte `/app/prompts/` NICHT existieren (wird per Bind-Mount hereingegeben). Setup:
    ```
    # ENTFERNEN Zeile 19: COPY prompts/ ./prompts/
    ```

    **2. `agent/docker-compose.yml`** — vollständig ersetzen durch (aus 02-PATTERNS.md § `agent/docker-compose.yml`):
    ```yaml
    services:
      agent:
        image: kea-tankstelle:v1.0.0
        container_name: kea-agent
        restart: unless-stopped
        env_file: .env
        volumes:
          - ./context.md:/config/context.md:ro
          - ./prompts:/app/prompts:ro
          - agent-data:/data
        logging:
          driver: json-file
          options:
            max-size: "10m"
            max-file: "3"

    volumes:
      agent-data:
    ```
    Wichtig: `build: .` und `image: kea-tankstelle:latest` (Zeilen 3-4 im Original) ersetzt durch `image: kea-tankstelle:v1.0.0` (ein Eintrag, versioned).

    **3. `agent/.env.example`** — komplett ersetzen durch die Ziel-Version aus 02-PATTERNS.md § `agent/.env.example` (siehe `<interfaces>` oben — kompletter Block).

    **4. `agent/README.md`** — Sektion "Setup" (aktuelle Zeilen 12-41) komplett ersetzen durch (aus 02-PATTERNS.md § `agent/README.md`):
    ```markdown
    ## Setup

    1. Deployment-Paket entpacken und Verzeichnis anlegen:
       ```
       mkdir -p /opt/kea
       cp -r deployment-paket/* /opt/kea/
       cd /opt/kea
       ```

    2. Docker-Image laden:
       ```
       docker load -i kea-tankstelle-v1.0.0.tar
       ```

    3. `.env` aus Template erstellen und ausfüllen:
       ```
       cp deployment/kunde-env.example .env
       chmod 600 .env
       nano .env   # IMAP_USER, IMAP_PASSWORD, IMAP_DRAFTS_FOLDER, OWN_EMAIL_ADDRESS, ANTHROPIC_API_KEY eintragen
       ```

    4. `context.md` aus Vorlage erstellen:
       ```
       cp deployment/context.md.tankstelle-erstversion.md context.md
       nano context.md   # Öffnungszeiten, FAQ, Ton, Signatur finalisieren
       ```

    5. Starten:
       ```
       docker compose up -d
       ```

    6. Logs beobachten (erster Poll nach ≤ 5 Min):
       ```
       docker compose logs -f agent
       ```
    ```
    Zusätzlich die "Alltag"-Tabelle (Zeilen 44-52) beibehalten, aber die Zeile "Update auf neue Version" (Zeile 51) ersetzen durch:
    ```markdown
    | Update auf neue Version | Vizionists liefert neuen Tarball → `docker load -i kea-tankstelle-vX.Y.Z.tar && docker compose up -d` |
    ```
    "Wo landen die Drafts?" (Zeile 56-60) und "Troubleshooting" (Zeile 62-67) und "Support" (Zeile 69-71) bleiben unverändert.

    **Kein Test** in diesem Task — Dockerfile/Compose-Änderungen werden im nächsten Task via Build-Skript indirekt getestet. Formales Test-Kriterium: `docker compose config` läuft ohne Fehler (Syntax-Check).
  </action>
  <verify>
    <automated>cd agent && ! grep "COPY prompts" Dockerfile && grep -c "image: kea-tankstelle:v1.0.0" docker-compose.yml && grep -c "./prompts:/app/prompts:ro" docker-compose.yml && grep -c "IMAP_USER=" .env.example && grep -c "# IMAP_HOST=" .env.example && grep -c "docker load" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `agent/Dockerfile` enthält NICHT mehr die Zeile `COPY prompts/`. Prüfbar via `! grep "COPY prompts" agent/Dockerfile`.
    - `agent/docker-compose.yml` enthält `image: kea-tankstelle:v1.0.0` und NICHT `build: .`. Prüfbar via `grep "image: kea-tankstelle:v1.0.0" agent/docker-compose.yml && ! grep "^  agent:$" agent/docker-compose.yml -A2 | grep "build:"`.
    - `agent/docker-compose.yml` hat den prompts-Bind-Mount `./prompts:/app/prompts:ro`. Prüfbar via `grep "./prompts:/app/prompts:ro" agent/docker-compose.yml`.
    - `agent/.env.example` hat `IMAP_HOST` NUR als auskommentierten Override (Zeile beginnt mit `# IMAP_HOST=`). Prüfbar via `grep -c "^# IMAP_HOST=" agent/.env.example` liefert ≥ 1 und `grep -c "^IMAP_HOST=" agent/.env.example` liefert 0.
    - `agent/.env.example` hat `IMAP_USER` weiterhin als aktives Pflichtfeld. Prüfbar via `grep -c "^IMAP_USER=" agent/.env.example` liefert 1.
    - `agent/.env.example` enthält Kommentar "Auto-Detect" oder ähnliches Wort. Prüfbar via `grep -i "auto-detect\|auto-detektier" agent/.env.example`.
    - `agent/README.md` enthält `docker load -i` als Setup-Kommando. Prüfbar via `grep "docker load -i" agent/README.md`.
    - `agent/README.md` enthält NICHT mehr `git clone`. Prüfbar via `! grep "git clone" agent/README.md`.
    - Docker-Compose-Syntax valid (kein YAML-Fehler): `cd agent && python -c "import yaml; yaml.safe_load(open('docker-compose.yml'))"` läuft ohne Fehler.
  </acceptance_criteria>
  <done>
    Dockerfile ohne COPY prompts/, docker-compose.yml auf image: v1.0.0 + prompts-Bind-Mount, .env.example mit 5 Pflichtfeldern + auskommentierten Optional-Overrides, README auf Tarball-Setup umgestellt.
  </done>
</task>

<task type="auto">
  <name>Task 2: scripts/build-deployment-package.sh + deployment/-Vorlagen (D-04, D-22)</name>
  <files>scripts/build-deployment-package.sh, deployment/kunde-env.example, deployment/vizionists-test-env.example, deployment/context.md.tankstelle-erstversion.md, deployment/context.md.vizionists-test.md</files>
  <read_first>
    - agent/.env.example (nach Task 1 — Struktur ist Ausgangsbasis für kunde-env.example und vizionists-test-env.example)
    - agent/context.md.example (Sektionsstruktur — Ausgangsbasis für beide context.md-Vorlagen)
    - .planning/phases/02-deployment-beim-kunden/02-CONTEXT.md (§ D-04 Delivery-Kommandos, § D-22 Zwei-Konfig-Trennung, § Specifics OSINT für Tankstelle)
    - .planning/phases/02-deployment-beim-kunden/02-RESEARCH.md (§ Docker-Image-Tarball — Build+Save-Kommandos; § Deployment-Paket-Struktur)
    - .planning/phases/02-deployment-beim-kunden/02-PATTERNS.md (§ Deployment-Artefakte ohne Code-Analog — Struktur-Vorlage)
  </read_first>
  <action>
    Fünf Dateien neu anlegen:

    **1. `scripts/build-deployment-package.sh`** (POSIX-Shell, min. 40 Zeilen). Ziel: Ein Kommando (`bash scripts/build-deployment-package.sh`) erzeugt ein `dist/deployment-paket-vX.Y.Z/`-Verzeichnis mit allem für USB-Delivery. Struktur:
    ```bash
    #!/usr/bin/env bash
    # scripts/build-deployment-package.sh
    # Erzeugt ein Deployment-Paket für USB-Delivery zum Kunden.
    # Voraussetzung: Docker + Docker Compose Plugin auf dem Build-Host.
    #
    # Aufruf: bash scripts/build-deployment-package.sh [VERSION]
    #   VERSION default: v1.0.0 (Semver-Tag)

    set -euo pipefail

    VERSION="${1:-v1.0.0}"
    IMAGE_TAG="kea-tankstelle:${VERSION}"
    DIST_DIR="dist/deployment-paket-${VERSION}"
    TAR_NAME="kea-tankstelle-${VERSION}.tar"

    ROOT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
    cd "$ROOT_DIR"

    echo "==> Build Docker-Image ${IMAGE_TAG}"
    docker build -t "${IMAGE_TAG}" agent/

    echo "==> Zielordner anlegen: ${DIST_DIR}"
    rm -rf "${DIST_DIR}"
    mkdir -p "${DIST_DIR}/deployment"
    mkdir -p "${DIST_DIR}/prompts"

    echo "==> docker save -> Tarball"
    docker save "${IMAGE_TAG}" -o "${DIST_DIR}/${TAR_NAME}"

    echo "==> docker-compose.yml kopieren"
    cp agent/docker-compose.yml "${DIST_DIR}/docker-compose.yml"

    echo "==> Prompts kopieren (Bind-Mount-Quelle)"
    cp agent/prompts/*.txt "${DIST_DIR}/prompts/"

    echo "==> Deployment-Templates kopieren"
    cp deployment/kunde-env.example "${DIST_DIR}/deployment/kunde-env.example"
    cp deployment/vizionists-test-env.example "${DIST_DIR}/deployment/vizionists-test-env.example"
    cp deployment/context.md.tankstelle-erstversion.md "${DIST_DIR}/deployment/context.md.tankstelle-erstversion.md"
    cp deployment/context.md.vizionists-test.md "${DIST_DIR}/deployment/context.md.vizionists-test.md"

    echo "==> README kopieren"
    cp agent/README.md "${DIST_DIR}/README.md"

    echo "==> SHA256-Checksum berechnen"
    ( cd "${DIST_DIR}" && sha256sum "${TAR_NAME}" > "${TAR_NAME}.sha256" )

    echo ""
    echo "==> Fertig. Inhalt:"
    ls -la "${DIST_DIR}"
    echo ""
    echo "USB-Transfer: den gesamten Ordner ${DIST_DIR} auf USB kopieren."
    echo "Auf Kundenserver: docker load -i ${TAR_NAME} && docker compose up -d"
    ```

    Wichtig: Kein `docker push`, keine Registry-Interaktion. Reines Local-Build + Ordner-Packing. `sha256sum` deckt USB-Tarball-Integrität ab (Threat T-02.04-01).

    **2. `deployment/kunde-env.example`** — Kunden-Template mit Platzhaltern. Analog zu agent/.env.example (nach Task 1), aber IMAP_USER/PASSWORD/DRAFTS_FOLDER/OWN_EMAIL_ADDRESS mit `<xxx>`-Platzhaltern und Kommentar-Hinweisen:
    ```env
    # ==== KUNDEN-KONFIGURATION ====
    # Vor-Ort-Setup: diese 5 Felder mit Kunde gemeinsam ausfüllen.

    # IMAP-Zugang (Kunde nennt E-Mail-Adresse + Passwort + gewünschten KI-Ordner-Namen)
    IMAP_USER=<info@tankstelle-name.de>
    IMAP_PASSWORD=<app-password-oder-mail-password>
    IMAP_DRAFTS_FOLDER=<KI-Entwürfe>   # Wird automatisch angelegt falls nicht vorhanden
    OWN_EMAIL_ADDRESS=<info@tankstelle-name.de>  # Meistens identisch zu IMAP_USER

    # Anthropic API-Key (Vizionists stellt aus Business-Account)
    ANTHROPIC_API_KEY=<sk-ant-xxx>

    # ==== OPTIONAL — Nur setzen wenn Auto-Detect fehlschlägt ====
    # Der Agent erkennt IMAP-Host aus der E-Mail-Domain automatisch.
    # Unterstützte Provider: GMX, Web.de, IONOS, T-Online, Gmail, Outlook/M365, Mailbox.org, Strato, Hetzner.
    # Manuelle Overrides:
    # IMAP_HOST=imap.eigener-server.de
    # IMAP_PORT=993
    # IMAP_USE_SSL=true
    # IMAP_SENT_FOLDER=Sent

    # ==== DEFAULTS — Normalerweise nicht ändern ====
    IMAP_INBOX_FOLDER=INBOX
    POLL_INTERVAL_SECONDS=300
    BACKFILL_DAYS=1
    OWN_DISPLAY_NAME=<Tankstelle Name>
    MODEL_CLASSIFY=claude-haiku-4-5
    MODEL_DRAFT=claude-sonnet-4-6
    LLM_MAX_TOKENS_DRAFT=600
    LLM_TEMPERATURE_DRAFT=0.3
    ENABLE_PII_REDACTION=true
    LOG_LEVEL=INFO
    CONTEXT_FILE=/config/context.md
    STATE_DB=/data/state.db
    PROMPTS_DIR=/app/prompts
    ```

    **3. `deployment/vizionists-test-env.example`** — Analog aber mit konkreten IONOS-Werten für `shala@vizionists.com`:
    ```env
    # ==== VIZIONISTS-TEST-KONFIGURATION ====
    # Pre-Deployment-Test bei Vizionists gegen shala@vizionists.com über IONOS.

    IMAP_USER=shala@vizionists.com
    IMAP_PASSWORD=<ionos-app-password>       # aus IONOS Kundencenter -> E-Mail -> App-Passwörter
    IMAP_DRAFTS_FOLDER=KEA-Test-Entwürfe     # Neuer Ordner — Auto-CREATE wird beim ersten Draft getestet
    OWN_EMAIL_ADDRESS=shala@vizionists.com

    ANTHROPIC_API_KEY=<sk-ant-xxx>

    # IONOS ist in der statischen Provider-Tabelle — kein Override nötig.
    # Auto-Detect liefert: imap.ionos.de:993 SSL, Sent="Sent".

    IMAP_INBOX_FOLDER=INBOX
    POLL_INTERVAL_SECONDS=300
    BACKFILL_DAYS=1
    OWN_DISPLAY_NAME=Vizionists Test
    MODEL_CLASSIFY=claude-haiku-4-5
    MODEL_DRAFT=claude-sonnet-4-6
    LLM_MAX_TOKENS_DRAFT=600
    LLM_TEMPERATURE_DRAFT=0.3
    ENABLE_PII_REDACTION=true
    LOG_LEVEL=INFO
    CONTEXT_FILE=/config/context.md
    STATE_DB=/data/state.db
    PROMPTS_DIR=/app/prompts
    ```

    **4. `deployment/context.md.tankstelle-erstversion.md`** — OSINT-basierter Rohbau. Struktur wie `agent/context.md.example`, aber mit Platzhaltern die auf Vizionists-Recherche verweisen:
    ```markdown
    # Firmen-Kontext für <Tankstellenname>

    <!-- Diese Erstversion basiert auf öffentlich verfügbaren Infos (Website, Google-My-Business, Impressum).
         Im Vor-Ort-Termin gemeinsam mit dem Betreiber finalisieren:
         - Ton-Vorgaben, Reklamations-Standard, Signatur präzisieren
         - Preise die nicht öffentlich stehen, ergänzen
         - FAQ mit spezifischen Kundenfragen ausbauen -->

    ## About
    <2–4 Sätze aus Website/Google-Eintrag: Wer? Wo? Seit wann? Was zeichnet aus?>

    ## Öffnungszeiten
    <aus Google-My-Business oder Website>
    Mo–Fr: <hh:mm–hh:mm>
    Sa: <hh:mm–hh:mm>
    So: <hh:mm–hh:mm>
    Feiertage: <geregelt oder geschlossen>

    ## Angebote / Preise / Produkte
    - Kraftstoffe: Tagespreis, siehe Aushang / Website
    - Auto-Waschanlage: <ja/nein, Preise wenn öffentlich>
    - Shop / Bistro: <Sortiment falls beworben, Öffnungszeiten falls abweichend>
    - Werkstatt: <ja/nein, Terminvereinbarung>
    - Sonstiges: <Reifenservice, DHL-Paketshop, etc. falls beworben>

    ## Häufige Fragen (FAQ)
    ### Habt ihr eine Waschanlage?
    <Antwort mit Verweis auf Preise wenn öffentlich>

    ### Nehmt ihr Karten / Bar / <Zahlungsmittel> an?
    <Antwort aus Website oder Google-Reviews-Trends>

    ### <Weitere häufige Frage aus Google-Reviews>
    <Antwort>

    ## Ton
    <-- Im Termin mit Betreiber finalisieren -->
    - Anrede: Sie (kein Du)
    - Kurz, freundlich, direkt
    - Bei Reklamationen deeskalierend
    - Konkrete Öffnungszeiten / Preise nennen wenn erfragt

    ## Signatur
    <-- Im Termin mit Betreiber finalisieren -->
    Mit freundlichen Grüßen

    <Vorname Nachname>
    <Tankstellenname>
    <Straße Nr, PLZ Ort>
    Tel: <Telefonnummer>
    E-Mail: <E-Mail-Adresse>
    Web: <Website falls vorhanden>
    ```

    **5. `deployment/context.md.vizionists-test.md`** — Vizionists-Test-Kontext für Pre-Deployment-Test:
    ```markdown
    # Firmen-Kontext für Vizionists (Test-Referenz)

    <!-- Test-Kontext für Pre-Deployment-Test bei Vizionists.
         Ziel: verifiziert dass der Agent Standard-Kundenanfragen sinnvoll draftet,
         mit realistischem Firmen-Bezug ohne echte Tankstellen-Daten. -->

    ## About
    Vizionists ist eine Software- und Beratungsagentur mit Sitz in Deutschland,
    spezialisiert auf KI-gestützte Automatisierung kleiner Betriebe.
    Ansprechpartner: shala@vizionists.com

    ## Öffnungszeiten
    Mo–Fr: 09:00–18:00
    Sa/So: geschlossen
    Feiertage: geschlossen

    ## Angebote / Preise / Produkte
    - KI-Email-Agent-Setup für kleine Betriebe: individuelles Angebot
    - Beratung zu Automatisierung: individuelles Angebot
    - Kein Standard-Preisliste — Angebote nach Erstgespräch

    ## Häufige Fragen (FAQ)
    ### Wie funktioniert euer KI-Agent?
    Der Agent liest neue E-Mails per IMAP, erkennt Kundenanfragen und legt Antwort-Drafts
    im Entwürfe-Ordner ab. Der Betreiber prüft und sendet manuell. Keine Auto-Send-Funktion.

    ### Wo werden die Daten verarbeitet?
    Auf dem Server des Betreibers (Docker-Container). LLM-Calls gehen an Anthropic (EU-SCC-Bindung).
    Keine zusätzliche Speicherung im Bot.

    ### Was kostet das?
    Individuelles Angebot nach Erstgespräch. Anthropic-API-Kosten trägt der Betreiber.

    ## Ton
    - Anrede: Sie
    - Sachlich, professionell, technisch präzise
    - Keine Marketing-Phrasen
    - Bei Detail-Fragen: kurze Antwort + Angebot für Erstgespräch

    ## Signatur
    Mit freundlichen Grüßen

    Enver Shala
    Vizionists
    shala@vizionists.com
    ```

    **Wichtig:** Die zwei `deployment/context.md.*.md`-Dateien haben denselben strukturellen Aufbau (About, Öffnungszeiten, Angebote, FAQ, Ton, Signatur) — konsistent mit `agent/context.md.example`. Der Unterschied ist Inhalt: einer ist OSINT-Rohbau mit Platzhaltern, der andere ist konkret-realistisch für Vizionists.

    **Kein Test in diesem Task** — Deployment-Templates sind Text-Vorlagen, Build-Skript wird in Plan 02.05 (Pre-Deployment-Test) mit realem Ausführen validiert. Formales Kriterium: Alle 5 Dateien existieren, Skript ist executable-bit-fähig (`chmod +x` empfohlen), Templates sind syntaktisch valid (bei .env-Dateien: keine Zeilen ohne `=` außer Kommentare).
  </action>
  <verify>
    <automated>test -x scripts/build-deployment-package.sh || chmod +x scripts/build-deployment-package.sh; test -f scripts/build-deployment-package.sh && test -f deployment/kunde-env.example && test -f deployment/vizionists-test-env.example && test -f deployment/context.md.tankstelle-erstversion.md && test -f deployment/context.md.vizionists-test.md && grep -c "docker save" scripts/build-deployment-package.sh && grep -c "sha256sum" scripts/build-deployment-package.sh && grep -c "IMAP_USER=" deployment/kunde-env.example && grep -c "shala@vizionists.com" deployment/vizionists-test-env.example</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/build-deployment-package.sh` existiert und ist ausführbar. Prüfbar via `test -x scripts/build-deployment-package.sh` (oder chmod +x wenn nötig).
    - Das Skript enthält `docker save`, `docker build`, `sha256sum` als Kommandos. Prüfbar via `grep -c "docker save\|docker build\|sha256sum" scripts/build-deployment-package.sh` liefert ≥ 3.
    - Das Skript nutzt `set -euo pipefail` als Fehler-Semantik. Prüfbar via `grep "set -euo pipefail" scripts/build-deployment-package.sh`.
    - `deployment/kunde-env.example` existiert mit ≥ 5 Pflichtfeld-Zeilen (IMAP_USER, IMAP_PASSWORD, IMAP_DRAFTS_FOLDER, OWN_EMAIL_ADDRESS, ANTHROPIC_API_KEY).
    - `deployment/vizionists-test-env.example` existiert und enthält `shala@vizionists.com`, `KEA-Test-Entwürfe` (für Auto-CREATE-Test).
    - `deployment/context.md.tankstelle-erstversion.md` existiert mit den Sektionen About, Öffnungszeiten, Angebote, FAQ, Ton, Signatur (analog agent/context.md.example). Prüfbar via `grep -c "^## " deployment/context.md.tankstelle-erstversion.md` liefert ≥ 6.
    - `deployment/context.md.vizionists-test.md` existiert mit denselben 6 Sektionen und konkretem Vizionists-Content (nennt "Vizionists" und "shala@vizionists.com").
    - Keine echten Secrets in den Templates: `grep -c "sk-ant-[a-zA-Z0-9]\{20,\}" deployment/*.example` liefert 0 (nur Platzhalter `<sk-ant-xxx>` oder `sk-ant-xxx`).
  </acceptance_criteria>
  <done>
    scripts/build-deployment-package.sh existiert und macht docker build + docker save + Ordner-Packing + sha256sum; deployment/-Ordner hat 4 Vorlagen (kunde-env, vizionists-test-env, tankstelle-erstversion.md, vizionists-test.md), alle mit den Sektionen aus context.md.example.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vizionists-Laptop → USB-Stick → Kundenserver | USB kann manipuliert werden (Datenaustausch außerhalb der Cloud) |
| deployment/*.example → .env (Kunden-System) | Templates werden vom Betreiber lokal kopiert, mit echten Secrets gefüllt |
| .env am Kundenserver | Enthält Klartext-Secrets (IMAP-Passwort, Anthropic-Key) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.04-01 | Tampering | USB-Tarball-Integrität | mitigate | `build-deployment-package.sh` erzeugt `.sha256`-Checksum-Datei. Vor `docker load` am Kundenserver: `sha256sum -c kea-tankstelle-v1.0.0.tar.sha256` prüfen. Dokumentiert im RUNBOOK (Plan 02.07). |
| T-02.04-02 | Information Disclosure | Secrets in `.env`-Templates | mitigate | Alle deployment/*.example nutzen `<placeholder>`-Werte oder `sk-ant-xxx`-Muster — keine echten Credentials im Repo. Test-Env für Vizionists wird beim Setup vor Ort mit echtem IONOS-App-Password lokal befüllt. |
| T-02.04-03 | Information Disclosure | `.env`-Datei am Kundenserver | mitigate | `chmod 600 .env` als Setup-Schritt in Runbook + README dokumentiert. Docker-User (uid 1000) kann `.env` per `env_file:` lesen, andere User nicht. |
| T-02.04-04 | Tampering | Docker-Image-Manipulation vor USB-Transfer | mitigate | `docker save` erzeugt das Image LOKAL auf dem Vizionists-Build-Host aus dem Repo — kein Zwischen-Registry-Push, keine externen Image-Layer-Downloads (außer python:3.13-slim Base im initialen `docker build`). SHA256 auf Tarball + Signatur der Base-Image durch Docker Hub. |
| T-02.04-05 | Denial of Service | Bind-Mount überschreibt Container-internen prompts/-Ordner-Content | accept | Wenn `./prompts/` am Host leer ist, ist `/app/prompts/` im Container leer → `config.py` findet keine `classify.txt`/`generate.txt` → RuntimeError beim Startup (Fail-Fast). Betreiber sieht Fehler in `docker compose logs` sofort und weiß dass prompts-Ordner leer ist. Akzeptables Fail-Fast-Verhalten. |
| T-02.04-06 | Supply Chain | Base-Image `python:3.13-slim` | mitigate | Base-Image ist offizielles Docker-Hub-Image (Docker Trusted Content). Build-Zeitpunkt der Base ist im Image-Manifest fixiert (kein `:latest`, sondern `:3.13-slim` mit Docker-Hub-Image-Signaturen). |
</threat_model>

<verification>
Automatisierte Verifikation für den gesamten Plan:

```bash
# YAML-Syntax für Compose:
python -c "import yaml; yaml.safe_load(open('agent/docker-compose.yml'))"

# Alle Deployment-Dateien existieren:
ls scripts/build-deployment-package.sh deployment/*.example deployment/*.md

# .env.example-Struktur:
grep -c "^IMAP_USER=" agent/.env.example        # -> 1
grep -c "^# IMAP_HOST=" agent/.env.example      # -> ≥ 1
grep -c "^IMAP_HOST=" agent/.env.example         # -> 0
```

**Manuelle Verifikation (optional, für Wave 3 in Plan 02.05):** Auf einem Host mit Docker installiert:
```bash
bash scripts/build-deployment-package.sh v1.0.0
ls -la dist/deployment-paket-v1.0.0/
# Erwartung: kea-tankstelle-v1.0.0.tar (~200 MB), .sha256, docker-compose.yml, README.md, deployment/, prompts/
```

Der Build läuft in ~1-2 Min. Ohne Docker-Verfügbarkeit auf dem Planungs-Host bleibt dieser Schritt zurückgehalten für Plan 02.05.
</verification>

<success_criteria>
1. `agent/Dockerfile` ohne `COPY prompts/`, `agent/docker-compose.yml` mit `image: kea-tankstelle:v1.0.0` und prompts-Bind-Mount.
2. `agent/.env.example` mit 5 aktiven Pflichtfeldern und IMAP_HOST/PORT/SSL/SENT_FOLDER als auskommentierte Optional-Overrides.
3. `agent/README.md` beschreibt Tarball-Delivery (`docker load`) statt `git clone`.
4. `scripts/build-deployment-package.sh` erzeugt ein vollständiges Deployment-Paket (Tarball + SHA256 + Compose + Templates + Prompts + README).
5. Zwei env-Templates (kunde-env, vizionists-test-env) und zwei context.md-Vorlagen (tankstelle-erstversion, vizionists-test) existieren mit konsistenter Struktur.
6. Kein Test-Regress: Wenn Docker verfügbar wäre, `docker compose config` würde ohne Fehler laufen.
</success_criteria>

<output>
Create `.planning/phases/02-deployment-beim-kunden/02.04-SUMMARY.md` when done — dokumentiere die genaue Tarball-Größe (falls Build durchgeführt) und die Checksum-Datei, und dass die Docker-Verifikation zu Plan 02.05 verschoben wird.
</output>
