---
phase: 07-agenten-chat-im-webui-v1-3
plan: 04
type: execute
wave: 4
depends_on: ["07-03"]
files_modified:
  - webui/src/templates/index.html
  - webui/src/main.py
  - webui/tests/test_endpoints_chat.py
  - webui/tests/fixtures/embed_test.html
  - deployment/kunde-env.example
  - deployment/vizionists-test-env.example
autonomous: true
requirements: [CHAT-01, CHAT-05]
must_haves:
  truths:
    - "Im Haupt-WebUI erscheint pro gewähltem Agent ein Chat-Bereich, der DASSELBE Partial rendert wie die embed-Route"
    - "Das eingebettete Partial funktioniert in einer nackten Test-HTML-Seite ohne WebUI-Chrome und ohne externe Ressourcen"
    - "Ein automatischer Test beweist: embed-HTML + referenzierte /static-Dateien enthalten keine externe URL"
    - "Die CHAT_*-Env-Variablen sind für den Betreiber dokumentiert (mit Defaults, Zero-Config lauffähig)"
  artifacts:
    - path: "webui/src/templates/index.html"
      provides: "Chat-Bereich pro Agent, der /chat/{agent_id}/embed einbindet (gleiche Partial-Quelle)"
    - path: "webui/tests/fixtures/embed_test.html"
      provides: "Nackte Test-HTML-Seite als Nachweis der Einbettbarkeit (CHAT-05, Phase-8-Vorarbeit)"
    - path: "deployment/kunde-env.example"
      provides: "Dokumentierte CHAT_RATE_LIMIT_PER_MIN / CHAT_MAX_TOKENS / CHAT_HISTORY_TOKEN_BUDGET"
  key_links:
    - from: "webui/src/templates/index.html"
      to: "GET /chat/{agent_id}/embed"
      via: "Einbindung des chrome-losen Partials im Haupt-WebUI"
      pattern: "/chat/"
    - from: "webui/tests/test_endpoints_chat.py"
      to: "GET /chat/{agent_id}/embed + /static/chat.js + /static/chat.css"
      via: "No-external-resource-Assertion über embed + Assets"
      pattern: "https?://"
---

<objective>
CHAT-05 abschließen: Der Haupt-WebUI-Chat rendert dasselbe chrome-lose Partial wie die embed-Route (eine Quelle, kein zweiter Chat-Code), und die Einbettbarkeit wird nachweisbar gemacht — nackte Test-HTML-Seite + automatischer „keine externen Ressourcen"-Test. Außerdem werden die CHAT_*-Limits (D-60) für den Betreiber dokumentiert.

Purpose: Die Phase-8-Vorarbeit (Outlook lädt genau dieses Partial) verifizierbar abschließen und den Chat für den Betreiber im normalen WebUI verfügbar machen.
Output: Chat-Integration in index.html, embed_test.html-Fixture, No-external-resource-Test, Env-Doku.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-agenten-chat-im-webui-v1-3/07-CONTEXT.md
@CLAUDE.md

<interfaces>
Aus webui/src/main.py (aus 07-01..03):
- GET /chat/{agent_id}/embed -> HTMLResponse (chrome-loses Partial, nur /static-Ressourcen)
- POST /chat/{agent_id}/send -> StreamingResponse (SSE)

Aus webui/src/templates/index.html:
- Bedingter Block `{% if agent_id %} … {% endif %}` mit den Agent-Fieldsets — hier wird der Chat-Bereich eingehängt
- CSP-Middleware in main.py: default-src 'self', frame-ancestors 'none' (relevant für iframe-Einbettung → Anmerkung unten)

Aus webui/tests/test_endpoints_chat.py:
- bestehende embed/send-Tests aus 07-01..03; authed_client/_setup_env-Muster

D-60 env-Namen: CHAT_RATE_LIMIT_PER_MIN=20, CHAT_MAX_TOKENS=2000, CHAT_HISTORY_TOKEN_BUDGET
deployment/kunde-env.example + vizionists-test-env.example: bestehende MODEL_CLASSIFY/MODEL_DRAFT-Kommentar-Struktur als Vorbild
</interfaces>

<scope_note>
Die CSP-Middleware setzt `frame-ancestors 'none'` + `X-Frame-Options: DENY`. Eine iframe-Einbettung über Origin-Grenzen (Outlook, HTTPS) ist ausdrücklich **Phase 8** und hier NICHT in Scope. Der CHAT-05-Nachweis in Phase 7 ist: (a) das Partial rendert eigenständig ohne base.html-Chrome, (b) es referenziert ausschließlich lokale /static-Ressourcen (keine externen URLs), (c) eine nackte Test-HTML-Seite demonstriert die Struktur der Einbettung. Frame-Policy-Anpassungen bleiben Phase 8 überlassen (nicht simplifizieren — bewusst dort verortet).
</scope_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chat-Bereich pro Agent im Haupt-WebUI (index.html) — gleiche Partial-Quelle</name>
  <read_first>
    - webui/src/templates/index.html (der {% if agent_id %}-Block, Fieldset-/Section-Muster, das Script am Ende)
    - webui/src/templates/chat.html (das chrome-lose Partial aus 07-01 — wird wiederverwendet, nicht dupliziert)
    - webui/static/chat.js + webui/static/chat.css (Assets)
    - webui/src/main.py (index-Route: welche Template-Variablen verfügbar sind, u. a. agent_id)
    - webui/tests/test_endpoints_config.py (Muster: index-Rendering-Assertions mit authed_client)
  </read_first>
  <action>
Binde in `webui/src/templates/index.html` innerhalb des `{% if agent_id %}`-Blocks (z. B. nach dem Schreibstil-Fieldset, vor `</form>`/nach dem Formular) einen neuen Chat-`<section class="chat-section">` ein, der DIESELBE Partial-Quelle nutzt statt Chat-Markup zu duplizieren. Bevorzugter Weg (kein Server-Roundtrip-Zwang, keine iframe-CSP-Probleme): ein `<div>` mit `hx-get="/chat/{{ agent_id }}/embed"`-Lazy-Load ist NICHT ideal (das Partial bringt eigenen html/head mit). Stattdessen: refaktoriere das Partial so, dass der reine Chat-Body in ein include-fähiges Fragment `webui/src/templates/_chat.html` ausgelagert wird; `chat.html` (embed-Route) UND `index.html` includen beide `{% include "_chat.html" %}`. So gibt es EINE Markup-Quelle. `chat.html` bleibt der chrome-lose Rahmen (doctype+head mit /static/chat.css + /static/chat.js), der `_chat.html` einbettet; `index.html` bindet `_chat.html` in seine bestehende Chrome ein und lädt `/static/chat.css` + `/static/chat.js` einmalig zusätzlich (in base.html oder inline am Section-Ende).
Stelle sicher: (a) der Chat-Bereich erscheint nur bei gewähltem, existierendem Agent; (b) `agent_id` wird korrekt an das Fragment/JS übergeben (data-attribut).
Ergänze in `webui/tests/test_endpoints_chat.py`: Test, dass GET / (authed, mit einem existierenden Agent via ?agent_id=…) den Chat-Bereich enthält (assert auf ein Chat-Marker-Element, z. B. `id="chat-log"` oder `class="chat-section"`), UND dass GET /chat/{id}/embed und der index-Chat dasselbe Kern-Markup-Fragment enthalten (assert auf identischen Marker aus _chat.html in beiden Responses).
  </action>
  <verify>
    <automated>cd webui && python -m pytest tests/test_endpoints_chat.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `webui/src/templates/_chat.html` existiert; sowohl chat.html als auch index.html enthalten `{% include "_chat.html" %}`: `grep -l "_chat.html" webui/src/templates/chat.html webui/src/templates/index.html` liefert beide.
    - index.html-Chat-Bereich erscheint nur im `{% if agent_id %}`-Block: der Marker steht innerhalb des Blocks.
    - Test: GET /?agent_id=<existing> enthält den Chat-Marker; embed und index teilen dasselbe Fragment-Markup.
    - Volle Suite grün: `cd webui && python -m pytest -q`.
  </acceptance_criteria>
  <done>Haupt-WebUI zeigt pro Agent einen Chat-Bereich aus derselben Partial-Quelle wie die embed-Route; Tests grün.</done>
</task>

<task type="auto">
  <name>Task 2: Einbettbarkeits-Nachweis (embed_test.html + No-external-resource-Test) + CHAT_*-Env-Doku</name>
  <read_first>
    - webui/src/templates/chat.html + _chat.html (was die embed-Route ausliefert)
    - webui/static/chat.js + webui/static/chat.css (referenzierte Assets)
    - webui/src/main.py (embed-Route)
    - deployment/kunde-env.example + deployment/vizionists-test-env.example (Kommentar-/Struktur-Muster)
    - webui/tests/test_security.py (Muster für negativ-Assertions über Response-Header/Body)
  </read_first>
  <action>
Erstelle `webui/tests/fixtures/embed_test.html`: eine minimale, eigenständige HTML-Seite OHNE Vizpatch-Chrome, die demonstriert, wie ein Fremd-Host (Phase 8: Outlook-Taskpane) das Chat-Partial einbindet — ausschließlich mit lokalen/relativen Referenzen auf `chat.css`/`chat.js` und einem Platzhalter-Container. Diese Datei dient als Fixture + menschlich nachvollziehbarer Nachweis; sie darf KEINE externe URL enthalten.
Ergänze in `webui/tests/test_endpoints_chat.py` einen automatischen CHAT-05-Nachweis-Test:
- GET /chat/{agent_id}/embed (authed) → 200; parse den HTML-Body: sammle alle `src=`/`href=`/`url(`-Referenzen; assert, dass KEINE mit `http://` oder `https://` oder `//` (protokoll-relativ) beginnt — nur relative `/static/...`-Pfade.
- Lade zusätzlich die referenzierten Asset-Dateien vom Dateisystem (`webui/static/chat.js`, `webui/static/chat.css`) und assert, dass ihr Inhalt keine externe URL / kein `@import url(http...)` / kein CDN enthält.
- assert, dass der embed-Body kein base.html-Chrome enthält (`<h1>Vizpatch` NICHT vorhanden) — Bestätigung „chrome-los".
Dokumentiere die CHAT_*-Limits (D-60) in `deployment/kunde-env.example` UND `deployment/vizionists-test-env.example`: neue kommentierte Sektion „# Agenten-Chat (Phase 7) — Kosten-/Missbrauchsschutz" mit `CHAT_RATE_LIMIT_PER_MIN=20`, `CHAT_MAX_TOKENS=2000`, `CHAT_HISTORY_TOKEN_BUDGET=<Default>` und je einem Kommentar, dass die Werte optional sind (Code hat Defaults, Zero-Config lauffähig).
  </action>
  <verify>
    <automated>cd webui && python -m pytest tests/test_endpoints_chat.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `webui/tests/fixtures/embed_test.html` existiert; `grep -Ec "https?://|(^|[^:])//" webui/tests/fixtures/embed_test.html` == 0 (keine externen/protokoll-relativen Referenzen).
    - No-external-resource-Test grün: embed-Body + chat.js + chat.css enthalten keine externe URL.
    - embed-Body enthält kein `<h1>Vizpatch`-Chrome.
    - `grep -c "CHAT_RATE_LIMIT_PER_MIN" deployment/kunde-env.example` ≥ 1 und `grep -c "CHAT_MAX_TOKENS" deployment/vizionists-test-env.example` ≥ 1.
    - Volle Suite grün: `cd webui && python -m pytest -q`.
  </acceptance_criteria>
  <done>Einbettbarkeit ist automatisiert bewiesen (keine externen Ressourcen, chrome-los) und die Chat-Limits sind für den Betreiber dokumentiert; Tests grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fremd-Host (Phase 8) → embed-Partial | das Partial wird potenziell außerhalb der WebUI geladen — darf keine externen Ressourcen ziehen |
| Betreiber → index.html-Chat | derselbe auth-geschützte Pfad wie der Rest der WebUI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12 | Tampering | externe Ressource im Partial (Supply-Chain/CDN) | mitigate | automatischer No-external-resource-Test über embed + chat.js + chat.css (D-61) |
| T-07-13 | Spoofing/Clickjacking | iframe-Einbettung des Partials | accept | frame-ancestors 'none' bleibt in Phase 7 aktiv (kein Cross-Origin-Frame); iframe-/HTTPS-Freigabe ist explizit Phase 8 (scope_note) |
| T-07-14 | Information Disclosure | Chat-Markup-Duplikat läuft auseinander | mitigate | eine Markup-Quelle (_chat.html include in embed + index); Test prüft geteiltes Fragment |
| T-07-SC | Tampering | keine neuen Package-Installs | accept | keine neuen Dependencies |
</threat_model>

<verification>
- `cd webui && python -m pytest -q` grün (gesamte webui-Suite).
- Manuell (optional): index.html mit gewähltem Agent zeigt funktionierenden Chat; embed-Route separat aufrufbar; embed_test.html im Browser demonstriert die Einbettung.
</verification>

<success_criteria>
- Haupt-WebUI-Chat und embed-Route rendern dasselbe Partial (eine Quelle) — CHAT-01/CHAT-05.
- Einbettbarkeit automatisiert nachgewiesen: keine externen Ressourcen, chrome-los — CHAT-05, D-61.
- CHAT_*-Limits dokumentiert — D-60.
</success_criteria>

<output>
Create `.planning/phases/07-agenten-chat-im-webui-v1-3/07-04-SUMMARY.md` when done
</output>
